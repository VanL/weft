# Ruff Suppression Registry

This is the operational exception ledger for
[Testing Strategy TS-3 and TS-3.1](specifications/08-Testing_Strategy.md). It
is not normative and is not required reading. Read or edit it only when a
local Ruff suppression or the aggregate raw-diagnostic inventory changes.

### Approved Ruff Suppression Registry [TS-3.1]

The governing ownership, grammar, and checker contract remain in
[Testing Strategy TS-3.1](specifications/08-Testing_Strategy.md). The records
below are the human-owned suppression entries, aggregate inventory, and
generated source index defined by that policy.

| Group | Rules | Approved cardinality | Protected invariant | Real proof | Rejected alternatives | Approval |
|-------|-------|----------------------|---------------------|------------|-----------------------|----------|
| `RUFF-SUP-001` | `C901` | `1` directives; raw: `C901=1` | Explicit mounts override provider defaults; env, runtime-home, copied-file, and diagnostic resolution remain one ordered result. | `tests/core/test_provider_cli_container_runtime.py::test_resolve_provider_container_runtime_respects_explicit_mount_target_override` | Split env, mount, and copy passes into remote helpers; rejected because it scatters one resolution contract. | Task 2 clean outside review PASS; owner approved 2026-08-05. |
| `RUFF-SUP-002` | `C901` | `2` directives; raw: `C901=2` | Codex option types, enums, authority limits, and sandbox precedence stay explicit at the provider boundary. | `tests/core/test_provider_cli_backend.py::test_provider_cli_runtime_rejects_raw_options_that_conflict_with_tool_profile` | Generic option-schema engine; rejected because option-specific security conflicts become indirect. | Task 2 clean outside review PASS; owner approved 2026-08-05. |
| `RUFF-SUP-003` | `C901` | `2` directives; raw: `C901=2` | Exact one-shot and session argv order, including first-turn-only and resume flags, remains locally reviewable. | `tests/core/test_provider_cli_backend.py::test_provider_cli_runtime_executes_one_shot_request`; `tests/core/test_provider_cli_session_backend.py::test_provider_cli_session_continues_across_turns` | One generalized argv builder; rejected because it hides turn-sensitive differences. | Task 2 clean outside review PASS; owner approved 2026-08-05. |
| `RUFF-SUP-005` | `C901` | `5` directives; raw: `C901=5` | Manager registration preserves lower-TID suppression, supersede-race repair, latest-self retention, terminal publish, and snapshot order. | `tests/core/test_manager.py::test_manager_registry_prunes_expired_rows_on_refresh`; `tests/core/test_manager.py::test_manager_active_heartbeat_race_preserves_superseded_record` | Generic registry repository or fragmented scans; rejected because it widens races and hides owner predicates. | Task 2 clean outside review PASS; owner approved 2026-08-05. |
| `RUFF-SUP-006` | `C901` | `1` directives; raw: `C901=1` | Host-PID and external-supervisor evidence preserve live, stale, and unknown semantics, including namespace ambiguity. | `tests/core/test_manager.py::test_manager_leadership_keeps_namespace_ambiguous_host_row_after_ping_timeout` | Authority strategy framework; rejected because the short proof ladder is clearer in one owner. | Task 2 clean outside review PASS; owner approved 2026-08-05. |
| `RUFF-SUP-007` | `C901` | `2` directives; raw: `C901=2` | Leadership resolution preserves stale pruning, unknown fail-closed behavior, launch fences, reservation requeue, persistent-child protection, and drain order. | `tests/core/test_manager.py::test_manager_leadership_yields_to_canonical_lower_manager`; `tests/core/test_manager.py::test_manager_leadership_yield_waits_while_persistent_children_exist` | Extract election and yield into a second control path; rejected because it could split dispatch authority. | Task 2 clean outside review PASS; owner approved 2026-08-05. |
| `RUFF-SUP-008` | `C901` | `2` directives; raw: `C901=2` | Child exit and terminal proof preserve multiprocessing-versus-OS precedence, startup/proof grace, ctrl-out-first evidence, and log fallback. | `tests/core/test_manager.py::test_cleanup_children_waits_for_terminal_proof_after_clean_exit`; `tests/core/test_manager.py::test_child_has_exited_trusts_live_host_pid_before_process_view` | Independent PID and queue-proof services; rejected because evidence precedence would become non-local. | Task 2 clean outside review PASS; owner approved 2026-08-05. |
| `RUFF-SUP-009` | `C901` | `2` directives; raw: `C901=2` | Child cleanup preserves bounded reap, reservation acknowledgement, service terminalization, and one-deadline STOP-to-kill escalation over process trees. | `tests/core/test_manager.py::test_manager_child_termination_uses_one_deadline_for_multiple_children`; `tests/core/test_manager.py::test_manager_cleanup_terminates_worker_descendants` | Split reap and termination stages opportunistically; rejected because budgets and descendant ownership can drift. | Task 2 clean outside review PASS; owner approved 2026-08-05. |
| `RUFF-SUP-010` | `C901` | `2` directives; raw: `C901=2` | Reserved messages honor KEEP, CLEAR, and REQUEUE exactly while stale internal reservations never delete live-manager work. | `tests/core/test_manager.py::test_manager_stop_mid_handler_requeues_reserved_work_unlaunched`; `tests/core/test_manager.py::test_manager_keeps_internal_reserved_when_manager_liveness_unknown` | Generic queue cleanup abstraction; rejected because internal protection and reserved-policy semantics differ. | Task 2 clean outside review PASS; owner approved 2026-08-05. |
| `RUFF-SUP-011` | `C901` | `1` directives; raw: `C901=1` | Broker reservation remains dispatch authority across control fences, validation, error policy, launch, and acknowledgement. | `tests/core/test_manager.py::test_manager_stop_mid_handler_requeues_reserved_work_unlaunched` | Split validation and launch into another dispatch path; rejected because it weakens reservation ownership. | Task 2 clean outside review PASS; owner approved 2026-08-05. |
| `RUFF-SUP-012` | `C901` | `3` directives; raw: `C901=3` | Singleton service convergence preserves evidence priority, canonical-owner selection, duplicate termination, and pending-spawn handling. | `tests/core/test_manager.py::test_reconcile_reuses_tracked_service_candidates`; `tests/core/test_manager.py::test_task_monitor_duplicate_tracked_child_force_kills_owned_process` | Separate evidence, convergence, and termination controllers; rejected because Manager must remain the sole supervision owner. | Task 2 clean outside review PASS; owner approved 2026-08-05. |
| `RUFF-SUP-013` | `C901` | `1` directives; raw: `C901=1` | Autostart compilation preserves task/pipeline distinction, defaults order, service metadata, lifecycle, and input fallback. | `tests/core/test_manager.py::test_manager_autostart_pipeline_target_launches_pipeline_run` | Reuse a generic nested-default merger; rejected because manifest semantics and TaskSpec defaults are not interchangeable. | Task 2 clean outside review PASS; owner approved 2026-08-05. |
| `RUFF-SUP-014` | `C901` | `1` directives; raw: `C901=1` | One reactor turn preserves the exact ordering of worker drain, registration, leadership, control, child cleanup, dispatch, service convergence, and idle shutdown. | `tests/core/test_manager.py::test_process_once_reconciles_internal_services_before_user_spawn_work` | Fragment the turn into phase objects; rejected because it reduces schedule locality and can create a second spine. | Task 2 clean outside review PASS; owner approved 2026-08-05. |
| `RUFF-SUP-016` | `C901` | `1` directives; raw: `C901=1` | Diagnostic proof precedence and exact liveness, source, detail, eligibility, and canonical-candidate fields remain stable. | `tests/commands/test_manager_commands.py::test_list_command_diagnostic_marks_lowest_live_manager_canonical` | Generic liveness strategy framework or authority extraction; rejected because the full proof ladder is clearer in one diagnostic owner. | Task 2 clean outside review PASS; owner approved 2026-08-05. |
| `RUFF-SUP-017` | `C901` | `3` directives; raw: `C901=3` | Manager stop preserves STOP-first confirmation, PID/process proof, optional force escalation, registry terminalization, and external-supervisor refusal. | `tests/commands/test_manager_commands.py::test_stop_command_waits_for_pid_exit_after_stopped_status`; `tests/commands/test_manager_commands.py::test_stop_command_force_replaces_active_registry_record` | Independent signal and registry cleanup flows; rejected because they can claim stopped before both boundaries agree. | Task 2 clean outside review PASS; owner approved 2026-08-05. |
| `RUFF-SUP-018` | `C901` | `1` directives; raw: `C901=1` | Detached manager startup preserves competing-manager convergence, acknowledgement recovery, liveness checks, settlement, and launcher cleanup. | `tests/commands/test_run.py::test_start_manager_treats_post_proof_ack_failure_as_nonfatal` | Break the startup race into generic polling helpers; rejected because handshake state becomes dispersed. | Task 2 clean outside review PASS; owner approved 2026-08-05. |
| `RUFF-SUP-019` | `C901` | `1` directives; raw: `C901=1` | Managed-service reduction keeps deterministic priority among terminal evidence, live owner, pending spawn, uncertainty, lifecycle limits, and backoff. | `tests/core/test_manager_services.py::test_all_managed_service_actions_have_table_coverage` | Meta-state-machine framework; rejected because the ordered pure reducer is already the inspectable policy table. | Task 2 clean outside review PASS; owner approved 2026-08-05. |
| `RUFF-SUP-020` | `C901` | `1` directives; raw: `C901=1` | Lifetime reports preserve TaskSpec summary authority while filling only absent state and metadata evidence. | `tests/core/monitor/test_lifetime_report.py::test_collation_lifetime_report_promotes_taskspec_and_monitor_context` | Generic mapping overlay helper; rejected because authoritative versus fallback fields become unclear. | Task 2 clean outside review PASS; owner approved 2026-08-05. |
| `RUFF-SUP-022` | `C901` | `1` directives; raw: `C901=1` | Monitor-store update chunks commit before the checkpoint advances, with per-TID merge and message refs in the same transaction sequence. | `tests/core/test_monitor_store.py::test_monitor_store_batch_ingest_updates_tasks_and_checkpoint` | Split checkpoint writing from ingestion orchestration; rejected because it obscures the commit boundary. | Task 2 clean outside review PASS; owner approved 2026-08-05. |
| `RUFF-SUP-023` | `C901` | `1` directives; raw: `C901=1` | Collation chooses the oldest eligible anchor, applies its age gate, collects only that TID, and stops at its first terminal row. | `tests/core/test_task_monitor_cleanup.py::test_task_monitor_cleanup_collates_terminal_task_log_for_anchor_tid` | General grouping iterator; rejected because FIFO anchor and stop semantics become implicit. | Task 2 clean outside review PASS; owner approved 2026-08-05. |
| `RUFF-SUP-024` | `C901` | `2` directives; raw: `C901=2` | Worker snapshots exhaustively classify copy/share/reset ownership and close every worker-local resource exactly once with ordered errors. | `tests/tasks/test_task_monitor.py::test_task_monitor_worker_local_snapshot_owns_mutable_runtime_resources`; `tests/tasks/test_task_monitor.py::test_task_monitor_worker_close_attempts_all_resources_and_reports_failure` | Reset registries or reflection-driven cleanup; rejected because explicit field and resource ownership is safer to audit. | Task 2 clean outside review PASS; owner approved 2026-08-05. |
| `RUFF-SUP-025` | `C901` | `2` directives; raw: `C901=2` | Forward and pre-checkpoint recovery preserve bounded selection, store-before-delete, report-before-delete, active protection, child-ref retirement, and checkpoint rules. | `tests/core/test_task_monitor_cleanup.py::test_task_monitor_cleanup_deletes_malformed_task_log`; `tests/tasks/test_task_monitor.py::test_task_monitor_jsonl_then_delete_recovers_precheckpoint_service_rows` | Unify both flows now; rejected because their checkpoint authority and eligibility differ. | Task 2 clean outside review PASS; owner approved 2026-08-05. |
| `RUFF-SUP-026` | `C901` | `1` directives; raw: `C901=1` | Summary emission precedes family disposition, with stale-open destruction protection and terminal-control bookkeeping preserved. | `tests/tasks/test_task_monitor.py::test_task_monitor_failed_summary_disposition_blocks_processor_delete` | Independent summary and disposition workers; rejected because deletion must not outrun durable reporting. | Task 2 clean outside review PASS; owner approved 2026-08-05. |
| `RUFF-SUP-027` | `C901` | `1` directives; raw: `C901=1` | Terminal-control cleanup preserves terminal versus suspect protection, report-before-delete, absolute slice deadlines, durable disposal/control marks, retirement, and next-slice scheduling. | `tests/tasks/test_task_monitor.py::test_task_monitor_terminal_control_cleanup_does_not_wait_for_retention` | Generic runtime queue sweeper or sharing with reserved cleanup; rejected because terminal-control proof and disposition are distinct. | Task 2 clean outside review PASS; owner approved 2026-08-05. |
| `RUFF-SUP-028` | `C901` | `1` directives; raw: `C901=1` | Manager `task_spawned` row trim preserves report-before-delete, exact reconciliation, child-ref retirement, and the open manager family. | `tests/tasks/test_task_monitor.py::test_task_monitor_trims_manager_task_spawned_rows_without_closing_manager_family` | Share with family retirement or orphan recovery; rejected because this is row-level compaction that must not close the family. | Task 2 clean outside review PASS; owner approved 2026-08-05. |
| `RUFF-SUP-029` | `C901` | `1` directives; raw: `C901=1` | Outbox decoding preserves raw fallback, base64 stream handling, stdout accumulation, stderr exclusion, final emission, and result transforms. | `tests/commands/test_result.py::test_process_outbox_message_handles_malformed_base64` | Separate envelope handlers; rejected because one wire-format decoder is easier to reason about end to end. | Task 2 clean outside review PASS; owner approved 2026-08-05. |
| `RUFF-SUP-030` | `C901` | `1` directives; raw: `C901=1` | Pipeline stage defaults preserve deep-copy isolation and ordered args, keyword args, env, and IO overlay without replacing explicit task values. | `tests/core/test_pipelines.py::test_pipeline_compiler_sets_defaults_input_override_for_interior_edge` | Generic nested-map merge; rejected because domain precedence becomes hidden. | Task 2 clean outside review PASS; owner approved 2026-08-05. |
| `RUFF-SUP-031` | `C901` | `1` directives; raw: `C901=1` | Exact-prune application preserves queue grouping, report-only rules, exact status, batch reconciliation, per-candidate errors, result order, and closure. | `tests/core/test_pruning_apply.py::test_exact_id_apply_reconcile_verifies_per_id_on_batch_under_deletion` | Backend-specific or caller-specific deletion loops; rejected because this is the single shared exact-delete authority. | Task 2 clean outside review PASS; owner approved 2026-08-05. |
| `RUFF-SUP-032` | `C901` | `1` directives; raw: `C901=1` | Retention selection visibly applies per-TID recency, newest-terminal, age, nonterminal, active-manager, and force-override protections. | `tests/commands/test_retention_prune.py::test_task_log_dry_run_reports_superseded_rows_without_deleting` | Declarative protection engine; rejected because override reasons and ordering become indirect. | Task 2 clean outside review PASS; owner approved 2026-08-05. |
| `RUFF-SUP-033` | `C901` | `1` directives; raw: `C901=1` | Manager registry pruning preserves malformed-row age, canonical normalization, per-manager recency, and active-manager liveness proof. | `tests/commands/test_runtime_prune.py::test_manager_prune_reports_superseded_and_stale_active_rows` | Generic runtime-registry selector or sharing with endpoints; rejected because manager liveness and grouping are distinct. | Task 2 clean outside review PASS; owner approved 2026-08-05. |
| `RUFF-SUP-034` | `C901` | `1` directives; raw: `C901=1` | Host terminal handoff preserves observation batching, reducer authority, first accepted stop, drain deadline, transport/process races, metrics, and diagnostics. | `tests/tasks/test_runner.py::test_real_pipe_exit_then_outcome_uses_production_handoff_driver`; `tests/tasks/test_runner.py::test_one_shot_stop_effect_cannot_reset_absolute_drain_deadline` | Opportunistic phase extraction or a new state framework; rejected pending a dedicated execution-path plan. | Task 2 clean outside review PASS; owner approved 2026-08-05. |
| `RUFF-SUP-035` | `C901` | `1` directive; raw: `C901=1` | Command-session startup preserves argv/env/cwd validation, pipe ownership, platform flags, optional monitoring, readiness, handles, and complete rollback. The agent-session path fell below the threshold during the reviewed monitor-lifecycle refactor and no longer carries this exception. | `tests/tasks/test_runner.py::test_interactive_session_collects_immediate_exit_stream_tail`; full host-runner owner suite. | Generic session factory; rejected because the remaining command-session resource lifecycle is clearer in its owner. | Original Task 2 clean outside review PASS; owner approved 2026-08-05. Cardinality reconciled after reviewed refactor 2026-08-07. |
| `RUFF-SUP-036` | `C901` | `1` directives; raw: `C901=1` | Subprocess monitoring preserves stream draining and cancellation, timeout, limit, exit, monitor, diagnostic, and final-output priority. | `tests/core/test_subprocess_runner.py::test_completed_process_at_timeout_wake_boundary_returns_ok`; `tests/tasks/test_runner.py::test_run_monitored_subprocess_emits_live_chunks_before_exit` | Local helper fragmentation or reducer conversion; rejected pending a dedicated execution-path plan. | Task 2 clean outside review PASS; owner approved 2026-08-05. |
| `RUFF-SUP-037` | `C901` | `2` directives; raw: `C901=2` | Incremental stream decoding preserves CR/LF normalization across chunks, raw/text fallback, decoder flush, and exactly one sentinel. | `tests/tasks/test_runner.py::test_run_monitored_subprocess_emits_live_chunks_before_exit` | Move normalization and decoder state to remote utilities; rejected because it reduces stream-protocol locality. | Task 2 clean outside review PASS; owner approved 2026-08-05. |
| `RUFF-SUP-038` | `C901` | `1` directives; raw: `C901=1` | State-machine definitions validate state/action/transition presence, terminal/sink rules, IDs, sources, targets, actions, reasons, and outgoing coverage together. | `tests/core/test_state_machines.py::test_duplicate_transition_ids_fail`; `tests/core/test_state_machines.py::test_terminal_and_sink_states_cannot_overlap` | Meta-schema or fragmented validators; rejected because the complete definition contract is clearer in one pass. | Task 2 clean outside review PASS; owner approved 2026-08-05. |
| `RUFF-SUP-039` | `C901` | `1` directives; raw: `C901=1` | Known-TID evidence preserves log, local, runtime, stale, claimed-output, and PONG precedence plus timestamp reconciliation. | `tests/commands/test_task_evidence.py::test_known_tid_ping_pong_updates_task_status` | Independent evidence-provider pipeline; rejected because which proof wins would become non-local. | Task 2 clean outside review PASS; owner approved 2026-08-05. |
| `RUFF-SUP-040` | `C901` | `1` directives; raw: `C901=1` | Reactor topology rejects noncanonical roles, support routes, empty names, and unapproved aliases before broker I/O. | `tests/tasks/test_task_execution.py::test_base_task_rejects_duplicate_queue_roles_before_broker_side_effects` | Generic graph validator; rejected because the small canonical role contract should remain explicit. | Task 2 clean outside review PASS; owner approved 2026-08-05. |
| `RUFF-SUP-041` | `C901` | `1` directives; raw: `C901=1` | Task drive preserves single-owner and reentrancy guards, termination priority, worker activity, bounded waits, and exactly-once finalization. | `tests/tasks/test_task_execution.py::test_base_task_run_until_stopped_finalizes_when_turn_raises` | Split stop checks and waiting into another loop object; rejected because it can alter reactor shutdown. | Task 2 clean outside review PASS; owner approved 2026-08-05. |
| `RUFF-SUP-042` | `C901` | `1` directives; raw: `C901=1` | Every runner outcome preserves its distinct TaskSpec state, event, pipeline status, diagnostics, deferred-control precedence, and raised error. | `tests/tasks/test_consumer_terminal_events.py::test_consumer_terminal_outcome_emits_one_state_event`; `tests/tasks/test_consumer_terminal_events.py::test_consumer_unknown_runner_outcome_status_fails_task` | Data-driven terminal dispatch; rejected because outcome-specific side effects become harder to audit. | Task 2 clean outside review PASS; owner approved 2026-08-05. |
| `RUFF-SUP-043` | `C901` | `1` directives; raw: `C901=1` | Heartbeat emission preserves stale-heap removal, ownership checks, supersede stop, failure reporting, rescheduling, and activity restoration. | `tests/tasks/test_heartbeat.py::test_heartbeat_late_wake_coalesces_to_one_emit` | Separate scheduling and ownership loops; rejected because one due-emission transaction is easier to reason about. | Task 2 clean outside review PASS; owner approved 2026-08-05. |
| `RUFF-SUP-044` | `C901` | `1` directives; raw: `C901=1` | Interactive finalization preserves prior terminal authority, reserved error policy, stdout/stderr final envelopes, terminal envelope, stream teardown, and close order. | `tests/commands/test_interactive_client.py::test_interactive_client_failure_overrides_stdout_final` | Separate state and transport finalizers; rejected because protocol ordering can diverge. | Task 2 clean outside review PASS; owner approved 2026-08-05. |
| `RUFF-SUP-045` | `C901` | `2` directives; raw: `C901=2` | Topology mutation preserves drive ownership, FIFO, generation fencing, waiter swap, atomic publish/rollback, fatal propagation, signalling, and exact closure. | `tests/tasks/test_multiqueue_watcher.py::test_background_post_replace_publication_failure_restores_old_waiter`; `tests/tasks/test_multiqueue_watcher.py::test_owner_fatal_exit_signals_every_queued_mutator` | Generic mutation transaction abstraction; rejected because resource ownership and SIGINT critical state become dispersed. | Task 2 clean outside review PASS; owner approved 2026-08-05. |
| `RUFF-SUP-046` | `C901` | `1` directives; raw: `C901=1` | Service-worker shutdown sends one stop per worker under bounded queue pressure and drains under the caller's deadline without deadlock. | `tests/tasks/test_service_task.py::test_service_task_full_sentinel_queue_respects_cleanup_deadline` | Generic thread-pool stop helper; rejected because Weft worker-result and queue-pressure semantics are owner-specific. | Task 2 clean outside review PASS; owner approved 2026-08-05. |
| `RUFF-SUP-047` | `C901` | `1` directives; raw: `C901=1` | Readiness preserves boot, ready, and startup-error interpretation, handshake diagnostics, process-exit late drain, sealed-channel join, and final response handling. | `tests/tasks/test_runner.py::test_agent_session_startup_error_survives_immediate_child_exit`; `tests/tasks/test_runner.py::test_agent_session_reports_hang_after_boot_handshake` | Generic session protocol framework or payload extraction; rejected because timing, process state, and payload meaning are one handshake. | Task 2 clean outside review PASS; owner approved 2026-08-05. |
| `RUFF-SUP-049` | `C901` | `1` directives; raw: `C901=1` | Agent termination preserves deadline-aware process-tree teardown, reap, kill fallback, and ordinary TERM-to-KILL escalation. | `tests/tasks/test_task_execution.py::test_agent_session_deadline_preserves_process_tree_kill_escalation` | Split deadline and ordinary termination without proof; rejected because absolute-budget semantics can drift. | Task 2 clean outside review PASS; owner approved 2026-08-05. |
| `RUFF-SUP-050` | `C901` | `1` directives; raw: `C901=1` | TaskSpec resolution preserves deep-copy isolation, TID and queue rewriting, defaults, runner/context inheritance, and the template-to-resolved boundary. | `tests/taskspec/test_taskspec_properties.py::test_resolve_taskspec_payload_does_not_mutate_input`; `tests/taskspec/test_taskspec_properties.py::test_resolved_taskspec_freezes_spec_and_io` | Generic recursive defaulting; rejected because canonicalization order and ownership become unclear. | Task 2 clean outside review PASS; owner approved 2026-08-05. |
| `RUFF-SUP-051` | `C901` | `1` directives; raw: `C901=1` | Agent validation preserves output coupling, unique tools, bounded LLM authority, and provider-CLI field, mode, scope, and tool restrictions. | `tests/specs/taskspec/test_agent_taskspec.py::test_validate_taskspec_reports_agent_errors` | Runtime-rule table; rejected because provider-specific errors and constraints become less local. | Task 2 clean outside review PASS; owner approved 2026-08-05. |
| `RUFF-SUP-052` | `C901` | `1` directives; raw: `C901=1` | Target validation preserves required and forbidden fields plus persistent provider-agent conversation constraints and exact errors. | `tests/specs/taskspec/test_agent_taskspec.py::test_agent_spec_rejects_function_target`; `tests/specs/taskspec/test_agent_taskspec.py::test_agent_per_task_conversation_requires_persistent_task` | Declarative target matrix; rejected because error ownership and cross-field conditions become indirect. | Task 2 clean outside review PASS; owner approved 2026-08-05. |
| `RUFF-SUP-053` | `C901` | `1` directives; raw: `C901=1` | State validation preserves timestamp ordering and created, running, spawning, and terminal field constraints. | `tests/specs/taskspec/test_state_transitions.py::test_generated_status_operation_sequences_never_leave_terminal_state` | Generic field-rule engine; rejected because the state contract is clearer as visible branches. | Task 2 clean outside review PASS; owner approved 2026-08-05. |
| `RUFF-SUP-054` | `C901` | `1` directives; raw: `C901=1` | Strict resolved TaskSpecs preserve template exemption and aggregated top-level, target, IO, state, and metadata requirement errors. | `tests/taskspec/test_taskspec_properties.py::test_resolved_taskspec_freezes_spec_and_io` | Split final validation among section helpers; rejected because the resolved-spec completion contract would be scattered. | Task 2 clean outside review PASS; owner approved 2026-08-05. |
| `RUFF-SUP-055` | `C901` | `1` directives; raw: `C901=1` | Declared-option parsing preserves long-option grammar, passthrough grouping, duplicates, normalization, choices, defaults, and required errors. | `tests/core/test_spec_run_input.py::test_parse_declared_run_input_args_parses_string_and_path_values`; `tests/core/test_spec_run_input.py::test_parse_declared_run_input_args_rejects_missing_required_option` | Separate tokenizer and validator phases; rejected because token consumption and declaration ownership are tightly coupled. | Task 2 clean outside review PASS; owner approved 2026-08-05. |
| `RUFF-SUP-056` | `C901` | `1` directives; raw: `C901=1` | Reserved cleanup preserves destruction-protected selection, report-before-delete, age and family limits, deadlines, durable checked marks, retirement, and progress. | `tests/tasks/test_task_monitor.py::test_task_monitor_reserved_cleanup_respects_min_age_gate` | Generic runtime queue sweeper or sharing with terminal-control cleanup; rejected because reserved eligibility and checked-state semantics differ. | Task 2 clean outside review PASS; owner approved 2026-08-05. |
| `RUFF-SUP-057` | `C901` | `1` directives; raw: `C901=1` | Orphan task-log recovery preserves summary gating, bounded family selection, re-ingest before exact deletion, child-ref retirement, and checked marks. | `tests/tasks/test_task_monitor.py::test_task_monitor_recovers_orphan_raw_task_log_rows_after_bad_raw_mark` | Share with ordinary ingest or dead-TID cleanup; rejected because orphan recovery starts from inconsistent Monitor state. | Task 2 clean outside review PASS; owner approved 2026-08-05. |
| `RUFF-SUP-058` | `C901` | `1` directives; raw: `C901=1` | Dead-TID coalescing preserves fetch, re-ingest, summary, family disposition, then exact child-ref deletion order for proven-dead tasks. | `tests/tasks/test_task_monitor.py::test_task_monitor_repair_path_exports_summary_before_deleting_rows` | Share with orphan recovery; rejected because dead-TID proof and mandatory summary/disposition sequence differ. | Task 2 clean outside review PASS; owner approved 2026-08-05. |
| `RUFF-SUP-059` | `C901` | `1` directives; raw: `C901=1` | Endpoint registry pruning preserves per-owner recency before task-status and TID-mapping live-owner proof, with superseded rows excluded from stale selection. | `tests/commands/test_runtime_prune.py::test_endpoint_prune_preserves_live_duplicate_claimants` | Generic runtime-registry selector or sharing with managers; rejected because endpoint grouping and owner proof are distinct. | Task 2 clean outside review PASS; owner approved 2026-08-05. |
| `RUFF-SUP-104` | `C901` | `1` directives; raw: `C901=1` | Import preview ordering preserves source metadata, bounded alias/queue/warning samples, totals, and the message-ID range. | `tests/commands/test_dump_load.py::test_import_report_formatting` | One helper per optional section; rejected because the linear rendering checklist is clearer and its final order remains local. | Task 2 clean outside review PASS; owner approved 2026-08-05. |
| `RUFF-SUP-105` | `C901` | `1` directives; raw: `C901=1` | Dump parsing preserves first-and-unique header rules, exact format/version, line-numbered errors, positive message IDs, and deduplicated runtime-only exclusions. | `tests/commands/test_dump_load.py::test_cmd_load_rejects_legacy_weft_dump_format`; `tests/commands/test_dump_load.py::test_cmd_load_rejects_reserved_zero_id_before_writes`; `tests/commands/test_dump_load.py::test_cmd_load_dry_run` | Record dispatch helpers carrying the mutable plan, header state, skipped-runtime set, and line context; rejected because they reduce streaming-parser locality. | Task 2 clean outside review PASS; owner approved 2026-08-05. |
| `RUFF-SUP-106` | `C901` | `1` directives; raw: `C901=1` | One-shot result waiting preserves task-local and log terminal-proof precedence, wrapper-loss fallback, visible/emitted output, quiet/completion grace, timeout, and queue cleanup. | `tests/commands/test_result.py::test_await_one_shot_result_retains_terminal_ctrl_out_proof`; `tests/commands/test_result.py::test_await_one_shot_result_prefers_task_completed_over_manager_wrapper_lost`; `tests/commands/test_result.py::test_await_one_shot_result_accepts_emitted_stream_when_log_event_is_missed` | The dedicated 2026-08-10 audit found that further extraction would pass the mutable temporal frame and evidence order across scopes or merge distinct persistent semantics; rejected because that reduces locality without exposing reusable policy. | Dedicated source/locality audit and clean review upheld retention; owner authorized the plan disposition 2026-08-10. |
| `RUFF-SUP-107` | `C901` | `1` directives; raw: `C901=1` | Realtime observation never consumes result/control queues and preserves snapshot, stream, state, terminal grace, result, end, cancellation, timeout, and cleanup order across three cursors. | `tests/core/test_ops_shared.py::test_realtime_events_uses_terminal_state_seen_during_materialization`; `tests/core/test_ops_shared.py::test_realtime_events_emits_state_when_terminal_derived_from_snapshot`; `tests/commands/test_result.py::test_iter_task_realtime_events_falls_back_on_malformed_io` | The dedicated 2026-08-10 audit found no small scanner/yield seam: moving cursors and terminal state into helpers would make readers reconstruct one observation protocol across scopes; rejected because it reduces locality without exposing reusable policy. | Dedicated source/locality audit and clean review upheld retention; owner authorized the plan disposition 2026-08-10. |
| `RUFF-SUP-108` | `C901` | `1` directives; raw: `C901=1` | Result materialization preserves precedence among TaskSpec metadata, default/custom/pipeline queue activity, log-derived metadata, terminal proof, batch boundaries, timeout, and cleanup. | `tests/commands/test_result.py::test_await_result_materialization_waits_for_taskspec_after_activity_event`; `tests/commands/test_result.py::test_await_result_materialization_falls_back_on_malformed_io`; `tests/core/test_ops_shared.py::test_realtime_events_uses_terminal_state_seen_during_materialization` | Parallel evidence discovery or detached return-payload builders; rejected because acquisition order is the justification for the selected result surface. | Task 2 clean outside review PASS; owner approved 2026-08-05. |
| `RUFF-SUP-109` | `C901` | `1` directives; raw: `C901=1` | Persistent result waiting returns exactly one work-item batch, tolerates documented boundary skew, avoids stream replay, leaves later batches queued, and preserves terminal/deadline/cleanup precedence. | `tests/commands/test_result.py::test_await_single_result_persistent_returns_one_work_item_batch`; `tests/commands/test_result.py::test_await_single_result_persistent_stream_mode_keeps_next_batch`; `tests/commands/test_result.py::test_await_single_result_tolerates_materialized_boundary_timestamp_skew` | The dedicated 2026-08-10 audit found that removing the suppression would pass cursor, quiet-window, boundary, completion, and deadline state among helpers or merge it with one-shot semantics; rejected because that splits one persistent batch protocol. | Dedicated source/locality audit and clean review upheld retention; owner authorized the plan disposition 2026-08-10. |
| `RUFF-SUP-113` | `C901` | `1` directives; raw: `C901=1` | Inline execution preserves target validation, context, argument/env/stdin normalization, TaskSpec construction, post-TID resolution, interactive/noninteractive wait selection, submission recovery, and structured receipt. | `tests/commands/test_run.py::test_execute_run_inline_returns_structured_result_without_rendering`; `tests/commands/test_run.py::test_run_inline_enqueues_task_before_ensuring_manager`; `tests/cli/test_cli_run.py::test_cli_run_reads_stdin` | New orchestration object or extraction of the completion closure with a large state carrier; rejected because template and input ownership would become indirect. | Task 2 clean outside review PASS; owner approved 2026-08-05. |
| `RUFF-SUP-114` | `C901` | `1` directives; raw: `C901=1` | Run-mode dispatch keeps pipeline, stored spec, command, and function mutually exclusive, rejects mode-specific options before side effects, and reaches exactly one execution path. | `tests/cli/test_cli_run.py::test_cli_run_requires_target`; `tests/cli/test_cli_run.py::test_cli_run_command_and_function_conflict`; `tests/cli/test_cli_run.py::test_cli_run_interactive_json_conflict` | Mode registry or scattered per-mode validators; rejected because mutual-exclusion grammar and error precedence belong together. | Task 2 clean outside review PASS; owner approved 2026-08-05. |
| `RUFF-SUP-115` | `C901` | `1` directives; raw: `C901=1` | Public submit overrides preserve section-specific merge rules, limits/runner nesting, persistent endpoint-claim validation after renaming, TaskSpec validation, and bundle-root identity. | `tests/core/test_client.py::test_submit_spec_and_pipeline_references_return_tasks`; `tests/commands/test_run.py::test_run_spec_via_manager_explicit_name_overrides_name_and_claims_endpoint`; `tests/commands/test_run.py::test_run_pipeline_explicit_name_overrides_pipeline_task_name` | Generic deep merge or per-section helpers carrying most optional arguments; rejected because boundary validation and cross-section precedence become hidden. | Task 2 clean outside review PASS; owner approved 2026-08-05. |
| `RUFF-SUP-116` | `C901` | `1` directives; raw: `C901=1` | Queue-first recovery never falsely rolls back a committed submission: spawned, rejected, queued, and reserved outcomes preserve deletion confirmation, bounded re-observation, claimed-row protection, and startup-error causality. | `tests/commands/test_run.py::test_run_inline_deletes_spawn_request_when_ensure_manager_fails`; `tests/commands/test_run.py::test_reconcile_submitted_spawn_can_wait_past_reserved_claim`; `tests/core/test_ops_shared.py::test_run_adapter_routes_manager_recovery_through_shared_submission` | Generic retry/dequeue-first recovery or detached outcome handlers; rejected because the queued-to-delete-to-reconcile transition and exception cause must remain visible. | Task 2 clean outside review PASS; owner approved 2026-08-05. |
| `RUFF-SUP-117` | `C901` | `1` directives; raw: `C901=1` | Stale-liveness classification preserves terminal authority and the precedence of host PID/runtime evidence, stale windows, live-manager records, newer canonical service owners, and same-owner registry proof. | `tests/commands/test_status.py::test_terminal_log_status_wins_over_weak_live_host_pid`; `tests/commands/test_status.py::test_cmd_status_marks_stale_internal_service_without_owner_failed`; `tests/commands/test_status.py::test_status_tasks_treat_fresh_runtime_less_internal_service_log_as_superseded` | Separate host and service classifiers that duplicate stale/runtime facts; rejected because evidence precedence would become non-local. | Task 2 clean outside review PASS; owner approved 2026-08-05. |
| `RUFF-SUP-118` | `C901` | `1` directives; raw: `C901=1` | Internal-service snapshots collect child, manager-log, registry, pending/reserved queue, manager-desire, and mapping-diagnostic evidence before selecting one ranked candidate per known service. | `tests/commands/test_status.py::test_status_services_prefer_live_duplicate_over_terminal_duplicate`; `tests/commands/test_status.py::test_status_services_report_pending_internal_spawn_request`; `tests/commands/test_status.py::test_status_services_report_task_monitor_external_log_diagnostics` | Additional collector orchestration beyond existing leaf helpers; rejected because completeness and the inputs to final ranking become harder to audit. | Task 2 clean outside review PASS; owner approved 2026-08-05. |
| `RUFF-SUP-119` | `C901` | `1` directives; raw: `C901=1` | Status watch preserves monotonic log cursors, TID/status filtering before output, JSON flushing, monitor-backed waits, interrupt success, honest failure, and resource closure. | `tests/commands/test_status.py::test_watch_task_events_uses_queue_monitor` | Detached record-building and rendering helpers; rejected because cursor advancement and filter/output order are clearer in one tail loop. | Task 2 clean outside review PASS; owner approved 2026-08-05. |
| `RUFF-SUP-122` | `C901` | `1` directives; raw: `C901=1` | Bounded stdin applies one UTF-8 byte limit to buffered-byte and text-only streams, treats closed/unreadable input as empty, and decodes only after the complete bounded read. | `tests/cli/test_cli_run.py::test_cli_run_reads_stdin`; `tests/cli/test_cli_run.py::test_cli_run_rejects_oversized_piped_stdin` | Generic reader adapter; rejected because it risks changing encoding, byte counting, and exception behavior across the two stream protocols. | Task 2 clean outside review PASS; owner approved 2026-08-05. |
| `RUFF-SUP-124` | `C901` | `1` directives; raw: `C901=1` | Process-tree termination snapshots descendants, signals leaves before root, waits boundedly, optionally kills survivors, reaps, and reports only confirmed terminated PIDs while tolerating process races. | `tests/tasks/test_runner.py::test_task_runner_timeout_terminates_command_descendants`; `tests/tasks/test_runner.py::test_command_session_terminate_kills_descendants`; `tests/commands/test_task_commands.py::test_stop_tasks_terminates_active_process_tree` | Split signaling, waiting, and reporting phases into remote helpers; rejected because shared process lists and leaf-before-root ordering would become non-local. | Task 2 clean outside review PASS; owner approved 2026-08-05. |
| `RUFF-SUP-126` | `C901` | `1` directives; raw: `C901=1` | Relative-duration formatting preserves sub-millisecond, millisecond, short-second, pluralization, descending-unit, rounding, and `max_units` behavior. | `tests/commands/test_status.py::test_cmd_status_text_output`; `tests/cli/test_status.py::test_status_reports_running_task_json` | One helper per duration band; rejected because a single visible threshold checklist is easier to comprehend and avoids duplicated rounding rules. | Task 2 clean outside review PASS; owner approved 2026-08-05. |
| `RUFF-SUP-201` | `C901` | `2` directives; raw: `C901=2` | Docker agent protocol tests preserve the prepared image identity and the ordered cancellation-to-container-kill-to-cancelled-outcome path. | `extensions/weft_docker/tests/test_agent_runner.py::test_agent_runner_uses_cached_image_tag_returned_by_ensure_agent_image`; `extensions/weft_docker/tests/test_agent_runner.py::test_agent_runner_reports_cancel_requested_as_cancelled` | Extract shared fake Docker classes or protocol steps; rejected because each stateful fake and its assertions form one temporal proof. | Task 2 clean outside review PASS; owner approved 2026-08-05. |
| `RUFF-SUP-202` | `C901` | `1` directive; raw: `C901=1` | Docker provider execution preserves preparation, mount binding, runtime-handle publication, streaming, cancellation, outcome mapping, and cleanup order. | `extensions/weft_docker/tests/test_agent_runner.py::test_agent_runner_uses_cached_image_tag_returned_by_ensure_agent_image`; `extensions/weft_docker/tests/test_agent_runner.py::test_agent_runner_reports_cancel_requested_as_cancelled` | Pass live lifecycle state among score-driven helpers; rejected pending a separate hardened execution-path plan. | Task 2 clean outside review PASS; owner approved 2026-08-05. |
| `RUFF-SUP-203` | `C901` | `2` directives; raw: `C901=2` | Work-item mounts preserve exact schema defaults and errors, required/optional reference semantics, absolute-path and filesystem-kind checks, and canonical output. | `extensions/weft_docker/tests/test_agent_runner.py::test_agent_runner_mounts_default_to_read_only`; `extensions/weft_docker/tests/test_agent_runner.py::test_resolve_work_item_mounts_reads_document_path_from_metadata`; `extensions/weft_docker/tests/test_agent_runner.py::test_resolve_work_item_mounts_rejects_relative_paths` | Per-field or staged validators; rejected because indexed mount context, error order, and resolved-path state would be duplicated or scattered. | Task 2 clean outside review PASS; owner approved 2026-08-05. |
| `RUFF-SUP-204` | `C901` | `1` directive; raw: `C901=1` | Docker command construction preserves shell-free argv order across limits, network policy, mounts, working directory, image, and inner invocation. | `tests/tasks/test_command_runner_parity.py::test_docker_command_runner_build_profile_materializes_mounts`; `extensions/weft_docker/tests/test_docker_plugin.py::test_command_runner_uses_container_workdir_without_mounting_host_workdir` | One option-appender helper per flag family; rejected because final argv precedence and completeness become non-local. | Task 2 clean outside review PASS; owner approved 2026-08-05. |
| `RUFF-SUP-205` | `C901` | `2` directives; raw: `C901=2` | Docker validation preserves the exact supported command and one-shot provider-agent lanes, mutual exclusions, profile materialization, descriptor/recipe requirements, mounts, preflight, and error precedence. | `extensions/weft_docker/tests/test_docker_plugin.py::test_docker_runner_accepts_one_shot_provider_cli_agent_with_recipe`; `extensions/weft_docker/tests/test_docker_plugin.py::test_docker_runner_rejects_conflicting_agent_mount_targets`; `extensions/weft_docker/tests/test_container_profiles.py::test_container_profile_materializes_defaults_paths_and_env_precedence` | Distribute checks by option family; rejected because cross-field restrictions and boundary error order would become implicit. | Task 2 clean outside review PASS; owner approved 2026-08-05. |
| `RUFF-SUP-206` | `C901` | `2` directives; raw: `C901=2` | Docker observation preserves ordered container lookup fallbacks and a complete, stable reduction from Docker state to `RunnerRuntimeDescription`. | `extensions/weft_docker/tests/test_docker_plugin.py::test_describe_runtime_falls_back_to_container_id_when_name_lookup_misses`; `extensions/weft_docker/tests/test_docker_plugin.py::test_describe_runtime_falls_back_to_container_list_when_name_get_misses` | Separate field projectors or lookup strategies; rejected because fallback precedence and the full external-to-domain mapping would be harder to audit. | Task 2 clean outside review PASS; owner approved 2026-08-05. |
| `RUFF-SUP-208` | `C901` | `1` directive; raw: `C901=1` | Microsandbox option parsing preserves the accepted vocabulary, mode/target coupling, image and executable requirements, mounts, environment, limits, and all cross-field errors in one canonical value. | `extensions/weft_microsandbox/tests/test_options.py::test_unknown_option_fails`; `extensions/weft_microsandbox/tests/test_options.py::test_max_connections_zero_conflicts_with_network_allow`; `extensions/weft_microsandbox/tests/test_options.py::test_workspace_mount_requires_guest_cwd` | Split normalization by option family; rejected because cross-field validation and error precedence would span helpers. | Task 2 clean outside review PASS; owner approved 2026-08-05. |
| `RUFF-SUP-209` | `C901` | `1` directive; raw: `C901=1` | Django public overrides preserve JSON-copy isolation, exact metadata/spec/limits/runner mapping, merge precedence, and decorated-task host-runner restriction. | `integrations/weft_django/tests/test_weft_django.py::test_as_taskspec_for_call_applies_public_submit_overrides`; `integrations/weft_django/tests/test_weft_django.py::test_deferred_native_payload_is_snapshotted_at_registration` | Generic deep merge or per-section override helpers; rejected because the public vocabulary and cross-section precedence become hidden. | Task 2 clean outside review PASS; owner approved 2026-08-05. |
| `RUFF-SUP-210` | `C901` | `1` directive; raw: `C901=1` | Import parsing preserves relative-target resolution, function scope, and `TYPE_CHECKING` scope attribution in one AST visitor. | `tests/architecture/test_import_boundaries.py::test_internal_import_boundaries`; `tests/architecture/test_import_boundaries.py::test_type_checking_runner_backedge_does_not_create_runtime_cycle` | Externalize visitor state; rejected because scope counters and edge attribution would become non-local. | Task 2 clean outside review PASS; owner approved 2026-08-05. |
| `RUFF-SUP-211` | `C901` | `1` directive; raw: `C901=1` | Parallel manager-reuse acceptance preserves concurrent submission, terminal waits, stable convergence, and complete failure snapshots in one ordered live scenario. | `tests/cli/test_cli_run.py::test_cli_run_parallel_no_wait_adopts_active_manager`; `tests/cli/test_cli_run.py::test_parallel_manager_reuse_converges_to_single_manager_under_repeated_bootstrap`; `tests/cli/test_cli_run.py::test_weft_harness_cleanup_preserves_sqlite_integrity_for_parallel_manager_reuse` | Extract protocol phases carrying results, deadlines, harness, and observations; rejected because temporal causality and diagnostics become non-local. | Task 2 clean outside review PASS; owner approved 2026-08-05. |
| `RUFF-SUP-212` | `C901` | `1` directive; raw: `C901=1` | A real manager process exposes the required running process title and the acceptance test performs bounded polling and reliable cleanup. | `tests/cli/test_manager_proctitle.py::test_manager_proctitle_updates_to_running` | Shared process-title polling fixture; rejected because platform guards, launched process, assertion, and cleanup form one small live protocol. | Task 2 clean outside review PASS; owner approved 2026-08-05. |
| `RUFF-SUP-213` | `C901` | `1` directive; raw: `C901=1` | The canonical CLI subprocess harness preserves backend-aware interpreter/environment selection, root preparation, timeout diagnostics, and output normalization. | `tests/cli/test_cli_run.py::test_cli_run_function_inline`; `tests/cli/test_cli_run.py::test_harness_wait_for_completion_reports_cancelled_task` | Split command construction and timeout handling into generic helpers; rejected because test execution policy becomes harder to audit. | Task 2 clean outside review PASS; owner approved 2026-08-05. |
| `RUFF-SUP-214` | `C901` | `1` directive; raw: `C901=1` | Manager termination kills a descendant process tree despite trapped `SIGTERM` and performs bounded reliable cleanup. | `tests/core/test_manager.py::test_manager_terminate_children_kills_sigterm_trapping_descendant_tree` | Reuse a generic process fake; rejected because the test encodes one exact OS-level process topology and cleanup sequence. | Task 2 clean outside review PASS; owner approved 2026-08-05. |
| `RUFF-SUP-215` | `C901` | `1` directive; raw: `C901=1` | The MCP stdio fixture preserves framing, initialization, tool listing/calls, errors, and shutdown as one stateful wire-protocol emulator. | `tests/tasks/test_agent_execution.py::test_consumer_processes_provider_cli_with_explicit_mcp_tool_profile`; `tests/tasks/test_agent_execution.py::test_consumer_live_provider_cli_mcp_smoke` | Build a generic fixture protocol framework or extract request handlers; rejected because protocol state and sequencing would be obscured. | Task 2 clean outside review PASS; owner approved 2026-08-05. |
| `RUFF-SUP-216` | `C901` | `1` directive; raw: `C901=1` | Test CLI context resolution preserves precedence among explicit context, manager spec context, run spec context, init target, and resolved cwd fallback. | `tests/cli/test_cli_run.py::test_cli_run_spec_path`; `tests/cli/test_cli_init.py::test_cmd_init_accepts_in_process_config_overrides` | Separate command-specific scanners; rejected because option precedence and shared path resolution would become dispersed. | Task 2 clean outside review PASS; owner approved 2026-08-05. |
| `RUFF-SUP-217` | `C901` | `1` directive; raw: `C901=1` | Completion waiting preserves reordered-log scanning, terminal-event recognition, cursor progress, timeout diagnostics, and queue closure. | `tests/test_harness_registration.py::test_wait_for_completion_treats_control_stop_as_terminal_event`; `tests/test_harness_registration.py::test_wait_for_completion_timeout_includes_tid_debug_snapshot`; `tests/test_harness_registration.py::test_wait_for_completion_records_polling_stats` | Extract polling steps carrying cursor, high-watermark, and deadline state; rejected because terminal interpretation becomes non-local. | Task 2 clean outside review PASS; owner approved 2026-08-05. |
| `RUFF-SUP-218` | `C901` | `2` directives; raw: `C901=2` | The long-session benchmark gives CLI/API surfaces equivalent run inputs and measures persistent work, aliases, submissions, observations, and cleanup over one explicit sequence. | `tests/cli/test_cli_run.py::test_cli_run_persistent_spec_no_wait_consumes_initial_piped_stdin`; `tests/cli/test_cli_run.py::test_cli_run_parallel_no_wait_adopts_active_manager` | Per-option argv builders or extracted benchmark phases; rejected because they add no reusable owner and hide what lies inside the measured interval. | Task 2 clean outside review PASS; owner approved 2026-08-05. |
| `RUFF-SUP-219` | `C901` | `1` directive; raw: `C901=1` | One deployable function target can independently or jointly exercise memory, CPU, sockets, files, output size, and duration while reliably cleaning resources. | `tests/tasks/test_task_execution.py::test_cleanup_on_exit_process_target`; `tests/tasks/test_runner.py::test_function_timeout_reports_timeout_when_no_result_is_ready` | Split each load dimension into separate targets; rejected because composition is the fixture's purpose and would complicate function-target deployment. | Task 2 clean outside review PASS; owner approved 2026-08-05. |
| `RUFF-SUP-220` | `C901` | `1` directive; raw: `C901=1` | A fatal watcher-owner exit releases every queued synchronous topology mutator rather than deadlocking callers. | `tests/tasks/test_multiqueue_watcher.py::test_owner_fatal_exit_signals_every_queued_mutator` | Extract event, waiter, or thread setup into shared concurrency fixtures; rejected because the deterministic schedule and causality would be hidden. | Task 2 clean outside review PASS; owner approved 2026-08-05. |
| `RUFF-SUP-221` | `C901` | `1` directive; raw: `C901=1` | Closing an AgentSession releases process and queue handles without enqueuing a late stop request. | `tests/tasks/test_runner.py::test_agent_session_close_releases_multiprocessing_handles` | Share generic fake process and queue classes; rejected because locally visible cleanup calls and forbidden late writes are the proof. | Task 2 clean outside review PASS; owner approved 2026-08-05. |
| `RUFF-SUP-222` | `C901` | `1` directive; raw: `C901=1` | TaskMonitor maintenance workers receive isolated snapshots/facades, cannot mutate reactor-owned state, and close every worker-owned resource. | `tests/tasks/test_task_monitor.py::test_task_monitor_worker_local_snapshot_owns_mutable_runtime_resources` | Split exhaustive state and identity assertions; rejected because omissions become easier and the ownership comparison becomes non-local. | Task 2 clean outside review PASS; owner approved 2026-08-05. |
| `RUFF-SUP-224` | `C901` | `1` directive; raw: `C901=1` | Coalescing evidence reports whether SHAs and retrieval cues resolve locally or name retrievable foreign evidence, including local-only ancestry, with honest exits. | Real invocation: `bin/coalesce-check`; governing evidence: `docs/coalescing.md` | Separate parse, reachability, attribution, and report owners; rejected because they share the same evidence positions, repositories, and resolution state. | Task 2 clean outside review PASS; owner approved 2026-08-05. |
| `RUFF-SUP-225` | `C901` | `1` directive; raw: `C901=1` | The Postgres test runner preserves mode validation, dependency checks, temporary container setup, pytest invocation, cleanup, and honest exit propagation. | `tests/system/test_pytest_pg_script.py::test_build_pytest_command_defaults_to_full_tests_tree`; `tests/system/test_pytest_pg_script.py::test_start_postgres_container_keeps_container_until_cleanup` | Further split `main` after existing Docker helpers; rejected because it would scatter the operator-visible lifecycle without a new owner. | Task 2 clean outside review PASS; owner approved 2026-08-05. |
| `RUFF-SUP-226` | `C901` | `2` directives; raw: `C901=2` | Release policy preserves safe local/remote tag decisions plus dirty-tree refusal, extension planning, prechecks, commit/tag/publish ordering, and dry-run behavior. | `tests/system/test_release_script.py::test_plan_tag_action_rejects_existing_tag_on_different_commit`; `tests/system/test_release_script.py::test_main_dry_run_retags_remote_when_requested`; `tests/system/test_release_script.py::test_build_precheck_commands_cover_release_gate_and_quality_gates` | Split the decision table or refactor release orchestration during lint activation; rejected because precedence would hide and mutation/rollback risk requires a separate hardened plan. | Task 2 clean outside review PASS; owner approved 2026-08-05. |
| `RUFF-SUP-227` | `C901` | `1` directive; raw: `C901=1` | Import-cycle detection preserves deterministic strongly connected components, including self-cycles, from one local Tarjan traversal. | `tests/architecture/test_import_boundaries.py::test_weft_has_no_eager_import_cycles`; `tests/architecture/test_import_boundaries.py::test_restored_runner_function_import_cycle_is_detected` | Extract index, stack, or low-link state; rejected because the algorithm becomes harder to verify and reason about. | Task 2 clean outside review PASS; owner approved 2026-08-05. |
| `RUFF-SUP-228` | `C901` | `1` directive; raw: `C901=1` | One repository-wide edge stream enforces core, commands, CLI, and client direction plus Rich and Typer ownership with complete violation reporting. | `tests/architecture/test_import_boundaries.py::test_internal_import_boundaries` | Split layer checks into separately executed tests; rejected because traversal is duplicated and partial enforcement becomes easier. | Task 2 clean outside review PASS; owner approved 2026-08-05. |
| `RUFF-SUP-229` | `C901` | `1` directive; raw: `C901=1` | Static imports, aliases, attributes, and dynamic `getattr` reaches remain on exported SimpleBroker surfaces under one alias interpretation. | `tests/architecture/test_import_boundaries.py::test_simplebroker_surface_guard_fires_for_import_forms`; `tests/architecture/test_import_boundaries.py::test_weft_uses_only_supported_simplebroker_surfaces` | Separate scanners per syntax form; rejected because alias state would be duplicated or interpreted inconsistently. | Task 2 clean outside review PASS; owner approved 2026-08-05. |
| `RUFF-SUP-230` | `C901` | `1` directive; raw: `C901=1` | Recursive CLI JSON registration preserves nested task/manager TIDs, scoped runtime PIDs, caller PIDs, and manager-role context. | `tests/test_harness_registration.py::test_register_from_json_routes_manager_tids_to_worker_tracking` | Separate dict/list walkers or role-specific traversals; rejected because recursion and inherited role context would be duplicated. | Task 2 clean outside review PASS; owner approved 2026-08-05. |
| `RUFF-SUP-231` | `C901` | `1` directive; raw: `C901=1` | A concurrent active heartbeat refresh cannot overwrite a superseded manager registry record. | `tests/core/test_manager.py::test_manager_active_heartbeat_race_preserves_superseded_record` | Replace the local interleaving queue with a generic queue fake; rejected because the decisive race schedule would be hidden. | Task 2 clean outside review PASS; owner approved 2026-08-05. |
| `RUFF-SUP-232` | `C901` | `4` directives; raw: `C901=4` | Each provider CLI fixture preserves its supported argv/input/output grammar, tool behavior, failures, framing, and deliberate provider differences. | `tests/tasks/test_agent_execution.py::test_task_runner_executes_provider_cli_agent_successfully`; `tests/core/test_provider_cli_backend.py::test_provider_cli_runtime_executes_one_shot_request`; `tests/core/test_provider_cli_session_backend.py::test_provider_cli_session_continues_across_turns` | Build cross-provider option handlers or a generic synthetic CLI framework; rejected because provider-specific parsed state and differences become indirect. | Task 2 clean outside review PASS; owner approved 2026-08-05. |
| `RUFF-SUP-233` | `C901` | `1` directive; raw: `C901=1` | Harness shutdown preserves manager-first signalling, task discovery, graceful-to-force escalation, PID settlement, and optional registry draining without task fan-out to worker identities. | `tests/test_harness_registration.py::test_harness_stop_active_managers_stops_registered_task_and_manager_tids`; `tests/test_harness_registration.py::test_harness_stop_active_managers_does_not_fan_out_worker_tid_as_task`; `tests/test_harness_registration.py::test_harness_stop_active_managers_does_not_fan_out_in_process_task_tid` | Extract escalation phases carrying issued-stop sets and live PID state; rejected because shutdown order and idempotence become implicit. | Task 2 clean outside review PASS; owner approved 2026-08-05. |
| `RUFF-SUP-234` | `C901` | `1` directive; raw: `C901=1` | Preserve-database cleanup uses linked deadlines to stop lingering work, reach process quiescence, prove database release, and report exact residual resources without deleting the database. | `tests/test_harness_registration.py::test_harness_cleanup_preserve_database_waits_for_database_release`; `tests/test_harness_registration.py::test_harness_cleanup_preserve_database_extends_windows_release_budget`; `tests/test_harness_registration.py::test_harness_cleanup_preserve_database_raises_if_database_stays_locked` | Split polling, stop escalation, and final diagnosis; rejected because deadlines could reset and cleanup precedence would span helpers. | Task 2 clean outside review PASS; owner approved 2026-08-05. |
| `RUFF-SUP-235` | `C901` | `1` directive; raw: `C901=1` | Database, WAL, and SHM releasability probes safely rename candidates and restore every moved file, surfacing restoration failure. | `tests/test_harness_registration.py::test_locked_database_cleanup_accepts_windows_short_path`; `tests/test_harness_registration.py::test_locked_database_cleanup_uses_configured_artifacts_when_candidate_removed` | Separate candidate mutation from restoration; rejected because transactional rollback ownership would be split. | Task 2 clean outside review PASS; owner approved 2026-08-05. |
| `RUFF-SUP-236` | `C901` | `1` directive; raw: `C901=1` | Dead-producer and sealed-channel evidence is reduced before a stale resource monitor can replace it with a limit result. | `tests/tasks/test_runner.py::test_agent_session_does_not_poll_limits_after_producer_exit` | Share generic process, receiver, or monitor fakes; rejected because the exact same-turn observation order is the proof. | Task 2 clean outside review PASS; owner approved 2026-08-05. |
| `RUFF-SUP-237` | `C901` | `1` directive; raw: `C901=1` | Persistent cleanup cannot extend the absolute drain deadline accepted by the AgentSession terminal protocol. | `tests/tasks/test_runner.py::test_session_stop_effect_cannot_reset_absolute_drain_deadline` | Extract fake-clock or session steps; rejected because clock advancement and deadline causality would become non-local. | Task 2 clean outside review PASS; owner approved 2026-08-05. |
| `RUFF-SUP-238` | `C901` | `1` directive; raw: `C901=1` | A production-shaped terminal backlog either retires within three cycles or reports the exact binding predicate with representative family evidence. | `tests/tasks/test_task_monitor.py::test_retirement_backlog_identifies_binding_stage` | Split workload seeding, monitor driving, and per-arm diagnosis; rejected because workload shape and diagnostic interpretation could drift. | Task 2 clean outside review PASS; owner approved 2026-08-05. |
| `RUFF-SUP-239` | `FLY002` | `12` directives; raw: `FLY002=12` | Each 7-to-18-line embedded program remains visually parseable as source; blank lines, indentation, and the intentional final-newline policy remain explicit. `bin/pytest-pg` intentionally omits a final newline; the other eleven include one. | The twelve owner probes named by MR-269..MR-280 execute or validate the generated modules; the expanded Ruff inventory maps exactly twelve residual FLY002 diagnostics to these sites. | Ruff's escaped single literals hide program structure. Parenthesized adjacent literals add `\n` noise at every line. `textwrap.dedent` adds normalization semantics and obscures byte construction. The same mechanical alternatives were accepted only for the eleven shorter fixtures where the actual formatted result remained local. | Three clean Python experts: initial disposition `NET POSITIVE`; two directive reviews found and corrected only plan defects; final exact directive review `NET POSITIVE`.; owner approved 2026-08-07. |
| `RUFF-SUP-240` | `SIM102` | `1` directive; raw: `SIM102=1` | The harness first proves 100 ms of task/PID quiescence, then invokes an effectful database release probe that performs garbage collection and Windows rename/restore checks. | Three preserve-database cleanup tests pass; source inspection establishes the 100 ms gate precedes the probe; exact residual Ruff inventory maps one SIM102 diagnostic to this gate. | Merge the elapsed-time gate with `_database_files_releasable()`; rejected because it hides the phase boundary and puts an effectful rollback-capable probe inside a compound predicate. | Fresh clean Python expert disposition and final exact review: `NET POSITIVE`; intermediate reviews drove proof tightening.; owner approved 2026-08-07. |
| `RUFF-SUP-241` | `SIM102` | `4` directives; raw: `SIM102=4` | Stop success requires ordered evidence. Absence must first be acceptable or previously observed, then the recorded PID must be dead, then the local-process absence-or-exit gate must pass. Outside the explicit foreground-serve early return, a stopped registry row requires PID death before that gate. Rate-limited fallback advances `pid_checked_at` before liveness and gate checks. | The direct disappearing-record test proves registry absence -> PID extraction -> PID death -> local-process gate. The stopped-status test fires the stopped-row and rate-limited PID paths; source inspection confirms `pid_checked_at = now` precedes that liveness probe. Exact residual Ruff inventory maps four SIM102 diagnostics to these stages. | Flatten nested checks into compound predicates; rejected because PID-death evidence and the local-process gate become one less-legible condition. A shared PID/process/deadline helper could serve all three branches, but would hide both gates behind a boolean and erase why absence, stopped-row, and rate-limited evidence authorize them. Extracting an entire evidence branch would instead move record or timestamp state across the boundary. | Fresh clean Python expert disposition `NET POSITIVE`; four exact reviews `NET NEGATIVE` for proof/wording defects; final closure review `NET POSITIVE` after all corrections.; owner approved 2026-08-07. |
| `RUFF-SUP-242` | `SIM102` | `1` directive; raw: `SIM102=1` | After failed topology publication, only a candidate that exists, was not installed, and remains rollback-owned may be closed; candidate config cleanup stays adjacent and independent. | Two publication-failure owner tests pass; exact residual Ruff inventory maps one SIM102 diagnostic to this rollback gate. | Merge publication failure, candidate state, and rollback ownership into one predicate; rejected because ownership and cleanup phases become less legible. Move rollback to a helper; rejected because candidate waiter/config and publication/install/ownership state would all cross the boundary. | Fresh clean Python expert disposition and final exact review: `NET POSITIVE`; intermediate reviews drove proof tightening.; owner approved 2026-08-07. |
| `RUFF-SUP-245` | `RUF022` | `1` directive; raw: `RUF022=1` | Runtime query functions lead, `serve_manager` stays visible, start/stop mutation capabilities remain beside their matching adapters, and read-only list/status adapters remain together at the end. No behavior depends on observable order. | Full manager-command owner suite; exact residual inventory maps one RUF022 finding to this list. | Alphabetic sorting breaks this visible structure and separates the start/stop mutation pairs. Additional subgroup constants would add indirection to one short inventory. | Fresh clean Python expert disposition: sorting `NET NEGATIVE`; exact review attempt 1 `NET NEGATIVE` for an adjacency overclaim; final review `NET POSITIVE` after correction.; owner approved 2026-08-07. |
| `RUFF-SUP-246` | `RUF022` | `1` directive; raw: `RUF022=1` | Value types, raw operations, command adapters, and alias operations remain distinct and locally comparable. No behavior depends on observable order. | Command and CLI queue owner suites; exact residual inventory maps one RUF022 finding to this list. | Alphabetic sorting destroys the clearest family index of the seven lists. Multiple concatenated subgroup lists would make membership and uniqueness harder to audit. | Fresh clean Python expert disposition: sorting `NET NEGATIVE`; final exact review `NET POSITIVE`.; owner approved 2026-08-07. |
| `RUFF-SUP-248` | `RUF022` | `1` directive; raw: `RUF022=1` | Base/mixin, consumer, observer, monitor/debug/service, and pipeline families remain adjacent. No behavior depends on observable order. | Simple task, observer behavior, and endpoint owner suites; exact residual inventory maps one RUF022 finding to this list. | Alphabetic sorting separates paired task roles. Subgroup concatenation adds more syntax than this small facade warrants. | Fresh clean Python expert disposition: sorting `NET NEGATIVE`; final exact review `NET POSITIVE`.; owner approved 2026-08-07. |
| `RUFF-SUP-249` | `RUF022` | `1` directive; raw: `RUF022=1` | Request/response constructors, parsers/classifiers, startup diagnostics, and response predicates follow protocol roles. No behavior depends on observable order. | Agent-session runner tests; exact residual inventory maps one RUF022 finding to this list. | Alphabetic sorting erases the protocol reading order. Separate subgroup inventories would fragment one compact protocol surface. | Fresh clean Python expert disposition: sorting `NET NEGATIVE`; final exact review `NET POSITIVE`.; owner approved 2026-08-07. |
| `RUFF-SUP-250` | `SIM905` | `2` directives; raw: `SIM905=2` | The 178-field expected ledger and 78-field replaced ledger remain compact, visually grouped, unordered classification sets; replaced remains a subset of expected. | Worker-local snapshot ownership and unclassified-field rejection tests; direct 178/78 uniqueness and subset probe; exact residual inventory maps two SIM905 findings to the `.split()` anchors. | Ruff's explicit item literals expand roughly 100 compact lines to about 262 one-item lines and increase scroll distance between related policies. They do not improve duplicate detection because `frozenset` still collapses duplicates; they exchange whitespace-separator risk for comma/adjacent-literal risk. | Fresh clean Python expert disposition: literal rewrite `NET NEGATIVE`; final exact preserve-and-suppress review `NET POSITIVE`.; owner approved 2026-08-07. |
| `RUFF-SUP-251` | `PLR0402` | `1` directive; raw: `PLR0402=1` | The heartbeat leaf names the public manager-runtime module directly and does not route its dependency through the `weft.core` package marker. | `tests/core/test_heartbeat_helpers.py::test_ensure_heartbeat_service_starts_service_on_first_use`; exact `test_restored_facade_backedges_are_detected` and `test_leaf_modules_do_not_import_their_own_package_facade` architecture proofs; exact residual inventory maps one PLR0402 finding to this line. | Ruff's `from weft.core import manager_runtime` form violates the syntactic package-marker rule. Importing `ensure_manager` directly would remove the module-qualified owner and its test seam: the first-use test patches the canonical manager-runtime module before exercising heartbeat startup. | Slice 2 facade removal reduced this group from three directives to its sole remaining direct leaf-module import; owner approved 2026-08-10. |
| `RUFF-SUP-252` | `PLR0402` | `1` directive; raw: `PLR0402=1` | The core monitor leaf names its sibling implementation owner directly instead of routing a dependency edge through the `weft.core` package marker. | Exact `test_restored_facade_backedges_are_detected`, `test_leaf_modules_do_not_import_their_own_package_facade`, `test_weft_has_no_eager_import_cycles`, and `test_core_task_evidence_import_does_not_initialize_manager_or_monitor` proofs plus monitor-runtime owner suites; exact residual inventory maps one PLR0402 finding to this line. | Ruff's `from weft.core import task_evidence` is runtime-equivalent and acyclic today but violates the syntactic package-marker rule and couples the leaf to package-initializer changes. Wide direct-symbol imports lose the cohesive namespace used by functions and annotations. | Fresh clean Python/architecture expert: Ruff rewrite `NET NEGATIVE`; exact reviews drove claim/proof tightening; final closure review `NET POSITIVE`.; owner approved 2026-08-07. |
| `RUFF-SUP-253` | `PLR0402` | `1` directive; raw: `PLR0402=1` | The command layer names its allowed lower-layer implementation dependency explicitly; the package-from syntax obscures that non-exported implementation owner even though Python resolves the same child module today. | Task-monitor owner suite plus exact `test_internal_import_boundaries`, `test_weft_has_no_eager_import_cycles`, and `test_core_task_evidence_import_does_not_initialize_manager_or_monitor` proofs; exact residual inventory maps one PLR0402 finding to this line. | Ruff's `from weft.core import task_evidence` is runtime-equivalent today but is inconsistent with the repository's explicit-owner convention. A direct three-symbol child import is viable, but removes the repeated `task_evidence.` domain-owner qualifier from calls and annotations. | Fresh clean Python/architecture expert: Ruff rewrite `NET NEGATIVE`; exact reviews drove ownership/proof/alternative corrections; final closure review `NET POSITIVE`.; owner approved 2026-08-07. |
| `RUFF-SUP-255` | `TRY004` | `1` directive; raw: `TRY004=1` | A malformed remote tool response remains an operational protocol failure (`RuntimeError`), distinct from invalid caller arguments. | Direct fake-transport test reaches the exact response shape and asserts class/message; exact residual inventory maps one TRY004 finding to this raise. | `TypeError` misclassifies data received from the fixture server as a local call-signature failure. | Fresh clean Python exception-contract expert: final exact review `NET POSITIVE`.; owner approved 2026-08-07. |
| `RUFF-SUP-256` | `TRY004` | `1` directive; raw: `TRY004=1` | A JSON-RPC error object remains an operational remote failure even when its optional message is not a string. | Direct response-stream test asserts fallback class/message; exact residual inventory maps one TRY004 finding to this raise. | `TypeError` would describe the malformed optional message rather than the governing remote request failure. | Fresh clean Python exception-contract expert: final exact review `NET POSITIVE`.; owner approved 2026-08-07. |
| `RUFF-SUP-257` | `TRY004` | `2` directives; raw: `TRY004=2` | Parsed JSON with an invalid configuration shape remains a value error. Missing and empty-list `mcpServers` values retain their separate proven normalization to `{}`. | Two direct raise-site cases assert exact class/message, including a truthy non-dict `mcpServers`; two characterization cases prove missing and empty-list values normalize to `{}`; exact residual inventory maps two TRY004 findings to these raises. | `TypeError` would change the fixture CLI's validation class for syntactically valid but semantically invalid JSON. Changing normalization is a separate behavior decision. | Fresh clean Python exception-contract expert: final exact review `NET POSITIVE`.; owner approved 2026-08-07. |
| `RUFF-SUP-258` | `TRY004` | `6` directives; raw: `TRY004=6` | Agent validation, environment materialization, and runner validation normalize malformed values inside an already-decoded TaskSpec payload to `ValueError` with field-qualified messages. Python object-contract failures outside this payload-validation boundary remain free to use `TypeError`. | Five existing direct cases plus two public agent-validation cases assert exact built-in class and full field-qualified messages across all three entry points; the two new public cases also assert no chained cause; exact residual inventory maps six TRY004 findings to these raises. | Changing only runner validation creates conflicting taxonomies and was independently `NET NEGATIVE`; candidate preserved at `/tmp/weft-rw-try004-runner-validation`. A coordinated three-module taxonomy change is a wider compatibility change without a governing spec requirement. | Prior five-site clean review `NET POSITIVE`; first enlarged-group review `NET NEGATIVE` for a proof overclaim; corrected fresh exact review `NET POSITIVE`.; owner approved 2026-08-07. |
| `RUFF-SUP-259` | `TRY004` | `2` directives; raw: `TRY004=2` | After JSON serializability is established, a wrong top-level object shape or wrong `spec` object shape remains a parameterization value-contract failure. Non-serializable Python objects retain their separate existing exception path. | Two direct adapter-result tests assert exact built-in class and full message; exact residual inventory maps two TRY004 findings to these raises. | `TypeError` is idiomatic for direct Python argument types but would reclassify decoded adapter result values and split these shape failures from the adjacent forbidden-TID and changed-`spec.type` `ValueError` contract. | Fresh clean Python exception-contract expert: exact review `NET POSITIVE`.; owner approved 2026-08-07. |
| `RUFF-SUP-260` | `TRY004` | `2` directives; raw: `TRY004=2` | Fields declared as plain callable shares must remain stateless function/type objects; fields declared as explicit shares must remain non-callable. A mismatch means the internal worker-snapshot policy is stale or violated. | One direct clone test mutates each classified field in turn, reaches both raises, asserts exact built-in class/full message, restores both fields, and completes normal cleanup; exact residual inventory maps two TRY004 findings to these raises. | `TypeError` would describe the incidental runtime shape while hiding the governing internal classification failure. Moving checks away from the clone loop would reduce locality between classification and enforcement. | Fresh clean Python exception-contract expert: final exact review `NET POSITIVE`.; owner approved 2026-08-07. |
| `RUFF-SUP-261` | `TRY004` | `1` directive; raw: `TRY004=1` | Transition definitions are declarative state-machine values. A source outside the accepted string/frozenset forms remains a constructor validation error beside missing states, duplicate IDs, and invalid transition topology. | Existing set-source test now asserts exact built-in class and full message; exact residual inventory maps one TRY004 finding to this raise. | `TypeError` is idiomatic for a direct argument contract but would split one malformed-transition validation family based only on which transition field failed. | Fresh clean Python exception-contract expert: exact review `NET POSITIVE`.; owner approved 2026-08-07. |
| `RUFF-SUP-262` | `TRY004` | `1` directive; raw: `TRY004=1` | Stored stage targets are declarative pipeline values. A missing/non-mapping `spec` section remains a stage-template validation error beside nested-pipeline, persistent, interactive, and streaming incompatibilities. | A public `compile_linear_pipeline` test supplies a non-mapping stage `spec` and asserts exact built-in class/full message; exact residual inventory maps one TRY004 finding to this raise. | `TypeError` would classify one invalid stage value by incidental Python shape and split the compiler's stage-template `ValueError` family. Directly testing the private validator would weaken boundary coverage. | Fresh clean Python exception-contract expert: exact review `NET POSITIVE`.; owner approved 2026-08-07. |
| `RUFF-SUP-263` | `TRY004` | `1` directive; raw: `TRY004=1` | Explicit-TID submission requires a broker connection that exposes callable `insert_messages`; its absence is a runtime dependency/capability failure. | A direct private-boundary test supplies an object without the method and asserts exact built-in class/full capability-based message; public explicit-TID integration still proves the supported path; exact residual inventory maps one TRY004 finding to this raise. | `TypeError` would describe the incidental missing/callability shape while obscuring the required broker capability. Retaining the old `simplebroker>=4.3.0` text was rejected because Weft already requires `simplebroker>=6.0.1`; missing support now indicates a connection/backend mismatch. Testing only through public submission cannot construct an unsupported real broker without replacing the boundary under test. | Fresh clean Python exception-contract expert: final exact review `NET POSITIVE`.; owner approved 2026-08-07. |
| `RUFF-SUP-264` | `TRY004` | `1` directive; raw: `TRY004=1` | A decoded non-object top-level JSON value is invalid stored spec data, not misuse of the Python `Path` argument. | A public reader test writes a list payload and asserts exact built-in class/full path-qualified message with no chained cause; existing missing/malformed tests prove the separate read/decode path; exact residual inventory maps one TRY004 finding to this raise. | `TypeError` would misclassify the decoded file value as a Python call-signature error. | Fresh clean Python exception-contract expert: exact review `NET POSITIVE`.; owner approved 2026-08-07. |
| `RUFF-SUP-265` | `TRY004` | `1` directive; raw: `TRY004=1` | A decoded non-object checkpoint value is invalid persisted monitor state, not misuse of the Python `Path` or monitor-name arguments. | A direct loader test writes a list payload and asserts exact built-in class/full path-qualified message with no chained cause; the public corrupt-checkpoint test proves command failure rendering; exact residual inventory maps one TRY004 finding to this raise. | `TypeError` would misclassify persisted JSON content as a Python call-signature error and escape `run_task_monitor()`'s current ValueError handling. | Fresh clean Python exception-contract expert: exact review `NET POSITIVE`.; owner approved 2026-08-07. |
| `RUFF-SUP-266` | `TRY004` | `1` directive; raw: `TRY004=1` | A decoded non-object bundled descriptor is invalid provider runtime data, not misuse of the provider-name Python argument. | A public loader test injects a fake package resource containing a JSON list and asserts exact built-in class/full resource-qualified message with no chained cause; supported-provider tests prove all five descriptor names declared by `[AR-7]`; exact residual inventory maps one TRY004 finding to this raise. | `TypeError` would misclassify decoded resource content as Python API misuse. | Fresh clean Python exception-contract expert: exact review `NET POSITIVE`.; owner approved 2026-08-07. |
| `RUFF-SUP-267` | `TRY004` | `6` directives; raw: `TRY004=6` | Values decoded from project-local `agents.json` or `.weft/config.json` must have object shapes at their persisted-data boundaries; malformed stored configuration uses one `ValueError` family. | Existing provider settings cases plus the project-context corrupt-config probe assert the exact built-in class and boundary-specific message; exact residual inventory maps six TRY004 findings to the six raises. | `TypeError` would misclassify user-authored persisted configuration as Python API misuse. Splitting by file would duplicate the same persisted-JSON value contract. | Cleanup plan Slice 8 added the project config boundary; owner approved 2026-08-10. |
| `RUFF-SUP-268` | `TRY004` | `1` directive; raw: `TRY004=1` | The registered adapter satisfies the one-shot `AgentRuntimeAdapter` protocol, but the requested persistent operation needs an additional callable `start_session` capability. | A direct public-boundary test registers a real execute-only adapter and asserts exact built-in `RuntimeError`, full message, and no chained cause; persistent provider-session tests prove the supported path; exact residual inventory maps one TRY004 finding to this raise. | `TypeError` would describe the attribute's incidental non-callable shape rather than the unavailable requested operation. Keeping `ValueError` would incorrectly blame valid caller configuration for a runtime capability gap. | Fresh clean Python refactor/exception-contract expert: exact review `NET POSITIVE`.; owner approved 2026-08-07. |
| `RUFF-SUP-269` | `TRY004` | `4` directives; raw: `TRY004=4` | `RunnerHandle.from_dict()` validates persisted queue/runtime payload values, not direct constructor arguments. Reached wrong required scalar types and nested mapping shapes remain one `ValueError` deserialization family. | Four public factory cases use truthy malformed values and assert exact built-in class/full message with no chained cause; the round-trip and legacy-key tests prove supported and adjacent invalid paths; exact residual inventory maps four TRY004 findings to the four raises. | `TypeError` would bypass existing persisted-handle readers that catch `ValueError` and make the same payload family vary by failed field. Direct `RunnerHandle(...)` misuse is separately changed to `TypeError`. Pre-existing `payload.get(...) or {}` normalization means falsy non-mapping nested values do not all reach these raises: empty `control` later fails missing authority, while empty `observations` or `metadata` normalize to `{}`. Changing that behavior needs separate contract authority, not a lint-driven edit. | Fresh clean Python refactor/exception-contract expert: exact review `NET POSITIVE`.; owner approved 2026-08-07. |
| `RUFF-SUP-270` | `TRY004` | `4` directives; raw: `TRY004=4` | The builtin's `work_item` is queue/stdin task data. Its object shape, decoded JSON root, `refresh`, and `providers` values use one user-input `ValueError` family. | Four public builtin cases replace only platform/Docker availability prerequisites, fire each exact branch in production parse order, and assert exact built-in class/full message with no chained cause; exact residual inventory maps four TRY004 findings to the four raises. | `TypeError` would classify task data by incidental in-process representation. A JSON string and its already-decoded mapping must retain the same value-validation family. Direct private-parser tests would prove less of the public task boundary. Pre-existing behavior accepts any non-string `Sequence` for `providers` despite the word “list” in the message; this lint slice does not alter that input contract. | Fresh clean Python exception-contract expert: exact review `NET POSITIVE`.; owner approved 2026-08-07. |
| `RUFF-SUP-271` | `TRY004` | `3` directives; raw: `TRY004=3` | Catalog construction decodes packaged JSON resources. Wrong root, `spec`, and `supported_platforms` shapes are invalid bundled values in one `ValueError` family. | Three public catalog cases replace only the builtin path inventory, fire each exact branch, and assert exact built-in class/full path-qualified message with no chained cause; existing packaged-asset and platform tests prove supported values; exact residual inventory maps three TRY004 findings to the three raises. | `TypeError` would misclassify packaged resource content as misuse of `builtin_task_catalog()`'s zero-argument Python API. The helper accepts any non-string `Sequence` despite the word “list” in the message, but normal JSON loading makes only arrays/lists observable at the public resource boundary; this lint slice does not alter that pre-existing helper behavior. | Fresh clean Python exception-contract expert: exact review `NET POSITIVE`.; owner approved 2026-08-07. |
| `RUFF-SUP-272` | `TRY004` | `1` directive; raw: `TRY004=1` | The extension plugin validates decoded TaskSpec sections supplied to `validate_taskspec()`. Non-object `spec`, `spec.runner`, and `spec.runner.options` values use the extension's field-qualified `ValueError` validation family. | Three public plugin cases reach the same helper with each exact field name and assert exact built-in class/full message with no chained cause; supported and adjacent validation tests remain in the owner suite; exact residual inventory maps one TRY004 finding to the raise. | `TypeError` would classify the same decoded TaskSpec value differently from the plugin's other configuration validation failures. Keeping an extension-owned group avoids coupling suppression ownership and cardinality to the core decoded-TaskSpec group. | Fresh clean Python exception-contract expert: exact review `NET POSITIVE`.; owner approved 2026-08-07. |
| `RUFF-SUP-273` | `TRY004` | `8` directives; raw: `TRY004=8` | The internal production `parse_options_from_payload()` parser normalizes decoded TaskSpec sections and runner-option values. Reached wrong mode, network, workspace, mounts, env, and section shapes use one field-qualified `ValueError` family alongside invalid values of the correct Python shape. | Six option-shape cases plus five named section/env cases exercise every exact raise and every shared-helper field name through the production parser; all assert exact built-in class/full message with no chained cause; exact residual inventory maps eight TRY004 findings to eight raises. | `TypeError` would split one decoded TaskSpec validation surface according to incidental Python shape. One extension-owned group keeps its inventory independent from other packages. Pre-existing `spec.get("env") or {}` normalizes falsy non-mappings to an empty environment, so the env proof uses a truthy list and does not claim all non-objects are rejected. `_normalize_mounts` accepts any non-string `Sequence`, including tuples, despite the word “list”; JSON normally yields lists, and this slice does not alter direct-call behavior. | Fresh clean Python exception-contract expert: exact review `NET POSITIVE`.; owner approved 2026-08-07. |
| `RUFF-SUP-274` | `TRY004` | `8` directives; raw: `TRY004=8` | `DockerProviderCLIRunner` normalizes decoded runner options for explicit and work-item mounts plus network. Wrong collection, entry, and scalar shapes use one field-qualified `ValueError` family alongside invalid values of the correct Python shape. | Nine constructor cases exercise every exact raise and both source/target names sharing `_normalize_required_text`; all assert exact built-in class/full message with no chained cause; supported mount behavior remains covered; exact residual inventory maps eight TRY004 findings to eight raises. | `TypeError` would split one runner-option validation surface by incidental Python shape. An extension/module-owned group keeps this runtime contract separate from Docker plugin validation and persisted profiles. Both mount normalizers accept any non-string `Sequence` despite saying “list”; JSON normally yields lists. Pre-existing `dict(runner_options or {})` normalizes falsy direct-call values before nested validation; this group does not claim to validate the outer constructor argument or change either behavior. | First clean exact review `NET NEGATIVE` for two non-firing cases; corrected fresh exact review `NET POSITIVE`.; owner approved 2026-08-07. |
| `RUFF-SUP-275` | `TRY004` | `7` directives; raw: `TRY004=7` | Docker plugin validation/materialization normalizes decoded TaskSpec, runner-option, build, mount, environment, and working-directory values. Wrong shapes remain one field-qualified `ValueError` configuration family beside invalid values of the correct Python shape. | Seven direct normalizer cases fire each exact raise and assert exact built-in class/full message with no chained cause; the full 33-test plugin suite covers public composition and supported paths; exact residual inventory maps seven TRY004 findings to seven raises. | `TypeError` would split one decoded configuration surface by incidental Python shape. Direct normalizer tests provide exact site proof without claiming that every shared-helper call name is independently exercised. `_normalize_mounts` and `_string_list` accept any non-string `Sequence`; `_string_list` stringifies elements despite saying “strings”; and `spec.get("env") or {}` normalizes falsy outer values. These pre-existing behaviors remain unchanged. | First clean exact review `NET NEGATIVE` for two inaccurate placement descriptions; corrected fresh exact review `NET POSITIVE`.; owner approved 2026-08-07. |
| `RUFF-SUP-276` | `TRY004` | `1` directive; raw: `TRY004=1` | A non-mapping result from `tomllib.load()` violates the standard-library decoder postcondition. It is an internal capability failure, not malformed caller input. | A direct test replaces only the decoder result, fires the otherwise unreachable defensive branch, and asserts exact built-in `RuntimeError`, full path-qualified message, and no chained cause; the full profile suite covers real TOML success and decode failure; exact residual inventory maps one TRY004 finding to the raise. | `TypeError` would blame the valid `Path` argument, while `ValueError` would classify an impossible decoder result as user-authored malformed TOML. The retained message is conservative but still describes the failed expected root shape; changing it is not needed for this lint slice. | Fresh clean Python refactor/locality review `NET POSITIVE`; separate fresh exact suppression review `NET POSITIVE`.; owner approved 2026-08-07. |
| `RUFF-SUP-277` | `TRY004` | `6` directives; raw: `TRY004=6` | Container-profile materialization normalizes persisted TOML profile fields and explicit TaskSpec environment values. Wrong collection, mapping-entry, scalar, and section shapes remain one field-qualified `ValueError` family beside invalid values of the correct Python shape. | Six direct normalizer cases fire each exact raise and assert exact built-in class/full message with no chained cause; the full profile suite covers public materialization and supported paths; exact residual inventory maps six TRY004 findings to six raises. | `TypeError` would split one configuration validation surface by incidental Python shape. `_resolve_profile_mounts` and `_string_list` accept any non-string `Sequence`; `_string_list` still rejects non-string elements through `_required_text`. Environment collisions follow the declared resolution order `profile_env`, then forwarded host env, then explicit TaskSpec env, with later sources winning. No logic depends on mapping iteration order, and `_mapping_of_strings` performs no key normalization or collision resolution. Pre-existing `env or {}` normalization of falsy values remains unchanged. | First clean exact review `NET NEGATIVE` because the tests did not prove both collision-precedence edges; corrected fresh exact review `NET POSITIVE`.; owner approved 2026-08-07. |
| `RUFF-SUP-278` | `BLE001` | `2` directives; raw: `BLE001=2` | Worker-start and runtime-handle callbacks are caller/extension-supplied observers. Their arbitrary failures must not replace container execution. | One real runner flow makes both callbacks raise, retains an `ok` outcome, and asserts exact bounded warnings without exception info or secret text; exact residual inventory maps two BLE001 findings to the catches. | Narrowing cannot be exhaustive for arbitrary callbacks. Removing containment would let observation failure replace execution. A helper or `contextlib.suppress` would separate each callback from its distinct warning and reduce locality. | Fresh clean Python refactor review and separate fresh exact suppression review: both `NET POSITIVE`.; owner approved 2026-08-07. |
| `RUFF-SUP-279` | `BLE001` | `2` directives; raw: `BLE001=2` | Stdout and stderr consumers are caller/extension-supplied observers. Their arbitrary failures must not replace execution or remove captured output from the outcome. | One real runner flow makes both callbacks raise, retains `ok`, stdout, and stderr values, and asserts exact bounded warnings without exception info, chunk data, or secret text; exact residual inventory maps two BLE001 findings to the catches. | Narrowing cannot be exhaustive for arbitrary callbacks. Removing containment would replace the task result; moving the catches would separate delivery from its failure policy. | Fresh clean Python refactor review and separate fresh exact suppression review: both `NET POSITIVE`.; owner approved 2026-08-07. |
| `RUFF-SUP-280` | `BLE001` | `1` directive; raw: `BLE001=1` | Provider parsing and result construction are extension/runtime boundaries. Any ordinary failure becomes the returned primary `RunnerOutcome(status="error", error=str(exc))`. | Two runs separately make the provider parser and result builder raise; each returns the exact error outcome with captured output preserved; exact residual inventory maps one BLE001 finding to the catch. | Provider adapters and result construction do not share a stable exhaustive exception taxonomy. Narrowing would let an unlisted provider failure escape instead of following the runner contract. An extra warning would duplicate the primary error outcome. | Fresh clean Python exact suppression review `NET POSITIVE`.; owner approved 2026-08-07. |
| `RUFF-SUP-281` | `BLE001` | `1` directive; raw: `BLE001=1` | Docker removal failure must not replace either a completed outcome or an in-flight primary failure. | Removal fails once after success and once while container start raises; success and the exact primary exception both survive, and each path emits one fixed warning without exception info, identity, or secret text; exact residual inventory maps one BLE001 finding to the catch. | Docker SDK, transport, and daemon cleanup failures do not expose one stable exhaustive exception set. Narrowing risks replacing the primary result with an unlisted cleanup failure. Moving cleanup out of `finally` would weaken the lifetime guarantee. | Fresh clean Python refactor review and separate fresh exact suppression review: both `NET POSITIVE`.; owner approved 2026-08-07. |
| `RUFF-SUP-283` | `BLE001`, `S110` | `1` directive; raw: `BLE001=1`, `S110=1` | The registered atexit callback must keep unexpected `Exception`-derived queue, context, or backend cleanup failure out of Python's atexit exception-reporting path. This is last-chance cleanup, not a claim that normal atexit execution lacks usable globals or streams. | A direct callback test constructs only the manager state required by the method, makes `_unregister_manager` raise an exact `RuntimeError`, proves the callback returns, and captures the manager logger at DEBUG plus stdout and stderr to prove observer-free containment; exact residual inventory maps both diagnostics to the single catch. | Narrowing is not exhaustive across the cleanup stack and its backends. Adding logging creates another fallible teardown operation that would itself need containment and cannot recover the failed unregister. `contextlib.suppress(Exception)` would obscure the policy and would not clear BLE001. | First clean review `NET NEGATIVE` corrected the atexit rationale and DEBUG-capture gap; different fresh clean rework review `NET POSITIVE`.; owner approved 2026-08-07. |
| `RUFF-SUP-284` | `BLE001` | `1` directive; raw: `BLE001=1` | Failure while removing the now-redundant interpreter-shutdown hook must not replace already completed manager resource cleanup. CPython's `atexit.unregister()` compares registered callbacks with equality, so an unrelated callback's arbitrary `__eq__` can raise any `Exception`. The callback reference is cleared before the attempt, so repeat cleanup does not retry. | A parameterized real-manager cleanup test injects failure at the `atexit.unregister` boundary instead of mutating process-global atexit state, invokes real Manager cleanup twice, proves exactly one unregister attempt, and asserts one fixed warning without exception info or exception text; exact residual inventory maps one BLE001 finding to the catch. | CPython defines no narrow failure family, and its documented equality comparison can invoke callback-owned code. Removing the catch would allow an unrelated registered callback's equality failure to replace successful manager cleanup. `contextlib.suppress(Exception)` would lose the warning and still require BLE001 disposition; extracting a helper would separate the operation from its failure policy. | Fresh clean Python exact suppression/locality review `NET POSITIVE`; concrete equality mechanism and boundary-injection proof wording corrected before approval.; owner approved 2026-08-07. |
| `RUFF-SUP-286` | `BLE001` | `1` directive; raw: `BLE001=1` | When `__enter__` fails, the original `BaseException` remains primary while every ordinary rollback failure is recorded by bounded step name and later rollback phases still run. Rollback order is cwd, prepared root under patched backend env, environment, then tempdir. | One firing test injects context initialization failure plus prepared-root rollback failure; proves primary exception identity, exact bounded note, declared phase order, restored cwd/environment, tempdir removal, and closed state. A separate invariant forbids inherited or local `__del__`; construction-site audit finds only context managers or explicit teardown owners. | Sequential cleanup without containment can replace the primary failure and skip later phases. A narrow tuple is not exhaustive across backend plugin, filesystem, environment, and tempdir operations. One adjacent loop keeps phase order and failure policy visible; per-phase helpers would fragment the transaction. | Fresh clean Python transactional-lifecycle, refactor/locality, and exact suppression review `NET POSITIVE`.; owner approved 2026-08-07. |
| `RUFF-SUP-287` | `BLE001` | `1` directive; raw: `BLE001=1` | A failure closing one live queue must not prevent attempts to close later queues bound to the harness database; otherwise one connection can keep teardown locked. Failure remains visible through a fixed warning without queue identity or backend exception data. | Two real Queue objects are supplied in a declared test sequence; the first close raises an exact `RuntimeError`, the second close still runs, and one fixed warning contains no exception info or secret text. Exact residual inventory maps one BLE001 to the catch. Production does not depend on `gc.get_objects()` order. | `Queue.close()` crosses arbitrary waiter and backend-connection close implementations with no stable exhaustive exception family. Removing containment can skip every later close. Moving the catch or logging into a helper would separate each queue from its local failure policy; `contextlib.suppress` would hide the warning and retain BLE001. | Fresh clean Python cleanup, refactor/locality, and exact suppression review `NET POSITIVE`.; owner approved 2026-08-07. |
| `RUFF-SUP-288` | `BLE001` | `2` directives; raw: `BLE001=2` | Caller-supplied runtime-handle and worker-start observers must not replace sandbox execution. | One real runner flow makes both lifecycle callbacks and both stream deliveries raise; the `ok` outcome, exact runtime-handle ID, and captured output survive. Exact ordered warnings contain no exception info, PID, handle, callback value, or secret text; residual inventory maps two BLE001 findings to the lifecycle catches. | Arbitrary callbacks have no exhaustive exception family. Removing containment lets observation replace execution. Extracting or merging the two sites would separate publication from its phase-specific warning. | Fresh clean Python runner-callback, refactor/locality, and exact suppression review `NET POSITIVE`; round-two evidence verification `PASS`.; owner approved 2026-08-07. |
| `RUFF-SUP-289` | `BLE001` | `1` directive; raw: `BLE001=1` | Caller-supplied stdout and stderr observers must not replace sandbox execution or remove captured output from the result. | The same real runner flow reaches the shared catch once for each explicit stream name, preserves both outputs and `ok`, and emits exact bounded stdout/stderr warnings with no chunk or exception data; exact residual inventory maps one source finding to the shared catch. | Arbitrary callbacks have no exhaustive exception family. Removing containment replaces the result. Duplicating the helper per stream would add code without changing the one shared delivery contract. | Fresh clean Python runner-callback, refactor/locality, and exact suppression review `NET POSITIVE`.; owner approved 2026-08-07. |
| `RUFF-SUP-290` | `BLE001` | `2` directives; raw: `BLE001=2` | Stop and remove are independent best-effort phases. Failure in either SDK operation must not replace the primary run result; stop failure must not prevent removal. | A direct async cleanup test makes both operations raise, proves declared call order `stop`, then `remove`, and asserts distinct fixed warnings without sandbox name, exception info, or secret text; exact residual inventory maps two BLE001 findings to the catches. | Installed Microsandbox exposes `MicrosandboxError` for documented SDK failures, but this `finally` boundary also contains unexpected ordinary SDK/FFI or adapter failures so cleanup cannot replace the primary outcome. Removing the first catch skips removal; removing the second can replace the primary result. Merging phases or extracting a helper would obscure their independent guarantees. | Fresh clean Python cleanup/refactor/locality and exact suppression review `NET POSITIVE`; exception-family rationale corrected before approval.; owner approved 2026-08-07. |
| `RUFF-SUP-291` | `BLE001` | `1` directive; raw: `BLE001=1` | One failed requested output copy must not prevent attempts for later declared copy-back items. Failure remains visible without exposing host or guest paths. | A direct async test supplies two declared `FileCopyBack` items, makes the first raise, proves the second still runs in declaration order, and asserts one fixed warning without exception info, path data, or secret text; exact residual inventory maps one BLE001 to the catch. | Installed `MicrosandboxError` covers documented SDK failures, but filesystem/SDK/FFI/adapter code may raise other ordinary exceptions; copy-back is best effort after execution and must not replace the primary result or skip later items. Moving containment outside the loop would lose per-item continuation; a helper would separate each declaration from its failure policy. | Fresh clean Python post-execution transfer, refactor/locality, and exact suppression review `NET POSITIVE`.; owner approved 2026-08-07. |
| `RUFF-SUP-292` | `BLE001` | `1` directive; raw: `BLE001=1` | A callable SDK config accessor is optional enrichment. Its failure must not erase the refreshed sandbox state and timestamps. | A direct async description test makes the config accessor raise, preserves exact state/name/timestamps with config omitted, and asserts one fixed warning without sandbox ID, exception info, or secret text; exact residual inventory maps one BLE001 to the catch. | `MicrosandboxError` covers documented SDK failure, but the config member is dynamically shaped and SDK/FFI or version-compatibility code may raise other ordinary exceptions. Removing containment converts optional enrichment into total description failure. A helper would separate the optional field from its local fallback. | Fresh clean Python dynamic-metadata, refactor/locality, and exact suppression review `NET POSITIVE`; callable ownership wording corrected before approval.; owner approved 2026-08-07. |
| `RUFF-SUP-294` | `BLE001` | `1` directive; raw: `BLE001=1` | Once cancellation is requested, kill failure must not skip the subsequent execution-task drain/cancel attempt or replace the cancelled result. | A direct async test makes kill raise with no execution side effect, shortens the real `wait_for` timeout, proves the retained execution task finishes cancelled, returns the cancellation sentinel, and asserts one fixed warning without sandbox identity, exception info, or secret text; exact residual inventory maps one BLE001 to the catch. | `MicrosandboxError` covers documented SDK failure, but cancellation cleanup must also preserve the cancellation result across unexpected ordinary SDK/FFI failures. Narrowing could let adapter drift skip task cleanup. Removing containment skips the drain/cancel attempt; merging kill and wait would obscure their separate policies. `wait_for` is not claimed as a hard wall-clock bound because it waits for cancellation completion. | First fresh review `NET NEGATIVE` corrected the kill-side-effect proof and bounded-wait overclaim; different fresh clean rework review `NET POSITIVE`.; owner approved 2026-08-07. |
| `RUFF-SUP-295` | `BLE001` | `2` directives; raw: `BLE001=2` | Queue handle close and feeder-thread join are independent cleanup phases. Failure in either must not replace the primary runner outcome; close failure must not skip join. | A direct cleanup test makes both methods raise, proves declared call order `close`, then `join`, and asserts distinct fixed warnings without queue identity, exception info, or secret text; exact residual inventory maps two BLE001 findings to the catches. | Multiprocessing queue cleanup reaches finalizers, synchronization primitives, and feeder-thread joining without one stable exhaustive exception family. Removing the first catch skips join; removing the second can replace the primary outcome. Merging phases or extracting a helper would weaken the visible independent-phase policy. | Fresh clean Python multiprocessing/refactor/locality and exact suppression review `NET POSITIVE`; warning privacy is an explicit tradeoff, and test doubles are evidence tooling rather than a production justification.; owner approved 2026-08-07. |
| `RUFF-SUP-298` | `BLE001` | `1` directive; raw: `BLE001=1` | Runtime-owned session cleanup failure during child teardown must not skip request-queue and response-sender cleanup or replace the worker outcome. No contract depends on the relative order of the two IPC closes. | A direct worker-entry test makes runtime `close` raise, proves it occurs before both IPC endpoints are attempted exactly once without ordering those endpoint attempts, and asserts one fixed warning without task/runtime identity, exception info, or secret text; exact residual inventory maps one BLE001 finding to the catch. | Runtime sessions are supplied by configured runtime adapters and do not share one exhaustive close-exception family. Removing containment skips later IPC cleanup and can replace the child outcome. Moving containment around all cleanup phases would weaken independent continuation; extracting one call would reduce locality without reuse. | First fresh review `NET NEGATIVE` rejected an invented dependency on relative IPC-close order; different fresh clean rework review `NET POSITIVE`.; owner approved 2026-08-07. |
| `RUFF-SUP-299` | `BLE001` | `1` directive; raw: `BLE001=1` | The explicit cleanup callback is a caller/adapter seam. Its failure must not make repeated session close fail, and the callback must not be retried after the session is marked closed. | A direct session test makes the callback raise, calls `close()` twice, proves one callback attempt, and asserts one fixed warning without callback/process identity, exception info, or secret text; exact residual inventory maps one BLE001 finding to the catch. | Arbitrary callbacks do not share an exhaustive exception family. Removing containment makes cleanup failure replace the owning shutdown path. Extracting the one callback invocation would reduce locality without reuse. No in-tree production caller currently supplies the callback; deleting the established extension-facing constructor seam requires a separate compatibility audit. | Fresh clean Python callback-boundary/refactor/locality and exact suppression review `NET POSITIVE`.; owner approved 2026-08-07. |
| `RUFF-SUP-300` | `BLE001` | `2` directives; raw: `BLE001=2` | Final metrics capture precedes monitor stop by declared cleanup policy. Failure in either adapter-owned phase must not prevent the next required action or ownership clearing. | A direct session test makes both methods raise, proves declared `last_metrics`, then `stop` order, proves monitor ownership clears and repeated stop is inert, and asserts distinct fixed warnings without monitor/process identity, exception info, or secret text; exact residual inventory maps two BLE001 findings to the catches. | Resource-monitor implementations are configured adapters and do not share one exhaustive exception family. Final metrics must precede `stop()` because an adapter may invalidate metric access while stopping. Removing the first catch skips stop; removing the second skips ownership clearing. Merging phases weakens independent continuation; extracting two calls reduces locality without reuse. | Fresh clean Python monitor-boundary/refactor/locality and exact suppression review `NET POSITIVE`.; owner approved 2026-08-07. |
| `RUFF-SUP-303` | `BLE001` | `2` directives; raw: `BLE001=2` | Final metrics capture precedes monitor stop by declared persistent-session cleanup policy. Failure in either adapter-owned phase must not prevent the next required action or ownership clearing. | A direct session test makes both methods raise, proves declared `last_metrics`, then `stop` order, proves monitor ownership clears and repeated stop is inert, and asserts distinct fixed warnings without monitor/process identity, exception info, or secret text; exact residual inventory maps two BLE001 findings to the catches. | Resource-monitor implementations are configured adapters and do not share one exhaustive exception family. Final metrics must precede `stop()` because an adapter may invalidate metric access while stopping. Removing the first catch skips stop; removing the second skips ownership clearing. | Fresh clean Python monitor-boundary/refactor/locality and exact suppression review `NET POSITIVE`.; owner approved 2026-08-07. |
| `RUFF-SUP-304` | `BLE001` | `1` directive; raw: `BLE001=1` | A configured-monitor snapshot is optional observation. Failure returns the last cached metrics and retains monitor ownership so a later call may recover. | A direct test seeds cached metrics, makes the first adapter snapshot raise, proves the cached object and attached monitor survive, then returns fresh metrics on the second call; it asserts one fixed warning for that failed read without monitor/process identity, exception info, or secret text; exact residual inventory maps one BLE001 finding to the catch. | Configured resource-monitor adapters do not share one exhaustive snapshot-exception family. Removing containment turns optional observation into session failure; detaching or stopping the monitor would prevent transient recovery. Extracting the local fallback would reduce locality without reuse. The property emits one warning per failed read; it is consumed at result boundaries, not in the monitor polling loop. | Fresh clean Python monitor-observation/refactor/locality and exact suppression review `NET POSITIVE`; warning-frequency and catch-comment wording corrected before closure.; owner approved 2026-08-07. |
| `RUFF-SUP-307` | `BLE001` | `1` directive; raw: `BLE001=1` | Stop-request publication is an adapter/IPC boundary. Its arbitrary failure must not skip process termination or final monitor and IPC cleanup. The stop payload is constructed outside the catch, and termination is guaranteed by an inner `finally`. | A direct queue-failure close test proves one publication attempt, skipped join, termination and both final cleanup phases, idempotent repeated close, and one fixed warning without process/queue identity, exception info, or secret text. Separate join tests prove `OSError` containment and that an unexpected `RuntimeError` still triggers termination and both final cleanup phases before propagating when those later phases succeed; exact residual inventory maps one BLE001 finding to queue `put`. | Multiprocessing queue implementations and adapter doubles do not expose one exhaustive publication-exception family. Keeping payload construction and liveness outside the catch prevents programming and lifecycle defects from being hidden. Splitting publication from join makes each exception policy local; nested `finally` blocks express termination ownership before resource release. If termination or final cleanup itself raises, existing Python exception priority applies and retains the join defect as context; this slice adds no broad containment or exception bookkeeping. | First fresh review `NET NEGATIVE` found a live-child leak on unexpected join failure; different fresh clean rework review `NET POSITIVE`.; owner approved 2026-08-07. |
| `RUFF-SUP-308` | `SIM115` | `1` directive; raw: `SIM115=1` | All requested descriptors remain open while resource monitoring samples the workload; cleanup closes every handle FIFO with per-handle `OSError` containment, then attempts every unlink. | Both fd/multiple-limit runner tests pass; a direct `--fds 3` target probe returns the requested result; exact residual inventory maps one SIM115 finding to the descriptor creation site. | The preserved ExitStack candidate at `/tmp/weft-rw-sim115-process-target` nested unrelated setup, changed close order, allowed close errors to escape, and could skip unlinking. `pop_all()` or wrapper machinery is less comprehensible than the explicit lifecycle. | Fresh clean Python expert: context-manager refactor `NET NEGATIVE`; exact one-site suppression recommended; owner approved 2026-08-07. |
| `RUFF-SUP-309` | `BLE001` | `1` directive; raw: `BLE001=1` | Queue enumeration crosses the selected SimpleBroker backend. Any ordinary exception raised by `db.list_queues()` must become the command's one-line error rather than escape as a user-facing CLI traceback. | A boundary-injection test makes a fake broker raise the real `psycopg_pool.PoolTimeout` class and asserts the exact one-line error; the full result owner suite passes. Source audit shows SimpleBroker's Postgres runner acquires from `ConnectionPool.getconn()` before its psycopg translation block, so pool exhaustion can cross `list_queues()` untranslated. | The preserved tuple candidate at `/tmp/weft-rw-result-enumeration` misses `PoolTimeout`; adding it would couple Weft to one backend and remain incomplete for arbitrary supported backend plugins. Moving the catch outward would hide aggregation defects; extraction would reduce locality. | First narrowing review `NET NEGATIVE`; different broad-boundary rework review `NET POSITIVE`; first exact review corrected scope/proof overclaims; different corrected exact review `NET POSITIVE`; owner approved 2026-08-07. |
| `RUFF-SUP-310` | `BLE001` | `1` directive; raw: `BLE001=1` | `WeftTestHarness.dump_debug_state()` is secondary evidence after `subprocess.TimeoutExpired`. Any ordinary diagnostic failure must not replace the primary timeout; fallback text must not expose exception payloads. | A direct `run_cli` test injects the exact `TimeoutExpired` object and a sentinel dump `RuntimeError`, proves object identity on re-raise, asserts the fixed fallback note, and excludes the sentinel secret. | The helper has no closed exception contract and crosses SimpleBroker queue construction/read/close plus state formatting. Removing containment replaces the primary timeout; logging or exception repr can expose task/backend data; moving the catch outward broadens scope; extraction reduces locality. | Behavior review `NET POSITIVE`; first exact review corrected a scope overclaim; different corrected exact review `NET POSITIVE`; owner approved 2026-08-07. |
| `RUFF-SUP-311` | `BLE001` | `5` directives; raw: `BLE001=5` | Configured subprocess resource-monitor failures remain local to startup, polling, and finalization. They must not replace cancellation, timeout, limit, or completion outcomes; cached metrics survive and failures remain visible through fixed phase warnings. | Six focused tests fire all five catches across startup, polling, cancellation, timeout, limit, and completion paths, proving cleanup order, cached-metric fallback, primary outcomes, fixed warnings, and payload privacy. | Configured adapters have no exhaustive exception family. The rejected `contextlib.suppress(Exception)` candidate hid policy; removing containment lets optional monitoring replace execution, while moving catches outward hides unrelated defects. | Initial hidden-boundary refactor `NET NEGATIVE`; explicit rework and final exact reviews `NET POSITIVE`; owner approved 2026-08-07. |
| `RUFF-SUP-312` | `BLE001` | `3` directives; raw: `BLE001=3` | One queue/task fixture cleanup failure must not replace the test result or prevent later declared resources from being closed or stopped. | Three direct fixture-generator tests make the first of two resources fail, prove the second cleanup runs, and assert fixed warnings without exception payloads. | Queue backends and task adapters expose no exhaustive cleanup family. Catching outside the loops loses per-resource continuation; extraction separates each cleanup from its local policy. | Behavior/locality and independent exact reviews `NET POSITIVE`; owner approved 2026-08-07. |
| `RUFF-SUP-313` | `BLE001` | `9` directives; raw: `BLE001=9` | Configured host resource-monitor failures remain local to startup, polling, finalization, and agent-startup cleanup. Monitoring must fail open without replacing task/session outcomes, while cached metrics, cleanup, ownership clearing, and fixed warnings remain intact. | Eight focused tests fire all nine sites and prove missing-PID and startup cleanup, poll caching, final fallback/stop, agent-startup cleanup, call order, primary outcomes, and payload privacy. | Configured adapters have no exhaustive exception family. The rejected silent-suppression candidate obscured policy; only the startup helper is shared across three call sites, while one-call observation/finalization policy stays inline for locality. | Initial hidden-boundary refactor `NET NEGATIVE`; corrected explicit rework and exact reviews `NET POSITIVE`; owner approved 2026-08-07. |
| `RUFF-SUP-314` | `BLE001` | `2` directives; raw: `BLE001=2` | Postgres test-schema cleanup failure must not replace teardown. Batch cleanup continues in declared schema order; single-root cleanup retains the still-live registration. | Direct batch and single-root tests fire both catches, prove continuation or retained registration, preserve scoped schema/config diagnostics, and exclude DSN and exception payloads. | Backend plugins expose no exhaustive cleanup family. Catching outside the batch loop skips later schemas; the rejected fixed-message candidate erased actionable schema/config context. | Initial diagnostic-erasing refactor `NET NEGATIVE`; corrected rework and final exact reviews `NET POSITIVE`; owner approved 2026-08-07. |
| `RUFF-SUP-315` | `BLE001` | `1` directive; raw: `BLE001=1` | A queue failure during secondary harness debug/timeout inspection returns one fixed fallback and closes the opened queue without replacing the primary operation. | A direct test makes `peek_many()` raise, asserts the exact fallback, one close, and no queue-name or exception-text disclosure. | Supported queue backends and plugins expose no exhaustive read family. Moving the catch outward can hide formatting defects or skip the local close policy; extraction reduces locality without reuse. | Behavior/locality and corrected exact reviews `NET POSITIVE`; owner approved 2026-08-07. |
| `RUFF-SUP-316` | `BLE001` | `1` directive; raw: `BLE001=1` | An unexpected ordinary process-helper failure for one identity-checked PID must not skip later registered PIDs or prevent all PID registries from being cleared. | A direct teardown test fails the first termination call independent of set iteration order, proves both PIDs are attempted, all four PID registries clear, and the warning contains no PID or exception payload. | The immediate helper already contains documented psutil/OS failures, but teardown still needs containment for unexpected ordinary helper/library failures. Catching outside the per-PID call loses continuation. | Behavior/locality and corrected exact reviews `NET POSITIVE`; owner approved 2026-08-07. |
| `RUFF-SUP-317` | `BLE001` | `5` directives; raw: `BLE001=5` | Microsandbox stop, kill, describe, post-cancel drain, and dynamic name adapters preserve their stable bool/missing/cancel/name fallbacks while emitting fixed private warnings. Provider-task cancellation maps to the requested result; outer coroutine cancellation propagates. | Direct async tests fire all five catches and prove exact results, warnings, cancellation partition, and absence of sandbox, fallback, or exception payloads. | SDK/FFI and compatible dynamic members expose no exhaustive ordinary family. The rejected `contextlib.suppress(Exception)` candidate hid policy and added bookkeeping; moving catches outward would hide unrelated conversion defects. | Hidden-boundary refactor `NET NEGATIVE`; explicit rework and exact reviews `NET POSITIVE`; owner approved 2026-08-07. |
| `RUFF-SUP-318` | `BLE001` | `6` directives; raw: `BLE001=6` | Ordered TaskSpec validation executes project adapters, profile callables, runner plugins, agent runtimes, and tool-profile hooks. An ordinary stage failure whose stringification succeeds becomes the exact stage/field issue while preserving caller payload and first-failure order. | One six-case test fires every stage and proves exact payload, stage, field, and verbatim message; command and CLI owner suites pass. | No exhaustive family spans arbitrary imports and extension calls. Moving catches outward loses stage precision; a wrapper reduces ordered locality. Verbatim exception text is an acknowledged pre-existing privacy tradeoff. | Behavior/locality and independent exact reviews `NET POSITIVE`; owner approved 2026-08-07. |
| `RUFF-SUP-319` | `BLE001` | `2` directives; raw: `BLE001=2` | Final task-log handler cleanup detaches first, attempts flush then close even for control-flow `BaseException`, and re-raises the first exact failure. A simultaneous close failure is intentionally secondary and discarded. | A direct test injects distinct non-`Exception` failures, proves both attempts in order, exact first-object identity, and detachment; existing tests prove fresh acquisition after single-phase failures. | Catching only `Exception` lets interruption skip close. A combined catch or context manager cannot express both attempts and first-object priority as clearly. | Behavior/locality and independent exact reviews `NET POSITIVE`; owner approved 2026-08-07. |
| `RUFF-SUP-320` | `BLE001` | `1` directive; raw: `BLE001=1` | Arbitrary record-mapping or JSON-hook failure drops the best-effort serve-log diagnostic so no schema-invalid partial event or record/exception data is emitted; control-flow `BaseException` propagates. | A dict subclass raises from `items()` and proves no escape, exact empty stderr, and no secret output. | The preserved partial-event candidate violated required event schema. Narrowing misses arbitrary hooks; moving containment outward hides sink/queue defects. | Partial-event refactor `NET NEGATIVE`; silent rework and independent exact reviews `NET POSITIVE`; owner approved 2026-08-07. |
| `RUFF-SUP-321` | `BLE001` | `1` directive; raw: `BLE001=1` | A replaceable stderr sink's arbitrary write/flush-hook failure must not replace manager work. Fallback uses fixed fd-2 bytes, with low-level `OSError` contained. | Direct tests prove exact fixed bytes without record data and containment of fallback `os.write` failure. | Replaceable file-like hooks have no exhaustive family. Retrying through stderr can recurse; logging record/exception data leaks fields. | Behavior/locality and independent exact reviews `NET POSITIVE`; owner approved 2026-08-07. |
| `RUFF-SUP-322` | `BLE001` | `3` directives; raw: `BLE001=3` | Worker-local store, sink, and queue close failures whose stringification succeeds become close-error entries; later resource categories continue. Exception text is retained verbatim, and queue errors retain queue names. | A six-case matrix fires all three catches for both worker kinds, proves store/sink continuation into later categories, and preserves typed errors plus retry/ownership state without claiming queue-map order. | The three resource owners expose no exhaustive close family. One outer catch loses per-resource ownership; helper extraction fragments the cleanup transaction. | First exact review corrected stringification, diagnostic, and queue-continuation overclaims; different corrected review `NET POSITIVE`; owner approved 2026-08-07. |
| `RUFF-SUP-323` | `BLE001` | `1` directive; raw: `BLE001=1` | Operational-log failure must not replace monitor work. It attempts one fixed data-free fd-2 warning; low-level `OSError` is contained. | Both direct test arms record the exact warning attempt before success or injected `OSError`, and prove the monitor survives without exception payload in the warning. | Dynamic record/emission hooks expose no exhaustive family. Logging the exception can leak or recurse; silent suppression loses bounded visibility. | First exact review corrected the write-success claim and failed-attempt proof; different corrected review `NET POSITIVE`; owner approved 2026-08-07. |
| `RUFF-SUP-324` | `BLE001` | `1` directive; raw: `BLE001=1` | If setup and rollback-close exceptions stringify successfully, setup remains primary while one verbatim close detail is appended and store ownership clears. | A direct dual-failure test proves the exact diagnostic, `_monitor_store is None`, and unavailable status. | Backend close has no exhaustive family. Removing containment replaces the setup failure; moving it outward loses priority and ownership locality. | First exact review corrected diagnostic scope and missing ownership proof; different corrected review `NET POSITIVE`; owner approved 2026-08-07. |
| `RUFF-SUP-325` | `BLE001` | `1` directive; raw: `BLE001=1` | An unexpected worker cleanup exception whose stringification succeeds becomes typed pending cleanup and unavailable monitor status; worker resources still close. Worker and close strings remain verbatim. | A direct custom exception outside the inner typed taxonomy proves first-error priority, unavailable status, separate close failure, sink-to-queue continuation, and no next slice. | Narrowing to the inner operational tuple would let unexpected worker failures escape transport; moving the catch outward obscures result mapping and `finally` cleanup. | Initial evidence review `NET NEGATIVE`; corrected firing review and different exact re-review `NET POSITIVE`; owner approved 2026-08-07. |
| `RUFF-SUP-326` | `BLE001` | `1` directive; raw: `BLE001=1` | An unexpected built-in worker exception whose stringification succeeds becomes a failed result, disables cleanup readiness for that cycle, and still closes resources. The string remains verbatim. | A direct custom exception outside inner operational families proves the exact failed result and sink/queue cleanup. | Worker code spans processor, broker, runtime, and cleanup seams without one exhaustive family. Removing containment can terminate the lane without a typed result. | First exact review corrected diagnostic scope; different corrected review `NET POSITIVE`; owner approved 2026-08-07. |
| `RUFF-SUP-327` | `BLE001` | `1` directive; raw: `BLE001=1` | Ordinary custom-processor configuration, resolution, or invocation exceptions whose stringification succeeds become failed results. The monitor survives, no checkpoint advances, and the string remains verbatim. | A direct invocation failure proves exact failed-cycle state, unchanged checkpoint, and a still-running monitor without claiming later-cycle success. | Configured loading and calls expose no exhaustive family. Moving containment outward loses processor-result mapping; a wrapper reduces locality without reuse. | First exact review corrected boundary, diagnostic, and recovery claims; different corrected review `NET POSITIVE`; owner approved 2026-08-07. |
| `RUFF-SUP-328` | `BLE001` | `1` directive; raw: `BLE001=1` | A replaceable stdin `isatty()` hook may raise any ordinary exception. The helper returns `False`, emits no data itself, and lets control-flow `BaseException` propagate. | A custom stream exception fires the catch and returns exact `False`; a direct control-flow probe confirms `BaseException` propagation. | `OSError` misses user-defined streams. Moving the catch outward hides selection defects; extraction reduces locality without reuse. | New firing test and independent exact review `NET POSITIVE`; owner approved 2026-08-07. |
| `RUFF-SUP-329` | `BLE001` | `1` directive; raw: `BLE001=1` | An optional external cancel callback's ordinary failure means no cancellation request and must not replace execution; control-flow `BaseException` propagates. | Direct tests prove missing, truthy, and raising callbacks plus the control-flow partition. | Arbitrary callbacks have no exhaustive family. Caller-level catches duplicate policy; extraction adds indirection to the shared helper. | Independent exact review `NET POSITIVE`; owner approved 2026-08-07. |
| `RUFF-SUP-332` | `BLE001` | `1` directive; raw: `BLE001=1` | Optional interactive monitor evidence fails silently so client/control and global-log channels remain authoritative; interpretation defects and control-flow `BaseException` remain outside the catch. | A direct session test fires a custom monitor failure, completes through the client channel, and proves the exact result with no output. | Monitor backends expose no exhaustive family. Logging can spam polls; moving the catch outward hides interpretation defects. | Behavior/locality and independent exact reviews `NET POSITIVE`; owner approved 2026-08-07. |
| `RUFF-SUP-334` | `BLE001` | `1` directive; raw: `BLE001=1` | An unexpected ordinary pytest-postgres failure after container assignment becomes exit 1 with verbatim stderr; nonzero exit prints container logs before cleanup. Startup failure before assignment can still strand partial state. | A direct test assigns a named container, fails schema initialization, proves exact stderr and logs-then-cleanup order. | Docker, environment, schema, and pytest operations lack one exhaustive family. Moving the catch outward includes argument parsing; extraction separates lifecycle policy. | Initial proof review `NET NEGATIVE`; corrected post-assignment proof and final exact review `NET POSITIVE`; owner approved 2026-08-07. |
| `RUFF-SUP-335` | `BLE001` | `1` directive; raw: `BLE001=1` | Active-manager selection is optional reconciliation evidence. Ordinary failure degrades that datum to `None`; task-log replay and snapshot reduction continue silently, with potentially reduced reconciliation quality. | A direct test fires a custom selection failure, reconstructs a real completed snapshot, and proves no output. | Registry/backend, normalization, liveness, and dynamic mappings share no exhaustive family. Wider containment hides replay/reducer defects. | Initial evidence review `NET NEGATIVE`; corrected proof and final reviews `NET POSITIVE`; owner approved 2026-08-07. |
| `RUFF-SUP-336` | `BLE001` | `1` directive; raw: `BLE001=1` | After queue acquisition, ordinary watch-loop failure whose stringification and stderr write succeed becomes exit 1; `KeyboardInterrupt` remains successful stop. Diagnostics are verbatim and unsanitized; cleanup/rendering failures may escape. | A direct watch test proves exact exit, stderr, and empty stdout; full owner suites pass. | Queue iteration, monitors, hooks, formatting, and streams lack one exhaustive family. Moving the catch changes scope or leaves loop failures as tracebacks. | Behavior/locality and exact reviews `NET POSITIVE`; owner approved 2026-08-07. |
| `RUFF-SUP-337` | `BLE001` | `1` directive; raw: `BLE001=1` | Ordinary initial context/filter/broker/manager-source failure whose stringification succeeds becomes the phase-specific command error. Later snapshot, service, watch, and rendering work remains outside. Diagnostics are verbatim and unsanitized. | A direct status test fires a custom broker-status failure and proves the exact returned result; full owner suites pass. | Initial sources span multiple open boundaries. A wider catch hides later defects; per-source catches fragment one result transaction. | Behavior/locality and exact reviews `NET POSITIVE`; owner approved 2026-08-07. |
| `RUFF-SUP-338` | `BLE001` | `4` directives; raw: `BLE001=4` | Test helper threads transfer every throwable, including direct `BaseException`, to the owning test thread without changing identity and are joined before handoff state is inspected. | Four non-`Exception` sentinel cases fire every catch, prove exact identity and thread termination, restore callables, and execute the owner lifecycles. | Python threads do not propagate target failures to callers. Narrowing, warnings, executors, or shared wrappers lose identity or lifecycle locality. | Behavior/locality and exact reviews `NET POSITIVE`; owner approved 2026-08-07. |
| `RUFF-SUP-339` | `BLE001` | `2` directives; raw: `BLE001=2` | Ordinary topology-request failure stays request-local and FIFO continues; fatal throwable releases every caller, clears transaction state, and re-raises exactly. Concurrent deferred SIGINT runs public stop but the more specific fatal wins under [QUEUE.8]. | Direct ordinary/fatal/collision tests prove FIFO, exhaustive release, exact identity, cleared state, stop execution, and drive-thread waiter cleanup without mapping/set order. | Narrowing loses unforeseen ordinary defects or arbitrary fatal identity. Branch merging or helpers obscure the local priority ladder and shared transaction state. | Two reviews found masking and proof/spec gaps; corrected final review `NET POSITIVE`; owner approved 2026-08-07. |
| `RUFF-SUP-340` | `BLE001` | `2` directives; raw: `BLE001=2` | During fatal pipeline bootstrap unwind, each admitted child STOP and registry deletion remain independent; cleanup `BaseException` cannot replace the original fatal. | Real queue tests fail two STOPs and post-delete registry cleanup, proving every phase, empty registry, scoped warnings, and exact primary identity. | Narrowing permits cleanup control flow to replace the primary; combined containment loses per-child continuation. | Multiple cleanup-priority reviews corrected masking and registry skips; final `NET POSITIVE`; owner approved 2026-08-07. |
| `RUFF-SUP-341` | `BLE001` | `1` directive; raw: `BLE001=1` | Ordinary pipeline-edge handoff failure becomes failed state/event and applies reserved policy; fatal exits propagate exactly. Error text is raw/unbounded and broken stringification may escape. | Real reservation proves default KEEP retains exact reserved payload and empties source; fatal proof retains running state and identity. | Handoff spans broker/cardinality/serialization/cleanup without exhaustive taxonomy; extraction separates one result transaction. | Corrected reserved-policy proof and exact review `NET POSITIVE`; owner approved 2026-08-07. |
| `RUFF-SUP-342` | `BLE001` | `1` directive; raw: `BLE001=1` | Ordinary host one-shot worker execution failure becomes terminal error with child traceback/diagnostics and closed sender; fatal exits propagate. | Direct ordinary/fatal worker tests prove exact result, traceback, close, and identity. | Configured target execution lacks exhaustive taxonomy; parent conversion loses child traceback. | Exact/locality review `NET POSITIVE`; owner approved 2026-08-07. |
| `RUFF-SUP-343` | `BLE001` | `2` directives; raw: `BLE001=2` | Ordinary host lifecycle callback failure preserves outcome with fixed private warnings; fatal exits propagate exactly while enclosing cleanup owns process/pipes. | Direct tests fire both callbacks for ordinary/fatal partitions and prove outcome/warnings/privacy/identity/cleanup. | Arbitrary callbacks lack exhaustive taxonomy; merging separates phase-specific publication warnings. | Exact/locality review `NET POSITIVE`; owner approved 2026-08-07. |
| `RUFF-SUP-344` | `BLE001` | `2` directives; raw: `BLE001=2` | Confirmed limit violation wins before metrics. Ordinary session monitor failure silently fails open, caches/stops/clears ownership; fatal exits propagate and retain ownership. | Mirrored session tests prove violation priority, fail-open cleanup/cache, and fatal identity in reverse order. | Configured adapters lack exhaustive taxonomy. Silent loss of enforcement is an explicit preserved RM-5.1 tradeoff. | Failing-first fix and exact review `NET POSITIVE`; owner approved 2026-08-07. |
| `RUFF-SUP-345` | `BLE001` | `2` directives; raw: `BLE001=2` | Detached-launch payload and launch ordinary failures return 2 through phase-specific stderr or structured stdout; fatal exits propagate without output. Diagnostics are raw/semantic-raw and unbounded. | Four tests prove both ordinary/fatal phases and channels in both orders. | One handshake partition owns both phases. Pre-existing post-spawn stderr-close failure can lose a live child handle, so no no-child guarantee exists. | Exact caveat/order review `NET POSITIVE`; owner approved 2026-08-07. |
| `RUFF-SUP-346` | `BLE001` | `2` directives; raw: `BLE001=2` | Manager launch-admission failure stays local, clears active/start/retry bookkeeping, and delegates one complete typed result. | Initial/retry tests prove exact request/defaults/error, one delegation, and all cleanup. | Queue/thread admission and partial unwind lack exhaustive taxonomy; wider containment hides request defects. | Corrected full-envelope review `NET POSITIVE`; owner approved 2026-08-07. |
| `RUFF-SUP-347` | `BLE001` | `3` directives; raw: `BLE001=3` | Manager, BaseTask, and Service daemon workers transport every throwable through complete typed owner channels. | Direct fatal tests prove exact identity and complete results/events/transitions without collection order. | Narrowing loses fatal identity; shared wrappers obscure distinct typed contracts. | Corrected metadata review `NET POSITIVE`; owner approved 2026-08-07. |
| `RUFF-SUP-348` | `BLE001` | `2` directives; raw: `BLE001=2` | Ordinary PONG extension or runner-description failure preserves core response/runtime identity with raw fallback errors; fatal exits propagate. | Real keyed PING/PONG and runtime-summary tests prove complete fallback plus fatal partitions. | Dynamic providers/plugins lack exhaustive taxonomy; wider catches hide core construction defects. | Corrected real-control-path review `NET POSITIVE`; owner approved 2026-08-07. |
| `RUFF-SUP-349` | `BLE001` | `1` directive; raw: `BLE001=1` | Consumer ordinary failure becomes complete per-work result; fatal exits cross the outer BaseTask lane unchanged. | One test proves timestamp/transition/streaming/outcome/error and fatal partition. | Wider or fatal containment loses per-work metadata or inner/outer partition. | Corrected result-field review `NET POSITIVE`; owner approved 2026-08-07. |
| `RUFF-SUP-350` | `BLE001` | `2` directives; raw: `BLE001=2` | Ordinary subprocess lifecycle callback failure preserves execution with fixed warnings. Fatal callback propagates exactly after guarded stop/wait/runtime-kill/poll/direct-kill/reap cleanup. | Real-process and deterministic seam tests prove every escalation/failure, production reap before finalizer, identity, and privacy. | Small helper owns shared multistage fatal cleanup; wider catches hide monitor/stream/execution defects. | Three reviews corrected cleanup and false proofs; final `NET POSITIVE`; owner approved 2026-08-07. |
| `RUFF-SUP-351` | `BLE001` | `1` directive; raw: `BLE001=1` | Named TaskSpec resolver failure returns none and command exit 1; fatal exits propagate. Raw error is unbounded and interpreted as Rich markup. | Direct ordinary/fatal tests prove exact fallback or identity/no output. | Named resolution spans open context/store/builtin/filesystem seams; wider containment hides later validation/rendering. | Exact caveat review `NET POSITIVE`; owner approved 2026-08-07. |
| `RUFF-SUP-353` | `BLE001` | `1` directive; raw: `BLE001=1` | Initial result context failure becomes exact phase tuple with raw unbounded diagnostics; fatal exits propagate. Later enumeration/wait/rendering stays outside. | Direct ordinary/fatal tests prove exact tuple or identity. | This context phase differs from RUFF-SUP-309 queue enumeration and remains separate. | Exact phase/locality review `NET POSITIVE`; owner approved 2026-08-07. |
| `RUFF-SUP-354` | `BLE001` | `1` directive; raw: `BLE001=1` | Ordinary manager-start failure is retained while durable queue-first reconciliation decides outcome; fatal exits bypass reconciliation. | Tests prove startup-then-reconcile order, exact results/causes, and fatal zero-reconciliation. | Wider containment hides reconciliation defects and corrupts queue-first policy. | Exact cause/order review `NET POSITIVE`; owner approved 2026-08-07. |
| `RUFF-SUP-355` | `BLE001` | `1` directive; raw: `BLE001=1` | Ordinary Monitor-store failure returns schema-missing none or fixed-shape unknown snapshot with raw unbounded error; fatal exits propagate. | Direct tests prove exact mapping/error or identity; mapping equality is order-independent. | Wider containment hides record-conversion defects. Fixed structure does not mean bounded text. | Exact caveat/locality review `NET POSITIVE`; owner approved 2026-08-07. |
| `RUFF-SUP-356` | `BLE001` | `2` directives; raw: `BLE001=2` | Persistent host-agent worker failures retain phase-specific traceback results; fatal exits propagate after exact cleanup. | Real production-worker and direct seam tests prove result transport, diagnostics, immediate exit, and cleanup cardinality. | Narrowing is incomplete across runtime/transport boundaries; parent-only conversion loses the child traceback. | Four rework reviews; final clean review `NET POSITIVE`; owner approved 2026-08-07. |
| `RUFF-SUP-357` | `BLE001` | `1` directive; raw: `BLE001=1` | Ordinary DOM checker failure returns exit 2 with raw diagnostics; fatal exits propagate. | Direct ordinary/fatal executable-boundary tests. | Per-operation catches fragment the process result boundary. | Clean exact review `NET POSITIVE`; owner approved 2026-08-07. |
| `RUFF-SUP-358` | `BLE001` | `1` directive; raw: `BLE001=1` | The fake provider-CLI test fixture selects the first usable config in declared mapping order and converts ordinary fake tool-call failure to exit 1. Weft does not ship MCP. | A three-entry fixture proves skip-then-first-usable selection and exact-once fake tool invocation; ordinary/fatal tests prove the boundary. | Selection stays outside the catch; narrower tool taxonomy is not exhaustive. | Clean exact order/locality review `NET POSITIVE`; owner approved 2026-08-07. |
| `RUFF-SUP-359` | `BLE001` | `1` directive; raw: `BLE001=1` | Test-only SIGINT mutation threads capture every throwable for parent-visible JSON evidence and always signal completion. | A custom fatal object is reported and the thread terminates. | Narrowing loses fatal thread evidence; helper indirection reduces locality. | Clean exact handoff review `NET POSITIVE`; owner approved 2026-08-07. |
| `RUFF-SUP-360` | `BLE001` | `1` directive; raw: `BLE001=1` | Ordinary long-session benchmark failure returns exit 1 on stderr; fatal exits propagate with no output. | Direct ordinary/fatal channel and identity tests. | Per-operation catches fragment one benchmark result boundary. | Clean exact review `NET POSITIVE`; owner approved 2026-08-07. |
| `RUFF-SUP-361` | `BLE001` | `1` directive; raw: `BLE001=1` | Ordinary polling benchmark failure returns exit 1 on stderr; fatal exits propagate with no output. | Direct ordinary/fatal channel and identity tests. | Per-operation catches fragment one benchmark result boundary. | Clean exact review `NET POSITIVE`; owner approved 2026-08-07. |
| `RUFF-SUP-362` | `BLE001` | `1` directive; raw: `BLE001=1` | Ordinary Microsandbox provider-result conversion failure becomes an error outcome while preserving execution evidence and tempdir cleanup; fatal exits propagate. | Parser/result-builder and fatal cleanup tests. | Narrowing is incomplete across provider adapters; wider containment hides runtime defects. | Clean exact review `NET POSITIVE`; owner approved 2026-08-07. |
| `RUFF-SUP-363` | `BLE001` | `1` directive; raw: `BLE001=1` | Provider-image failures are reported per item while later providers continue in the declared provider sequence; no mapping-order contract is used. | Two-provider ordinary continuation and fatal identity tests. | Catching outside the loop loses continuation; helper extraction separates the owned report. | Clean exact order/locality review `NET POSITIVE`; owner approved 2026-08-07. |
| `RUFF-SUP-364` | `BLE001` | `1` directive; raw: `BLE001=1` | Ordinary Monitor-store access failure returns the exact unavailable status shape; fatal exits propagate. | Direct full-shape and fatal-identity tests. | Wider containment hides successful-status construction defects. | Clean exact review `NET POSITIVE`; owner approved 2026-08-07. |
| `RUFF-SUP-365` | `BLE001` | `1` directive; raw: `BLE001=1` | Ordinary runtime-plugin resolution/description failure returns exact unknown best-effort evidence; fatal exits propagate. | Direct fallback-shape and fatal-identity tests. | Wider containment hides evidence interpretation defects. | Clean exact review `NET POSITIVE`; owner approved 2026-08-07. |
| `RUFF-SUP-366` | `BLE001` | `1` directive; raw: `BLE001=1` | Ordinary caller-owned diagnostic failure remains secondary to the shared reactor timeout assertion; fatal exits propagate unchanged. | `tests/system/test_reactor_driver.py::test_drive_until_keeps_timeout_primary_when_diagnostics_raise`; fatal diagnostic identity test. | Narrow exception lists cannot cover caller-defined diagnostic adapters; allowing ordinary diagnostic failure to escape masks the timeout evidence this callback exists to enrich. | Slice 1 independent implementation review PASS; owner authorized plan implementation 2026-08-11. |
| `RUFF-SUP-367` | `C901` | `1` directive; raw: `C901=1` | Run-result formatting keeps human/JSON output selection and exit mapping in the CLI adapter while the command seam remains structured. | `tests/cli/test_cli_run.py`; `tests/cli/test_cli_pipeline.py` | Split formatters would obscure the exact historical output and exit mapping without reducing semantic work. | [PY-2] adapter boundary; focused CLI parity suite green 2026-08-12. |
| `RUFF-SUP-368` | `C901` | `1` directive; raw: `C901=1` | Result formatting keeps single, all, and stream modes in the one Typer callback that owns their shell routing and exit policy. | `tests/cli/test_cli_result.py`; `tests/cli/test_cli_result_all.py` | Moving branches into commands would violate the I/O-only adapter contract; fragmenting them would hide one-call ownership. | [PY-2] adapter boundary; focused CLI parity suite green 2026-08-12. |
| `RUFF-SUP-369` | `C901` | `1` directive; raw: `C901=1` | Stored-spec preparation preserves parameterization, run-input adaptation, override order, and runtime-context routing as one atomic pre-commit pipeline. Run-input adapters are user-supplied callables whose ordinary failures must cross the public seam as `SubmissionValidationError`. | `tests/core/test_client.py`; `tests/commands/test_submission.py` | Splitting the sequence would make precedence and the pre-TID failure boundary non-local. An exhaustive adapter exception list is impossible and would leak newly introduced adapter failures as raw exceptions. | [PY-3] submission boundary; focused client suites green 2026-08-12. |
| `RUFF-SUP-370` | `BLE001` | `1` directive; raw: `BLE001=1` | Iterator cleanup contains any ordinary cleanup failure so it cannot mask the primary typed error. | `tests/commands/test_boundary.py` | Enumerating backend cleanup exception classes would make cleanup completeness depend on implementation details. | [PY-2] public command boundary; remediation plan final pass 2026-08-12. |

Global raw-`noqa` inventory: `BLE001=124`, `C901=137`, `E402=22`, `F401=5`, `FLY002=12`, `PLR0402=3`, `RUF022=4`, `S110=1`, `SIM102=6`, `SIM115=1`, `SIM905=2`, `TRY004=69`

<!-- BEGIN GENERATED RUFF SUPPRESSION INDEX -->
| Group | Locations | Directives | Raw diagnostics |
|-------|-----------|-----------:|-----------------|
| `RUFF-SUP-001` | `weft/core/agents/provider_cli/container_runtime.py::resolve_provider_container_runtime` | 1 | `C901=1` |
| `RUFF-SUP-002` | `weft/core/agents/provider_cli/registry.py::CodexProvider.resolve_options`; `weft/core/agents/provider_cli/registry.py::CodexProvider.validate_options` | 2 | `C901=2` |
| `RUFF-SUP-003` | `weft/core/agents/provider_cli/registry.py::CodexProvider.build_invocation`; `weft/core/agents/provider_cli/registry.py::CodexProvider.build_session_invocation` | 2 | `C901=2` |
| `RUFF-SUP-005` | `weft/core/manager.py::Manager._prune_expired_manager_registry_entries`; `weft/core/manager.py::Manager._prune_older_self_registry_entries`; `weft/core/manager.py::Manager._register_manager`; `weft/core/manager.py::Manager._self_registry_status_timestamp`; `weft/core/manager.py::Manager._unregister_manager` | 5 | `C901=5` |
| `RUFF-SUP-006` | `weft/core/manager.py::Manager._manager_record_liveness` | 1 | `C901=1` |
| `RUFF-SUP-007` | `weft/core/manager.py::Manager._active_dispatch_manager_records`; `weft/core/manager.py::Manager._maybe_yield_leadership` | 2 | `C901=2` |
| `RUFF-SUP-008` | `weft/core/manager.py::Manager._child_has_exited`; `weft/core/manager.py::Manager._child_terminal_proof_visible` | 2 | `C901=2` |
| `RUFF-SUP-009` | `weft/core/manager.py::Manager._cleanup_children`; `weft/core/manager.py::Manager._terminate_children` | 2 | `C901=2` |
| `RUFF-SUP-010` | `weft/core/manager.py::Manager._apply_spawn_reserved_policy`; `weft/core/manager.py::Manager._cleanup_stale_internal_reserved_queues` | 2 | `C901=2` |
| `RUFF-SUP-011` | `weft/core/manager.py::Manager._handle_work_message` | 1 | `C901=1` |
| `RUFF-SUP-012` | `weft/core/manager.py::Manager._reconcile_managed_services`; `weft/core/manager.py::Manager._service_candidate_from_service_owner_record`; `weft/core/manager.py::Manager._terminate_duplicate_service_candidates` | 3 | `C901=3` |
| `RUFF-SUP-013` | `weft/core/manager.py::Manager._build_autostart_spawn_payload` | 1 | `C901=1` |
| `RUFF-SUP-014` | `weft/core/manager.py::Manager._process_reactor_turn` | 1 | `C901=1` |
| `RUFF-SUP-016` | `weft/core/manager_runtime.py::_manager_record_diagnostic` | 1 | `C901=1` |
| `RUFF-SUP-017` | `weft/core/manager_runtime.py::_await_manager_stop_confirmation`; `weft/core/manager_runtime.py::_mark_manager_stopped`; `weft/core/manager_runtime.py::stop_manager` | 3 | `C901=3` |
| `RUFF-SUP-018` | `weft/core/manager_runtime.py::start_manager` | 1 | `C901=1` |
| `RUFF-SUP-019` | `weft/core/manager_services.py::reduce_managed_service_state` | 1 | `C901=1` |
| `RUFF-SUP-020` | `weft/core/monitor/lifetime_report.py::_taskspec_from_collation` | 1 | `C901=1` |
| `RUFF-SUP-022` | `weft/core/monitor/store.py::MonitorStore.record_task_log_updates` | 1 | `C901=1` |
| `RUFF-SUP-023` | `weft/core/monitor/task_log_collation.py::collate_next_task_log_group` | 1 | `C901=1` |
| `RUFF-SUP-024` | `weft/core/monitor/task_monitor.py::TaskMonitor._close_worker_local_resources`; `weft/core/monitor/task_monitor.py::TaskMonitor._worker_local_monitor_clone` | 2 | `C901=2` |
| `RUFF-SUP-025` | `weft/core/monitor/task_monitor.py::TaskMonitor._ingest_retained_task_log_rows`; `weft/core/monitor/task_monitor.py::TaskMonitor._recover_pre_checkpoint_task_log_rows` | 2 | `C901=2` |
| `RUFF-SUP-026` | `weft/core/monitor/task_monitor.py::TaskMonitor._emit_monitor_store_summaries` | 1 | `C901=1` |
| `RUFF-SUP-027` | `weft/core/monitor/task_monitor.py::TaskMonitor._run_terminal_control_cleanup_slice` | 1 | `C901=1` |
| `RUFF-SUP-028` | `weft/core/monitor/task_monitor.py::TaskMonitor._trim_manager_task_spawned_task_log_rows` | 1 | `C901=1` |
| `RUFF-SUP-029` | `weft/core/outbox.py::process_outbox_message` | 1 | `C901=1` |
| `RUFF-SUP-030` | `weft/core/pipelines.py::_merge_stage_defaults` | 1 | `C901=1` |
| `RUFF-SUP-031` | `weft/core/pruning/apply.py::apply_exact_prune_candidates` | 1 | `C901=1` |
| `RUFF-SUP-032` | `weft/core/pruning/retention.py::_task_log_candidates` | 1 | `C901=1` |
| `RUFF-SUP-033` | `weft/core/pruning/runtime.py::_manager_candidates` | 1 | `C901=1` |
| `RUFF-SUP-034` | `weft/core/runners/host.py::HostTaskRunner._run_one_shot_terminal_handoff` | 1 | `C901=1` |
| `RUFF-SUP-035` | `weft/core/runners/host.py::HostTaskRunner.start_session` | 1 | `C901=1` |
| `RUFF-SUP-036` | `weft/core/runners/subprocess_runner.py::run_monitored_subprocess` | 1 | `C901=1` |
| `RUFF-SUP-037` | `weft/core/runners/subprocess_runner.py::_start_stream_reader` | 2 | `C901=2` |
| `RUFF-SUP-038` | `weft/core/state_machines.py::StateMachine._validate` | 1 | `C901=1` |
| `RUFF-SUP-039` | `weft/core/task_evidence.py::known_tid_evidence` | 1 | `C901=1` |
| `RUFF-SUP-040` | `weft/core/tasks/base.py::BaseTask._validate_reactor_topology` | 1 | `C901=1` |
| `RUFF-SUP-041` | `weft/core/tasks/base.py::BaseTask.run_until_stopped` | 1 | `C901=1` |
| `RUFF-SUP-042` | `weft/core/tasks/consumer.py::Consumer._ensure_outcome_ok` | 1 | `C901=1` |
| `RUFF-SUP-043` | `weft/core/tasks/heartbeat.py::HeartbeatTask._emit_due_registrations` | 1 | `C901=1` |
| `RUFF-SUP-044` | `weft/core/tasks/interactive.py::InteractiveTaskMixin._interactive_finalize_session` | 1 | `C901=1` |
| `RUFF-SUP-045` | `weft/core/tasks/multiqueue_watcher.py::MultiQueueWatcher._apply_pending_topology_mutations`; `weft/core/tasks/multiqueue_watcher.py::MultiQueueWatcher._apply_topology_mutation_on_owner` | 2 | `C901=2` |
| `RUFF-SUP-046` | `weft/core/tasks/service.py::ServiceTask._stop_service_worker` | 1 | `C901=1` |
| `RUFF-SUP-047` | `weft/core/tasks/sessions.py::AgentSession.wait_ready` | 1 | `C901=1` |
| `RUFF-SUP-049` | `weft/core/tasks/sessions.py::AgentSession.terminate` | 1 | `C901=1` |
| `RUFF-SUP-050` | `weft/core/taskspec/model.py::resolve_taskspec_payload` | 1 | `C901=1` |
| `RUFF-SUP-051` | `weft/core/taskspec/model.py::AgentSection.validate_runtime_constraints` | 1 | `C901=1` |
| `RUFF-SUP-052` | `weft/core/taskspec/model.py::SpecSection.validate_target` | 1 | `C901=1` |
| `RUFF-SUP-053` | `weft/core/taskspec/model.py::StateSection.validate_state_consistency` | 1 | `C901=1` |
| `RUFF-SUP-054` | `weft/core/taskspec/model.py::TaskSpec._validate_strict_requirements` | 1 | `C901=1` |
| `RUFF-SUP-055` | `weft/core/taskspec/run_input.py::parse_declared_option_args` | 1 | `C901=1` |
| `RUFF-SUP-056` | `weft/core/monitor/task_monitor.py::TaskMonitor._run_reserved_cleanup_slice` | 1 | `C901=1` |
| `RUFF-SUP-057` | `weft/core/monitor/task_monitor.py::TaskMonitor._recover_orphan_task_log_rows` | 1 | `C901=1` |
| `RUFF-SUP-058` | `weft/core/monitor/task_monitor.py::TaskMonitor._coalesce_and_delete_dead_task_log_rows_for_tids` | 1 | `C901=1` |
| `RUFF-SUP-059` | `weft/core/pruning/runtime.py::_endpoint_candidates` | 1 | `C901=1` |
| `RUFF-SUP-104` | `weft/commands/load.py::ImportReport.format_preview` | 1 | `C901=1` |
| `RUFF-SUP-105` | `weft/commands/load.py::_parse_import_file` | 1 | `C901=1` |
| `RUFF-SUP-106` | `weft/commands/_result_wait.py::await_one_shot_result` | 1 | `C901=1` |
| `RUFF-SUP-107` | `weft/commands/events.py::iter_task_realtime_events` | 1 | `C901=1` |
| `RUFF-SUP-108` | `weft/commands/result.py::_await_result_materialization` | 1 | `C901=1` |
| `RUFF-SUP-109` | `weft/commands/result.py::_await_single_result` | 1 | `C901=1` |
| `RUFF-SUP-113` | `weft/commands/run.py::_execute_inline` | 1 | `C901=1` |
| `RUFF-SUP-114` | `weft/commands/run.py::execute_run` | 1 | `C901=1` |
| `RUFF-SUP-115` | `weft/commands/submission.py::apply_submit_overrides` | 1 | `C901=1` |
| `RUFF-SUP-116` | `weft/commands/submission.py::ensure_manager_after_submission` | 1 | `C901=1` |
| `RUFF-SUP-117` | `weft/commands/system.py::_stale_liveness_reason` | 1 | `C901=1` |
| `RUFF-SUP-118` | `weft/commands/system.py::_collect_internal_service_snapshots` | 1 | `C901=1` |
| `RUFF-SUP-119` | `weft/commands/system.py::_watch_task_events` | 1 | `C901=1` |
| `RUFF-SUP-122` | `weft/helpers/__init__.py::read_limited_stdin` | 1 | `C901=1` |
| `RUFF-SUP-124` | `weft/helpers/__init__.py::terminate_process_tree` | 1 | `C901=1` |
| `RUFF-SUP-126` | `weft/helpers/__init__.py::_format_duration` | 1 | `C901=1` |
| `RUFF-SUP-201` | `extensions/weft_docker/tests/test_agent_runner.py::test_agent_runner_reports_cancel_requested_as_cancelled`; `extensions/weft_docker/tests/test_agent_runner.py::test_agent_runner_uses_cached_image_tag_returned_by_ensure_agent_image` | 2 | `C901=2` |
| `RUFF-SUP-202` | `extensions/weft_docker/weft_docker/agent_runner.py::DockerProviderCLIRunner.run_with_hooks` | 1 | `C901=1` |
| `RUFF-SUP-203` | `extensions/weft_docker/weft_docker/agent_runner.py::_normalize_work_item_mounts`; `extensions/weft_docker/weft_docker/agent_runner.py::_resolve_work_item_mounts` | 2 | `C901=2` |
| `RUFF-SUP-204` | `extensions/weft_docker/weft_docker/plugin.py::DockerCommandRunner._build_docker_command` | 1 | `C901=1` |
| `RUFF-SUP-205` | `extensions/weft_docker/weft_docker/plugin.py::DockerRunnerPlugin._validate_agent_taskspec`; `extensions/weft_docker/weft_docker/plugin.py::DockerRunnerPlugin.validate_taskspec` | 2 | `C901=2` |
| `RUFF-SUP-206` | `extensions/weft_docker/weft_docker/plugin.py::_describe_runtime`; `extensions/weft_docker/weft_docker/plugin.py::_lookup_container` | 2 | `C901=2` |
| `RUFF-SUP-208` | `extensions/weft_microsandbox/weft_microsandbox/_options.py::parse_options` | 1 | `C901=1` |
| `RUFF-SUP-209` | `integrations/weft_django/weft_django/client.py::_apply_taskspec_payload_overrides` | 1 | `C901=1` |
| `RUFF-SUP-210` | `tests/architecture/test_import_boundaries.py::_parse_import_edges` | 1 | `C901=1` |
| `RUFF-SUP-211` | `tests/cli/test_cli_run.py::_run_parallel_manager_reuse_cycle` | 1 | `C901=1` |
| `RUFF-SUP-212` | `tests/cli/test_manager_proctitle.py::test_manager_proctitle_updates_to_running` | 1 | `C901=1` |
| `RUFF-SUP-213` | `tests/conftest.py::run_cli` | 1 | `C901=1` |
| `RUFF-SUP-214` | `tests/core/test_manager.py::test_manager_terminate_children_kills_sigterm_trapping_descendant_tree` | 1 | `C901=1` |
| `RUFF-SUP-215` | `tests/fixtures/mcp_stdio_fixture.py::main` | 1 | `C901=1` |
| `RUFF-SUP-216` | `tests/helpers/test_backend.py::cli_context_root` | 1 | `C901=1` |
| `RUFF-SUP-217` | `tests/helpers/weft_harness.py::WeftTestHarness.wait_for_completion` | 1 | `C901=1` |
| `RUFF-SUP-218` | `tests/long_session_surface_benchmark.py::CliSurface.run_task`; `tests/long_session_surface_benchmark.py::_run_long_session` | 2 | `C901=2` |
| `RUFF-SUP-219` | `tests/tasks/process_target.py::run_task` | 1 | `C901=1` |
| `RUFF-SUP-220` | `tests/tasks/test_multiqueue_watcher.py::test_owner_fatal_exit_signals_every_queued_mutator` | 1 | `C901=1` |
| `RUFF-SUP-221` | `tests/tasks/test_runner.py::test_agent_session_close_releases_multiprocessing_handles` | 1 | `C901=1` |
| `RUFF-SUP-222` | `tests/tasks/test_task_monitor.py::test_task_monitor_worker_local_snapshot_owns_mutable_runtime_resources` | 1 | `C901=1` |
| `RUFF-SUP-224` | `bin/coalesce-check::main` | 1 | `C901=1` |
| `RUFF-SUP-225` | `bin/pytest-pg::main` | 1 | `C901=1` |
| `RUFF-SUP-226` | `bin/release.py::main`; `bin/release.py::plan_tag_action` | 2 | `C901=2` |
| `RUFF-SUP-227` | `tests/architecture/test_import_boundaries.py::_strongly_connected_components` | 1 | `C901=1` |
| `RUFF-SUP-228` | `tests/architecture/test_import_boundaries.py::test_internal_import_boundaries` | 1 | `C901=1` |
| `RUFF-SUP-229` | `tests/architecture/test_import_boundaries.py::_simplebroker_surface_violations` | 1 | `C901=1` |
| `RUFF-SUP-230` | `tests/conftest.py::_register_from_json` | 1 | `C901=1` |
| `RUFF-SUP-231` | `tests/core/test_manager.py::test_manager_active_heartbeat_race_preserves_superseded_record` | 1 | `C901=1` |
| `RUFF-SUP-232` | `tests/fixtures/provider_cli_fixture.py::_run_claude`; `tests/fixtures/provider_cli_fixture.py::_run_codex`; `tests/fixtures/provider_cli_fixture.py::_run_gemini_or_qwen`; `tests/fixtures/provider_cli_fixture.py::_run_opencode` | 4 | `C901=4` |
| `RUFF-SUP-233` | `tests/helpers/weft_harness.py::WeftTestHarness._stop_active_managers` | 1 | `C901=1` |
| `RUFF-SUP-234` | `tests/helpers/weft_harness.py::WeftTestHarness._cleanup_preserving_database` | 1 | `C901=1` |
| `RUFF-SUP-235` | `tests/helpers/weft_harness.py::WeftTestHarness._database_files_releasable` | 1 | `C901=1` |
| `RUFF-SUP-236` | `tests/tasks/test_runner.py::test_agent_session_does_not_poll_limits_after_producer_exit` | 1 | `C901=1` |
| `RUFF-SUP-237` | `tests/tasks/test_runner.py::test_session_stop_effect_cannot_reset_absolute_drain_deadline` | 1 | `C901=1` |
| `RUFF-SUP-238` | `tests/tasks/test_task_monitor.py::test_retirement_backlog_identifies_binding_stage` | 1 | `C901=1` |
| `RUFF-SUP-239` | `bin/pytest-pg::_initialize_broker_schema`; `tests/cli/test_cli_run.py::test_cli_run_spec_bundle_declared_args_shape_work_item`; `tests/cli/test_cli_run.py::test_cli_run_spec_bundle_parameterization_materializes_before_run_input`; `tests/cli/test_cli_run.py::test_cli_run_spec_bundle_parameterization_requires_declared_parameter`; `tests/cli/test_cli_validate.py::test_validate_taskspec_bundle_directory_loads_bundle_local_environment_profile`; `tests/core/test_agent_resolution.py::test_bundle_local_resolver_and_tool_profile`; `tests/core/test_environment_profiles.py::test_materialize_runner_environment_loads_bundle_local_profile`; `tests/core/test_spec_parameterization.py::test_materialize_taskspec_template_rejects_spec_type_change`; `tests/core/test_spec_parameterization.py::test_materialize_taskspec_template_request_payload_is_json_like_copy`; `tests/core/test_spec_parameterization.py::test_materialize_taskspec_template_supports_bundle_local_adapter`; `tests/helpers/long_session_utils.py::write_command_script`; `tests/tasks/test_runner.py::test_task_runner_agent_session_startup_uses_dedicated_ready_timeout` | 12 | `FLY002=12` |
| `RUFF-SUP-240` | `tests/helpers/weft_harness.py::WeftTestHarness._cleanup_preserving_database` | 1 | `SIM102=1` |
| `RUFF-SUP-241` | `weft/core/manager_runtime.py::_await_manager_stop_confirmation` | 4 | `SIM102=4` |
| `RUFF-SUP-242` | `weft/core/tasks/multiqueue_watcher.py::MultiQueueWatcher._apply_topology_mutation_on_owner` | 1 | `SIM102=1` |
| `RUFF-SUP-245` | `weft/commands/manager.py::<module>` | 1 | `RUF022=1` |
| `RUFF-SUP-246` | `weft/commands/queue.py::<module>` | 1 | `RUF022=1` |
| `RUFF-SUP-248` | `weft/core/tasks/__init__.py::<module>` | 1 | `RUF022=1` |
| `RUFF-SUP-249` | `weft/core/tasks/agent_session_protocol.py::<module>` | 1 | `RUF022=1` |
| `RUFF-SUP-250` | `weft/_constants.py::<module>` | 2 | `SIM905=2` |
| `RUFF-SUP-251` | `weft/core/heartbeat.py::<module>` | 1 | `PLR0402=1` |
| `RUFF-SUP-252` | `weft/core/monitor/runtime.py::<module>` | 1 | `PLR0402=1` |
| `RUFF-SUP-253` | `weft/commands/task_monitor.py::<module>` | 1 | `PLR0402=1` |
| `RUFF-SUP-255` | `tests/fixtures/mcp_stdio_fixture.py::call_fixture_tool` | 1 | `TRY004=1` |
| `RUFF-SUP-256` | `tests/fixtures/mcp_stdio_fixture.py::_read_response` | 1 | `TRY004=1` |
| `RUFF-SUP-257` | `tests/fixtures/provider_cli_fixture.py::_load_claude_mcp_servers` | 2 | `TRY004=2` |
| `RUFF-SUP-258` | `weft/core/agents/validation.py::_require_mapping`; `weft/core/environment_profiles.py::_mapping_of_strings`; `weft/core/environment_profiles.py::_require_mapping`; `weft/core/environment_profiles.py::_require_text`; `weft/core/runner_validation.py::_require_mapping`; `weft/core/runner_validation.py::_require_text` | 6 | `TRY004=6` |
| `RUFF-SUP-259` | `weft/core/taskspec/parameterization.py::invoke_parameterization_adapter`; `weft/core/taskspec/parameterization.py::materialize_taskspec_template` | 2 | `TRY004=2` |
| `RUFF-SUP-260` | `weft/core/monitor/task_monitor.py::TaskMonitor._worker_local_monitor_clone` | 2 | `TRY004=2` |
| `RUFF-SUP-261` | `weft/core/state_machines.py::StateMachine._source_states` | 1 | `TRY004=1` |
| `RUFF-SUP-262` | `weft/core/pipelines.py::_validate_stage_template` | 1 | `TRY004=1` |
| `RUFF-SUP-263` | `weft/core/spawn_requests.py::_write_spawn_request_with_timestamp` | 1 | `TRY004=1` |
| `RUFF-SUP-264` | `weft/core/spec_store.py::_read_json` | 1 | `TRY004=1` |
| `RUFF-SUP-265` | `weft/commands/task_monitor.py::_load_checkpoint` | 1 | `TRY004=1` |
| `RUFF-SUP-266` | `weft/core/agents/provider_cli/container_runtime.py::get_provider_container_runtime_descriptor` | 1 | `TRY004=1` |
| `RUFF-SUP-267` | `weft/context.py::_load_project_config`; `weft/core/agents/provider_cli/settings.py::_load_json_mapping`; `weft/core/agents/provider_cli/settings.py::_provider_settings_mapping`; `weft/core/agents/provider_cli/settings.py::ensure_provider_cli_project_executable`; `weft/core/agents/provider_cli/settings.py::load_provider_cli_project_settings` | 6 | `TRY004=6` |
| `RUFF-SUP-268` | `weft/core/agents/runtime.py::start_agent_runtime_session` | 1 | `TRY004=1` |
| `RUFF-SUP-269` | `weft/ext.py::RunnerHandle.from_dict` | 4 | `TRY004=4` |
| `RUFF-SUP-270` | `weft/builtins/agent_images.py::_normalize_provider_list`; `weft/builtins/agent_images.py::_parse_prepare_request` | 4 | `TRY004=4` |
| `RUFF-SUP-271` | `weft/builtins/__init__.py::_normalize_supported_platforms`; `weft/builtins/__init__.py::builtin_task_catalog` | 3 | `TRY004=3` |
| `RUFF-SUP-272` | `extensions/weft_macos_sandbox/weft_macos_sandbox/plugin.py::_require_mapping` | 1 | `TRY004=1` |
| `RUFF-SUP-273` | `extensions/weft_microsandbox/weft_microsandbox/_options.py::_mapping_of_strings`; `extensions/weft_microsandbox/weft_microsandbox/_options.py::_normalize_mode`; `extensions/weft_microsandbox/weft_microsandbox/_options.py::_normalize_mounts`; `extensions/weft_microsandbox/weft_microsandbox/_options.py::_normalize_network`; `extensions/weft_microsandbox/weft_microsandbox/_options.py::_normalize_workspace_mode`; `extensions/weft_microsandbox/weft_microsandbox/_options.py::_require_mapping` | 8 | `TRY004=8` |
| `RUFF-SUP-274` | `extensions/weft_docker/weft_docker/agent_runner.py::_normalize_mounts`; `extensions/weft_docker/weft_docker/agent_runner.py::_normalize_optional_text`; `extensions/weft_docker/weft_docker/agent_runner.py::_normalize_required_text`; `extensions/weft_docker/weft_docker/agent_runner.py::_normalize_work_item_mounts` | 8 | `TRY004=8` |
| `RUFF-SUP-275` | `extensions/weft_docker/weft_docker/plugin.py::_mapping_of_strings`; `extensions/weft_docker/weft_docker/plugin.py::_normalize_mounts`; `extensions/weft_docker/weft_docker/plugin.py::_normalize_required_text`; `extensions/weft_docker/weft_docker/plugin.py::_optional_string`; `extensions/weft_docker/weft_docker/plugin.py::_require_mapping`; `extensions/weft_docker/weft_docker/plugin.py::_string_list` | 7 | `TRY004=7` |
| `RUFF-SUP-276` | `extensions/weft_docker/weft_docker/profiles.py::_load_profile_file` | 1 | `TRY004=1` |
| `RUFF-SUP-277` | `extensions/weft_docker/weft_docker/profiles.py::_mapping_of_strings`; `extensions/weft_docker/weft_docker/profiles.py::_require_mapping`; `extensions/weft_docker/weft_docker/profiles.py::_required_text`; `extensions/weft_docker/weft_docker/profiles.py::_resolve_profile_mounts`; `extensions/weft_docker/weft_docker/profiles.py::_string_list` | 6 | `TRY004=6` |
| `RUFF-SUP-278` | `extensions/weft_docker/weft_docker/agent_runner.py::DockerProviderCLIRunner.run_with_hooks` | 2 | `BLE001=2` |
| `RUFF-SUP-279` | `extensions/weft_docker/weft_docker/agent_runner.py::DockerProviderCLIRunner.run_with_hooks` | 2 | `BLE001=2` |
| `RUFF-SUP-280` | `extensions/weft_docker/weft_docker/agent_runner.py::DockerProviderCLIRunner.run_with_hooks` | 1 | `BLE001=1` |
| `RUFF-SUP-281` | `extensions/weft_docker/weft_docker/agent_runner.py::DockerProviderCLIRunner.run_with_hooks` | 1 | `BLE001=1` |
| `RUFF-SUP-283` | `weft/core/manager.py::Manager._atexit_unregister` | 1 | `BLE001=1`, `S110=1` |
| `RUFF-SUP-284` | `weft/core/manager.py::Manager._unregister_atexit_callback` | 1 | `BLE001=1` |
| `RUFF-SUP-286` | `tests/helpers/weft_harness.py::WeftTestHarness._rollback_failed_enter` | 1 | `BLE001=1` |
| `RUFF-SUP-287` | `tests/helpers/weft_harness.py::WeftTestHarness._close_live_database_queues` | 1 | `BLE001=1` |
| `RUFF-SUP-288` | `extensions/weft_microsandbox/weft_microsandbox/plugin.py::MicrosandboxRunner._execute`; `extensions/weft_microsandbox/weft_microsandbox/plugin.py::_safe_callback` | 2 | `BLE001=2` |
| `RUFF-SUP-289` | `extensions/weft_microsandbox/weft_microsandbox/plugin.py::_emit_stream` | 1 | `BLE001=1` |
| `RUFF-SUP-290` | `extensions/weft_microsandbox/weft_microsandbox/_runtime.py::_cleanup_sandbox` | 2 | `BLE001=2` |
| `RUFF-SUP-291` | `extensions/weft_microsandbox/weft_microsandbox/_runtime.py::_copy_back_files` | 1 | `BLE001=1` |
| `RUFF-SUP-292` | `extensions/weft_microsandbox/weft_microsandbox/_runtime.py::MicrosandboxRuntime._describe_async` | 1 | `BLE001=1` |
| `RUFF-SUP-294` | `extensions/weft_microsandbox/weft_microsandbox/_runtime.py::_exec_with_cancel` | 1 | `BLE001=1` |
| `RUFF-SUP-295` | `weft/core/runners/host.py::HostTaskRunner._close_mp_queue` | 2 | `BLE001=2` |
| `RUFF-SUP-298` | `weft/core/runners/host.py::_agent_session_worker_entry` | 1 | `BLE001=1` |
| `RUFF-SUP-299` | `weft/core/tasks/sessions.py::CommandSession.close` | 1 | `BLE001=1` |
| `RUFF-SUP-300` | `weft/core/tasks/sessions.py::CommandSession.stop_monitor` | 2 | `BLE001=2` |
| `RUFF-SUP-303` | `weft/core/tasks/sessions.py::AgentSession.stop_monitor` | 2 | `BLE001=2` |
| `RUFF-SUP-304` | `weft/core/tasks/sessions.py::AgentSession.last_metrics` | 1 | `BLE001=1` |
| `RUFF-SUP-307` | `weft/core/tasks/sessions.py::AgentSession.close` | 1 | `BLE001=1` |
| `RUFF-SUP-308` | `tests/tasks/process_target.py::run_task` | 1 | `SIM115=1` |
| `RUFF-SUP-309` | `weft/commands/result.py::_collect_all_results` | 1 | `BLE001=1` |
| `RUFF-SUP-310` | `tests/conftest.py::run_cli` | 1 | `BLE001=1` |
| `RUFF-SUP-311` | `weft/core/runners/subprocess_runner.py::_last_monitor_metrics`; `weft/core/runners/subprocess_runner.py::_stop_monitor`; `weft/core/runners/subprocess_runner.py::run_monitored_subprocess` | 5 | `BLE001=5` |
| `RUFF-SUP-312` | `tests/conftest.py::broker_env`; `tests/conftest.py::queue_factory`; `tests/conftest.py::task_factory` | 3 | `BLE001=3` |
| `RUFF-SUP-313` | `weft/core/runners/host.py::HostTaskRunner._run_one_shot_terminal_handoff`; `weft/core/runners/host.py::HostTaskRunner.start_agent_session`; `weft/core/runners/host.py::_start_optional_monitor` | 9 | `BLE001=9` |
| `RUFF-SUP-314` | `tests/helpers/test_backend.py::cleanup_postgres_schema_for_root`; `tests/helpers/test_backend.py::cleanup_prepared_roots` | 2 | `BLE001=2` |
| `RUFF-SUP-315` | `tests/helpers/weft_harness.py::WeftTestHarness._peek_queue_lines` | 1 | `BLE001=1` |
| `RUFF-SUP-316` | `tests/helpers/weft_harness.py::WeftTestHarness._terminate_pid` | 1 | `BLE001=1` |
| `RUFF-SUP-317` | `extensions/weft_microsandbox/weft_microsandbox/_runtime.py::MicrosandboxRuntime._describe_async`; `extensions/weft_microsandbox/weft_microsandbox/_runtime.py::MicrosandboxRuntime._kill_async`; `extensions/weft_microsandbox/weft_microsandbox/_runtime.py::MicrosandboxRuntime._stop_async`; `extensions/weft_microsandbox/weft_microsandbox/_runtime.py::_exec_with_cancel`; `extensions/weft_microsandbox/weft_microsandbox/_runtime.py::_sandbox_name` | 5 | `BLE001=5` |
| `RUFF-SUP-318` | `weft/commands/specs.py::_validate_task_spec_payload` | 6 | `BLE001=6` |
| `RUFF-SUP-319` | `weft/core/monitor/external_log.py::_PathWriter._close_handler_locked` | 2 | `BLE001=2` |
| `RUFF-SUP-320` | `weft/core/serve_log.py::emit_serve_log_record` | 1 | `BLE001=1` |
| `RUFF-SUP-321` | `weft/core/serve_log.py::emit_serve_log_record` | 1 | `BLE001=1` |
| `RUFF-SUP-322` | `weft/core/monitor/task_monitor.py::TaskMonitor._close_worker_local_resources` | 3 | `BLE001=3` |
| `RUFF-SUP-323` | `weft/core/monitor/task_monitor.py::TaskMonitor._emit_task_monitor_log` | 1 | `BLE001=1` |
| `RUFF-SUP-324` | `weft/core/monitor/task_monitor.py::TaskMonitor._ensure_monitor_store` | 1 | `BLE001=1` |
| `RUFF-SUP-325` | `weft/core/monitor/task_monitor.py::TaskMonitor._run_terminal_control_cleanup_worker` | 1 | `BLE001=1` |
| `RUFF-SUP-326` | `weft/core/monitor/task_monitor.py::TaskMonitor._run_builtin_cycle_worker` | 1 | `BLE001=1` |
| `RUFF-SUP-327` | `weft/core/monitor/task_monitor.py::TaskMonitor._run_custom_monitor_processor` | 1 | `BLE001=1` |
| `RUFF-SUP-328` | `weft/helpers/__init__.py::stdin_is_tty` | 1 | `BLE001=1` |
| `RUFF-SUP-329` | `weft/helpers/__init__.py::safe_cancel` | 1 | `BLE001=1` |
| `RUFF-SUP-332` | `weft/commands/run.py::_InteractiveRunLifecycle._poll_monitor_terminal` | 1 | `BLE001=1` |
| `RUFF-SUP-334` | `bin/pytest-pg::main` | 1 | `BLE001=1` |
| `RUFF-SUP-335` | `weft/commands/system.py::_collect_task_snapshot_records` | 1 | `BLE001=1` |
| `RUFF-SUP-336` | `weft/commands/system.py::_watch_task_events` | 1 | `BLE001=1` |
| `RUFF-SUP-337` | `weft/commands/system.py::_legacy_cmd_status` | 1 | `BLE001=1` |
| `RUFF-SUP-338` | `tests/commands/test_task_evidence.py::_probe_with_task`; `tests/commands/test_task_evidence.py::test_known_tid_ping_pong_updates_task_status`; `tests/core/test_manager.py::test_manager_cleanup_waits_for_active_child_launch_worker`; `tests/core/test_manager.py::test_manager_late_child_launch_self_reaps_after_cleanup_deadline` | 4 | `BLE001=4` |
| `RUFF-SUP-339` | `weft/core/tasks/multiqueue_watcher.py::MultiQueueWatcher._apply_pending_topology_mutations` | 2 | `BLE001=2` |
| `RUFF-SUP-340` | `weft/core/tasks/pipeline.py::PipelineTask._bootstrap_children` | 2 | `BLE001=2` |
| `RUFF-SUP-341` | `weft/core/tasks/pipeline.py::PipelineEdgeTask._handle_work_message` | 1 | `BLE001=1` |
| `RUFF-SUP-342` | `weft/core/runners/host.py::_worker_entry` | 1 | `BLE001=1` |
| `RUFF-SUP-343` | `weft/core/runners/host.py::HostTaskRunner.run_with_hooks` | 2 | `BLE001=2` |
| `RUFF-SUP-344` | `weft/core/tasks/sessions.py::AgentSession.poll_limits`; `weft/core/tasks/sessions.py::CommandSession.poll_limits` | 2 | `BLE001=2` |
| `RUFF-SUP-345` | `weft/manager_detached_launcher.py::main` | 2 | `BLE001=2` |
| `RUFF-SUP-346` | `weft/core/manager.py::Manager._launch_child_task`; `weft/core/manager.py::Manager._retry_stale_child_launches` | 2 | `BLE001=2` |
| `RUFF-SUP-347` | `weft/core/manager.py::Manager._run_child_launch_worker`; `weft/core/tasks/base.py::BaseTask._submit_worker_lane`; `weft/core/tasks/service.py::ServiceTask._run_service_worker_thread` | 3 | `BLE001=3` |
| `RUFF-SUP-348` | `weft/core/tasks/base.py::BaseTask._control_runtime_summary`; `weft/core/tasks/base.py::BaseTask._pong_extension_fields` | 2 | `BLE001=2` |
| `RUFF-SUP-349` | `weft/core/tasks/consumer.py::Consumer._run_reactor_work_item` | 1 | `BLE001=1` |
| `RUFF-SUP-350` | `weft/core/runners/subprocess_runner.py::run_monitored_subprocess` | 2 | `BLE001=2` |
| `RUFF-SUP-351` | `weft/cli/validate_taskspec.py::_resolve_taskspec_source` | 1 | `BLE001=1` |
| `RUFF-SUP-353` | `weft/commands/result.py::_legacy_cmd_result` | 1 | `BLE001=1` |
| `RUFF-SUP-354` | `weft/commands/submission.py::ensure_manager_after_submission` | 1 | `BLE001=1` |
| `RUFF-SUP-355` | `weft/commands/tasks.py::_monitor_store_task_snapshot` | 1 | `BLE001=1` |
| `RUFF-SUP-356` | `weft/core/runners/host.py::_agent_session_worker_entry` | 2 | `BLE001=2` |
| `RUFF-SUP-357` | `bin/check-dom15-fixtures::<module>` | 1 | `BLE001=1` |
| `RUFF-SUP-358` | `tests/fixtures/provider_cli_fixture.py::_execute_fixture_request` | 1 | `BLE001=1` |
| `RUFF-SUP-359` | `tests/helpers/multiqueue_sigint_probe.py::main` | 1 | `BLE001=1` |
| `RUFF-SUP-360` | `tests/long_session_surface_benchmark.py::main` | 1 | `BLE001=1` |
| `RUFF-SUP-361` | `tests/multiqueue_polling_benchmark.py::main` | 1 | `BLE001=1` |
| `RUFF-SUP-362` | `extensions/weft_microsandbox/weft_microsandbox/plugin.py::MicrosandboxRunner._execute` | 1 | `BLE001=1` |
| `RUFF-SUP-363` | `weft/builtins/agent_images.py::prepare_agent_images_task` | 1 | `BLE001=1` |
| `RUFF-SUP-364` | `weft/core/monitor/store.py::MonitorStore.status` | 1 | `BLE001=1` |
| `RUFF-SUP-365` | `weft/core/task_evidence.py::describe_runtime` | 1 | `BLE001=1` |
| `RUFF-SUP-366` | `tests/helpers/reactor_driver.py::drive_until` | 1 | `BLE001=1` |
| `RUFF-SUP-367` | `weft/cli/run.py::render_run_result` | 1 | `C901=1` |
| `RUFF-SUP-368` | `weft/cli/app.py::result_command` | 1 | `C901=1` |
| `RUFF-SUP-369` | `weft/commands/submission.py::prepare_spec` | 1 | `C901=1` |
| `RUFF-SUP-370` | `weft/commands/_boundary.py::_TranslatedIterator._close_source` | 1 | `BLE001=1` |
<!-- END GENERATED RUFF SUPPRESSION INDEX -->
