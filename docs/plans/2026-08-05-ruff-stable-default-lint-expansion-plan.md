# Ruff Stable-Default Lint Expansion Plan

Status: completed
Source specs: docs/specifications/08-Testing_Strategy.md [TS-3], [TS-3.1]
Superseded by: none

Class: 5+P. The change materially revises the repository-wide verification
contract and how future Python work is implemented and reviewed. The cleanup
will touch runtime, test, integration, extension, and tool surfaces.

Plan type: implementation with spec revision.

Hardening: required. Although product behavior is intended to stay unchanged,
expanded lint findings occur in the durable execution spine, exception
containment, resource cleanup, subprocess handling, public typing surfaces,
and extension adapters. A lint-driven edit can cross those boundaries while
still looking mechanical.

## 1. Goal

Make Weft use the same effective Ruff rule set as SimpleBroker: Ruff 0.16.1's
stable defaults extended with the existing `E`, `W`, `F`, `I`, `B`, `C901`,
`C4`, and `UP` families. Resolve the resulting findings without changing
runtime behavior, weakening error containment, or fragmenting cohesive code.
The expanded policy must be an exact, firing repository gate rather than a
configuration accident.

## 2. Requested Outcomes

- [ ] Replace `lint.select` with `lint.extend-select` while retaining exactly
  `E`, `W`, `F`, `I`, `B`, `C901`, `C4`, and `UP`.
- [ ] Match SimpleBroker's reviewed effective inventory exactly: 453 enabled
  Ruff 0.16.1 rules, 282 more than Weft's current 171 and none removed.
- [ ] Preserve the McCabe threshold, C901 registry, global ignores, preview
  posture, exact extensionless-Python discovery, CI ordering, and explicit
  formatter ownership.
- [ ] Resolve every expanded-policy diagnostic through a reviewed,
  behavior-preserving edit where practical.
- [ ] Make zero new suppressions the target. Any irreducible finding must use
  [TS-3.1], name the protected invariant and real proof, receive independent
  review and explicit owner approval, and activate atomically with its source
  directive and generated index.
- [ ] Prove with the real repo-managed Ruff binary that one stable-default
  rule outside the prior set and one retained legacy-family rule both fire.
- [ ] Update active contributor guidance to use `ruff check .` without
  rewriting historical plans or lessons.
- [ ] Pass the full test, type, lint, suppression, format, documentation, and
  traceability gates before completion.

## 3. Source Documents

- `docs/specifications/08-Testing_Strategy.md` [TS-3], [TS-3.1] is the
  governing static-analysis and suppression contract.
- `docs/plans/2026-08-04-ruff-complexity-and-suppression-registry-plan.md` is
  completed and owns the current C901 activation, discovery policy, exact
  enabled-rule fixture, and suppression tool.
- `../simplebroker/docs/plans/2026-07-29-ruff-lint-expansion-plan.md` is the
  completed reference implementation. Reuse its policy shape and lessons, but
  not its old diagnostic counts, file list, suppressions, or Python target.
- `../simplebroker/pyproject.toml` and
  `../simplebroker/tests/fixtures/ruff-enabled-rules.txt` provide the current
  reference configuration and reviewed 453-code inventory.
- `AGENTS.md`, `docs/agent-context/engineering-principles.md`,
  `docs/agent-context/runbooks/writing-plans.md`,
  `docs/agent-context/runbooks/hardening-plans.md`, and
  `docs/agent-context/runbooks/review-loops-and-agent-bootstrap.md` govern
  implementation and review.
- Ruff references used by the SimpleBroker plan:
  <https://github.com/astral-sh/ruff/releases/tag/0.16.0>,
  <https://astral.sh/blog/ruff-v0.16.0>, and
  <https://docs.astral.sh/ruff/linter/#rule-selection>.

## 4. Spec Baseline

- `d0254ce45a757a7351fa0e7ca76f389790a1800b` is the authoring baseline for
  [TS-3], [TS-3.1], Ruff 0.16.1, the current 171-code fixture, the
  118-group/147-directive C901 registry, and the `C901=147`, `E402=22`,
  `F401=5` raw-`noqa` inventory.
- Atomic promotion commit:
  `bb6e47769a3f6f3c8f279931c11f364a7e67a7f8`. Commit identity is recorded here
  for execution traceability and intentionally remains outside the normative
  testing spec.

## 5. Current Structure And Key Files

### Read first

- `pyproject.toml`: explicitly selects eight families, shadowing Ruff 0.16.1's
  expanded stable defaults.
- `.github/workflows/test.yml`: already runs `ruff check .`, suppression check,
  explicit formatter roots, and full mypy roots in the required order.
- `tests/specs/test_ruff_policy.py`: real-Ruff proof for effective rules,
  tracked-file discovery, C901, CI shape, cleanliness, and suppression checks.
- `tests/fixtures/ruff-enabled-rules.txt`: exact accepted effective codes;
  currently 171.
- `bin/ruff_suppression_index.py`: the only reconciliation path for normal
  Ruff, raw `--ignore-noqa`, source directives, [TS-3.1] rows, and the generated
  index. Do not create a second suppression path.
- `docs/specifications/08-Testing_Strategy.md` [TS-3], [TS-3.1].
- `README.md`, `AGENTS.md`, and
  `docs/agent-context/runbooks/testing-patterns.md`: active contributor
  commands that still show narrower lint paths.

### Expected policy/documentation changes

- `pyproject.toml`
- `tests/fixtures/ruff-enabled-rules.txt`
- `tests/specs/test_ruff_policy.py`
- `docs/specifications/08-Testing_Strategy.md`
- `README.md`
- `AGENTS.md`
- `docs/agent-context/runbooks/testing-patterns.md`
- this plan and `docs/plans/README.md`
- `.github/workflows/test.yml` only if a firing test shows drift; its current
  command order should remain unchanged
- `bin/ruff_suppression_index.py` and its tests only if a real non-C901
  candidate proves the current general-rule support insufficient
- `docs/lessons.md` only if implementation exposes a durable new failure mode

### Cleanup scope

Every path in the locked expanded-policy JSON is authoritative. Highest-count
current paths include `weft/core/tasks/sessions.py`,
`weft/core/runners/host.py`, `weft/core/taskspec/model.py`,
`weft/_constants.py`, `weft/core/manager.py`,
`weft/core/manager_runtime.py`, `weft/core/monitor/task_monitor.py`,
`weft/core/runners/subprocess_runner.py`, the Docker and Microsandbox adapter
modules, `tests/test_harness_registration.py`, `tests/conftest.py`,
`tests/helpers/weft_harness.py`, `tests/tasks/test_runner.py`, and
`tests/tasks/test_multiqueue_watcher.py`.

Do not turn this list into an allowlist. Regenerate exact diagnostics at
implementation start and after every accepted cleanup slice.

```text
pyproject.toml selection + discovery
          |
          v
ruff check .  --->  normal clean gate
          |
          +-------> raw --ignore-noqa inventory
                         |
                         v
              ruff_suppression_index.py
                         |
        [TS-3.1] rows <-> source directives <-> generated index
```

The expansion changes only the first box's effective selection. It reuses the
remaining discovery, suppression, CI, and exact-fixture paths.

### Comprehension gate

Before editing, the implementer must answer:

1. Why does `select` -> `extend-select` add 282 rules without removing any
   current rule, and which exact fixture proves the union?
2. Why must broad process, plugin, retry, and cleanup catches retain failure
   containment even when `BLE001` or `S110` fires?
3. Which path makes a local suppression durable, and why are global/per-file
   ignores, blanket directives, and a second allowlist prohibited?
4. Why must Weft retain six exact extensionless Python paths instead of
   SimpleBroker's broad `bin/*`, which would parse Weft's Bash tools?

## 6. Measured Baseline

Corrected on 2026-08-06 at the authoring baseline with repo Ruff 0.16.1 and a
temporary `pyproject.toml` identical to the real project configuration except
for the single intended `select` -> `extend-select` mutation. This preserves
`isort.known-first-party`, McCabe, target, ignores, discovery, and every other
non-selection setting while simulating activation:

- Expanded inventory: 453 codes; SimpleBroker inventory: 453 codes; set
  difference empty in both directions.
- Current Weft inventory: 171; added: 282; removed: zero.
- Normal diagnostics: 599 across 146 files and 38 rule codes.
- Ruff-offered fixes: 228. Fix availability is not permission to apply it.
- Raw diagnostics: 773 across 156 files and 41 codes. The difference is the
  current `C901=147`, `E402=22`, `F401=5` local inventory.

| Rule | Count | Primary review risk |
|---|---:|---|
| `BLE001` | 186 | Exception containment and process/plugin boundaries |
| `TRY004` | 75 | Public exception type and caller compatibility |
| `S110` | 46 | Intentional silent cleanup/retry behavior |
| `RUF100` | 38 | Stale existing `noqa` codes; registry integrity |
| `SIM102` | 25 | Branch precedence and logical locality |
| `RUF022` | 25 | Export membership/order contracts |
| `FLY002` | 24 | String construction and output fidelity |
| `SIM117` | 21 | Context-manager enter/exit and cleanup ordering |
| `RUF059` | 19 | Test readability and unused values |
| `PIE807` | 17 | Redundant lambdas; callable behavior |
| `RUF012` | 15 | Mutable class state and test-fixture intent |
| `PYI064` | 15 | Protocol/signature typing contracts |
| `PYI063` | 11 | Positional-only signature compatibility |
| `ISC004` | 10 | String concatenation and exact output |
| `PLW1510` | 10 | Explicit subprocess failure policy |
| `PLR0402` | 9 | Import shape and monkeypatch behavior |
| `PLR1711` | 7 | Explicit return semantics |
| `RET501` | 7 | Explicit return semantics |
| `SIM103` | 5 | Boolean branch simplification |
| `FURB188` | 4 | Path method choice and error behavior |
| `SIM114` | 4 | Branch combination and message fidelity |
| `PIE810` | 3 | Starts/ends-with tuple semantics |
| `RUF046` | 3 | Numeric cast/precedence behavior |
| `S102` | 3 | Intentional `exec` in architecture tests |
| `PIE790` | 2 | Redundant pass cleanup |
| `S112` | 2 | Intentional continue-on-error behavior |
| `SIM905` | 2 | Split construction behavior |
| `FURB136` | 1 | Redundant string operation |
| `FURB167` | 1 | Regex flag placement |
| `PLC0208` | 1 | Set iteration in a test |
| `PYI025` | 1 | Collection annotation contract |
| `PYI034` | 1 | Context-manager public typing contract |
| `RUF009` | 1 | Dataclass default construction |
| `RUF019` | 1 | Unnecessary key check |
| `RUF034` | 1 | Suspicious branch equivalence |
| `SIM115` | 1 | File/resource lifetime |
| `SIM118` | 1 | Mapping membership expression |
| `TRY203` | 1 | Immediate re-raise and traceback semantics |

The implementation must rerun the exact probe; it must not copy these counts
if intervening accepted work changed the baseline.

### Exact reproduction probe

Run from the repository root after loading `.envrc`. This changes only the
selection key in a temporary project config. Do not use `--isolated` for the
diagnostic baseline: it also discards `isort.known-first-party` and creates 75
false `I001` findings that the real activation does not produce.

```bash
./.venv/bin/python - <<'PY'
from __future__ import annotations

import json
import re
import subprocess
import tempfile
from collections import Counter
from pathlib import Path

root = Path.cwd()
source = (root / "pyproject.toml").read_text(encoding="utf-8")
marker = "[tool.ruff.lint]\nselect = ["
if source.count(marker) != 1:
    raise SystemExit("expected exactly one Ruff lint.select table entry")
expanded = source.replace(
    marker,
    "[tool.ruff.lint]\nextend-select = [",
    1,
)

with tempfile.TemporaryDirectory() as directory:
    config = Path(directory) / "pyproject.toml"
    config.write_text(expanded, encoding="utf-8")
    command = [
        str(root / ".venv/bin/ruff"),
        "check",
        "--config",
        str(config),
        "--output-format",
        "json",
        ".",
    ]
    for label, extra in (("normal", []), ("raw", ["--ignore-noqa"])):
        result = subprocess.run(
            [*command, *extra], cwd=root, text=True, capture_output=True
        )
        if result.returncode not in {0, 1}:
            raise SystemExit(
                f"{label}: Ruff exit {result.returncode}: {result.stderr}"
            )
        diagnostics = json.loads(result.stdout)
        rules = Counter(item["code"] for item in diagnostics)
        files = {
            Path(item["filename"]).resolve().relative_to(root).as_posix()
            for item in diagnostics
        }
        offered_fixes = sum(item["fix"] is not None for item in diagnostics)
        print(
            label,
            len(diagnostics),
            len(files),
            offered_fixes,
            sorted(rules.items()),
        )

    settings = subprocess.run(
        [
            str(root / ".venv/bin/ruff"),
            "check",
            "--config",
            str(config),
            "--show-settings",
            "weft/__init__.py",
        ],
        cwd=root,
        text=True,
        capture_output=True,
        check=True,
    )
    match = re.search(
        r"linter\.rules\.enabled = \[\n(?P<rules>.*?)\n\]",
        settings.stdout,
        re.DOTALL,
    )
    if match is None:
        raise SystemExit("Ruff enabled-rule settings were not found")
    enabled = set(re.findall(r"\(([A-Z]+\d+)\)", match.group("rules")))

simplebroker = set(
    (root.parent / "simplebroker/tests/fixtures/ruff-enabled-rules.txt")
    .read_text(encoding="utf-8")
    .splitlines()
)
current = set(
    (root / "tests/fixtures/ruff-enabled-rules.txt")
    .read_text(encoding="utf-8")
    .splitlines()
)
print("effective", len(enabled))
print("current", len(current))
print("added", len(enabled - current))
print("removed", sorted(current - enabled))
print("only in Weft", sorted(enabled - simplebroker))
print("only in SimpleBroker", sorted(simplebroker - enabled))
if current - enabled:
    raise SystemExit("expanded policy removed existing Weft rules")
PY
```

The tracked-file discovery policy remains independently proven by
`tests/specs/test_ruff_policy.py`. Counts alone are insufficient.

## 7. Invariants And Constraints

### Product and architecture

- No public CLI, client, TaskSpec, queue, result, status, or persisted payload
  contract changes.
- TID format/immutability, forward-only state transitions, reserved-queue
  policy, resolved `spec`/`io` immutability, spawn context, and runtime-only
  `weft.state.*` behavior remain unchanged.
- The `TaskSpec -> Manager -> Consumer -> TaskRunner -> queues/state log` spine
  remains the only execution path.
- A broad catch at a real process, plugin, interpreter, retry, cleanup, or
  best-effort boundary must not be narrowed merely to satisfy a rule.
- Error types/messages are observable where callers depend on them. `TRY004`
  is a prompt to inspect the boundary, not authority to replace an exception.
- Context-manager rewrites preserve acquisition, partial-enter cleanup, exit
  order, suppression, and traceback behavior.
- Subprocess calls preserve fatal/nonfatal policy. `check=False` may make an
  existing policy explicit; `check=True` requires proof nonzero is already
  fatal.
- Public typing and exported membership remain consumer-compatible.

### Policy

- Ruff stays at the current locked version; a version refresh is separate.
- Target `py312`, line length 88, McCabe 10, and ignores `E501`/`B008` remain.
- Preview stays disabled; per-file ignores stay empty.
- Six explicit extensionless Python tools remain exact; Bash stays excluded.
- CI remains normal Ruff -> suppression -> explicit formatter -> full mypy.
- C901 group IDs/cardinalities/raw findings/index remain coherent.
- No global rule ignore, threshold raise, blanket directive, baseline
  allowlist, or second suppression system.

### Scope and design

- No new dependency.
- Never run repository-wide `ruff check --fix .` or `--unsafe-fixes`. Apply one
  reviewed rule batch at a time and format only changed Python paths.
- Do not refactor cohesive code merely to satisfy `SIM`, `TRY`, or another
  style rule. A narrow approved suppression is better than behavior drift or
  reduced logical locality.
- Do not add generic exception, validation, context-manager, subprocess, or
  cleanup frameworks.
- Historical plans and dated lessons remain historical evidence; do not edit
  them to show current commands.

## 8. Proposed Spec Delta

Promotion strategy: **B, atomic**. Preparatory cleanup may land under the
current policy. Promote this [TS-3] text only with `lint.extend-select`, the
453-code fixture, firing tests, contributor docs, and approved [TS-3.1]
additions. This avoids an active spec claiming a gate that is not yet enabled.

### [TS-3] replace the opening paragraph

Replace the current paragraph beginning `Weft's Python lint gate uses` with:

> Weft's Python lint gate uses the stable default rule set of the Ruff version
> locked in `uv.lock`, extended with the repository's reviewed `E`, `W`, `F`,
> `I`, `B`, `C901`, `C4`, and `UP` rule families. The configuration extends
> Ruff's defaults rather than replacing them. The gate covers every tracked
> first-party `.py`/`.pyi` file and Python-shebang repository tool. Ruff owns
> Python file discovery; configuration includes tracked extensionless Python
> tools explicitly and must not parse Bash tools as Python.

### [TS-3] replace the owner/verification paragraph

Replace the paragraph beginning `Owner: pyproject.toml owns rule selection`
with:

> Owner: `pyproject.toml` owns rule selection and discovery; the root CI lint
> job enforces it. Boundary: `weft/`, `tests/`, `integrations/`, `extensions/`,
> and Python tools under `bin/`. Verification:
> `tests/specs/test_ruff_policy.py` invokes the real repo-managed Ruff binary,
> compares effective discovery and the complete enabled-rule set with reviewed
> inventories, proves every behavior-affecting policy setting fires, and proves
> that a stable-default rule outside the retained families and a retained-family
> rule both fire. Required action: a Ruff version or rule-selection change
> intentionally reviews and updates the enabled-rule inventory before changing
> the lock or configuration.

### [TS-3] add to the existing requirements

> - configured rule families extend Ruff's stable defaults; preview rules
>   remain opt-in and are not part of the default gate;
> - intentionally broad exception, retry, plugin, process, and best-effort
>   cleanup boundaries retain runtime behavior through explicit structure where
>   practical; a suppression is the reviewed last resort, not the default
>   alternative to a behavior-changing rewrite;
> - changing a public exception type, signature, resource lifetime, subprocess
>   policy, or output shape to satisfy lint requires direct compatibility proof
>   from owning tests or downstream type checks.

Keep existing requirements for `ruff check .`, ignores, formatter scope,
atomic activation, C901, raw audits, and suppression reconciliation. [TS-3.1]
already supports arbitrary registered rule sets and changes only if Task 7
produces owner-approved exceptions.

## 9. Dependency-Ordered Tasks

### Task 1: Independent plan and spec-delta review

- Read this plan, [TS-3]/[TS-3.1], the completed Weft C901 plan, SimpleBroker's
  completed lint-expansion plan, current policy code/tests, representative
  high-risk findings, and named exception/cohesion/policy-fixture lessons.
- Look for missing invariants, weak accounting, behavior changes disguised as
  style, broad exemptions, and tests that could pass without real Ruff.
- Done when a clean reviewer returns `PASS`, or all blockers are fixed and a
  scoped re-review passes.
- Stop before implementation if the reviewer cannot implement confidently or
  believes the cleanup would degrade failure containment.

### Task 2: Reproduce and freeze implementation evidence

- Rerun Section 6's temporary expanded-config probe. Record command, Ruff version,
  453-code set, current/added/removed rule sets, normal/raw JSON counts, offered
  fix counts, and git SHA in Section 19.
- Compare exact sets with both Weft's current fixture and SimpleBroker. Any
  removed current rule or SimpleBroker difference is a stop condition.
- Populate Section 16 for every affected function/class under `BLE001`,
  `TRY004`, `S110`, `S112`, `S102`, `SIM117`, `SIM115`, `PLW1510`, `PYI025`,
  `PYI034`, `PYI063`, `PYI064`, `RUF009`, `RUF012`, `RUF034`, and `TRY203`.
  Also include `PLR0402` sites governed by the facade-backedge architecture
  test and both `SIM905` multiline field-ledger sites; neither is mechanical.
  Group only findings sharing one owner, invariant, disposition, and proof.
- Each row records path/symbol, rules/count, current behavior, disposition,
  exact proof, and review state. Do not edit production source in this task.
- Stop on count drift, missing discovery, or undocumented public contracts.

### Task 3: Add failing policy proofs without activation

- Update `tests/specs/test_ruff_policy.py` in the worktree, but never commit a
  permanently red policy test separately from activation.
- Require `lint.select` absent; `lint.extend-select` exactly the eight current
  families; and target, ignores, McCabe, preview, per-file ignores, and six
  extensionless paths unchanged.
- Add real-stdin probes proving `BLE001` from stable defaults and a retained
  family rule absent from the curated defaults (use `B904` if still valid).
- Keep exact enabled-rule comparison; replace the fixture only after the
  453-code set is independently checked against SimpleBroker.
- Extract pure `_assert_ruff_policy(ruff, lint)` and `_assert_lint_job(job)`
  helpers so repository-owned structural policy can be mutation-tested without
  rewriting the real project file. The complete mutation matrix is:
  `select` versus `extend-select`; omitted/extra family; changed effective
  inventory code; target version; either global ignore; McCabe threshold;
  preview; per-file ignores; each exact extensionless path; Bash inclusion;
  normal-Ruff/suppression/format/mypy CI order; formatter ownership; and CI
  preview activation.
- Use the real Ruff binary where settings affect Ruff behavior:
  - parse `--show-settings` to prove target `py312`, McCabe 10, preview false,
    and the exact effective code set;
  - run stdin probes showing `E501` and `B008` are ignored, then use a temporary
    mutated config to show removal of each ignore makes its diagnostic fire;
  - use a temporary mutated config to show a per-file `F401` ignore hides a
    real probe and is rejected by `_assert_ruff_policy`;
  - show `select` loses the stable-default sentinel while `extend-select`
    retains both sentinels.
- Use `_assert_lint_job` mutations for CI ordering and formatter ownership,
  because those are repository workflow contracts rather than Ruff semantics.
- Record expected RED against current `lint.select`. Do not mock Ruff.
- Preserve C901, discovery, CI, normal-clean, and suppression tests.

### Task 4: Low-risk mechanical batches

- Work from Task 2 JSON and group by rule. Start with `RUF100`,
  `RUF022`, `PIE807`, `PIE790`, `PIE810`, `SIM118`, `FURB136`,
  and `FURB167`. Do not include `SIM905`: Ruff's rewrite expands the compact
  multiline ledgers into unreadable 100-plus-item line literals. Apply
  `PLR0402` only where the architecture test proves no facade-backedge contract;
  the seven production facade imports are Task 7 manual-risk findings.
- Use Ruff fixes for one named rule batch only. Never enable unsafe fixes.
- For `RUF100`, reconcile every changed `noqa` with [TS-3.1], raw inventory,
  and the suppression check. Never remove an approved group pointer blindly.
- For `RUF022`, preserve exact public membership and any proven order contract.
- Format changed paths only; run closest tests, current/expanded Ruff, relevant
  mypy, and suppression check.
- An independent reviewer inspects each coherent rule batch before acceptance.
  A batch is one rule and one mechanical rewrite shape; split when owner,
  semantics, or proof differs.

### Task 5: Typing, class-state, and data-model findings

- Address `PYI025`, `PYI034`, `PYI063`, `PYI064`, `RUF009`, and `RUF012` as
  contract work, not formatting.
- Read protocols/models and all implementations/callers before changing
  positional-only markers, context-manager returns, class variables, or
  defaults.
- Use `ClassVar`, `field(default_factory=...)`, or signature changes only when
  runtime and mypy consumers prove existing behavior.
- For test classes, distinguish intentional shared state from accidental
  instance defaults; no blanket `ClassVar` conversion.
- Add focused runtime/signature tests and run full configured mypy plus closest
  integration/extension tests.
- Every non-mechanical refactor enters Task 7's locality review queue.
- Stop and propose a suppression if Ruff's preferred shape breaks a public or
  downstream contract.

### Task 6: Control flow, resource, subprocess, and output

- Address `SIM102`, `SIM103`, `SIM114`, `SIM117`, `SIM115`, `PLW1510`,
  `FLY002`, `ISC004`, `RUF019`, `RUF034`, `RUF046`, `RUF059`, `RET501`,
  `PLR1711`, `FURB188`, `SIM905`, and remaining local simplifications.
- Preserve branch precedence and locality. Do not flatten nesting that conveys
  distinct proof stages or cleanup authority.
- For context/file lifetime, prove enter/exit order, partial-enter failure,
  suppression, lifetime, and traceback with real resource tests.
- For subprocesses, use `check=False` for intentionally inspected status;
  `check=True` only when nonzero is already fatal and characterized.
- Preserve exact CLI/log/file output for string rewrites.
- Treat `RUF034` as a possible real defect: write a failing test if branches
  should differ; remove the condition only if identical behavior is proven.
- Every behavior-relevant refactor enters Task 7's locality review queue.

### Task 7: Exception, security, and architecture boundaries and the rework queue

- Address `BLE001`, `TRY004`, `S110`, `S112`, `S102`, `TRY203`, and
  architecture-governed `PLR0402` last.
- Narrow catches only to exceptions raised by the enclosed boundary. Move
  serialization, validation, programming, and invariant checks out of
  best-effort broker/cleanup catches only when existing behavior permits it.
  Follow the `docs/lessons.md` exception-boundary lesson.
- Preserve broad catches at process, plugin, interpreter, cleanup, callback,
  and retry seams where extension failures must be contained.
- Use clearer structure or `contextlib.suppress` only when logging, traceback,
  hooks, cleanup, and retry behavior remain exact.
- Do not add logging to an intentionally silent path without checking output,
  observability, and retry-volume contracts.
- Change exception types only after direct caller/public compatibility proof.
- Keep intentional `exec` confined to architecture-test fixtures. If needed,
  make it a specific suppression candidate; do not weaken the test.
- Preserve the commands/core facade rule proven by
  `tests/architecture/test_import_boundaries.py::test_restored_facade_backedges_are_detected`.
  Do not accept Ruff's child-module import rewrite where that test requires a
  facade import; prefer a reviewed narrow suppression over an alias rename that
  hides the architecture from readers.
- Preserve traceback identity and hooks around immediate re-raise paths.

#### Fresh Python-expert locality review

After every behavior-relevant Section 16 owner/invariant group refactored in
Tasks 5-7, dispatch a fresh clean subagent acting as a Python expert. That
ledger group is the review unit; do not combine unrelated symbols into a large
review or create one review per trivial diagnostic inside the same cohesive
change. Give the reviewer the exact baseline/candidate diff, owning tests, rule
finding, and invariant. It returns `NET POSITIVE` or `NET NEGATIVE`, focusing
on logical locality and comprehensibility as well as correctness.

- `NET POSITIVE`: accept only after focused tests, mypy scope, expanded Ruff,
  and suppression check pass.
- `NET NEGATIVE`: do not revert or discard. Preserve the candidate in a
  dedicated worktree and append it to Section 17's FIFO queue with concrete
  improvement criteria. Rework the head, then use a different fresh reviewer.
- No queued/reworking/awaiting-review candidate may remain at activation. If
  the original cohesive code is best, make the finding a suppression candidate
  rather than forcing a worse refactor.
- Owner-directed checkpoint commits are required before candidate worktrees
  and after accepted behavior-relevant refactors. Review does not authorize a
  commit.

#### Suppression gate

- Zero new suppressions is the target.
- Before any directive, append a proposed [TS-3.1] row to Section 18 with
  stable ID, rules/cardinality, exact Section 16 ledger IDs, exact
  `path::qualified_symbol` sites, proposed source snippets, invariant, real
  proof, rejected alternatives, and review result.
- Map candidate raw diagnostics one-to-one to ledger rows, proposed groups,
  sites/snippets, proof, reviewer verdict, and owner approval. Before
  activation, validate locations and counts with the temporary expanded-config
  probe from Section 6; do not run the repository suppression tool and pretend
  the old 171-rule config can reconcile rules it does not enable.
- Obtain clean independent review and explicit owner approval for every row.
  Approval is attached to those exact sites/snippets, not only a rule/count or
  group rationale. Plan approval is not suppression approval.
- Task 7 records approved candidate material only. It does not edit [TS-3.1],
  source directives, global raw inventory, exact fixtures, or generated index.
  Those changes belong to Task 8's one candidate activation state.
- No rejected, pending, or unreviewed candidate may remain at activation.

### Task 8: Atomic policy and spec activation

- Apply Section 8's exact [TS-3] delta. Keep [TS-3.1] unchanged unless Task 7
  produced approved rows. Add the plan backlink and update nearby mapping if
  ownership changed.
- Rename `lint.select` to `lint.extend-select`; preserve the eight families,
  target, ignores, McCabe, discovery, CI order, and formatter scope.
- In the same candidate activation state, apply every approved [TS-3.1] human
  row, exact approved source directive, global raw inventory change, exact
  group/directive fixture change, and generated index change. Only now run
  `bin/ruff_suppression_index.py --write` and `--check`, because normal/raw Ruff
  must see the newly enabled rules for reconciliation. If this state does not
  reconcile, do not split or partially land it.
- Replace the enabled-rule fixture with the reviewed 453-code set, exactly
  matching SimpleBroker at Ruff 0.16.1.
- Complete Task 3 tests and observe the prior RED turn GREEN.
- Update `README.md`, `AGENTS.md`, and `testing-patterns.md` to use
  `ruff check .` for the repository gate. Keep clearly targeted examples.
- Do not change CI unless the policy test proves its current shape wrong.
- Run normal Ruff, `RUF100`, suppression, policy tests, format, and mypy before
  acceptance. Record the promotion baseline in Sections 4 and 19.

### Task 9: Full verification and completed-work review

- Run Section 11 from current accepted state.
- Normal Ruff must be clean. Raw findings must equal [TS-3.1]/global inventory.
- Review the full diff for containment, error contracts, resource ordering,
  subprocess policy, typing, exports, output, discovery, and formatter drift.
- Give a clean reviewer the promoted spec, plan, config, fixture/tests,
  suppression state/tool, complete source diff, ledgers, and evidence.
- Ask whether any fix changed behavior or reduced locality/comprehensibility,
  and whether any suppression exceeds its approved invariant.
- Resolve blockers and run scoped re-review.
- Mark plan/index completed only after implementation/verification closure is
  committed at owner direction. Status closure is a separate ledger commit.

## 10. Testing Strategy

### Failing-test-first

- Policy proof fails under `lint.select`, then passes after atomic activation.
- Semantic fixes start with the smallest test proving existing behavior or the
  defect. If Ruff finds a real bug, observe the regression test RED first.
- For provably mechanical edits, record pre/post diagnostic plus unchanged
  closest behavior test as the sanctioned equivalent proof.
- Never weaken a test, output assertion, or type contract for a lint rewrite.

### Keep these real

- Real Ruff subprocesses for config, discovery, firing, rule-set, and clean
  gates.
- `WeftTestHarness`, real queues/process paths, and real extension adapters for
  lifecycle, resource, retry, subprocess, or queue changes.
- Mock only already-external/nondeterministic seams. Do not create a parallel
  mock-heavy remediation suite.

### Per-slice proof

1. Closest owning tests.
2. Current Ruff on changed paths.
3. Isolated expanded-policy probe and regenerated counts.
4. Relevant mypy package for typing/signature changes.
5. Suppression check after any `noqa` or C901-adjacent edit.
6. Required batch or locality review before acceptance.

## 11. Verification And Gates

Focused policy gates:

```bash
. ./.envrc
./.venv/bin/python -m pytest -n 0 \
  tests/specs/test_ruff_policy.py \
  tests/specs/test_ruff_suppression_index.py -q
./.venv/bin/ruff check .
./.venv/bin/ruff check --extend-select RUF100 .
./.venv/bin/python bin/ruff_suppression_index.py --check
./.venv/bin/ruff format --check weft tests integrations/weft_django \
  extensions/weft_docker extensions/weft_macos_sandbox \
  extensions/weft_microsandbox
```

Final gates:

```bash
. ./.envrc
./.venv/bin/python -m pytest
./.venv/bin/python -m pytest -m ""
./.venv/bin/mypy weft bin integrations/weft_django/weft_django \
  extensions/weft_docker/weft_docker \
  extensions/weft_macos_sandbox/weft_macos_sandbox \
  extensions/weft_microsandbox/weft_microsandbox \
  --config-file pyproject.toml
./.venv/bin/ruff check .
./.venv/bin/ruff check --extend-select RUF100 .
./.venv/bin/ruff format --check weft tests integrations/weft_django \
  extensions/weft_docker extensions/weft_macos_sandbox \
  extensions/weft_microsandbox
./.venv/bin/python bin/ruff_suppression_index.py --check
./.venv/bin/python -m pytest -n 0 \
  tests/specs/test_plan_metadata.py tests/specs/test_spec_hygiene.py -q
bin/check-dom15-fixtures
bin/check-doc-paths
../backstitch/.venv/bin/backstitch check \
  --repo-root . --no-config \
  --spec-root docs/specifications --plan-root docs/plans \
  --code-root weft --code-root tests --code-root bin \
  --code-root integrations --code-root extensions --format json
uv lock --check
git diff --check
```

Run `./.venv/bin/python bin/pytest-pg --all` if accepted cleanup touches shared
queue, manager, watcher, monitor, result, or runtime behavior. Run the closest
extension/integration suite for every changed extension/integration symbol.

`bin/check-doc-paths` is advisory and may retain the exact eight reviewed
baseline claims or reach zero; this work adds none. Backstitch needs the
explicit roots above and must add no error/warning keyed to [TS-3], [TS-3.1],
this plan, Ruff policy surfaces, suppression tool, or changed code. Compare
issues by code/path/symbol/section, not aggregate count. If the sibling tool is
absent, record a blocker rather than claim success.

## 12. Rollout And Rollback

The merge gate is rollout. Preparatory behavior-preserving cleanup may land as
reviewed checkpoints while old policy remains active. Spec, `extend-select`,
fixture, tests, docs, and approved suppressions activate atomically only when
the expanded probe is clean.

Rollback the atomic activation as a unit for configuration/discovery/CI
failure. Accepted behavior-preserving cleanup may remain. If cleanup proves
behavior-changing, repair or revert that specific checkpoint; do not disable
the expanded policy to hide it.

No data migration, persisted-format change, deployment ordering, or one-way
door exists. CI's clean Ruff -> suppression -> format -> mypy -> test sequence
is the observable rollout signal.

## 13. Stop And Re-plan Gates

Stop for owner direction if:

- the locked effective set no longer matches SimpleBroker;
- a fix wants a public API/error/queue/TaskSpec/result/runtime change;
- an invariant needs a global/per-file ignore, threshold change, blanket
  directive, or unregistered suppression;
- a `NET NEGATIVE` refactor cannot be reworked without locality/behavior harm;
- proof requires a second execution path or mock-heavy harness;
- atomic activation cannot stay coherent;
- full verification exposes a regression not owned by a cleanup slice.

## 14. Out Of Scope

- Preview rules or `select = ["ALL"]`.
- Ruff/Python target refresh.
- New lint/type/format tools, dependencies, or CI topology.
- Formatter expansion to `.`, Markdown, or historical docs.
- Rewriting completed plans or dated lessons for current commands.
- Refactoring cohesive runtime/test/extension owners for size or style alone.
- Behavior, exception, logging, retry, cleanup, subprocess, storage, queue,
  CLI, or typing changes without direct compatibility proof and scope review.
- Copying SimpleBroker's suppressions, counts, Python target, or broad `bin/*`.

## 15. Deviation Log

| Spec ref | Planned behavior | Actual behavior | Rationale | Spec proposal |
|---|---|---|---|---|
| [TS-3.1] Task 4 `RUF100` preflight | Apply only the 38 candidate-policy stale-directive fixes. | A first command used `--select RUF100 --fix`, which made all other suppressed rules appear disabled and proposed 193 removals. The result was rejected before review and every `noqa` hunk was mechanically restored; suppression reconciliation and the expected 38-count inventory pass again. | Ruff's CLI `--select` replaces the effective selection for unused-directive analysis. The full candidate rule set must remain active while the fix scope is restricted separately. | None. Durable command-safety lesson added; no broad result was accepted. |

## 16. Manual-Risk Finding Ledger

Populate Task 2 before source edits. Group only one owner/invariant/disposition.

### Task 2 inventory

Historical Task 2 baseline snapshot, generated read-only from repository SHA `d0254ce45a757a7351fa0e7ca76f389790a1800b` on 2026-08-06. The authoritative corrected activation simulation (temporary `pyproject.toml` identical to the repository config except `lint.select` -> `lint.extend-select`) is 599 normal diagnostics across 146 files and 38 codes; 773 raw diagnostics across 156 files and 41 codes; 453 effective rules, exact with SimpleBroker. The isolated owner-attribution pass below is extended with the twelve Task 6 FLY002, six Task 6 SIM102, and seven Task 4 RUF022 suppression candidates. Row states in this historical snapshot describe pre-edit disposition; current activation and approval state is authoritative in Sections 18 and 19.

- Required manual-risk findings: **415** across **280 owner/category rows**.
- Required rule counts: `BLE001=186, FLY002=12, PLW1510=10, PYI025=1, PYI034=1, PYI063=11, PYI064=15, RUF009=1, RUF012=15, RUF022=7, RUF034=1, S102=3, S110=46, S112=2, SIM102=6, SIM115=1, SIM117=21, TRY004=75, TRY203=1`.
- Additional requested facade/simplification findings: **11** (`PLR0402=9, SIM905=2`).
- Total ledger rows: **290**. Blocked rows: **0**. Rows marked `pending` or `candidate` still require the named compatibility, locality, or suppression review before an edit is accepted.
- Symbol attribution matches `bin/ruff_suppression_index.py`: nested functions are attributed to their enclosing outermost function; methods remain class-qualified; lines outside a function are `<module>`.

| ID | Path::outermost owner | Rules/counts and sites | Current invariant | Proposed disposition | Closest exact proof | Review state |
|---|---|---|---|---|---|---|
| MR-001 | `bin/check-dom15-fixtures::<module>` | `BLE001=1`; `BLE001@214` | The top-level checker boundary converts any internal failure into a clean diagnostic and exit 2; no traceback reaches the caller. | Preserve the broad no-traceback boundary and register an exact BLE001 suppression candidate. | `./bin/check-dom15-fixtures` | candidate: independent suppression review |
| MR-002 | `bin/coalesce-check::git` | `PLW1510=1`; `PLW1510@62` | The subprocess return code is currently non-fatal here and is either inspected by the caller or intentionally ignored by a probe/test. | Add explicit check=False. Use check=True only if a nonzero result is already proven fatal. | `./bin/coalesce-check` | ready: mechanical candidate |
| MR-003 | `bin/launch_manager.py::_wait_for_registry` | initial `BLE001=1`; accepted `BLE001=0` | A supported operational registry-read failure is treated as no entries for that poll; the bounded startup wait continues. Unexpected result-shape/programming defects escape. | Narrow to `(BrokerError, OSError, RuntimeError)`, matching current SimpleBroker domain/backend, OS/storage, and connection/lifecycle families. | `./.venv/bin/python -m pytest -q tests/system/test_launch_manager_script.py`; targeted Ruff and mypy | accepted: three retry-then-active cases plus unexpected `TypeError` propagation; fresh clean Python expert `NET POSITIVE`; residual upstream risk is SimpleBroker's coarse `RuntimeError` taxonomy |
| MR-004 | `bin/pytest-pg::_kill_pytest_process_tree` | accepted `BLE001=0` | A missing optional process-tree helper falls back to `Popen.kill`; operational kill failure remains best effort. Unexpected helper-import or Popen defects must escape. | Narrow helper import to `ImportError` and Popen kill to `OSError`; prove both fallback and unexpected `RuntimeError` propagation. | `./.venv/bin/python -m pytest -q tests/system/test_pytest_pg_script.py` | accepted: 25 owner tests pass; focused Ruff/mypy/diff checks pass; clean Python review `NET POSITIVE` |
| MR-005 | `bin/pytest-pg::_send_pytest_interrupt` | accepted `BLE001=0` | POSIX process-group and direct signal delivery failures remain best effort only for the OS family; programming defects escape. | Narrow both signal catches to `OSError`; prove containment and unexpected `RuntimeError` propagation. | `./.venv/bin/python -m pytest -q tests/system/test_pytest_pg_script.py` | accepted: exact signal paths fire; clean Python review `NET POSITIVE` |
| MR-006 | `bin/pytest-pg::_terminate_pytest_process_tree` | accepted `BLE001=0` | A missing optional process-tree helper falls back to `Popen.terminate`; operational terminate failure remains best effort. Unexpected helper-import or Popen defects must escape. | Narrow helper import to `ImportError` and Popen terminate to `OSError`; prove both fallback and unexpected `RuntimeError` propagation. | `./.venv/bin/python -m pytest -q tests/system/test_pytest_pg_script.py` | accepted: exact fallback paths fire; clean Python review `NET POSITIVE` |
| MR-007 | `bin/pytest-pg::main` | `BLE001=1`; `BLE001@572` | After container assignment, unexpected ordinary environment/schema/pytest failures become exit 1 with verbatim stderr; nonzero exit prints logs before cleanup. | Preserve the local command boundary and register exact group RUFF-SUP-334. | `./.venv/bin/python -m pytest -q tests/system/test_pytest_pg_script.py` | accepted: initial proof `NET NEGATIVE`; corrected firing proof and exact review `NET POSITIVE`; owner approved 2026-08-07 |
| MR-008 | `extensions/weft_docker/tests/test_docker_plugin.py::<module>` | `PLR0402=1`; `PLR0402@16` | The module-qualified import currently preserves a facade/module object used for attribute lookup or monkeypatching. | Convert to from-import only after proving import identity and monkeypatch targets; otherwise register a PLR0402 suppression candidate. | `./.venv/bin/python -m pytest -q extensions/weft_docker/tests/test_docker_plugin.py` | pending: facade/monkeypatch identity review |
| MR-009 | `extensions/weft_docker/tests/test_docker_plugin.py::test_command_runner_cleans_up_container_when_runtime_start_fails` | `RUF012=1`; `RUF012@1017` | Mutable attributes on the local fake are intentionally shared at class scope within the owning test unless the test resets/overrides them. | Declare ClassVar when sharing is intentional; otherwise initialize per instance. The owning test must prove the choice. | `./.venv/bin/python -m pytest -q extensions/weft_docker/tests/test_docker_plugin.py -k test_command_runner_cleans_up_container_when_runtime_start_fails` | pending: shared-vs-instance state review |
| MR-010 | `extensions/weft_docker/tests/test_docker_plugin.py::test_command_runner_waits_for_container_to_leave_created_before_runtime_handle` | `RUF012=1`; `RUF012@811` | Mutable attributes on the local fake are intentionally shared at class scope within the owning test unless the test resets/overrides them. | Declare ClassVar when sharing is intentional; otherwise initialize per instance. The owning test must prove the choice. | `./.venv/bin/python -m pytest -q extensions/weft_docker/tests/test_docker_plugin.py -k test_command_runner_waits_for_container_to_leave_created_before_runtime_handle` | pending: shared-vs-instance state review |
| MR-011 | `extensions/weft_docker/tests/test_docker_plugin.py::test_describe_runtime_falls_back_to_container_id_when_name_lookup_misses` | `RUF012=1`; `RUF012@686` | Mutable attributes on the local fake are intentionally shared at class scope within the owning test unless the test resets/overrides them. | Declare ClassVar when sharing is intentional; otherwise initialize per instance. The owning test must prove the choice. | `./.venv/bin/python -m pytest -q extensions/weft_docker/tests/test_docker_plugin.py -k test_describe_runtime_falls_back_to_container_id_when_name_lookup_misses` | pending: shared-vs-instance state review |
| MR-012 | `extensions/weft_docker/tests/test_docker_plugin.py::test_describe_runtime_falls_back_to_container_list_when_name_get_misses` | `RUF012=1`; `RUF012@743` | Mutable attributes on the local fake are intentionally shared at class scope within the owning test unless the test resets/overrides them. | Declare ClassVar when sharing is intentional; otherwise initialize per instance. The owning test must prove the choice. | `./.venv/bin/python -m pytest -q extensions/weft_docker/tests/test_docker_plugin.py -k test_describe_runtime_falls_back_to_container_list_when_name_get_misses` | pending: shared-vs-instance state review |
| MR-013 | `extensions/weft_docker/tests/test_docker_plugin.py::test_wait_for_container_runtime_start_fails_when_created_state_sticks` | `RUF012=1`; `RUF012@968` | Mutable attributes on the local fake are intentionally shared at class scope within the owning test unless the test resets/overrides them. | Declare ClassVar when sharing is intentional; otherwise initialize per instance. The owning test must prove the choice. | `./.venv/bin/python -m pytest -q extensions/weft_docker/tests/test_docker_plugin.py -k test_wait_for_container_runtime_start_fails_when_created_state_sticks` | pending: shared-vs-instance state review |
| MR-014 | `extensions/weft_docker/weft_docker/_sdk.py::wait_for_container_runtime_start` | accepted `BLE001=0` | Docker reload failures map to the existing runtime-start diagnostic, while non-SDK defects escape. | Narrow reload containment to `docker.errors.DockerException`; prove SDK fallback and unexpected `RuntimeError` propagation. | `./.venv/bin/python -m pytest -q extensions/weft_docker/tests/test_docker_plugin.py` | accepted: full 87-test extension suite, focused Ruff/mypy, and clean Python review `NET POSITIVE` |
| MR-015a | `extensions/weft_docker/weft_docker/agent_runner.py::DockerProviderCLIRunner.run_with_hooks` lifecycle callbacks | `BLE001=2, S110=2`; `BLE001@241; S110@241; BLE001@246; S110@246` | Worker-start and runtime-handle callback failures must not replace container execution, but lost lifecycle publication should be visible without exposing callback values, handles, or exception text. | Retain broad callback containment, emit distinct fixed warnings, and use `RUFF-SUP-278` for arbitrary extension callbacks. | `./.venv/bin/python -m pytest -q extensions/weft_docker/tests/test_agent_runner.py -k uses_cached_image` | accepted for S110: both callbacks fire, success survives, fixed messages contain no sensitive values or exception info; fresh clean refactor review `NET POSITIVE`; BLE001 candidate pending exact review and owner approval |
| MR-015b | same owner, stdout/stderr callbacks | `BLE001=2, S110=2`; `BLE001@286; S110@286; BLE001@291; S110@291` | Stream callback failures must not replace execution or remove captured output from the returned outcome, but lost stream delivery should be visible without logging chunk or exception text. | Retain broad callback containment, add distinct fixed warnings, and use `RUFF-SUP-279` for arbitrary stream consumers. | same Docker agent-runner suite | accepted for S110: both callbacks fire, output and success survive, fixed messages contain no chunk or exception data; fresh clean refactor review `NET POSITIVE`; BLE001 candidate pending exact review and owner approval |
| MR-015c | same owner, provider result conversion | `BLE001=1`; `BLE001@333` | Provider parsing or result construction failure becomes the primary `RunnerOutcome(status="error", error=str(exc))`; this branch is not silent. | Preserve broad conversion to the returned error outcome and use `RUFF-SUP-280`; provider adapters and result construction do not expose a stable exhaustive exception taxonomy. | same Docker agent-runner suite | candidate: parser and result-builder failures both return exact error outcomes; pending clean exact review and owner approval |
| MR-015d | same owner, container cleanup | `BLE001=1, S110=1`; `BLE001@361; S110@361` | Container removal failure must not replace a completed outcome or an in-flight primary exception, but leaked cleanup should be visible without container identity or exception text. | Retain broad cleanup containment, add one fixed warning, and use `RUFF-SUP-281` for variable Docker SDK/transport/daemon failures. | same Docker agent-runner suite | accepted for S110: removal fails after success and during a primary start failure; both outcomes survive and fixed warning contains no identity or exception data; fresh clean refactor review `NET POSITIVE`; BLE001 candidate pending exact review and owner approval |
| MR-016 | `extensions/weft_docker/weft_docker/agent_runner.py::_normalize_mounts` | `TRY004=2`; `TRY004@571; TRY004@575` | Wrong explicit-mount collection and entry shapes are malformed decoded runner-option values in the Docker agent runner's `ValueError` family. | Preserve exact classes/messages and use shared `RUFF-SUP-274`. | `./.venv/bin/python -m pytest -q extensions/weft_docker/tests/test_agent_runner.py` | candidate: both constructor paths fire; pending clean exact review and owner approval |
| MR-017 | `extensions/weft_docker/weft_docker/agent_runner.py::_normalize_optional_text` | `TRY004=1`; `TRY004@761` | A wrong-shape network option is malformed decoded runner-option data, adjacent to empty-string validation in the same `ValueError` family. | Preserve exact class/message and use shared `RUFF-SUP-274`. | same Docker agent-runner suite | candidate: exact constructor path fires; pending review and owner approval |
| MR-018 | `extensions/weft_docker/weft_docker/agent_runner.py::_normalize_required_text` | `TRY004=1`; `TRY004@770` | Wrong source/target scalar shapes are malformed explicit-mount values, adjacent to empty-string validation in the same `ValueError` family. | Preserve exact class/messages and use shared `RUFF-SUP-274`. | same Docker agent-runner suite | candidate: both named constructor paths fire through one exact raise; pending review and owner approval |
| MR-019 | `extensions/weft_docker/weft_docker/agent_runner.py::_normalize_work_item_mounts` | `TRY004=4`; `TRY004@599; TRY004@604; TRY004@625; TRY004@628` | Wrong work-item-mount collection, entry, `read_only`, and `required` shapes are malformed decoded runner-option values. | Preserve exact classes/messages and use shared `RUFF-SUP-274`. | same Docker agent-runner suite | candidate: first review `NET NEGATIVE` exposed two non-firing cases; corrected four exact constructor paths pending fresh review and owner approval |
| MR-020 | `extensions/weft_docker/weft_docker/images.py::_image_exists` | accepted `BLE001=0` | Only `ImageNotFound` is a cache miss. Other Docker API/auth/daemon failures must escape and must not trigger a build. | Catch `docker.errors.ImageNotFound`; prove miss-to-build and `APIError` propagation with no build. | `./.venv/bin/python -m pytest -q extensions/weft_docker/tests/test_agent_images.py` | accepted: exact cache-miss/error split fires; clean Python review `NET POSITIVE` |
| MR-021 | `extensions/weft_docker/weft_docker/plugin.py::_cleanup_process` | accepted `BLE001=0` | Operational kill failure returns without waiting; bounded wait contains OS failure or timeout. Unexpected defects from either phase escape. | Narrow kill to `OSError` and wait to `(OSError, subprocess.TimeoutExpired)`; directly prove all expected and unexpected paths. | `./.venv/bin/python -m pytest -q extensions/weft_docker/tests/test_docker_plugin.py -k cleanup_process` | accepted: 5 focused tests and Ruff pass; clean Python review `NET POSITIVE` |
| MR-022 | `extensions/weft_docker/weft_docker/plugin.py::_docker_container_liveness` | accepted `BLE001=0` | Docker SDK reload failure maps to `unknown` liveness; non-SDK defects escape. | Catch `docker.errors.DockerException`; prove fallback and unexpected-defect propagation. | `./.venv/bin/python -m pytest -q extensions/weft_docker/tests/test_docker_plugin.py` | accepted: clean Python review `NET POSITIVE` |
| MR-023 | `extensions/weft_docker/weft_docker/plugin.py::_docker_kill` | accepted `BLE001=0` | Docker SDK kill failure maps to `False`; non-SDK defects escape. | Catch `docker.errors.DockerException`; prove fallback and unexpected-defect propagation. | `./.venv/bin/python -m pytest -q extensions/weft_docker/tests/test_docker_plugin.py` | accepted: clean Python review `NET POSITIVE` |
| MR-024 | `extensions/weft_docker/weft_docker/plugin.py::_docker_runtime_liveness` | accepted `BLE001=0` | Stable SDK-loader unavailability and Docker SDK reload failure map to `unknown`; client/context/runtime defects outside the loader escape. | Limit `RuntimeError` to `_load_docker_sdk()` and catch `docker.errors.DockerException` for reload; prove both fallbacks and non-SDK propagation. | `./.venv/bin/python -m pytest -q extensions/weft_docker/tests/test_docker_plugin.py` | accepted: lexical loader scope and both error partitions reviewed `NET POSITIVE` |
| MR-025 | `extensions/weft_docker/weft_docker/plugin.py::_docker_stop` | accepted `BLE001=0` | Docker SDK stop failure maps to `False`; non-SDK defects escape. | Catch `docker.errors.DockerException`; prove fallback and unexpected-defect propagation. | `./.venv/bin/python -m pytest -q extensions/weft_docker/tests/test_docker_plugin.py` | accepted: clean Python review `NET POSITIVE` |
| MR-026 | `extensions/weft_docker/weft_docker/plugin.py::_lookup_container` | accepted `BLE001=0` | Docker SDK container-list failure retains the existing no-match fallback; non-SDK defects escape. | Catch `docker.errors.DockerException`; prove fallback and unexpected-defect propagation. | `./.venv/bin/python -m pytest -q extensions/weft_docker/tests/test_docker_plugin.py` | accepted: clean Python review `NET POSITIVE` |
| MR-027 | `extensions/weft_docker/weft_docker/plugin.py::_mapping_of_strings` | `TRY004=1`; `TRY004@1491` | Non-string environment mapping entries are malformed decoded TaskSpec values in the Docker plugin's `ValueError` validation family. | Preserve exact class/message and use shared `RUFF-SUP-275`. | `./.venv/bin/python -m pytest -q extensions/weft_docker/tests/test_docker_plugin.py` | candidate: exact direct normalizer branch fires; pending clean exact review and owner approval |
| MR-028 | `extensions/weft_docker/weft_docker/plugin.py::_normalize_mounts` | `TRY004=2`; `TRY004@1432; TRY004@1447` | Wrong mounts collection and `read_only` shapes are malformed decoded runner-option values. | Preserve exact classes/messages and use shared `RUFF-SUP-275`. | same Docker plugin suite | candidate: both exact direct normalizer branches fire; pending review and owner approval |
| MR-029 | `extensions/weft_docker/weft_docker/plugin.py::_normalize_required_text` | `TRY004=1`; `TRY004@1473` | Wrong text scalar shapes are malformed build/mount/network option values, adjacent to empty-string validation in the same `ValueError` family. | Preserve exact class/message and use shared `RUFF-SUP-275`. | same Docker plugin suite | candidate: exact direct normalizer branch fires; pending review and owner approval |
| MR-030 | `extensions/weft_docker/weft_docker/plugin.py::_optional_string` | `TRY004=1`; `TRY004@1500` | A wrong-shape `spec.working_dir` is malformed decoded TaskSpec data at plugin materialization. | Preserve exact class/message and use shared `RUFF-SUP-275`. | same Docker plugin suite | candidate: exact direct normalizer branch fires; pending review and owner approval |
| MR-031 | `extensions/weft_docker/weft_docker/plugin.py::_remove_container` | accepted `BLE001=0` | Docker SDK removal failure remains best effort; non-SDK defects escape. | Catch `docker.errors.DockerException`; prove fallback and unexpected-defect propagation. | `./.venv/bin/python -m pytest -q extensions/weft_docker/tests/test_docker_plugin.py` | accepted: clean Python review `NET POSITIVE` |
| MR-032 | `extensions/weft_docker/weft_docker/plugin.py::_require_mapping` | `TRY004=1`; `TRY004@1482` | Non-object TaskSpec/build/mount sections are malformed decoded configuration values in the plugin's field-qualified `ValueError` family. | Preserve exact class/message and use shared `RUFF-SUP-275`. | same Docker plugin suite | candidate: exact direct normalizer branch fires; public composition remains covered; pending review and owner approval |
| MR-033 | `extensions/weft_docker/weft_docker/plugin.py::_string_list` | `TRY004=1`; `TRY004@1509` | A wrong-shape `docker_args` value is malformed decoded runner-option data. | Preserve exact class/message and use shared `RUFF-SUP-275`. | same Docker plugin suite | candidate: exact direct normalizer branch fires; pending review and owner approval |
| MR-034 | `extensions/weft_docker/weft_docker/profiles.py::_load_profile_file` | `TRY004=1`; `TRY004@209` | `tomllib.load()` is typed and documented to return a dictionary; a non-mapping result is a decoder contract breach, not malformed TOML that a caller can supply through the real decoder. | Change the defensive branch from `ValueError` to `RuntimeError`; retain the full path-qualified message and no-cause behavior; because TRY004 still rejects the semantically correct exception, use `RUFF-SUP-276`. | `./.venv/bin/python -m pytest -q extensions/weft_docker/tests/test_container_profiles.py` | refactor: exact clean review `NET POSITIVE`; suppression candidate pending separate exact review and owner approval |
| MR-035 | `extensions/weft_docker/weft_docker/profiles.py::_mapping_of_strings` | `TRY004=1`; `TRY004@392` | Non-string environment entries are malformed materialization values in the Docker profile `ValueError` family. | Preserve exact class/message and use shared `RUFF-SUP-277`. | `./.venv/bin/python -m pytest -q extensions/weft_docker/tests/test_container_profiles.py` | candidate: exact direct normalizer branch fires; pending clean exact review and owner approval |
| MR-036 | `extensions/weft_docker/weft_docker/profiles.py::_require_mapping` | `TRY004=1`; `TRY004@419` | Non-object profile sections and mount/build entries are malformed decoded configuration values. | Preserve exact class/message and use shared `RUFF-SUP-277`. | same Docker profile suite | candidate: exact direct normalizer branch fires; public composition remains covered; pending review and owner approval |
| MR-037 | `extensions/weft_docker/weft_docker/profiles.py::_required_text` | `TRY004=1`; `TRY004@410` | Wrong scalar shapes in profile and materialization fields are part of the same field-qualified validation family as empty strings. | Preserve exact class/message and use shared `RUFF-SUP-277`. | same Docker profile suite | candidate: exact direct normalizer branch fires; pending review and owner approval |
| MR-038 | `extensions/weft_docker/weft_docker/profiles.py::_resolve_profile_mounts` | `TRY004=2`; `TRY004@321; TRY004@330` | Wrong mounts collection and `read_only` shapes are malformed persisted profile values. | Preserve exact classes/messages and use shared `RUFF-SUP-277`. | same Docker profile suite | candidate: both exact direct normalizer branches fire; pending review and owner approval |
| MR-039 | `extensions/weft_docker/weft_docker/profiles.py::_string_list` | `TRY004=1`; `TRY004@401` | Wrong environment-forwarding and `docker_args` list shapes are malformed persisted profile values. | Preserve exact class/message and use shared `RUFF-SUP-277`. | same Docker profile suite | candidate: exact direct normalizer branch fires; owner approved 2026-08-07 |
| MR-040 | `extensions/weft_macos_sandbox/weft_macos_sandbox/plugin.py::_require_mapping` | `TRY004=1`; live `TRY004@348` | Non-object `spec`, `spec.runner`, and `spec.runner.options` sections are malformed decoded TaskSpec values at the extension validation boundary. | Preserve exact class/messages and use `RUFF-SUP-272`. | `./.venv/bin/python -m pytest -q extensions/weft_macos_sandbox/tests/test_macos_sandbox_plugin.py`; extension mypy | candidate: all three public named paths fire through one exact raise; clean exact review `NET POSITIVE`; owner approved 2026-08-07 |
| MR-041 | `extensions/weft_microsandbox/tests/test_controls.py::<module>` | `RUF012=2`; `RUF012@15; RUF012@16` | Mutable attributes on the local fake are intentionally shared at class scope within the owning test unless the test resets/overrides them. | Declare ClassVar when sharing is intentional; otherwise initialize per instance. The owning test must prove the choice. | `./.venv/bin/python -m pytest -q extensions/weft_microsandbox/tests/test_controls.py` | pending: shared-vs-instance state review |
| MR-042 | `extensions/weft_microsandbox/tests/test_runtime_adapter.py::test_copy_into_guest_recursively_copies_directory_contents` | `RUF012=2`; `RUF012@149; RUF012@150` | Mutable attributes on the local fake are intentionally shared at class scope within the owning test unless the test resets/overrides them. | Declare ClassVar when sharing is intentional; otherwise initialize per instance. The owning test must prove the choice. | `./.venv/bin/python -m pytest -q extensions/weft_microsandbox/tests/test_runtime_adapter.py -k test_copy_into_guest_recursively_copies_directory_contents` | pending: shared-vs-instance state review |
| MR-043 | `extensions/weft_microsandbox/weft_microsandbox/_options.py::_mapping_of_strings` | `TRY004=1`; `TRY004@301` | A truthy non-object `spec.env` is malformed decoded TaskSpec data in the extension's field-qualified `ValueError` family. | Preserve exact class/message and use shared `RUFF-SUP-273`. | `./.venv/bin/python -m pytest -q extensions/weft_microsandbox/tests/test_options.py extensions/weft_microsandbox/tests/test_plugin_validation.py` | candidate: exact production-parser branch fires; clean exact review `NET POSITIVE`; owner approved 2026-08-07; falsy normalization recorded in Section 18 |
| MR-044 | `extensions/weft_microsandbox/weft_microsandbox/_options.py::_normalize_mode` | `TRY004=1`; `TRY004@230` | A wrong-shape mode is a malformed runner-option value, adjacent to invalid mode strings in the same `ValueError` family. | Preserve exact class/message and use shared `RUFF-SUP-273`. | same Microsandbox option/validation suites | candidate: exact production-parser branch fires; clean exact review `NET POSITIVE`; owner approved 2026-08-07 |
| MR-045 | `extensions/weft_microsandbox/weft_microsandbox/_options.py::_normalize_mounts` | `TRY004=3`; `TRY004@271; TRY004@275; TRY004@286` | Wrong mounts collection, entry, and `read_only` shapes are malformed decoded runner-option values. | Preserve exact classes/messages and use shared `RUFF-SUP-273`. | same Microsandbox option/validation suites | candidate: all three exact production-parser branches fire; clean exact review `NET POSITIVE`; owner approved 2026-08-07 |
| MR-046 | `extensions/weft_microsandbox/weft_microsandbox/_options.py::_normalize_network` | `TRY004=1`; `TRY004@241` | A wrong-shape network value is malformed runner-option data, adjacent to invalid network strings in the same `ValueError` family. | Preserve exact class/message and use shared `RUFF-SUP-273`. | same Microsandbox option/validation suites | candidate: exact production-parser branch fires; clean exact review `NET POSITIVE`; owner approved 2026-08-07 |
| MR-047 | `extensions/weft_microsandbox/weft_microsandbox/_options.py::_normalize_workspace_mode` | `TRY004=1`; `TRY004@252` | A wrong-shape workspace mode is malformed runner-option data, adjacent to invalid mode strings in the same `ValueError` family. | Preserve exact class/message and use shared `RUFF-SUP-273`. | same Microsandbox option/validation suites | candidate: exact production-parser branch fires; clean exact review `NET POSITIVE`; owner approved 2026-08-07 |
| MR-048 | `extensions/weft_microsandbox/weft_microsandbox/_options.py::_require_mapping` | `TRY004=1`; `TRY004@307` | Non-object `spec`, `spec.runner`, `spec.runner.options`, and `spec.agent` sections are malformed decoded TaskSpec values. | Preserve exact class/messages and use shared `RUFF-SUP-273`. | same Microsandbox option/validation suites | candidate: all four named production-parser paths fire through one exact raise; clean exact review `NET POSITIVE`; owner approved 2026-08-07 |
| MR-049 | `extensions/weft_microsandbox/weft_microsandbox/_runtime.py::MicrosandboxRuntime._describe_async` | initial `BLE001=2, S110=1`; accepted `BLE001=2, S110=0` | Failure reading optional SDK configuration metadata must not erase the core sandbox description. Lost optional metadata should remain visible without sandbox identity or SDK exception text. The separate get/refresh catch retains its missing-description fallback. | Retain broad config-accessor containment, emit one fixed warning, and use `RUFF-SUP-292`; disposition the get/refresh BLE001 separately. | `./.venv/bin/python -m pytest -q -n 0 extensions/weft_microsandbox/tests/test_runtime_adapter.py -k describe_reports_optional_configuration_failure`; full owner suite; targeted/current Ruff; extension mypy | accepted: config accessor fails; state/timestamps survive and config is omitted; exact bounded warning; fresh clean review `NET POSITIVE`; suppression owner approved 2026-08-07 |
| MR-050 | `extensions/weft_microsandbox/weft_microsandbox/_runtime.py::MicrosandboxRuntime._kill_async` | `BLE001=1`; `BLE001@234` | Any SDK lookup or kill failure maps to `False`; the runtime control adapter does not leak provider exceptions. | Preserve the SDK-to-bool control contract. Narrow only with complete provider exception proof; otherwise register a BLE001 suppression candidate. | `./.venv/bin/python -m pytest -q extensions/weft_microsandbox/tests/test_runtime_adapter.py extensions/weft_microsandbox/tests/test_command_runner.py` | candidate: independent suppression/locality review |
| MR-051 | `extensions/weft_microsandbox/weft_microsandbox/_runtime.py::MicrosandboxRuntime._stop_async` | `BLE001=1`; `BLE001@224` | Any SDK lookup or stop failure maps to `False`; the runtime control adapter does not leak provider exceptions. | Preserve the SDK-to-bool control contract. Narrow only with complete provider exception proof; otherwise register a BLE001 suppression candidate. | `./.venv/bin/python -m pytest -q extensions/weft_microsandbox/tests/test_runtime_adapter.py extensions/weft_microsandbox/tests/test_command_runner.py` | candidate: independent suppression/locality review |
| MR-052 | `extensions/weft_microsandbox/weft_microsandbox/_runtime.py::_cleanup_sandbox` | initial `BLE001=2, S110=2`; candidate `BLE001=2, S110=0` | Sandbox stop and removal are independent best-effort cleanup phases. A stop failure must not skip removal, and each failure should remain visible without sandbox identity or SDK exception text. | Retain broad per-phase containment, emit distinct fixed warnings, and use cleanup group `RUFF-SUP-290`. | `./.venv/bin/python -m pytest -q -n 0 extensions/weft_microsandbox/tests/test_runtime_adapter.py`; targeted/current Ruff; extension mypy | candidate: both phases raise, removal follows stop failure, exact bounded warnings; pending fresh clean review |
| MR-053 | `extensions/weft_microsandbox/weft_microsandbox/_runtime.py::_copy_back_files` | initial `BLE001=1, S110=1`; accepted `BLE001=1, S110=0` | Failure copying one requested guest output must not prevent later declared copy-back items. Lost output must remain visible without host path, guest path, or SDK exception text. | Retain broad per-item containment, emit one fixed warning, continue in declaration order, and use `RUFF-SUP-291`. | `./.venv/bin/python -m pytest -q -n 0 extensions/weft_microsandbox/tests/test_runtime_adapter.py -k copy_back_reports_failure_and_continues`; full owner suite; targeted/current Ruff; extension mypy | accepted: first copy fails, second runs, exact bounded warning; fresh clean review `NET POSITIVE`; suppression owner approved 2026-08-07 |
| MR-054 | `extensions/weft_microsandbox/weft_microsandbox/_runtime.py::_exec_with_cancel` | initial `BLE001=2, S110=1`; accepted `BLE001=2, S110=0` | Sandbox kill failure during requested cancellation must not skip the subsequent execution-task drain/cancel attempt or replace the cancelled result. Lost kill should remain visible without sandbox or SDK exception data. The separate post-kill wait catch retains its timeout/error cancellation fallback. | Retain broad kill containment, emit one fixed warning, attempt to drain/cancel the execution task, and use `RUFF-SUP-294`; disposition the wait catch separately. | `./.venv/bin/python -m pytest -q -n 0 extensions/weft_microsandbox/tests/test_runtime_adapter.py -k kill_failure_and_drains_exec_task`; full owner suite; targeted/current Ruff; extension mypy | accepted: first review `NET NEGATIVE` rejected a kill side effect and hard-bounded-wait claim; corrected different fresh review `NET POSITIVE`; suppression owner approved 2026-08-07 |
| MR-055 | `extensions/weft_microsandbox/weft_microsandbox/_runtime.py::_is_timeout_error` | `BLE001=1`; `BLE001@458` | The owner catches arbitrary boundary failure and follows its existing fallback/result/state path instead of allowing the failure to escape. | Audit the enclosed calls. Narrow only if the complete raised set and existing fallback are proven; otherwise register a BLE001 suppression candidate. | `./.venv/bin/python -m pytest -q extensions/weft_microsandbox/tests/test_runtime_adapter.py extensions/weft_microsandbox/tests/test_command_runner.py` | candidate: independent suppression/locality review |
| MR-056 | `extensions/weft_microsandbox/weft_microsandbox/_runtime.py::_mkdir_guest` | initial `BLE001=1, S110=1`; accepted `BLE001=0, S110=0` | Documented SDK filesystem failure during guest preparation remains warning-visible and does not skip a following declared copy. Unexpected programming or adapter failures must escape rather than allow execution with incompletely staged directories. | Load the SDK's `MicrosandboxError` base, narrow containment to that family, emit one fixed warning, and reject `RUFF-SUP-293`. | `./.venv/bin/python -m pytest -q -n 0 extensions/weft_microsandbox/tests/test_runtime_adapter.py -k 'guest_mkdir_failure or unexpected_mkdir_error'`; full owner suite; targeted/current Ruff; extension mypy | accepted after two `NET NEGATIVE` reviews and narrowed fresh review `NET POSITIVE`: supported SDK error continues; exact unexpected `RuntimeError` escapes; no suppression |
| MR-057 | `extensions/weft_microsandbox/weft_microsandbox/_runtime.py::_sandbox_name` | `BLE001=1`; `BLE001@438` | The owner catches arbitrary boundary failure and follows its existing fallback/result/state path instead of allowing the failure to escape. | Audit the enclosed calls. Narrow only if the complete raised set and existing fallback are proven; otherwise register a BLE001 suppression candidate. | `./.venv/bin/python -m pytest -q extensions/weft_microsandbox/tests/test_runtime_adapter.py extensions/weft_microsandbox/tests/test_command_runner.py` | candidate: independent suppression/locality review |
| MR-058 | `extensions/weft_microsandbox/weft_microsandbox/plugin.py::MicrosandboxRunner._execute` | initial `BLE001=2, S110=1`; accepted `BLE001=2, S110=0` | Runtime-handle publication is a caller-supplied observer and must not replace sandbox execution. Lost publication should remain visible without handle, PID, or exception data. The separate value-builder catch remains a primary error-outcome boundary. | Retain broad handle-callback containment, add one fixed warning, and use lifecycle group `RUFF-SUP-288`; disposition the value-builder BLE001 separately. | `./.venv/bin/python -m pytest -q -n 0 extensions/weft_microsandbox/tests/test_command_runner.py extensions/weft_microsandbox/tests/test_controls.py`; targeted/current Ruff; extension mypy | accepted: handle failure fires with worker/stream failures; successful outcome, exact handle ID, and bounded warnings survive; fresh clean review `NET POSITIVE` plus round-two evidence `PASS`; suppression owner approved 2026-08-07 |
| MR-059 | `extensions/weft_microsandbox/weft_microsandbox/plugin.py::_emit_stream` | initial `BLE001=1, S110=1`; accepted `BLE001=1, S110=0` | Caller-supplied stdout/stderr consumers must not replace execution or remove captured output from the result. Lost delivery should identify only the stream. | Retain broad containment, pass an explicit stream name to the adjacent helper, emit a fixed warning, and use `RUFF-SUP-289`. | same Microsandbox command/control owner suites | accepted: both stdout and stderr failures fire through one catch; output and outcome survive; fresh clean review `NET POSITIVE`; suppression owner approved 2026-08-07 |
| MR-060 | `extensions/weft_microsandbox/weft_microsandbox/plugin.py::_safe_callback` | initial `BLE001=1, S110=1`; accepted `BLE001=1, S110=0` | Worker-start publication is a caller-supplied observer and must not replace sandbox execution. Lost publication should remain visible without PID or exception data. | Retain broad containment, emit one fixed warning, and use shared lifecycle group `RUFF-SUP-288`. | same Microsandbox command/control owner suites | accepted: worker failure fires with handle/stream failures; successful outcome survives; fresh clean review `NET POSITIVE`; suppression owner approved 2026-08-07 |
| MR-061 | `integrations/weft_django/tests/test_weft_django.py::test_deferred_native_submission_validates_unknown_overrides_before_commit` | `SIM117=1`; `SIM117@354` | Nested context managers currently acquire in lexical order and release in reverse order while preserving exception propagation. | Combine with one with statement only if enter/exit/suppression order is identical; run the exact owner test and fresh locality review. | `./.venv/bin/python -m pytest -q integrations/weft_django/tests/test_weft_django.py -k test_deferred_native_submission_validates_unknown_overrides_before_commit` | ready: behavior-preserving candidate |
| MR-062 | `integrations/weft_django/tests/test_weft_django.py::test_deferred_spec_reference_validates_missing_reference_before_commit` | `SIM117=1`; `SIM117@364` | Nested context managers currently acquire in lexical order and release in reverse order while preserving exception propagation. | Combine with one with statement only if enter/exit/suppression order is identical; run the exact owner test and fresh locality review. | `./.venv/bin/python -m pytest -q integrations/weft_django/tests/test_weft_django.py -k test_deferred_spec_reference_validates_missing_reference_before_commit` | ready: behavior-preserving candidate |
| MR-063 | `integrations/weft_django/tests/test_weft_django.py::test_enqueue_on_commit_rollbacks_do_not_bind` | `SIM117=1`; `SIM117@326` | Nested context managers currently acquire in lexical order and release in reverse order while preserving exception propagation. | Combine with one with statement only if enter/exit/suppression order is identical; run the exact owner test and fresh locality review. | `./.venv/bin/python -m pytest -q integrations/weft_django/tests/test_weft_django.py -k test_enqueue_on_commit_rollbacks_do_not_bind` | ready: behavior-preserving candidate |
| MR-064 | `integrations/weft_django/tests/test_weft_django.py::test_transport_validation` | `SIM117=1`; `SIM117@599` | Nested context managers currently acquire in lexical order and release in reverse order while preserving exception propagation. | Combine with one with statement only if enter/exit/suppression order is identical; run the exact owner test and fresh locality review. | `./.venv/bin/python -m pytest -q integrations/weft_django/tests/test_weft_django.py -k test_transport_validation` | ready: behavior-preserving candidate |
| MR-065 | `integrations/weft_django/tests/test_weft_django.py::test_url_import_requires_authz_setting` | `SIM117=1`; `SIM117@592` | Nested context managers currently acquire in lexical order and release in reverse order while preserving exception propagation. | Combine with one with statement only if enter/exit/suppression order is identical; run the exact owner test and fresh locality review. | `./.venv/bin/python -m pytest -q integrations/weft_django/tests/test_weft_django.py -k test_url_import_requires_authz_setting` | ready: behavior-preserving candidate |
| MR-066 | `integrations/weft_django/weft_django/channels.py::TaskEventsConsumer._stream_events` | `TRY203=1`; `TRY203@123` | CancelledError and other failures propagate naturally through the existing `finally`, which sets the cancel event and closes the iterator. | Remove only the redundant catch/re-raise; add a direct cancellation firing test. | `./.venv/bin/python -m pytest -q integrations/weft_django/tests/test_weft_django.py -k 'channels_connect_returns_before_follow_stream_finishes or channels_stream_cancellation_propagates_after_iterator_close'` | accepted: clean post-refactor review `NET POSITIVE` |
| MR-067 | `integrations/weft_django/weft_django/client.py::_apply_taskspec_payload_overrides` | initial `TRY004=2`; `TRY004@232; TRY004@245`; accepted `TRY004=0` | The private helper's supported callers supply mappings with dict `spec` sections, and JSON round-tripping `dict(source)` must return an object. Either non-dict result violates a private object/serializer contract rather than a public payload boundary. | Change both `ValueError` raises to `TypeError`, preserve exact messages, and test each private defensive branch directly. | full Django integration suite; targeted Ruff; integration-package mypy | first mixed review `NET NEGATIVE`; rejected candidate preserved at `/tmp/weft-rw-django-try004-boundary`; coordinated TypeError rework review `NET POSITIVE` |
| MR-068 | `tests/architecture/test_import_boundaries.py::test_agent_backend_package_exports_keep_identity` | `S102=1`; `S102@941` | Test-only `exec` exercises real star-import grammar into an isolated namespace so exported object identity can be checked in process. | Preserve the runtime import-form proof and use shared group RUFF-SUP-254 in Section 18. | `./.venv/bin/python -m pytest -q tests/architecture/test_import_boundaries.py -k test_agent_backend_package_exports_keep_identity` | candidate: clean exact security review `NET POSITIVE`; owner approved 2026-08-07 |
| MR-069 | `tests/architecture/test_import_boundaries.py::test_commands_manager_export_supports_attribute_from_and_star_imports` | `S102=2`; `S102@978; S102@982` | Test-only `exec` exercises explicit from-import and star-import grammar into isolated namespaces while preserving same-process module identity. | Preserve the runtime import-form proofs and use shared group RUFF-SUP-254 in Section 18. | `./.venv/bin/python -m pytest -q tests/architecture/test_import_boundaries.py -k test_commands_manager_export_supports_attribute_from_and_star_imports` | candidate: clean exact security review `NET POSITIVE`; owner approved 2026-08-07 |
| MR-070 | `tests/cli/test_cli.py::TestCLIConstants.test_constants_override` | `SIM117=1`; `SIM117@118` | Nested context managers currently acquire in lexical order and release in reverse order while preserving exception propagation. | Combine with one with statement only if enter/exit/suppression order is identical; run the exact owner test and fresh locality review. | `./.venv/bin/python -m pytest -q tests/cli/test_cli.py -k test_constants_override` | ready: behavior-preserving candidate |
| MR-071 | `tests/cli/test_cli.py::TestModuleExecution.test_module_help` | `PLW1510=1`; `PLW1510@77` | The subprocess return code is currently non-fatal here and is either inspected by the caller or intentionally ignored by a probe/test. | Add explicit check=False. Use check=True only if a nonzero result is already proven fatal. | `./.venv/bin/python -m pytest -q tests/cli/test_cli.py -k test_module_help` | ready: mechanical candidate |
| MR-072 | `tests/cli/test_cli.py::TestModuleExecution.test_module_no_args` | `PLW1510=1`; `PLW1510@87` | The subprocess return code is currently non-fatal here and is either inspected by the caller or intentionally ignored by a probe/test. | Add explicit check=False. Use check=True only if a nonzero result is already proven fatal. | `./.venv/bin/python -m pytest -q tests/cli/test_cli.py -k test_module_no_args` | ready: mechanical candidate |
| MR-073 | `tests/cli/test_cli.py::TestModuleExecution.test_module_version` | `PLW1510=1`; `PLW1510@67` | The subprocess return code is currently non-fatal here and is either inspected by the caller or intentionally ignored by a probe/test. | Add explicit check=False. Use check=True only if a nonzero result is already proven fatal. | `./.venv/bin/python -m pytest -q tests/cli/test_cli.py -k test_module_version` | ready: mechanical candidate |
| MR-074 | `tests/cli/test_cli_result.py::_wait_for_outbox` | initial `BLE001=1, S110=1`; `BLE001@41; S110@41`; accepted `BLE001=0, S110=0` | `Queue.peek_one()` documents `OperationalError` for locked/busy backend reads. That transient failure does not fail result CLI setup; the helper closes that exact queue and retries until its existing deadline. Other exceptions remain programming/test failures and escape. | Narrow to `OperationalError` and use explicit `ready = False`, preserving silent transient retry without broad containment or an empty handler. | `./.venv/bin/python -m pytest -q tests/cli/test_cli_result.py` | broad-suppression review `NET NEGATIVE`; exact documented failure-then-success rework with each distinct queue closed reviewed `NET POSITIVE`; accepted with no suppression |
| MR-075 | `tests/cli/test_env_file_bootstrap.py::_probe_default_export_path` | `PLW1510=1`; `PLW1510@71` | The subprocess return code is currently non-fatal here and is either inspected by the caller or intentionally ignored by a probe/test. | Add explicit check=False. Use check=True only if a nonzero result is already proven fatal. | `./.venv/bin/python -m pytest -q tests/cli/test_env_file_bootstrap.py` | ready: mechanical candidate |
| MR-076 | `tests/cli/test_env_file_bootstrap.py::_run_module` | `PLW1510=1`; `PLW1510@38` | The subprocess return code is currently non-fatal here and is either inspected by the caller or intentionally ignored by a probe/test. | Add explicit check=False. Use check=True only if a nonzero result is already proven fatal. | `./.venv/bin/python -m pytest -q tests/cli/test_env_file_bootstrap.py` | ready: mechanical candidate |
| MR-077 | `tests/commands/test_queue.py::test_watch_queue_closes_generator_when_limit_stops_iteration` | `RUF012=1`; `RUF012@381` | Mutable attributes on the local fake are intentionally shared at class scope within the owning test unless the test resets/overrides them. | Declare ClassVar when sharing is intentional; otherwise initialize per instance. The owning test must prove the choice. | `./.venv/bin/python -m pytest -q tests/commands/test_queue.py -k test_watch_queue_closes_generator_when_limit_stops_iteration` | pending: shared-vs-instance state review |
| MR-078 | `tests/commands/test_queue.py::test_watch_queue_uses_queue_monitor` | `RUF012=1`; `RUF012@340` | Mutable attributes on the local fake are intentionally shared at class scope within the owning test unless the test resets/overrides them. | Declare ClassVar when sharing is intentional; otherwise initialize per instance. The owning test must prove the choice. | `./.venv/bin/python -m pytest -q tests/commands/test_queue.py -k test_watch_queue_uses_queue_monitor` | pending: shared-vs-instance state review |
| MR-079 | `tests/commands/test_task_evidence.py::_probe_with_task` | `BLE001=1`; `BLE001@227` | A helper thread transfers every throwable to the owning test thread without changing identity and is joined before handoff state is inspected. | Preserve the local thread handoff and use shared group RUFF-SUP-338. | `./.venv/bin/python -m pytest -q tests/commands/test_task_evidence.py` | candidate: firing proof plus behavior/locality and exact reviews `NET POSITIVE`; owner approved 2026-08-07 |
| MR-080 | `tests/commands/test_task_evidence.py::test_known_tid_ping_pong_updates_task_status` | `BLE001=1`; `BLE001@724` | A helper thread transfers every throwable to the owning test thread without changing identity and is joined before handoff state is inspected. | Preserve the local thread handoff and use shared group RUFF-SUP-338. | `./.venv/bin/python -m pytest -q tests/commands/test_task_evidence.py -k test_known_tid_ping_pong_updates_task_status` | candidate: firing proof plus behavior/locality and exact reviews `NET POSITIVE`; owner approved 2026-08-07 |
| MR-081 | `tests/conftest.py::_register_cli_outputs` | `BLE001=1, S112=1`; `BLE001@471; S112@471` | Non-JSON CLI output is ordinary mixed output and must be ignored; failures outside JSON decoding must remain visible in the test infrastructure. | Catch only `json.JSONDecodeError`; add firing tests for malformed input and an unexpected parser failure. | `./.venv/bin/python -m pytest -q tests/system/test_helpers.py -k 'register_cli_outputs'` | accepted: fresh clean Python expert `NET POSITIVE`; candidate `BLE001=0, S112=0` |
| MR-082 | `tests/conftest.py::broker_env` | `BLE001=1, S110=1`; `BLE001@339; S110@339` | Queue-close failures must not replace the test result or prevent later queue cleanup, but teardown failure must remain visible without reproducing backend exception text. | Retain broad containment; emit one fixed warning and continue closing in creation order. | `./.venv/bin/python -m pytest -q tests/system/test_helpers.py -k FixtureCleanupDiagnostics` | accepted: fresh clean Python expert `NET POSITIVE`; candidate `BLE001=1, S110=0` |
| MR-083 | `tests/conftest.py::queue_factory` | `BLE001=1, S110=1`; `BLE001@310; S110@310` | Queue-close failures must not replace the test result or prevent later queue cleanup, but teardown failure must remain visible without reproducing backend exception text. | Retain broad containment; emit one fixed warning and continue closing in creation order. | `./.venv/bin/python -m pytest -q tests/system/test_helpers.py -k FixtureCleanupDiagnostics` | accepted: fresh clean Python expert `NET POSITIVE`; candidate `BLE001=1, S110=0` |
| MR-084 | `tests/conftest.py::run_cli` | `BLE001=1`; `BLE001@439` | Timeout diagnostics are secondary evidence. Any ordinary failure from `WeftTestHarness.dump_debug_state()` must not replace the primary `TimeoutExpired`; fallback text must not expose exception payloads. | Retain the local broad diagnostic boundary, replace exception repr with one fixed line, and use exact `RUFF-SUP-310`. | `./.venv/bin/python -m pytest -q tests/system/test_helpers.py::TestFixtureCleanupDiagnostics` | candidate: exact TimeoutExpired identity, fallback-note, and secret-exclusion proof; behavior review `NET POSITIVE`; first exact review corrected a scope overclaim; different corrected exact review `NET POSITIVE`; owner approval pending |
| MR-085 | `tests/conftest.py::run_cli` | `PLW1510=1`; `PLW1510@416` | The subprocess return code is currently non-fatal here and is either inspected by the caller or intentionally ignored by a probe/test. | Add explicit check=False. Use check=True only if a nonzero result is already proven fatal. | `./.venv/bin/python -m pytest -q tests/conftest.py` | ready: mechanical candidate |
| MR-086 | `tests/conftest.py::task_factory` | `BLE001=1, S110=1`; `BLE001@362; S110@362` | Task-stop failures must not replace the test result or prevent later task cleanup, but teardown failure must remain visible without reproducing task exception text. | Retain broad containment; emit one fixed warning and continue stopping in creation order. | `./.venv/bin/python -m pytest -q tests/system/test_helpers.py -k FixtureCleanupDiagnostics` | accepted: fresh clean Python expert `NET POSITIVE`; candidate `BLE001=1, S110=0` |
| MR-087 | `tests/core/test_manager.py::test_manager_cleanup_waits_for_active_child_launch_worker` | `BLE001=1`; `BLE001@4240` | A helper thread transfers every throwable to the owning test thread without changing identity and is joined before handoff state is inspected. | Preserve the local thread handoff and use shared group RUFF-SUP-338. | `./.venv/bin/python -m pytest -q tests/core/test_manager.py -k test_manager_cleanup_waits_for_active_child_launch_worker` | candidate: firing proof plus behavior/locality and exact reviews `NET POSITIVE`; owner approved 2026-08-07 |
| MR-088 | `tests/core/test_manager.py::test_manager_late_child_launch_self_reaps_after_cleanup_deadline` | `BLE001=1`; `BLE001@4330` | A helper thread transfers every throwable to the owning test thread without changing identity and is joined before handoff state is inspected. | Preserve the local thread handoff and use shared group RUFF-SUP-338. | `./.venv/bin/python -m pytest -q tests/core/test_manager.py -k test_manager_late_child_launch_self_reaps_after_cleanup_deadline` | candidate: firing proof plus behavior/locality and exact reviews `NET POSITIVE`; owner approved 2026-08-07 |
| MR-089 | `tests/core/test_manager.py::test_manager_terminal_envelope_does_not_cache_child_ctrl_out_queue` | `RUF012=1`; `RUF012@4197` | Mutable attributes on the local fake are intentionally shared at class scope within the owning test unless the test resets/overrides them. | Declare ClassVar when sharing is intentional; otherwise initialize per instance. The owning test must prove the choice. | `./.venv/bin/python -m pytest -q tests/core/test_manager.py -k test_manager_terminal_envelope_does_not_cache_child_ctrl_out_queue` | pending: shared-vs-instance state review |
| MR-090 | `tests/core/test_manager.py::test_manager_terminal_envelope_skips_when_task_terminal_proof_exists` | `RUF012=1`; `RUF012@4270` | Mutable attributes on the local fake are intentionally shared at class scope within the owning test unless the test resets/overrides them. | Declare ClassVar when sharing is intentional; otherwise initialize per instance. The owning test must prove the choice. | `./.venv/bin/python -m pytest -q tests/core/test_manager.py -k test_manager_terminal_envelope_skips_when_task_terminal_proof_exists` | pending: shared-vs-instance state review |
| MR-091 | `tests/core/test_monitor_store.py::_monitor_message_ids` | `SIM117=1`; `SIM117@141` | Nested context managers currently acquire in lexical order and release in reverse order while preserving exception propagation. | Combine with one with statement only if enter/exit/suppression order is identical; run the exact owner test and fresh locality review. | `./.venv/bin/python -m pytest -q tests/core/test_monitor_store.py` | ready: behavior-preserving candidate |
| MR-092 | `tests/core/test_monitor_store.py::_monitor_table_count` | `SIM117=1`; `SIM117@129` | Nested context managers currently acquire in lexical order and release in reverse order while preserving exception propagation. | Combine with one with statement only if enter/exit/suppression order is identical; run the exact owner test and fresh locality review. | `./.venv/bin/python -m pytest -q tests/core/test_monitor_store.py` | ready: behavior-preserving candidate |
| MR-093 | `tests/core/test_monitor_store.py::test_monitor_store_lists_raw_deleted_child_refs_for_repair` | `SIM117=1`; `SIM117@1245` | Nested context managers currently acquire in lexical order and release in reverse order while preserving exception propagation. | Combine with one with statement only if enter/exit/suppression order is identical; run the exact owner test and fresh locality review. | `./.venv/bin/python -m pytest -q tests/core/test_monitor_store.py -k test_monitor_store_lists_raw_deleted_child_refs_for_repair` | ready: behavior-preserving candidate |
| MR-094 | `tests/core/test_monitor_store.py::test_monitor_store_prunes_legacy_message_tombstones` | `SIM117=1`; `SIM117@1312` | Nested context managers currently acquire in lexical order and release in reverse order while preserving exception propagation. | Combine with one with statement only if enter/exit/suppression order is identical; run the exact owner test and fresh locality review. | `./.venv/bin/python -m pytest -q tests/core/test_monitor_store.py -k test_monitor_store_prunes_legacy_message_tombstones` | ready: behavior-preserving candidate |
| MR-095 | `tests/core/test_monitor_store.py::test_monitor_store_rejects_newer_schema_version` | `SIM117=1`; `SIM117@1486` | Nested context managers currently acquire in lexical order and release in reverse order while preserving exception propagation. | Combine with one with statement only if enter/exit/suppression order is identical; run the exact owner test and fresh locality review. | `./.venv/bin/python -m pytest -q tests/core/test_monitor_store.py -k test_monitor_store_rejects_newer_schema_version` | ready: behavior-preserving candidate |
| MR-096 | `tests/core/test_monitor_store.py::test_store_sidecar_session_rolls_back_on_exception` | `SIM117=1`; `SIM117@162` | Nested context managers currently acquire in lexical order and release in reverse order while preserving exception propagation. | Combine with one with statement only if enter/exit/suppression order is identical; run the exact owner test and fresh locality review. | `./.venv/bin/python -m pytest -q tests/core/test_monitor_store.py -k test_store_sidecar_session_rolls_back_on_exception` | ready: behavior-preserving candidate |
| MR-097 | `tests/core/test_ops_shared.py::test_realtime_events_emits_state_when_terminal_derived_from_snapshot` | `RUF012=1`; `RUF012@400` | Mutable attributes on the local fake are intentionally shared at class scope within the owning test unless the test resets/overrides them. | Declare ClassVar when sharing is intentional; otherwise initialize per instance. The owning test must prove the choice. | `./.venv/bin/python -m pytest -q tests/core/test_ops_shared.py -k test_realtime_events_emits_state_when_terminal_derived_from_snapshot` | pending: shared-vs-instance state review |
| MR-098 | `tests/core/test_ops_shared.py::test_realtime_events_uses_terminal_state_seen_during_materialization` | `RUF012=1`; `RUF012@316` | Mutable attributes on the local fake are intentionally shared at class scope within the owning test unless the test resets/overrides them. | Declare ClassVar when sharing is intentional; otherwise initialize per instance. The owning test must prove the choice. | `./.venv/bin/python -m pytest -q tests/core/test_ops_shared.py -k test_realtime_events_uses_terminal_state_seen_during_materialization` | pending: shared-vs-instance state review |
| MR-099 | `tests/core/test_runner_plugins.py::<module>` | `RUF009=1`; `RUF009@16` | Each fake plugin currently receives an equal immutable RunnerCapabilities default. | Use field(default_factory=RunnerCapabilities); prove fake-plugin behavior. | `./.venv/bin/python -m pytest -q tests/core/test_runner_plugins.py` | ready: mechanical dataclass candidate |
| MR-100 | `tests/core/test_runtime_handle_liveness.py::<module>` | `PLR0402=1`; `PLR0402@7` | The module-qualified import currently preserves a facade/module object used for attribute lookup or monkeypatching. | Convert to from-import only after proving import identity and monkeypatch targets; otherwise register a PLR0402 suppression candidate. | `./.venv/bin/python -m pytest -q tests/core/test_runtime_handle_liveness.py` | pending: facade/monkeypatch identity review |
| MR-101 | `tests/fixtures/mcp_stdio_fixture.py::_read_response` | `TRY004=1`; `TRY004@306` | A JSON-RPC error response is a failed remote operation, so it remains `RuntimeError` even when its optional message field has an invalid type. | Preserve `RuntimeError` and use exact `RUFF-SUP-256`; do not recast remote failure as a caller argument error. | `./.venv/bin/python -m pytest -q tests/system/test_runtime_fixtures.py` | candidate: firing exception-contract test; pending exact suppression review and owner approval |
| MR-102 | `tests/fixtures/mcp_stdio_fixture.py::_server_command` | `TRY004=2`; `TRY004@267; TRY004@271` | These two branches reject invalid local argument types. The sole caller catches all exceptions, and other invalid `args` shapes already produce native `TypeError`. | Raise `TypeError` with unchanged messages; prove exact class/message for both branches. | `./.venv/bin/python -m pytest -q tests/system/test_runtime_fixtures.py` | accepted: fresh clean Python expert `NET POSITIVE`; candidate `TRY004=0` |
| MR-103 | `tests/fixtures/mcp_stdio_fixture.py::call_fixture_tool` | `TRY004=1`; `TRY004@113` | A malformed content collection returned by the remote fixture server is an operational protocol failure, not invalid caller input. | Preserve `RuntimeError` and use exact `RUFF-SUP-255`. | `./.venv/bin/python -m pytest -q tests/system/test_runtime_fixtures.py` | candidate: firing exception-contract test; pending exact suppression review and owner approval |
| MR-104 | `tests/fixtures/provider_cli_fixture.py::_execute_fixture_request` | `BLE001=1`; `BLE001@598` | The owner catches arbitrary boundary failure and follows its existing fallback/result/state path instead of allowing the failure to escape. | Audit the enclosed calls. Narrow only if the complete raised set and existing fallback are proven; otherwise register a BLE001 suppression candidate. | `./.venv/bin/python -m pytest -q tests/fixtures/provider_cli_fixture.py` | candidate: independent suppression/locality review |
| MR-105 | `tests/fixtures/provider_cli_fixture.py::_load_claude_mcp_servers` | `TRY004=2`; `TRY004@651; TRY004@654` | Syntactically valid JSON with the wrong configuration shape remains a value-validation failure. Missing and empty-list `mcpServers` values normalize to `{}` under the proven pre-existing contract; the second raise covers truthy non-dicts. | Preserve `ValueError` class/messages and use exact shared `RUFF-SUP-257`; do not change the separate normalization behavior in this lint slice. | `./.venv/bin/python -m pytest -q tests/system/test_runtime_fixtures.py` | candidate: two firing exception-contract cases; pending exact suppression review and owner approval |
| MR-106 | `tests/helpers/multiqueue_sigint_probe.py::main` | `BLE001=1`; `BLE001@103` | The owner catches arbitrary boundary failure and follows its existing fallback/result/state path instead of allowing the failure to escape. | Audit the enclosed calls. Narrow only if the complete raised set and existing fallback are proven; otherwise register a BLE001 suppression candidate. | `./.venv/bin/python -m pytest -q tests/helpers/multiqueue_sigint_probe.py` | candidate: independent suppression/locality review |
| MR-107 | `tests/helpers/test_backend.py::cleanup_postgres_schema_for_root` | `BLE001=1`; `BLE001@252` | The owner catches arbitrary boundary failure and follows its existing fallback/result/state path instead of allowing the failure to escape. | Audit the enclosed calls. Narrow only if the complete raised set and existing fallback are proven; otherwise register a BLE001 suppression candidate. | `./.venv/bin/python -m pytest -q tests/helpers/test_backend.py` | candidate: independent suppression/locality review |
| MR-108 | `tests/helpers/test_backend.py::cleanup_prepared_roots` | `BLE001=1, S112=1`; `BLE001@217; S112@217` | Backend-specific cleanup failures remain contained so every discovered schema is attempted, but the failed schema and config must remain diagnosable without exposing the DSN or backend exception text. | Retain broad best-effort containment; emit a fixed structured warning and continue in the declared traversal order. | `./.venv/bin/python -m pytest -q tests/system/test_test_backend.py` | accepted after rework: final fresh clean Python expert `NET POSITIVE`; rejected set-based proof preserved at `/tmp/weft-rw-s112-backend-logging`; candidate `BLE001=1, S112=0` |
| MR-109 | `tests/helpers/weft_harness.py::WeftTestHarness.__del__`, `WeftTestHarness.__enter__`, and adjacent rollback | initial finalizer `BLE001=1, S110=1`; accepted rework removes both and introduces rollback `BLE001=1` | Harness cleanup owns processes, queues, environment, cwd, files, and tempdirs. Running it from `__del__` violates the durable no-broker-I/O-from-finalizers lesson. Failed `__enter__` must instead preserve its primary failure while explicitly rolling back every completed setup phase. | Remove `__del__`; make entry transactional. Roll back cwd, prepared backend root while patched backend env remains active, environment, then tempdir; attempt every phase and attach only failed step names to the primary exception. Propose `RUFF-SUP-286` for the rollback loop's broad containment. | `./.venv/bin/python -m pytest -q -n 0 tests/test_harness_registration.py`; repository-wide construction-site audit; targeted/current Ruff; order, primary-identity, note, env/cwd, tempdir, and no-finalizer assertions | accepted rework: suppression review and first removal review both `NET NEGATIVE`; transactional source refactor and exact residual review `NET POSITIVE`; suppression owner approved 2026-08-07 |
| MR-110 | `tests/helpers/weft_harness.py::WeftTestHarness.__enter__` | `PYI034=1`; `PYI034@103` | __enter__ returns the same harness instance; callers rely on the context manager type. | Annotate with Self only after runtime and full mypy proof. | `./.venv/bin/python -m pytest -q tests/helpers/weft_harness.py` | ready: typing-contract candidate |
| MR-111 | `tests/helpers/weft_harness.py::WeftTestHarness._close_live_database_queues` | initial `BLE001=1, S110=1`; accepted `BLE001=1, S110=0` | One queue-close failure must not prevent later live queues from closing, because an unclosed connection can keep the harness database locked. Failure must remain visible without queue identity or backend exception text. | Retain broad containment, emit one fixed warning, continue in the supplied GC traversal sequence, and use `RUFF-SUP-287` for the residual BLE001. No behavior depends on GC traversal order. | `./.venv/bin/python -m pytest -q -n 0 tests/test_harness_registration.py -k queue_cleanup_reports_failure_and_continues`; full owner suite; targeted/current Ruff | accepted: first close fails, second still runs, exact bounded warning; fresh clean refactor/locality and exact suppression review `NET POSITIVE`; suppression owner approved 2026-08-07 |
| MR-112 | `tests/helpers/weft_harness.py::WeftTestHarness._format_debug_payload` | `BLE001=1`; `BLE001@242` | The owner catches arbitrary boundary failure and follows its existing fallback/result/state path instead of allowing the failure to escape. | Audit the enclosed calls. Narrow only if the complete raised set and existing fallback are proven; otherwise register a BLE001 suppression candidate. | `./.venv/bin/python -m pytest -q tests/helpers/weft_harness.py` | candidate: independent suppression/locality review |
| MR-113 | `tests/helpers/weft_harness.py::WeftTestHarness._load_tid_mapping_payloads` | `BLE001=1`; `BLE001@696` | The owner catches arbitrary boundary failure and follows its existing fallback/result/state path instead of allowing the failure to escape. | Audit the enclosed calls. Narrow only if the complete raised set and existing fallback are proven; otherwise register a BLE001 suppression candidate. | `./.venv/bin/python -m pytest -q tests/helpers/weft_harness.py` | candidate: independent suppression/locality review |
| MR-114 | `tests/helpers/weft_harness.py::WeftTestHarness._peek_queue_lines` | `BLE001=1`; `BLE001@260` | The owner catches arbitrary boundary failure and follows its existing fallback/result/state path instead of allowing the failure to escape. | Audit the enclosed calls. Narrow only if the complete raised set and existing fallback are proven; otherwise register a BLE001 suppression candidate. | `./.venv/bin/python -m pytest -q tests/helpers/weft_harness.py` | candidate: independent suppression/locality review |
| MR-115 | `tests/helpers/weft_harness.py::WeftTestHarness._terminate_pid` | `BLE001=1`; `BLE001@1135` | The owner catches arbitrary boundary failure and follows its existing fallback/result/state path instead of allowing the failure to escape. | Audit the enclosed calls. Narrow only if the complete raised set and existing fallback are proven; otherwise register a BLE001 suppression candidate. | `./.venv/bin/python -m pytest -q tests/helpers/weft_harness.py` | candidate: independent suppression/locality review |
| MR-116 | `tests/long_session_surface_benchmark.py::ApiSurface._invoke_run` | `SIM117=1`; `SIM117@526` | Nested context managers currently acquire in lexical order and release in reverse order while preserving exception propagation. | Combine with one with statement only if enter/exit/suppression order is identical; run the exact owner test and fresh locality review. | `./.venv/bin/python -m pytest -q -n 0 tests/test_long_session_surface_benchmark.py` | ready: behavior-preserving candidate; corrected because pytest collects no tests from the benchmark module itself |
| MR-117 | `tests/long_session_surface_benchmark.py::main` | `BLE001=1`; `BLE001@1192` | The owner catches arbitrary boundary failure and follows its existing fallback/result/state path instead of allowing the failure to escape. | Audit the enclosed calls. Narrow only if the complete raised set and existing fallback are proven; otherwise register a BLE001 suppression candidate. | `./.venv/bin/python -m pytest -q tests/long_session_surface_benchmark.py` | candidate: independent suppression/locality review |
| MR-118 | `tests/multiqueue_polling_benchmark.py::main` | `BLE001=1`; `BLE001@709` | The owner catches arbitrary boundary failure and follows its existing fallback/result/state path instead of allowing the failure to escape. | Audit the enclosed calls. Narrow only if the complete raised set and existing fallback are proven; otherwise register a BLE001 suppression candidate. | `./.venv/bin/python -m pytest -q tests/multiqueue_polling_benchmark.py` | candidate: independent suppression/locality review |
| MR-119 | `tests/system/test_constants.py::TestLoadConfig.test_log_tasks_retention_period_rejects_zero` | `SIM117=1`; `SIM117@635` | Nested context managers currently acquire in lexical order and release in reverse order while preserving exception propagation. | Combine with one with statement only if enter/exit/suppression order is identical; run the exact owner test and fresh locality review. | `./.venv/bin/python -m pytest -q tests/system/test_constants.py -k test_log_tasks_retention_period_rejects_zero` | ready: behavior-preserving candidate |
| MR-120 | `tests/system/test_constants.py::TestLoadConfig.test_removed_task_monitor_env_rejects` | `SIM117=1`; `SIM117@664` | Nested context managers currently acquire in lexical order and release in reverse order while preserving exception propagation. | Combine with one with statement only if enter/exit/suppression order is identical; run the exact owner test and fresh locality review. | `./.venv/bin/python -m pytest -q tests/system/test_constants.py -k test_removed_task_monitor_env_rejects` | ready: behavior-preserving candidate |
| MR-121 | `tests/system/test_constants.py::TestLoadConfig.test_removed_task_monitor_task_log_cutoff_rejects` | `SIM117=1`; `SIM117@646` | Nested context managers currently acquire in lexical order and release in reverse order while preserving exception propagation. | Combine with one with statement only if enter/exit/suppression order is identical; run the exact owner test and fresh locality review. | `./.venv/bin/python -m pytest -q tests/system/test_constants.py -k test_removed_task_monitor_task_log_cutoff_rejects` | ready: behavior-preserving candidate |
| MR-122 | `tests/system/test_constants.py::TestLoadConfig.test_reserved_cleanup_min_age_rejects_negative` | `SIM117=1`; `SIM117@579` | Nested context managers currently acquire in lexical order and release in reverse order while preserving exception propagation. | Combine with one with statement only if enter/exit/suppression order is identical; run the exact owner test and fresh locality review. | `./.venv/bin/python -m pytest -q tests/system/test_constants.py -k test_reserved_cleanup_min_age_rejects_negative` | ready: behavior-preserving candidate |
| MR-123 | `tests/system/test_constants.py::TestLoadConfig.test_task_monitor_batch_size_rejects_zero` | `SIM117=1`; `SIM117@604` | Nested context managers currently acquire in lexical order and release in reverse order while preserving exception propagation. | Combine with one with statement only if enter/exit/suppression order is identical; run the exact owner test and fresh locality review. | `./.venv/bin/python -m pytest -q tests/system/test_constants.py -k test_task_monitor_batch_size_rejects_zero` | ready: behavior-preserving candidate |
| MR-124 | `tests/system/test_constants.py::TestLoadConfig.test_task_monitor_interval_rejects_below_heartbeat_minimum` | `SIM117=1`; `SIM117@591` | Nested context managers currently acquire in lexical order and release in reverse order while preserving exception propagation. | Combine with one with statement only if enter/exit/suppression order is identical; run the exact owner test and fresh locality review. | `./.venv/bin/python -m pytest -q tests/system/test_constants.py -k test_task_monitor_interval_rejects_below_heartbeat_minimum` | ready: behavior-preserving candidate |
| MR-125 | `tests/system/test_constants.py::TestLoadConfig.test_task_monitor_store_write_batch_size_rejects_zero` | `SIM117=1`; `SIM117@624` | Nested context managers currently acquire in lexical order and release in reverse order while preserving exception propagation. | Combine with one with statement only if enter/exit/suppression order is identical; run the exact owner test and fresh locality review. | `./.venv/bin/python -m pytest -q tests/system/test_constants.py -k test_task_monitor_store_write_batch_size_rejects_zero` | ready: behavior-preserving candidate |
| MR-126 | `tests/system/test_constants.py::TestLoadConfig.test_task_monitor_task_log_scan_limit_rejects_zero` | `SIM117=1`; `SIM117@613` | Nested context managers currently acquire in lexical order and release in reverse order while preserving exception propagation. | Combine with one with statement only if enter/exit/suppression order is identical; run the exact owner test and fresh locality review. | `./.venv/bin/python -m pytest -q tests/system/test_constants.py -k test_task_monitor_task_log_scan_limit_rejects_zero` | ready: behavior-preserving candidate |
| MR-127 | `tests/tasks/process_target.py::run_task` | `SIM115=1`; `SIM115@59` | Each temporary file intentionally remains open through the simulated workload so resource monitoring observes `open_files`; the same function closes every handle and unlinks every path before return. | Preserve workload-length ownership under exact `RUFF-SUP-308`; a `with` rewrite would shorten or awkwardly fragment the intended lifetime. | `./.venv/bin/python -m pytest -q tests/specs/resource_management/test_resource_metrics.py tests/tasks/test_task_execution.py -k cleanup_on_exit_process_target` | accepted: context-manager refactor `NET NEGATIVE`; exact one-site suppression owner-approved 2026-08-07 |
| MR-128 | `tests/tasks/test_command_runner_parity.py::_ensure_docker_image_available` | `PLW1510=2`; `PLW1510@182; PLW1510@196` | The subprocess return code is currently non-fatal here and is either inspected by the caller or intentionally ignored by a probe/test. | Add explicit check=False. Use check=True only if a nonzero result is already proven fatal. | `./.venv/bin/python -m pytest -q tests/tasks/test_command_runner_parity.py` | ready: mechanical candidate |
| MR-129 | `tests/tasks/test_command_runner_parity.py::_skip_unavailable_runner` | `BLE001=1`; `BLE001@165` | The owner catches arbitrary boundary failure and follows its existing fallback/result/state path instead of allowing the failure to escape. | Audit the enclosed calls. Narrow only if the complete raised set and existing fallback are proven; otherwise register a BLE001 suppression candidate. | `./.venv/bin/python -m pytest -q tests/tasks/test_command_runner_parity.py` | candidate: independent suppression/locality review |
| MR-130 | `tests/tasks/test_multiqueue_watcher.py::test_background_add_queue_rebinds_exact_set_on_drive_owner` | `BLE001=1`; `BLE001@129` | The owner catches arbitrary boundary failure and follows its existing fallback/result/state path instead of allowing the failure to escape. | Audit the enclosed calls. Narrow only if the complete raised set and existing fallback are proven; otherwise register a BLE001 suppression candidate. | `./.venv/bin/python -m pytest -q tests/tasks/test_multiqueue_watcher.py -k test_background_add_queue_rebinds_exact_set_on_drive_owner` | candidate: independent suppression/locality review |
| MR-131 | `tests/tasks/test_multiqueue_watcher.py::test_background_add_with_preexisting_message_forces_immediate_discovery` | `BLE001=1`; `BLE001@578` | The owner catches arbitrary boundary failure and follows its existing fallback/result/state path instead of allowing the failure to escape. | Audit the enclosed calls. Narrow only if the complete raised set and existing fallback are proven; otherwise register a BLE001 suppression candidate. | `./.venv/bin/python -m pytest -q tests/tasks/test_multiqueue_watcher.py -k test_background_add_with_preexisting_message_forces_immediate_discovery` | candidate: independent suppression/locality review |
| MR-132 | `tests/tasks/test_multiqueue_watcher.py::test_background_membership_errors_return_to_requesting_thread` | `BLE001=1`; `BLE001@1816` | The owner catches arbitrary boundary failure and follows its existing fallback/result/state path instead of allowing the failure to escape. | Audit the enclosed calls. Narrow only if the complete raised set and existing fallback are proven; otherwise register a BLE001 suppression candidate. | `./.venv/bin/python -m pytest -q tests/tasks/test_multiqueue_watcher.py -k test_background_membership_errors_return_to_requesting_thread` | candidate: independent suppression/locality review |
| MR-133 | `tests/tasks/test_multiqueue_watcher.py::test_background_mutation_after_drive_reservation_waits_for_owner_claim` | `BLE001=1`; `BLE001@327` | The owner catches arbitrary boundary failure and follows its existing fallback/result/state path instead of allowing the failure to escape. | Audit the enclosed calls. Narrow only if the complete raised set and existing fallback are proven; otherwise register a BLE001 suppression candidate. | `./.venv/bin/python -m pytest -q tests/tasks/test_multiqueue_watcher.py -k test_background_mutation_after_drive_reservation_waits_for_owner_claim` | candidate: independent suppression/locality review |
| MR-134 | `tests/tasks/test_multiqueue_watcher.py::test_background_mutation_committed_before_stop_returns_success` | `BLE001=1`; `BLE001@1060` | The owner catches arbitrary boundary failure and follows its existing fallback/result/state path instead of allowing the failure to escape. | Audit the enclosed calls. Narrow only if the complete raised set and existing fallback are proven; otherwise register a BLE001 suppression candidate. | `./.venv/bin/python -m pytest -q tests/tasks/test_multiqueue_watcher.py -k test_background_mutation_committed_before_stop_returns_success` | candidate: independent suppression/locality review |
| MR-135 | `tests/tasks/test_multiqueue_watcher.py::test_background_mutation_from_handler_is_rejected_before_effects` | `BLE001=1`; `BLE001@1763` | The owner catches arbitrary boundary failure and follows its existing fallback/result/state path instead of allowing the failure to escape. | Audit the enclosed calls. Narrow only if the complete raised set and existing fallback are proven; otherwise register a BLE001 suppression candidate. | `./.venv/bin/python -m pytest -q tests/tasks/test_multiqueue_watcher.py -k test_background_mutation_from_handler_is_rejected_before_effects` | candidate: independent suppression/locality review |
| MR-136 | `tests/tasks/test_multiqueue_watcher.py::test_background_mutation_racing_stop_never_binds_after_owner_close` | `BLE001=1`; `BLE001@929` | The owner catches arbitrary boundary failure and follows its existing fallback/result/state path instead of allowing the failure to escape. | Audit the enclosed calls. Narrow only if the complete raised set and existing fallback are proven; otherwise register a BLE001 suppression candidate. | `./.venv/bin/python -m pytest -q tests/tasks/test_multiqueue_watcher.py -k test_background_mutation_racing_stop_never_binds_after_owner_close` | candidate: independent suppression/locality review |
| MR-137 | `tests/tasks/test_multiqueue_watcher.py::test_background_post_replace_publication_failure_restores_old_waiter` | `BLE001=1`; `BLE001@863` | The owner catches arbitrary boundary failure and follows its existing fallback/result/state path instead of allowing the failure to escape. | Audit the enclosed calls. Narrow only if the complete raised set and existing fallback are proven; otherwise register a BLE001 suppression candidate. | `./.venv/bin/python -m pytest -q tests/tasks/test_multiqueue_watcher.py -k test_background_post_replace_publication_failure_restores_old_waiter` | candidate: independent suppression/locality review |
| MR-138 | `tests/tasks/test_multiqueue_watcher.py::test_background_queue_open_failure_preserves_old_generation` | `BLE001=1`; `BLE001@704` | The owner catches arbitrary boundary failure and follows its existing fallback/result/state path instead of allowing the failure to escape. | Audit the enclosed calls. Narrow only if the complete raised set and existing fallback are proven; otherwise register a BLE001 suppression candidate. | `./.venv/bin/python -m pytest -q tests/tasks/test_multiqueue_watcher.py -k test_background_queue_open_failure_preserves_old_generation` | candidate: independent suppression/locality review |
| MR-139 | `tests/tasks/test_multiqueue_watcher.py::test_background_rebind_before_first_strategy_start_closes_unbound_cached_waiter` | `BLE001=1`; `BLE001@391` | The owner catches arbitrary boundary failure and follows its existing fallback/result/state path instead of allowing the failure to escape. | Audit the enclosed calls. Narrow only if the complete raised set and existing fallback are proven; otherwise register a BLE001 suppression candidate. | `./.venv/bin/python -m pytest -q tests/tasks/test_multiqueue_watcher.py -k test_background_rebind_before_first_strategy_start_closes_unbound_cached_waiter` | candidate: independent suppression/locality review |
| MR-140 | `tests/tasks/test_multiqueue_watcher.py::test_background_remove_queue_rebinds_exact_remaining_set` | `BLE001=1`; `BLE001@193` | The owner catches arbitrary boundary failure and follows its existing fallback/result/state path instead of allowing the failure to escape. | Audit the enclosed calls. Narrow only if the complete raised set and existing fallback are proven; otherwise register a BLE001 suppression candidate. | `./.venv/bin/python -m pytest -q tests/tasks/test_multiqueue_watcher.py -k test_background_remove_queue_rebinds_exact_remaining_set` | candidate: independent suppression/locality review |
| MR-141 | `tests/tasks/test_multiqueue_watcher.py::test_background_strategy_replacement_failure_fails_request_and_retries_drive` | `BLE001=1`; `BLE001@785` | The owner catches arbitrary boundary failure and follows its existing fallback/result/state path instead of allowing the failure to escape. | Audit the enclosed calls. Narrow only if the complete raised set and existing fallback are proven; otherwise register a BLE001 suppression candidate. | `./.venv/bin/python -m pytest -q tests/tasks/test_multiqueue_watcher.py -k test_background_strategy_replacement_failure_fails_request_and_retries_drive` | candidate: independent suppression/locality review |
| MR-142 | `tests/tasks/test_multiqueue_watcher.py::test_owner_fatal_exit_signals_every_queued_mutator` | `BLE001=1`; `BLE001@1469` | The owner catches arbitrary boundary failure and follows its existing fallback/result/state path instead of allowing the failure to escape. | Audit the enclosed calls. Narrow only if the complete raised set and existing fallback are proven; otherwise register a BLE001 suppression candidate. | `./.venv/bin/python -m pytest -q tests/tasks/test_multiqueue_watcher.py -k test_owner_fatal_exit_signals_every_queued_mutator` | candidate: independent suppression/locality review |
| MR-143 | `tests/tasks/test_multiqueue_watcher.py::test_same_waiter_replacement_publication_failure_preserves_installed_owner` | `BLE001=1`; `BLE001@1541` | The owner catches arbitrary boundary failure and follows its existing fallback/result/state path instead of allowing the failure to escape. | Audit the enclosed calls. Narrow only if the complete raised set and existing fallback are proven; otherwise register a BLE001 suppression candidate. | `./.venv/bin/python -m pytest -q tests/tasks/test_multiqueue_watcher.py -k test_same_waiter_replacement_publication_failure_preserves_installed_owner` | candidate: independent suppression/locality review |
| MR-144 | `tests/tasks/test_task_execution.py::test_base_task_foreign_wait_rejects_before_waiter_effects` | `BLE001=1`; `BLE001@480` | The owner catches arbitrary boundary failure and follows its existing fallback/result/state path instead of allowing the failure to escape. | Audit the enclosed calls. Narrow only if the complete raised set and existing fallback are proven; otherwise register a BLE001 suppression candidate. | `./.venv/bin/python -m pytest -q tests/tasks/test_task_execution.py -k test_base_task_foreign_wait_rejects_before_waiter_effects` | candidate: independent suppression/locality review |
| MR-145 | `tests/tasks/test_task_execution.py::test_base_task_process_once_rejects_a_second_drive_thread_before_policy` | `BLE001=1`; `BLE001@203` | The owner catches arbitrary boundary failure and follows its existing fallback/result/state path instead of allowing the failure to escape. | Audit the enclosed calls. Narrow only if the complete raised set and existing fallback are proven; otherwise register a BLE001 suppression candidate. | `./.venv/bin/python -m pytest -q tests/tasks/test_task_execution.py -k test_base_task_process_once_rejects_a_second_drive_thread_before_policy` | candidate: independent suppression/locality review |
| MR-146 | `tests/tasks/test_task_execution.py::test_base_task_rejects_reentrant_same_owner_turn` | `BLE001=1`; `BLE001@425` | The owner catches arbitrary boundary failure and follows its existing fallback/result/state path instead of allowing the failure to escape. | Audit the enclosed calls. Narrow only if the complete raised set and existing fallback are proven; otherwise register a BLE001 suppression candidate. | `./.venv/bin/python -m pytest -q tests/tasks/test_task_execution.py -k test_base_task_rejects_reentrant_same_owner_turn` | candidate: independent suppression/locality review |
| MR-147 | `tests/tasks/test_task_execution.py::test_base_task_stop_waits_for_starting_interlock` | `BLE001=1`; `BLE001@1078` | The owner catches arbitrary boundary failure and follows its existing fallback/result/state path instead of allowing the failure to escape. | Audit the enclosed calls. Narrow only if the complete raised set and existing fallback are proven; otherwise register a BLE001 suppression candidate. | `./.venv/bin/python -m pytest -q tests/tasks/test_task_execution.py -k test_base_task_stop_waits_for_starting_interlock` | candidate: independent suppression/locality review |
| MR-148 | `weft/_constants.py::<module>` | `SIM905=2`; `SIM905@1165; SIM905@1237` | Two compact whitespace-delimited ledgers classify TaskMonitor worker-snapshot fields as unordered frozensets: 178 expected and 78 replaced fields, with replaced a subset of expected. No consumer depends on declaration order. | Preserve the compact ledgers and propose one exact two-directive SIM905 group in Section 18. | `./.venv/bin/python -m pytest -q -n 0 tests/tasks/test_task_monitor.py::test_task_monitor_worker_local_snapshot_owns_mutable_runtime_resources tests/tasks/test_task_monitor.py::test_task_monitor_worker_snapshot_rejects_unclassified_stateful_field`; direct cardinality/subset probe | candidate: literal rewrite disposition `NET NEGATIVE`; clean final exact review `NET POSITIVE`; pending explicit owner approval |
| MR-149 | `weft/_constants.py::<module>` | `PYI064=15`; `PYI064@833; PYI064@1304; PYI064@1307; PYI064@1310; PYI064@1313; PYI064@1316; PYI064@1690; PYI064@1693; PYI064@1696; PYI064@1699; PYI064@1702; PYI064@1705; PYI064@1938; PYI064@1941; PYI064@1944` | Each Final constant has a single literal value and consumers rely on its inferred literal type. | Replace Final[Literal[value]] with bare Final; prove exact constants and full mypy. | `./.venv/bin/python -m pytest -q tests/system/test_constants.py tests/system/test_constants_properties.py && ./.venv/bin/mypy weft --config-file pyproject.toml` | ready: mechanical typing candidate |
| MR-150 | `weft/builtins/__init__.py::_normalize_supported_platforms` | `TRY004=1`; `TRY004@173` | A wrong-shape `supported_platforms` value comes from a packaged builtin TaskSpec and belongs to the catalog's resource-data `ValueError` family. | Preserve exact class/message and use shared `RUFF-SUP-271`. | `./.venv/bin/python -m pytest -q tests/system/test_builtin_contract.py tests/core/test_builtin_platform_support.py` | candidate: public catalog branch fires; clean exact review `NET POSITIVE`; owner approved 2026-08-07 |
| MR-151 | `weft/builtins/__init__.py::builtin_task_catalog` | `TRY004=2`; `TRY004@115; TRY004@119` | A decoded non-object builtin root or non-object `spec` is malformed packaged TaskSpec data, not Python API misuse. | Preserve exact classes/messages and use shared `RUFF-SUP-271`. | `./.venv/bin/python -m pytest -q tests/system/test_builtin_contract.py tests/core/test_builtin_platform_support.py` | candidate: both public catalog branches fire; clean exact review `NET POSITIVE`; owner approved 2026-08-07 |
| MR-152 | `weft/builtins/agent_images.py::_normalize_provider_list` | `TRY004=1`; `TRY004@164` | The builtin consumes queue/stdin work-item values; a non-list `providers` field is malformed task input in the same `ValueError` family as invalid provider entries and unsupported fields. | Preserve exact class/message and use shared `RUFF-SUP-270`. | `./.venv/bin/python -m pytest -q tests/core/test_builtin_agent_images.py` | candidate: public builtin branch fires; clean exact review `NET POSITIVE`; owner approved 2026-08-07 |
| MR-153 | `weft/builtins/agent_images.py::_parse_prepare_request` | `TRY004=3`; `TRY004@134; TRY004@137; TRY004@154` | Non-object work items, decoded non-object JSON, and non-boolean `refresh` are malformed task-input values rather than misuse of the Python function signature. | Preserve exact classes/messages and use shared `RUFF-SUP-270`. | `./.venv/bin/python -m pytest -q tests/core/test_builtin_agent_images.py` | candidate: all three public builtin branches fire; clean exact review `NET POSITIVE`; owner approved 2026-08-07 |
| MR-154 | `weft/builtins/agent_images.py::prepare_agent_images_task` | `BLE001=1`; `BLE001@80` | The owner catches arbitrary boundary failure and follows its existing fallback/result/state path instead of allowing the failure to escape. | Audit the enclosed calls. Narrow only if the complete raised set and existing fallback are proven; otherwise register a BLE001 suppression candidate. | `./.venv/bin/python -m pytest -q tests/core/test_builtin_agent_images.py` | candidate: independent suppression/locality review |
| MR-155 | `weft/cli/validate_taskspec.py::_resolve_taskspec_source` | `BLE001=1`; `BLE001@116` | Named-spec resolver failure returns none and the command exits 1 after Rich-rendered raw diagnostics; fatal exits propagate. Explicit paths and later validation/rendering remain outside. | Preserve the local named-resolution boundary and use RUFF-SUP-351. | `./.venv/bin/python -m pytest -q tests/cli/test_cli_validate.py` | candidate: exact behavior/locality review `NET POSITIVE`; owner approved 2026-08-07 |
| MR-156 | `weft/commands/_dump_support.py::cmd_dump` | `BLE001=2`; `BLE001@85; BLE001@118` | Context and export phases each map ordinary failures to exact phase-specific command results with verbatim diagnostics. | Preserve the two local phase boundaries and register exact group RUFF-SUP-330. | `./.venv/bin/python -m pytest -q tests/commands/test_dump_load.py` | accepted: behavior/locality and exact reviews `NET POSITIVE`; owner approved 2026-08-07 |
| MR-157 | `weft/commands/_load_support.py::_execute_import` | initial `BLE001=1`; accepted `BLE001=0` | Snapshot restoration performs only filesystem copy/unlink operations. Operational restore failure must retain both apply and restore details; unexpected restore defects must propagate with the apply failure as context. | Narrow the restore catch to `OSError`; preserve the outer arbitrary apply-failure rollback boundary. | `./.venv/bin/python -m pytest -q tests/commands/test_dump_load.py`; production mypy; targeted BLE001 | accepted: 23 owner tests pass; `OSError` conversion/cause and unexpected `RuntimeError` propagation fire directly; fresh clean Python expert `NET POSITIVE` |
| MR-158 | `weft/commands/_load_support.py::cmd_load` | `BLE001=3`; `BLE001@521; BLE001@537; BLE001@550` | Context, plan, and non-`ImportError` apply phases map ordinary failures to exact phase-specific command results with verbatim diagnostics. | Preserve the three local phase boundaries and register exact group RUFF-SUP-331. | `./.venv/bin/python -m pytest -q tests/commands/test_dump_load.py` | accepted: rejected formatter candidate; corrected behavior/locality and exact reviews `NET POSITIVE`; owner approved 2026-08-07 |
| MR-159 | `weft/commands/_task_snapshot_reducer.py::<module>` | `PYI025=1`; `PYI025@14` | The annotation accepts the abstract set interface, including immutable and custom set-like values; runtime behavior is unaffected. | Alias `collections.abc.Set` as `AbstractSet` and retain the abstract contract; run mypy plus reducer tests. | `./.venv/bin/python -m pytest -q tests/commands/test_task_snapshot_reducer.py && ./.venv/bin/mypy weft --config-file pyproject.toml` | ready: mechanical typing candidate |
| MR-160 | `weft/commands/events.py::<module>` | `PLR0402=1`; `PLR0402@16` | The command leaf needs one task-evidence helper; no caller depends on rebinding the owner module after consumer import. | Import `queue_names_for_tid` directly from the relative child module. | `./.venv/bin/python -m pytest -q tests/commands/test_result.py tests/core/test_ops_shared.py`; facade-backedge architecture tests | accepted: clean post-refactor review `NET POSITIVE` |
| MR-161 | `weft/commands/init.py::cmd_init` | `BLE001=1`; `BLE001@91` | Ordinary backend/plugin init failure returns 1 with established quiet/non-quiet output; fatal exits propagate. Context construction and config persistence remain outside. | Preserve the init transaction boundary and use RUFF-SUP-352. | `./.venv/bin/python -m pytest -q tests/cli/test_cli_init.py` | candidate: exact behavior/locality review `NET POSITIVE`; owner approved 2026-08-07 |
| MR-162 | `weft/commands/result.py::<module>` | `PLR0402=1`; `PLR0402@15` | The command leaf names its sibling implementation module directly; routing through `weft.commands` is runtime-equivalent today but violates the executable own-facade dependency rule. | Preserve the direct sibling owner and use shared group RUFF-SUP-251 in Section 18. | `./.venv/bin/python -m pytest -q tests/commands/test_result.py tests/cli/test_cli_result.py`; facade-backedge architecture tests | candidate: clean closure review `NET POSITIVE`; owner approved 2026-08-07 |
| MR-163 | `weft/commands/result.py::_collect_all_results` | `BLE001=1`; `BLE001@438` | Queue enumeration crosses the selected SimpleBroker backend. Any ordinary exception raised by `db.list_queues()` must become the command's one-line error rather than escape as a CLI traceback. | Preserve the localized broad command boundary and use exact `RUFF-SUP-309`; a narrowed tuple is incomplete until SimpleBroker guarantees backend-error translation. | `./.venv/bin/python -m pytest -q tests/commands/test_result.py tests/cli/test_cli_result.py` | accepted refactor: narrowed tuple `NET NEGATIVE`; candidate preserved at `/tmp/weft-rw-result-enumeration`; different broad-boundary rework review `NET POSITIVE`; exact row reworking after wording review |
| MR-164 | `weft/commands/result.py::cmd_result` | `BLE001=1`; `BLE001@949` | Initial context/backend resolution failure becomes the exact phase tuple with verbatim diagnostics; fatal exits propagate. Later validation, enumeration, waiting, aggregation, and rendering remain outside. | Preserve separately from RUFF-SUP-309 and use RUFF-SUP-353. | `./.venv/bin/python -m pytest -q tests/commands/test_result.py tests/cli/test_cli_result.py` | candidate: exact behavior/locality review `NET POSITIVE`; owner approved 2026-08-07 |
| MR-165 | `weft/commands/run.py::_execute_inline` | `BLE001=1`; `BLE001@1191` | Inline managed-execution failure becomes the structured submission error with verbatim diagnostics. | Preserve the local command-result boundary and register shared group RUFF-SUP-333. | `./.venv/bin/python -m pytest -q tests/commands/test_run.py tests/commands/test_interactive_client.py` | accepted: behavior/locality and exact reviews `NET POSITIVE`; owner approved 2026-08-07 |
| MR-166 | `weft/commands/run.py::_execute_spec_via_manager` | `BLE001=1`; `BLE001@1328` | TaskSpec managed-execution failure becomes its distinct structured submission error with verbatim diagnostics. | Preserve the local command-result boundary and register shared group RUFF-SUP-333. | `./.venv/bin/python -m pytest -q tests/commands/test_run.py tests/commands/test_interactive_client.py` | accepted: behavior/locality and exact reviews `NET POSITIVE`; owner approved 2026-08-07 |
| MR-167a | `weft/commands/run.py::_run_interactive_session._poll_interactive_monitor_terminal` | `BLE001=1`; `BLE001@636` | Monitor-store read failure returns `False` silently so task-local client/control and global-log evidence remain authoritative. | Preserve the optional-evidence boundary and register exact group RUFF-SUP-332. | `./.venv/bin/python -m pytest -q tests/commands/test_run.py tests/commands/test_interactive_client.py` | accepted: exact fallback proof and behavior/locality review `NET POSITIVE`; owner approved 2026-08-07 |
| MR-167b | `weft/commands/run.py::_run_interactive_session._await_completion` | initial `BLE001=1, S110=1`; `BLE001@685; S110@685`; accepted `BLE001=0, S110=0` | PromptToolkit documents that `Application.exit()` before `run()` does not stop the later application, and direct cross-thread future mutation is unsafe. Task completion must release the prompt whether it arrives before prompt startup or while the application runs. | Keep `completion_event` authoritative; use a prompt-thread `pre_run` exit check and schedule the same check with `loop.call_soon_threadsafe()` when running. Contain only the documented closed-loop `RuntimeError` race. | `./.venv/bin/python -m pytest -q tests/commands/test_run.py -k interactive_prompt_completion` | accepted: real PromptToolkit before-run and active-run completion proofs; fresh clean concurrency/locality review `NET POSITIVE`; no suppression |
| MR-168 | `weft/commands/specs.py::_validate_task_spec_payload` | `BLE001=6`; `BLE001@523; BLE001@534; BLE001@548; BLE001@564; BLE001@579; BLE001@595` | The owner catches arbitrary boundary failure and follows its existing fallback/result/state path instead of allowing the failure to escape. | Audit the enclosed calls. Narrow only if the complete raised set and existing fallback are proven; otherwise register a BLE001 suppression candidate. | `./.venv/bin/python -m pytest -q tests/commands/test_specs.py` | candidate: independent suppression/locality review |
| MR-169 | `weft/commands/submission.py::<module>` | `PLR0402=1`; `PLR0402@19` | The command leaf needs two related spec-resolution helpers; no caller depends on rebinding the owner module after consumer import. | Import both helpers directly from the relative child module. | `./.venv/bin/python -m pytest -q tests/commands/test_submission.py tests/core/test_ops_shared.py`; facade-backedge architecture tests | accepted: clean post-refactor review `NET POSITIVE` |
| MR-170 | `weft/commands/submission.py::apply_submit_overrides` | `TRY004=1`; `TRY004@93` | A valid `TaskSpec` always dumps its required `spec: SpecSection` model as an object; a non-mapping value means the supplied object's `model_dump()` contract returned the wrong type. | Change `ValueError` to `TypeError` and preserve the exact message; characterize the otherwise unreachable invariant through a real `TaskSpec` with the dump seam replaced. | `./.venv/bin/python -m pytest -q tests/commands/test_submission.py tests/core/test_ops_shared.py`; targeted Ruff; source-module mypy | accepted: exact dump-contract test and clean refactor review `NET POSITIVE` |
| MR-171 | `weft/commands/submission.py::ensure_manager_after_submission` | `BLE001=1`; `BLE001@220` | Ordinary manager-start failure is retained while durable queue-first reconciliation decides spawned/rejected/rolled-back/uncertain outcome; fatal exits bypass reconciliation. | Preserve the local startup-capture boundary and use RUFF-SUP-354. | `./.venv/bin/python -m pytest -q tests/commands/test_submission.py` | candidate: exact cause/order/locality review `NET POSITIVE`; owner approved 2026-08-07 |
| MR-172 | `weft/commands/system.py::<module>` | `PLR0402=1`; `PLR0402@22` | The command leaf names its sibling implementation module directly; routing through `weft.commands` is runtime-equivalent today but violates the executable own-facade dependency rule. | Preserve the direct sibling owner and use shared group RUFF-SUP-251 in Section 18. | `./.venv/bin/python -m pytest -q tests/commands/test_status.py tests/cli/test_cli_system.py`; facade-backedge architecture tests | candidate: clean closure review `NET POSITIVE`; owner approved 2026-08-07 |
| MR-173 | `weft/commands/system.py::_collect_task_snapshot_records` | `BLE001=1`; `BLE001@852` | Active-manager selection is optional reconciliation evidence. Failure degrades that datum to `None` while task-log replay and snapshot reduction continue silently. | Preserve the local optional-evidence boundary and use exact group RUFF-SUP-335. | `./.venv/bin/python -m pytest -q tests/commands/test_status.py tests/cli/test_cli_system.py` | candidate: initial evidence review `NET NEGATIVE`; corrected firing proof and two clean reviews `NET POSITIVE`; owner approved 2026-08-07 |
| MR-174 | `weft/commands/system.py::_watch_task_events` | `BLE001=1`; `BLE001@1586` | After queue acquisition, ordinary monitor/watch-loop failures become exit 1 with verbatim stderr while `KeyboardInterrupt` remains the successful stop path. | Preserve the local watch-loop boundary and use exact group RUFF-SUP-336. | `./.venv/bin/python -m pytest -q tests/commands/test_status.py tests/cli/test_cli_system.py` | candidate: behavior/locality and exact reviews `NET POSITIVE`; owner approved 2026-08-07 |
| MR-175 | `weft/commands/system.py::cmd_status` | `BLE001=1`; `BLE001@1614` | Ordinary initial context/filter/broker/manager-source failures become the phase-specific command error; later snapshots, services, watch, and rendering remain outside the catch. | Preserve the local initial-source boundary and use exact group RUFF-SUP-337. | `./.venv/bin/python -m pytest -q tests/commands/test_status.py tests/cli/test_cli_system.py` | candidate: behavior/locality and exact reviews `NET POSITIVE`; owner approved 2026-08-07 |
| MR-176 | `weft/commands/task_monitor.py::<module>` | `PLR0402=1`; `PLR0402@27` | This command depends on the non-exported `weft.core.task_evidence` implementation module. The package-from spelling is runtime-equivalent today but obscures that explicit owner. | Preserve the explicit lower-layer owner and use RUFF-SUP-253 in Section 18. | `./.venv/bin/python -m pytest -q tests/commands/test_task_monitor.py`; exact `test_internal_import_boundaries`, eager-cycle, and lightweight core-import architecture tests | candidate: exact reviews drove claim/proof tightening; clean closure review `NET POSITIVE`; owner approved 2026-08-07 |
| MR-177 | `weft/commands/task_monitor.py::_load_checkpoint` | `TRY004=1`; `TRY004@225` | Syntactically valid checkpoint JSON with a non-object top-level value is invalid persisted monitor state and remains a `ValueError`, distinct from chained read/decode failures. | Preserve exact class/message and use `RUFF-SUP-265`. | `./.venv/bin/python -m pytest -q tests/commands/test_task_monitor.py` | candidate: exact list-checkpoint contract test; pending review and owner approval |
| MR-178 | `weft/commands/tasks.py::<module>` | `PLR0402=1`; `PLR0402@23` | The command leaf names its sibling implementation module directly; routing through `weft.commands` is runtime-equivalent today but violates the executable own-facade dependency rule. | Preserve the direct sibling owner and use shared group RUFF-SUP-251 in Section 18. | `./.venv/bin/python -m pytest -q tests/commands/test_task_commands.py`; facade-backedge architecture tests | candidate: clean closure review `NET POSITIVE`; owner approved 2026-08-07 |
| MR-179 | `weft/commands/tasks.py::_monitor_store_task_snapshot` | `BLE001=1`; `BLE001@545` | Ordinary Monitor-store failure returns schema-missing none or a fixed-shape unknown snapshot with raw error; fatal exits and successful-record interpretation defects propagate. | Preserve the local store fallback and use RUFF-SUP-355. | `./.venv/bin/python -m pytest -q tests/commands/test_task_commands.py` | candidate: exact behavior/locality review `NET POSITIVE`; owner approved 2026-08-07 |
| MR-180 | `weft/core/agents/provider_cli/container_runtime.py::get_provider_container_runtime_descriptor` | `TRY004=1`; `TRY004@404` | Syntactically valid bundled descriptor JSON with a non-object root is invalid provider runtime data and remains a `ValueError`, distinct from chained JSON decode failure. | Preserve exact class/message and use `RUFF-SUP-266`. | `./.venv/bin/python -m pytest -q tests/core/test_provider_cli_container_runtime.py` | candidate: public loader fake-resource test; clean exact review `NET POSITIVE`; owner approved 2026-08-07 |
| MR-181 | `weft/core/agents/provider_cli/settings.py::_load_json_mapping` | `TRY004=1`; `TRY004@253` | A decoded non-object settings root is invalid persisted project configuration and remains a `ValueError`. | Preserve exact class/message and use shared `RUFF-SUP-267`. | `./.venv/bin/python -m pytest -q tests/core/test_provider_cli_settings.py` | candidate: exact public-load contract test; clean exact review `NET POSITIVE`; owner approved 2026-08-07 |
| MR-182 | `weft/core/agents/provider_cli/settings.py::_provider_settings_mapping` | `TRY004=2`; `TRY004@218; TRY004@226` | Non-object `provider_cli` and `provider_cli.providers` values are invalid persisted project configuration and remain `ValueError`. | Preserve exact classes/messages and use shared `RUFF-SUP-267`. | `./.venv/bin/python -m pytest -q tests/core/test_provider_cli_settings.py` | candidate: exact public-load contract tests; clean exact review `NET POSITIVE`; owner approved 2026-08-07 |
| MR-183 | `weft/core/agents/provider_cli/settings.py::ensure_provider_cli_project_executable` | `TRY004=1`; `TRY004@103` | A non-object existing provider entry is invalid persisted project configuration and remains a `ValueError`. | Preserve exact class/message and use shared `RUFF-SUP-267`. | `./.venv/bin/python -m pytest -q tests/core/test_provider_cli_settings.py` | candidate: exact public-write contract test; clean exact review `NET POSITIVE`; owner approved 2026-08-07 |
| MR-184 | `weft/core/agents/provider_cli/settings.py::load_provider_cli_project_settings` | `TRY004=1`; `TRY004@56` | A non-object provider entry is invalid persisted project configuration and remains a `ValueError`. | Preserve exact class/message and use shared `RUFF-SUP-267`. | `./.venv/bin/python -m pytest -q tests/core/test_provider_cli_settings.py` | candidate: exact public-load contract test; clean exact review `NET POSITIVE`; owner approved 2026-08-07 |
| MR-185 | `weft/core/agents/runtime.py::start_agent_runtime_session` | `TRY004=1`; `TRY004@292` | The runtime registry intentionally accepts execute-only adapters; persistent startup probes the separate optional `start_session` capability. Missing capability is runtime state, not invalid caller data. | Change `ValueError` to `RuntimeError`, preserve the message, and use `RUFF-SUP-268` because TRY004 also flags `RuntimeError` under `not callable`. | `./.venv/bin/python -m pytest -q tests/core/test_agent_runtime.py tests/core/test_provider_cli_session_backend.py`; targeted Ruff and mypy | accepted refactor: clean review `NET POSITIVE`; exact suppression candidate owner approved 2026-08-07 |
| MR-186 | `weft/core/agents/validation.py::_require_mapping` | `TRY004=1`; `TRY004@105` | Malformed object-valued sections inside a decoded TaskSpec payload use the shared field-qualified `ValueError` validation taxonomy. | Preserve exact class/messages and extend shared `RUFF-SUP-258`. | `./.venv/bin/python -m pytest -q tests/core/test_agent_validation.py` | candidate: exact `spec` and `spec.agent` public firing cases; corrected enlarged-group review `NET POSITIVE`; owner approved 2026-08-07 |
| MR-187 | `weft/core/callable.py::make_callable` | `PLW1510=1`; `PLW1510@105` | The subprocess return code is currently non-fatal here and is either inspected by the caller or intentionally ignored by a probe/test. | Add explicit check=False. Use check=True only if a nonzero result is already proven fatal. | `./.venv/bin/python -m pytest -q tests/core/test_callable.py` | ready: mechanical candidate |
| MR-188 | `weft/core/environment_profiles.py::_mapping_of_strings` | `TRY004=1`; `TRY004@173` | Malformed values inside a decoded TaskSpec payload use the shared `ValueError` validation taxonomy across environment materialization and runner validation. | Preserve exact class/message and use shared `RUFF-SUP-258`. | `./.venv/bin/python -m pytest -q tests/core/test_runner_validation.py` | candidate: exact firing contract test; owner approved 2026-08-07 |
| MR-189 | `weft/core/environment_profiles.py::_require_mapping` | `TRY004=1`; `TRY004@180` | Malformed values inside a decoded TaskSpec payload use the shared `ValueError` validation taxonomy across environment materialization and runner validation. | Preserve exact class/message and use shared `RUFF-SUP-258`. | `./.venv/bin/python -m pytest -q tests/core/test_runner_validation.py` | candidate: exact cross-entry-point contract test; owner approved 2026-08-07 |
| MR-190 | `weft/core/environment_profiles.py::_require_text` | `TRY004=1`; `TRY004@186` | Malformed values inside a decoded TaskSpec payload use the shared `ValueError` validation taxonomy across environment materialization and runner validation. | Preserve exact class/message and use shared `RUFF-SUP-258`. | `./.venv/bin/python -m pytest -q tests/core/test_runner_validation.py` | candidate: exact cross-entry-point contract test; owner approved 2026-08-07 |
| MR-191 | `weft/core/manager.py::Manager._atexit_unregister` | `BLE001=1, S110=1`; `BLE001@6914; S110@6914` | The interpreter-shutdown callback must keep unexpected `Exception`-derived queue/context cleanup failure out of Python's atexit exception-reporting path. This last-chance cleanup remains silent so it does not add another fallible teardown operation that would itself need containment. | Preserve broad silent teardown containment and use combined `RUFF-SUP-283`; do not add an observer solely to report failure from this last-chance observer-free fallback. | `./.venv/bin/python -m pytest -q tests/core/test_manager.py -k atexit_callback_silences_shutdown_cleanup_failure` | reworked candidate: direct failure proof captures manager logs at DEBUG plus both streams; first clean review `NET NEGATIVE` corrected inaccurate teardown rationale and incomplete log proof; pending fresh review and owner approval |
| MR-192 | `weft/core/manager.py::Manager._launch_child_task` | `BLE001=1`; `BLE001@1078` | Initial child-launch admission failure stays request-local, clears all launch bookkeeping, and delegates the full typed failure once. | Preserve the local boundary and use shared group RUFF-SUP-346. | `./.venv/bin/python -m pytest -q tests/core/test_manager.py tests/specs/manager_architecture/test_spawn_retry.py` | candidate: corrected evidence and exact review `NET POSITIVE`; owner approved 2026-08-07 |
| MR-193 | `weft/core/manager.py::Manager._retry_stale_child_launches` | `BLE001=1`; `BLE001@949` | Stale-retry admission failure stays request-local, clears all launch bookkeeping, and delegates the full typed failure once. | Preserve the local boundary and use shared group RUFF-SUP-346. | `./.venv/bin/python -m pytest -q tests/core/test_manager.py tests/specs/manager_architecture/test_spawn_retry.py` | candidate: corrected evidence and exact review `NET POSITIVE`; owner approved 2026-08-07 |
| MR-194 | `weft/core/manager.py::Manager._run_child_launch_worker` | `BLE001=1`; `BLE001@1139` | Worker fatal exits cross the daemon-thread boundary through the typed launch result with exact request/defaults/error identity. | Preserve the local fatal transport and use shared group RUFF-SUP-347. | `./.venv/bin/python -m pytest -q tests/core/test_manager.py tests/specs/manager_architecture/test_spawn_retry.py` | candidate: corrected evidence and exact review `NET POSITIVE`; owner approved 2026-08-07 |
| MR-195 | `weft/core/manager.py::Manager._unregister_atexit_callback` | initial `BLE001=1, S110=1`; `BLE001@6933; S110@6933`; accepted `BLE001=1, S110=0` | Failure to remove the registered callback during normal cleanup must not replace completed manager cleanup. The callback reference is cleared before the attempt, so repeat cleanup does not retry; failed removal should still be visible without exception text. | Retain broad best-effort containment and emit one fixed warning; review residual BLE001 separately. | `./.venv/bin/python -m pytest -q tests/core/test_manager.py -k cleanup_unregisters_registered_atexit_callback` | accepted: success and failure paths prove one attempt across repeated cleanup plus bounded warning privacy; fresh clean Python review `NET POSITIVE`; residual BLE001 pending separate review |
| MR-196 | `weft/core/manager_runtime.py::_stop_manager` | `BLE001=1`; `BLE001@1884` | The owner catches arbitrary boundary failure and follows its existing fallback/result/state path instead of allowing the failure to escape. | Audit the enclosed calls. Narrow only if the complete raised set and existing fallback are proven; otherwise register a BLE001 suppression candidate. | `./.venv/bin/python -m pytest -q tests/commands/test_manager_commands.py tests/cli/test_cli_manager.py` | candidate: independent suppression/locality review |
| MR-197 | `weft/core/manager_runtime.py::_terminate_manager_process` | `BLE001=2`; `BLE001@1704; BLE001@1711` | The owner catches arbitrary boundary failure and follows its existing fallback/result/state path instead of allowing the failure to escape. | Audit the enclosed calls. Narrow only if the complete raised set and existing fallback are proven; otherwise register a BLE001 suppression candidate. | `./.venv/bin/python -m pytest -q tests/commands/test_manager_commands.py tests/cli/test_cli_manager.py` | candidate: independent suppression/locality review |
| MR-198 | `weft/core/monitor/external_log.py::_PathWriter._close_handler_locked` | `BLE001=2`; `BLE001@115; BLE001@119` | The owner catches arbitrary boundary failure and follows its existing fallback/result/state path instead of allowing the failure to escape. | Audit the enclosed calls. Narrow only if the complete raised set and existing fallback are proven; otherwise register a BLE001 suppression candidate. | `./.venv/bin/python -m pytest -q tests/core/test_monitor_external_log.py` | candidate: independent suppression/locality review |
| MR-199 | `weft/core/monitor/runtime.py::<module>` | `PLR0402=1`; `PLR0402@27` | The core leaf names `weft.core.task_evidence` directly; routing through `weft.core` is runtime-equivalent and acyclic today but violates the executable own-facade dependency rule and couples the leaf to future facade changes. | Preserve the direct sibling owner and use RUFF-SUP-252 in Section 18. | `./.venv/bin/python -m pytest -q tests/core/test_task_monitoring.py tests/tasks/test_task_monitor.py`; facade-backedge, eager-cycle, and lightweight core-import architecture tests | candidate: exact reviews drove claim/proof tightening; clean closure review `NET POSITIVE`; owner approved 2026-08-07 |
| MR-200 | `weft/core/monitor/store.py::MonitorStore.status` | `BLE001=1`; `BLE001@2387` | The owner catches arbitrary boundary failure and follows its existing fallback/result/state path instead of allowing the failure to escape. | Audit the enclosed calls. Narrow only if the complete raised set and existing fallback are proven; otherwise register a BLE001 suppression candidate. | `./.venv/bin/python -m pytest -q tests/core/test_monitor_store.py` | candidate: independent suppression/locality review |
| MR-201 | `weft/core/monitor/task_monitor.py::TaskMonitor._close_worker_local_resources` | `BLE001=3`; `BLE001@1009; BLE001@1017; BLE001@1039` | The owner catches arbitrary boundary failure and follows its existing fallback/result/state path instead of allowing the failure to escape. | Audit the enclosed calls. Narrow only if the complete raised set and existing fallback are proven; otherwise register a BLE001 suppression candidate. | `./.venv/bin/python -m pytest -q tests/core/test_task_monitoring.py tests/core/test_task_monitor_cleanup.py` | candidate: independent suppression/locality review |
| MR-202 | `weft/core/monitor/task_monitor.py::TaskMonitor._emit_task_monitor_log` | `BLE001=1`; `BLE001@1304` | The owner catches arbitrary boundary failure and follows its existing fallback/result/state path instead of allowing the failure to escape. | Audit the enclosed calls. Narrow only if the complete raised set and existing fallback are proven; otherwise register a BLE001 suppression candidate. | `./.venv/bin/python -m pytest -q tests/core/test_task_monitoring.py tests/core/test_task_monitor_cleanup.py` | candidate: independent suppression/locality review |
| MR-203 | `weft/core/monitor/task_monitor.py::TaskMonitor._ensure_monitor_store` | `BLE001=1`; `BLE001@2292` | The owner catches arbitrary boundary failure and follows its existing fallback/result/state path instead of allowing the failure to escape. | Audit the enclosed calls. Narrow only if the complete raised set and existing fallback are proven; otherwise register a BLE001 suppression candidate. | `./.venv/bin/python -m pytest -q tests/core/test_task_monitoring.py tests/core/test_task_monitor_cleanup.py` | candidate: independent suppression/locality review |
| MR-204 | `weft/core/monitor/task_monitor.py::TaskMonitor._run_builtin_cycle_worker` | `BLE001=1`; `BLE001@5214` | The owner catches arbitrary boundary failure and follows its existing fallback/result/state path instead of allowing the failure to escape. | Audit the enclosed calls. Narrow only if the complete raised set and existing fallback are proven; otherwise register a BLE001 suppression candidate. | `./.venv/bin/python -m pytest -q tests/core/test_task_monitoring.py tests/core/test_task_monitor_cleanup.py` | candidate: independent suppression/locality review |
| MR-205 | `weft/core/monitor/task_monitor.py::TaskMonitor._run_custom_monitor_processor` | `BLE001=1`; `BLE001@5483` | The owner catches arbitrary boundary failure and follows its existing fallback/result/state path instead of allowing the failure to escape. | Audit the enclosed calls. Narrow only if the complete raised set and existing fallback are proven; otherwise register a BLE001 suppression candidate. | `./.venv/bin/python -m pytest -q tests/core/test_task_monitoring.py tests/core/test_task_monitor_cleanup.py` | candidate: independent suppression/locality review |
| MR-206 | `weft/core/monitor/task_monitor.py::TaskMonitor._run_terminal_control_cleanup_worker` | `BLE001=1`; `BLE001@3650` | The owner catches arbitrary boundary failure and follows its existing fallback/result/state path instead of allowing the failure to escape. | Audit the enclosed calls. Narrow only if the complete raised set and existing fallback are proven; otherwise register a BLE001 suppression candidate. | `./.venv/bin/python -m pytest -q tests/core/test_task_monitoring.py tests/core/test_task_monitor_cleanup.py` | candidate: independent suppression/locality review |
| MR-207 | `weft/core/monitor/task_monitor.py::TaskMonitor._worker_local_monitor_clone` | `TRY004=2`; `TRY004@840; TRY004@849` | A field that violates its declared worker-snapshot sharing classification is an internal runtime invariant failure, not invalid caller input. | Preserve exact `RuntimeError` class/messages and use shared `RUFF-SUP-260`. | `./.venv/bin/python -m pytest -q tests/tasks/test_task_monitor.py -k worker_snapshot` | candidate: both exact branches fire; pending review and owner approval |
| MR-208 | `weft/core/pipelines.py::_validate_stage_template` | `TRY004=1`; `TRY004@288` | A stored stage payload without a mapping `spec` section is malformed declarative pipeline data and belongs to the stage-template `ValueError` compatibility family. | Preserve exact class/message and use `RUFF-SUP-262`. | `./.venv/bin/python -m pytest -q tests/core/test_pipelines.py` | candidate: public compiler path fires exact branch; pending review and owner approval |
| MR-209 | `weft/core/resource_monitor.py::<module>` | `BLE001=1`; `BLE001@24` | The owner catches arbitrary boundary failure and follows its existing fallback/result/state path instead of allowing the failure to escape. | Audit the enclosed calls. Narrow only if the complete raised set and existing fallback are proven; otherwise register a BLE001 suppression candidate. | `./.venv/bin/python -m pytest -q tests/specs/resource_management/test_monitor_compat.py tests/specs/resource_management/test_resource_metrics.py` | candidate: independent suppression/locality review |
| MR-210 | `weft/core/resource_monitor.py::PsutilResourceMonitor.__init__` | `RUF034=1`; `RUF034@178` | Both conditional branches currently assign None, so psutil availability does not change initial _process state. | Replace the redundant conditional with None and retain delayed start_monitoring initialization; run resource-monitor tests. | `./.venv/bin/python -m pytest -q tests/specs/resource_management/test_monitor_compat.py tests/specs/resource_management/test_resource_metrics.py` | ready: behavior-preserving defect fix |
| MR-211 | `weft/core/resource_monitor.py::PsutilResourceMonitor.check_limits` | initial `BLE001=1`; accepted `BLE001=0` | Concrete psutil-monitor unavailability, not-started, stopped, and exited-process paths raise `RuntimeError` and fail open; unexpected defects must propagate. | Narrow to `RuntimeError`; prove real unavailable/exited paths and unexpected `ValueError` propagation. | `./.venv/bin/python -m pytest -q tests/specs/resource_management/test_monitor_compat.py tests/specs/resource_management/test_resource_metrics.py` | accepted: behavior/locality review `NET POSITIVE` |
| MR-212 | `weft/core/runner_validation.py::_require_mapping` | `TRY004=1`; `TRY004@132` | Malformed values inside a decoded TaskSpec payload use the shared `ValueError` validation taxonomy across environment materialization and runner validation. | Preserve exact class/message and use shared `RUFF-SUP-258`. | `./.venv/bin/python -m pytest -q tests/core/test_runner_validation.py` | candidate: exact cross-entry-point contract test; negative isolated-TypeError refactor preserved at `/tmp/weft-rw-try004-runner-validation`; owner approved 2026-08-07 |
| MR-213 | `weft/core/runner_validation.py::_require_text` | `TRY004=1`; `TRY004@138` | Malformed values inside a decoded TaskSpec payload use the shared `ValueError` validation taxonomy across environment materialization and runner validation. | Preserve exact class/message and use shared `RUFF-SUP-258`. | `./.venv/bin/python -m pytest -q tests/core/test_runner_validation.py` | candidate: exact cross-entry-point contract test; negative isolated-TypeError refactor preserved at `/tmp/weft-rw-try004-runner-validation`; owner approved 2026-08-07 |
| MR-214 | `weft/core/runners/host.py::HostTaskRunner._close_mp_queue` | initial `BLE001=2, S110=2`; candidate `BLE001=2, S110=0` | Multiprocessing queue close and feeder-thread join are independent cleanup phases. Close failure must not skip join, and each failure should remain visible without queue or exception data. | Retain broad per-phase containment, emit distinct fixed warnings, and use `RUFF-SUP-295`. | `./.venv/bin/python -m pytest -q -n 0 tests/tasks/test_runner.py -k host_queue_cleanup_reports_each_failure_and_continues`; targeted/current Ruff; host mypy | candidate: both phases raise, join follows close failure, exact bounded warnings; fresh clean review `NET POSITIVE` |
| MR-215 | `weft/core/runners/host.py::HostTaskRunner._close_process_handle` | initial `BLE001=1, S110=1`; accepted rework `BLE001=0, S110=0` | Process-handle cleanup runs in failure and finally paths. A live-process rejection or platform-handle close failure must not replace the primary runner outcome, but unexpected defects should escape. | Catch the declared `ValueError` live-process rejection and platform `OSError`, emit one fixed warning, and reject `RUFF-SUP-296`. | `./.venv/bin/python -m pytest -q -n 0 tests/tasks/test_runner.py -k host_process_handle_cleanup`; targeted/current Ruff; host mypy | first broad proposal review `NET NEGATIVE`; narrowed rework covers both supported families and propagates unexpected `RuntimeError`; different fresh clean review `NET POSITIVE` |
| MR-216 | `weft/core/runners/host.py::HostTaskRunner._run_one_shot_terminal_handoff` | `BLE001=3`; `BLE001@507; BLE001@598; BLE001@763` | The owner catches arbitrary boundary failure and follows its existing fallback/result/state path instead of allowing the failure to escape. | Audit the enclosed calls. Narrow only if the complete raised set and existing fallback are proven; otherwise register a BLE001 suppression candidate. | `./.venv/bin/python -m pytest -q tests/tasks/test_runner.py tests/core/test_terminal_handoff.py` | candidate: independent suppression/locality review |
| MR-217 | `weft/core/runners/host.py::HostTaskRunner._stop_process` | initial `BLE001=1, S110=1`; accepted `BLE001=0, S110=0` | An OS-level wait failure after tree termination must not itself skip direct terminate/kill escalation when liveness remains observable, but an unexpected ownership or programming defect should escape. Lost wait observation should remain visible without process or exception data. | Narrow to `OSError`, emit one fixed warning, continue the existing escalation order after a successful liveness recheck, and reject `RUFF-SUP-297`. | `./.venv/bin/python -m pytest -q -n 0 tests/tasks/test_runner.py -k host_process_stop`; targeted/current Ruff; host mypy | accepted: OS wait failure follows exact escalation when liveness remains observable; unexpected `RuntimeError` escapes; fresh clean review `NET POSITIVE` |
| MR-218 | `weft/core/runners/host.py::HostTaskRunner.run_with_hooks` | `BLE001=2, S110=0`; `BLE001@477; BLE001@482` | Worker-start and runtime-handle observer failure remains non-fatal to the spawned outcome; each emits a distinct fixed warning without callback/runtime/exception data. Control-flow `BaseException` propagates exactly while enclosing runner cleanup owns the process and pipes. | Preserve the two local callback boundaries and use exact group RUFF-SUP-343. | `./.venv/bin/python -m pytest -q tests/tasks/test_runner.py` | candidate: S110 and BLE001 behavior/locality/exact reviews `NET POSITIVE`; owner approved 2026-08-07 |
| MR-219 | `weft/core/runners/host.py::HostTaskRunner.start_agent_session` | `BLE001=1`; `BLE001@1095` | The owner catches arbitrary boundary failure and follows its existing fallback/result/state path instead of allowing the failure to escape. | Audit the enclosed calls. Narrow only if the complete raised set and existing fallback are proven; otherwise register a BLE001 suppression candidate. | `./.venv/bin/python -m pytest -q tests/tasks/test_runner.py tests/core/test_terminal_handoff.py` | candidate: independent suppression/locality review |
| MR-220 | `weft/core/runners/host.py::HostTaskRunner.start_session` | `BLE001=1`; `BLE001@1040` | The owner catches arbitrary boundary failure and follows its existing fallback/result/state path instead of allowing the failure to escape. | Audit the enclosed calls. Narrow only if the complete raised set and existing fallback are proven; otherwise register a BLE001 suppression candidate. | `./.venv/bin/python -m pytest -q tests/tasks/test_runner.py tests/core/test_terminal_handoff.py` | candidate: independent suppression/locality review |
| MR-221 | `weft/core/runners/host.py::_agent_session_worker_entry` | residual `BLE001=2`; `BLE001@313; BLE001@347`; session close separately approved RUFF-SUP-298 | Ordinary work-item failure becomes a traceback-bearing execute result; outer startup failure remains startup_error while post-ready failure becomes execute result. Fatal exits propagate. Session closes first, then both IPC endpoints exactly once without endpoint-order contract. | Preserve the two execution/result boundaries and use RUFF-SUP-356; keep independent session-close cleanup under RUFF-SUP-298. | `./.venv/bin/python -m pytest -q tests/tasks/test_runner.py` | candidate: four reviews corrected protocol frame, cleanup cardinality, and synthetic production-frame proof; final different review `NET POSITIVE`; owner approved 2026-08-07 |
| MR-222 | `weft/core/runners/host.py::_worker_entry` | `BLE001=1`; `BLE001@189` | Ordinary function/agent/command execution failure becomes one terminal error outcome carrying child traceback and host diagnostics; sender closes. Control-flow `BaseException` propagates exactly. | Preserve the child execution boundary and use exact group RUFF-SUP-342. | `./.venv/bin/python -m pytest -q tests/tasks/test_runner.py tests/core/test_terminal_handoff.py` | candidate: firing proof and behavior/locality/exact review `NET POSITIVE`; owner approved 2026-08-07 |
| MR-223 | `weft/core/runners/subprocess_runner.py::run_monitored_subprocess` | residual `BLE001=2, S110=0`; `BLE001@105; BLE001@110` | Ordinary lifecycle callback failure remains nonfatal with fixed private warnings. Callback `BaseException` propagates exactly only after a guarded stop/wait/runtime-kill/poll/direct-kill/reap ladder in which cleanup failure stays subordinate. | Preserve the two callback sites and shared fatal-reap helper; use exact group RUFF-SUP-350. Configured-monitor sites are separately approved under RUFF-SUP-311. | `./.venv/bin/python -m pytest -q tests/core/test_subprocess_runner.py` | candidate: three reviews corrected production cleanup, unguarded poll/kill, and false finalizer proof; final clean review `NET POSITIVE`; owner approved 2026-08-07 |
| MR-224 | `weft/core/serve_log.py::emit_serve_log_record` | `BLE001=2`; `BLE001@149; BLE001@155` | The owner catches arbitrary boundary failure and follows its existing fallback/result/state path instead of allowing the failure to escape. | Audit the enclosed calls. Narrow only if the complete raised set and existing fallback are proven; otherwise register a BLE001 suppression candidate. | `./.venv/bin/python -m pytest -q tests/commands/test_serve.py tests/cli/test_cli_serve.py` | candidate: independent suppression/locality review |
| MR-225 | `weft/core/spawn_requests.py::_write_spawn_request_with_timestamp` | `TRY004=1`; `TRY004@122` | A broker connection missing the required exact-ID insertion capability violates the runtime integration contract; it is not invalid spawn-request data or a direct argument-type error. | Keep `RuntimeError`, replace the obsolete SimpleBroker 4.3.0 floor with capability-based guidance, and use `RUFF-SUP-263`. | `./.venv/bin/python -m pytest -q tests/core/test_spawn_requests.py` | candidate: exact capability-failure test; pending refactor/suppression review and owner approval |
| MR-226 | `weft/core/spec_store.py::_read_json` | `TRY004=1`; `TRY004@72` | Syntactically valid stored JSON with a non-object top-level value is malformed spec data and remains a `ValueError`, distinct from read/decode failures chained from their causes. | Preserve exact class/message and use `RUFF-SUP-264`. | `./.venv/bin/python -m pytest -q tests/core/test_spec_store.py` | candidate: exact list-payload contract test; pending review and owner approval |
| MR-227 | `weft/core/state_machines.py::StateMachine._source_states` | `TRY004=1`; `TRY004@262` | An invalid transition source is malformed declarative machine data and remains part of the constructor's `ValueError` validation family. | Preserve exact class/message and use `RUFF-SUP-261`. | `./.venv/bin/python -m pytest -q tests/core/test_state_machines.py -k plain_set_source` | candidate: exact firing contract test; pending review and owner approval |
| MR-228 | `weft/core/task_evidence.py::describe_runtime` | `BLE001=1`; `BLE001@772` | The owner catches arbitrary boundary failure and follows its existing fallback/result/state path instead of allowing the failure to escape. | Audit the enclosed calls. Narrow only if the complete raised set and existing fallback are proven; otherwise register a BLE001 suppression candidate. | `./.venv/bin/python -m pytest -q tests/commands/test_task_evidence.py` | candidate: independent suppression/locality review |
| MR-229 | `weft/core/tasks/base.py::BaseTask._control_runtime_summary` | `BLE001=1`; `BLE001@1770` | Ordinary runner-description failure preserves runtime identity/metadata with unknown state and verbatim error; fatal exits propagate. | Preserve the optional diagnostic fallback and use shared group RUFF-SUP-348. | `./.venv/bin/python -m pytest -q tests/tasks/test_task_execution.py tests/tasks/test_control_channel.py` | candidate: corrected evidence and exact review `NET POSITIVE`; owner approved 2026-08-07 |
| MR-230 | `weft/core/tasks/base.py::BaseTask._pong_extension_fields` | `BLE001=1`; `BLE001@1712` | Ordinary extension failure remains nested while keyed core PONG/request correlation survive; fatal exits propagate. | Preserve the optional diagnostic fallback and use shared group RUFF-SUP-348. | `./.venv/bin/python -m pytest -q tests/tasks/test_task_execution.py tests/tasks/test_control_channel.py` | candidate: corrected real-control-path evidence and exact review `NET POSITIVE`; owner approved 2026-08-07 |
| MR-231 | `weft/core/tasks/base.py::BaseTask._submit_worker_lane` | `BLE001=1`; `BLE001@673` | Every worker throwable crosses the daemon-thread boundary through a typed lane result with `value=None` and exact error identity. | Preserve the local fatal transport and use shared group RUFF-SUP-347. | `./.venv/bin/python -m pytest -q tests/tasks/test_task_execution.py tests/tasks/test_control_channel.py` | candidate: corrected evidence and exact review `NET POSITIVE`; owner approved 2026-08-07 |
| MR-232 | `weft/core/tasks/consumer.py::Consumer._run_reactor_work_item` | `BLE001=1`; `BLE001@343` | Ordinary execution failure becomes the exact Consumer result with timestamp, transition flag, streaming false, outcome none, and identical error; fatal exits cross the outer lane. | Preserve the inner ordinary boundary and use RUFF-SUP-349. | `./.venv/bin/python -m pytest -q tests/tasks/test_consumer_terminal_events.py tests/tasks/test_task_execution.py` | candidate: corrected evidence and exact review `NET POSITIVE`; owner approved 2026-08-07 |
| MR-233 | `weft/core/tasks/multiqueue_watcher.py::MultiQueueWatcher._apply_pending_topology_mutations` | `BLE001=2`; `BLE001@662; BLE001@665` | Ordinary topology-request failure stays request-local and later deque entries continue FIFO; a fatal throwable releases every caller and clears transaction state before exact re-raise. Deferred SIGINT commits stop but the more specific transaction failure wins. | Preserve the local exception ladder and use exact group RUFF-SUP-339; keep [QUEUE.8] synchronized with the collision-priority rule. | `./.venv/bin/python -m pytest -q tests/tasks/test_multiqueue_watcher.py` | candidate: first review found deferred-SIGINT masking; second review required spec and stop/thread proof; corrected different review `NET POSITIVE`; owner approved 2026-08-07 |
| MR-234 | `weft/core/tasks/pipeline.py::PipelineEdgeTask._handle_work_message` | `BLE001=1`; `BLE001@164` | An ordinary handoff exception whose stringification succeeds becomes the edge's failed state/event and applies the configured reserved policy; control-flow `BaseException` propagates exactly. | Preserve the local result boundary and use exact group RUFF-SUP-341. | `./.venv/bin/python -m pytest -q tests/tasks/test_pipeline_runtime.py` | candidate: first exact review required reserved-policy proof; corrected different review `NET POSITIVE`; owner approved 2026-08-07 |
| MR-235 | `weft/core/tasks/pipeline.py::PipelineTask._bootstrap_children` | initial `BLE001=1`; corrected `BLE001=2`; `BLE001@460; BLE001@468` | Ordinary submission failure becomes a pipeline failure and rolls back only admitted children. Fatal submission failure attempts every admitted-child STOP and registry deletion as independent subordinate phases, then re-raises the exact original object even if cleanup raises another `BaseException`. | Keep ownership and priority local; preserve the two cleanup catches and use exact group RUFF-SUP-340. | `./.venv/bin/python -m pytest -q tests/tasks/test_pipeline_runtime.py` | candidate: first review found overbroad empty-list rollback and missing fatal cleanup; second review found cleanup masking/registry skip; corrected different review `NET POSITIVE`; owner approved 2026-08-07 |
| MR-236 | `weft/core/tasks/service.py::ServiceTask._run_service_worker_thread` | `BLE001=1`; `BLE001@562` | Every worker throwable is published through the complete typed Service error event with exact metadata/identity and inactive/stopping transition. | Preserve the local fatal transport and use shared group RUFF-SUP-347. | `./.venv/bin/python -m pytest -q tests/tasks/test_service_task.py` | candidate: corrected evidence and exact review `NET POSITIVE`; owner approved 2026-08-07 |
| MR-237 | `weft/core/tasks/sessions.py::AgentSession._close_ipc_resources` | initial `BLE001=1, S110=1`; accepted `BLE001=0, S110=0` | Supported process-wrapper close failures must not make agent-session IPC cleanup fail, while unexpected ownership/state or programming defects should escape. Lost handle cleanup should remain visible without process or exception data. | Narrow to `(OSError, ValueError)`, emit one fixed warning, and reject `RUFF-SUP-302`. | `./.venv/bin/python -m pytest -q -n 0 tests/tasks/test_runner.py -k agent_session_ipc_cleanup`; targeted/current Ruff; sessions mypy | accepted: both supported families contained after all owned endpoints are attempted without relative-order claim; unexpected `RuntimeError` escapes; fresh clean review `NET POSITIVE` |
| MR-238 | `weft/core/tasks/sessions.py::AgentSession._join_and_drain_ready_response` | initial `BLE001=1, S110=1`; accepted rework `BLE001=0, S110=0` | An OS-level join failure while observing a dead startup child must not itself skip the late-response drain; unexpected ownership/state or programming defects should escape. Lost process observation should remain visible without process or exception data. No correctness claim depends on the relative order of a successful join attempt and drain. | Narrow to `OSError`, emit one fixed warning, preserve one join attempt and one zero-timeout drain after supported join failure, and reject `RUFF-SUP-301`. | `./.venv/bin/python -m pytest -q -n 0 tests/tasks/test_runner.py -k agent_startup_late_drain`; targeted/current Ruff; sessions mypy | first review `NET NEGATIVE` rejected an unsupported relative-order contract; rework proves independent exact attempt counts and result; unexpected `RuntimeError` escapes without masking the defect; different fresh clean review `NET POSITIVE` |
| MR-239 | `weft/core/tasks/sessions.py::AgentSession.close` | initial `BLE001=1, S110=1`; accepted rework `BLE001=1, S110=0` | Stop-request publication is an arbitrary multiprocessing queue boundary whose failure must not skip termination or final monitor/IPC cleanup. Join has a narrower OS-failure boundary; unexpected lifecycle/programming defects must still trigger termination before propagation when later cleanup succeeds. | Isolate broad queue `put` containment with one fixed warning, narrow join containment to `OSError`, place termination in an inner `finally`, and use `RUFF-SUP-307` only for queue publication. | `./.venv/bin/python -m pytest -q -n 0 tests/tasks/test_task_execution.py -k agent_session_close`; targeted/current Ruff; sessions mypy | first review `NET NEGATIVE` found that unexpected join defects bypassed termination after `_closed=True`; rework proves termination plus both final cleanup phases run before the original `RuntimeError` propagates when later cleanup succeeds; different fresh clean review `NET POSITIVE` |
| MR-240 | `weft/core/tasks/sessions.py::AgentSession.last_metrics` | initial `BLE001=1, S110=1`; candidate `BLE001=1, S110=0` | Optional configured-monitor snapshot failure must return the cached metrics without detaching the monitor, so a later read can recover. Lost observation should remain visible without monitor, process, or exception data. | Retain broad adapter containment, emit one fixed warning per failed property read, and use `RUFF-SUP-304`. | `./.venv/bin/python -m pytest -q -n 0 tests/tasks/test_runner.py -k agent_session_metrics_failure_returns_cache_and_later_recovers`; targeted/current Ruff; sessions mypy | candidate: first snapshot raises and returns cache; monitor remains; second snapshot refreshes; exact bounded warning; fresh clean review `NET POSITIVE` |
| MR-241 | `weft/core/tasks/sessions.py::AgentSession.poll_limits` | `BLE001=1`; `BLE001@784` | A confirmed limit violation wins before optional metrics access. Ordinary configured-monitor failure silently fails open, captures cached metrics during cleanup, stops once, and clears ownership; control-flow `BaseException` propagates exactly and retains ownership. | Preserve the local session policy and use shared group RUFF-SUP-344. | `./.venv/bin/python -m pytest -q tests/tasks/test_runner.py tests/tasks/test_agent_execution.py` | candidate: failing-first correctness fix and clean review `NET POSITIVE`; owner approved 2026-08-07 |
| MR-242 | `weft/core/tasks/sessions.py::AgentSession.stop_monitor` | initial `BLE001=2, S110=2`; candidate `BLE001=2, S110=0` | Final metrics capture and monitor stop are ordered, independent best-effort phases. Snapshot failure must not skip stop; either failure must still clear persistent-session monitor ownership and remain visible without monitor or exception data. | Retain broad per-phase containment, emit distinct fixed warnings, and use `RUFF-SUP-303`. | `./.venv/bin/python -m pytest -q -n 0 tests/tasks/test_runner.py -k agent_session_reports_monitor_cleanup_failures_and_clears_owner`; targeted/current Ruff; sessions mypy | candidate: both phases raise in declared order; monitor cleared; repeated stop inert; exact bounded warnings; fresh clean review `NET POSITIVE` |
| MR-243 | `weft/core/tasks/sessions.py::AgentSession.terminate` | initial `BLE001=4, S110=4`; accepted rework `BLE001=0, S110=0` | OS-level join failures are ordinary process-observation failures across the deadline/non-deadline and initially alive/dead branches. They should remain visible while preserving each branch's existing bounded escalation/return policy; unexpected ownership/state or programming defects should escape. | Narrow all four join catches to `OSError`, emit branch-specific fixed warnings, and reject `RUFF-SUP-305`. | `./.venv/bin/python -m pytest -q -n 0 tests/tasks/test_task_execution.py -k agent_session_terminate`; targeted/current Ruff; sessions mypy | first review `NET NEGATIVE` requested a firing unexpected-defect proof; four-case matrix covers every OSError catch and branch action counts without order claims; representative `RuntimeError` propagates without warning; different fresh clean review `NET POSITIVE` |
| MR-244 | `weft/core/tasks/sessions.py::AgentSession.wait_ready` | initial `BLE001=1, S110=1`; accepted `BLE001=0, S110=0` | After startup response-channel seal, an OS-level child join failure must not itself suppress the final late-response probe. Unexpected ownership/state or programming defects should escape. | Narrow to `OSError`, emit one fixed warning, and reject `RUFF-SUP-306`. | `./.venv/bin/python -m pytest -q -n 0 tests/tasks/test_runner.py -k agent_wait_ready`; targeted/current Ruff; sessions mypy | accepted: OSError yields one join attempt and one ready late probe without relative-order claim; representative `RuntimeError` escapes before treating the probe as successful; fresh clean review `NET POSITIVE` |
| MR-245 | `weft/core/tasks/sessions.py::CommandSession.close` | initial `BLE001=1, S110=1`; candidate `BLE001=1, S110=0` | Failure from the explicit cleanup callback must not make idempotent command-session close fail. Lost cleanup should remain visible without callback, process, or exception data. | Retain broad callback containment, emit one fixed warning, and use `RUFF-SUP-299`. | `./.venv/bin/python -m pytest -q -n 0 tests/tasks/test_task_execution.py -k command_session_reports_cleanup_callback_failure_once`; targeted/current Ruff; sessions mypy | candidate: callback raises; repeated close attempts callback once; exact bounded warning; fresh clean review `NET POSITIVE` |
| MR-246 | `weft/core/tasks/sessions.py::CommandSession.poll_limits` | `BLE001=1`; `BLE001@200` | A confirmed limit violation wins before optional metrics access. Ordinary configured-monitor failure silently fails open, captures cached metrics during cleanup, stops once, and clears ownership; control-flow `BaseException` propagates exactly and retains ownership. | Preserve the local session policy and use shared group RUFF-SUP-344. | `./.venv/bin/python -m pytest -q tests/tasks/test_runner.py tests/tasks/test_agent_execution.py` | candidate: failing-first correctness fix and clean review `NET POSITIVE`; owner approved 2026-08-07 |
| MR-247 | `weft/core/tasks/sessions.py::CommandSession.stop_monitor` | initial `BLE001=2, S110=2`; candidate `BLE001=2, S110=0` | Final metrics capture and monitor stop are ordered, independent best-effort phases. Snapshot failure must not skip stop; either failure must still clear monitor ownership and remain visible without monitor or exception data. | Retain broad per-phase containment, emit distinct fixed warnings, and use `RUFF-SUP-300`. | `./.venv/bin/python -m pytest -q -n 0 tests/tasks/test_task_execution.py -k command_session_reports_monitor_cleanup_failures_and_clears_owner`; targeted/current Ruff; sessions mypy | candidate: both phases raise in declared order; monitor cleared; repeated stop is inert; exact bounded warnings; fresh clean review `NET POSITIVE` |
| MR-248 | `weft/core/taskspec/model.py::AgentSection.model_post_init` | `PYI063=1`; `PYI063@923` | Pydantic calls model_post_init with one positional context argument; the double-underscore name already prevents keyword use by callers. | Use explicit positional-only slash consistently across all overrides; prove Pydantic construction and mypy. | `./.venv/bin/python -m pytest -q tests/taskspec/test_taskspec.py tests/taskspec/test_taskspec_properties.py && ./.venv/bin/mypy weft --config-file pyproject.toml` | ready: typing-contract candidate |
| MR-249 | `weft/core/taskspec/model.py::AgentTemplateSection.model_post_init` | `PYI063=1`; `PYI063@815` | Pydantic calls model_post_init with one positional context argument; the double-underscore name already prevents keyword use by callers. | Use explicit positional-only slash consistently across all overrides; prove Pydantic construction and mypy. | `./.venv/bin/python -m pytest -q tests/taskspec/test_taskspec.py tests/taskspec/test_taskspec_properties.py && ./.venv/bin/mypy weft --config-file pyproject.toml` | ready: typing-contract candidate |
| MR-250 | `weft/core/taskspec/model.py::AgentToolSection.model_post_init` | `PYI063=1`; `PYI063@769` | Pydantic calls model_post_init with one positional context argument; the double-underscore name already prevents keyword use by callers. | Use explicit positional-only slash consistently across all overrides; prove Pydantic construction and mypy. | `./.venv/bin/python -m pytest -q tests/taskspec/test_taskspec.py tests/taskspec/test_taskspec_properties.py && ./.venv/bin/mypy weft --config-file pyproject.toml` | ready: typing-contract candidate |
| MR-251 | `weft/core/taskspec/model.py::LimitsSection.model_post_init` | `PYI063=1`; `PYI063@338` | Pydantic calls model_post_init with one positional context argument; the double-underscore name already prevents keyword use by callers. | Use explicit positional-only slash consistently across all overrides; prove Pydantic construction and mypy. | `./.venv/bin/python -m pytest -q tests/taskspec/test_taskspec.py tests/taskspec/test_taskspec_properties.py && ./.venv/bin/mypy weft --config-file pyproject.toml` | ready: typing-contract candidate |
| MR-252 | `weft/core/taskspec/model.py::ParameterizationSection.model_post_init` | `PYI063=1`; `PYI063@698` | Pydantic calls model_post_init with one positional context argument; the double-underscore name already prevents keyword use by callers. | Use explicit positional-only slash consistently across all overrides; prove Pydantic construction and mypy. | `./.venv/bin/python -m pytest -q tests/taskspec/test_taskspec.py tests/taskspec/test_taskspec_properties.py && ./.venv/bin/mypy weft --config-file pyproject.toml` | ready: typing-contract candidate |
| MR-253 | `weft/core/taskspec/model.py::RunInputArgumentSection.model_post_init` | `PYI063=1`; `PYI063@458` | Pydantic calls model_post_init with one positional context argument; the double-underscore name already prevents keyword use by callers. | Use explicit positional-only slash consistently across all overrides; prove Pydantic construction and mypy. | `./.venv/bin/python -m pytest -q tests/taskspec/test_taskspec.py tests/taskspec/test_taskspec_properties.py && ./.venv/bin/mypy weft --config-file pyproject.toml` | ready: typing-contract candidate |
| MR-254 | `weft/core/taskspec/model.py::RunInputSection.model_post_init` | `PYI063=1`; `PYI063@635` | Pydantic calls model_post_init with one positional context argument; the double-underscore name already prevents keyword use by callers. | Use explicit positional-only slash consistently across all overrides; prove Pydantic construction and mypy. | `./.venv/bin/python -m pytest -q tests/taskspec/test_taskspec.py tests/taskspec/test_taskspec_properties.py && ./.venv/bin/mypy weft --config-file pyproject.toml` | ready: typing-contract candidate |
| MR-255 | `weft/core/taskspec/model.py::RunInputStdinSection.model_post_init` | `PYI063=1`; `PYI063@549` | Pydantic calls model_post_init with one positional context argument; the double-underscore name already prevents keyword use by callers. | Use explicit positional-only slash consistently across all overrides; prove Pydantic construction and mypy. | `./.venv/bin/python -m pytest -q tests/taskspec/test_taskspec.py tests/taskspec/test_taskspec_properties.py && ./.venv/bin/mypy weft --config-file pyproject.toml` | ready: typing-contract candidate |
| MR-256 | `weft/core/taskspec/model.py::RunnerSection.model_post_init` | `PYI063=1`; `PYI063@406` | Pydantic calls model_post_init with one positional context argument; the double-underscore name already prevents keyword use by callers. | Use explicit positional-only slash consistently across all overrides; prove Pydantic construction and mypy. | `./.venv/bin/python -m pytest -q tests/taskspec/test_taskspec.py tests/taskspec/test_taskspec_properties.py && ./.venv/bin/mypy weft --config-file pyproject.toml` | ready: typing-contract candidate |
| MR-257 | `weft/core/taskspec/model.py::SpecSection.model_post_init` | `PYI063=1`; `PYI063@1097` | Pydantic calls model_post_init with one positional context argument; the double-underscore name already prevents keyword use by callers. | Use explicit positional-only slash consistently across all overrides; prove Pydantic construction and mypy. | `./.venv/bin/python -m pytest -q tests/taskspec/test_taskspec.py tests/taskspec/test_taskspec_properties.py && ./.venv/bin/mypy weft --config-file pyproject.toml` | ready: typing-contract candidate |
| MR-258 | `weft/core/taskspec/model.py::TaskSpec.model_post_init` | `PYI063=1`; `PYI063@1431` | Pydantic calls model_post_init with one positional context argument; the double-underscore name already prevents keyword use by callers. | Use explicit positional-only slash consistently across all overrides; prove Pydantic construction and mypy. | `./.venv/bin/python -m pytest -q tests/taskspec/test_taskspec.py tests/taskspec/test_taskspec_properties.py && ./.venv/bin/mypy weft --config-file pyproject.toml` | ready: typing-contract candidate |
| MR-259 | `weft/core/taskspec/parameterization.py::invoke_parameterization_adapter` | `TRY004=1`; `TRY004@84` | A JSON-serializable adapter result with the wrong required top-level object shape is a parameterization value-contract failure. | Preserve exact `ValueError` class/message and use shared `RUFF-SUP-259`. | `./.venv/bin/python -m pytest -q tests/core/test_spec_parameterization.py` | candidate: exact firing contract test; pending review and owner approval |
| MR-260 | `weft/core/taskspec/parameterization.py::materialize_taskspec_template` | `TRY004=1`; `TRY004@120` | A JSON-serializable adapter result whose `spec` field has the wrong required object shape is a parameterization value-contract failure. | Preserve exact `ValueError` class/message and use shared `RUFF-SUP-259`. | `./.venv/bin/python -m pytest -q tests/core/test_spec_parameterization.py` | candidate: exact firing contract test; pending review and owner approval |
| MR-261 | `weft/ext.py::RunnerHandle.__post_init__` | `TRY004=2`; `TRY004@59; TRY004@66` | Direct construction supplies Python `control` and `observations` arguments to the public dataclass; non-mapping values violate that object API contract. | Change both raises from `ValueError` to `TypeError`, preserving exact messages. | `./.venv/bin/python -m pytest -q tests/tasks/test_runner.py -k runner_handle`; targeted Ruff and source-module mypy | accepted: both exact constructor branches fire; clean review `NET POSITIVE` |
| MR-262 | `weft/ext.py::RunnerHandle.from_dict` | `TRY004=4`; `TRY004@151; TRY004@153; TRY004@155; TRY004@157` | `from_dict()` decodes persisted runtime-handle mapping payloads; the four reached wrong-shape branches are malformed persisted values in the same `ValueError` family as legacy keys, unsupported kinds, empty IDs, authority, and host-PID validation. | Preserve exact classes/messages and use `RUFF-SUP-269`. | `./.venv/bin/python -m pytest -q tests/tasks/test_runner.py -k runner_handle` | candidate: all four exact branches fire with truthy malformed values; clean review `NET POSITIVE`; owner approved 2026-08-07; pre-existing falsy normalization recorded in Section 18 |
| MR-263 | `weft/helpers/__init__.py::safe_cancel` | `BLE001=1`; `BLE001@87` | The owner catches arbitrary boundary failure and follows its existing fallback/result/state path instead of allowing the failure to escape. | Audit the enclosed calls. Narrow only if the complete raised set and existing fallback are proven; otherwise register a BLE001 suppression candidate. | `./.venv/bin/python -m pytest -q tests/system/test_helpers.py` | candidate: independent suppression/locality review |
| MR-264 | `weft/helpers/__init__.py::stdin_is_tty` | `BLE001=1`; `BLE001@77` | The owner catches arbitrary boundary failure and follows its existing fallback/result/state path instead of allowing the failure to escape. | Audit the enclosed calls. Narrow only if the complete raised set and existing fallback are proven; otherwise register a BLE001 suppression candidate. | `./.venv/bin/python -m pytest -q tests/system/test_helpers.py` | candidate: independent suppression/locality review |
| MR-265 | `weft/helpers/__init__.py::write_file_atomically` | `BLE001=1`; `BLE001@817` | The owner catches arbitrary boundary failure and follows its existing fallback/result/state path instead of allowing the failure to escape. | Audit the enclosed calls. Narrow only if the complete raised set and existing fallback are proven; otherwise register a BLE001 suppression candidate. | `./.venv/bin/python -m pytest -q tests/system/test_helpers.py` | candidate: independent suppression/locality review |
| MR-266 | `weft/manager_detached_launcher.py::_terminate_runtime` | initial `BLE001=2`; accepted `BLE001=0` | OS-level terminate/kill failure remains best-effort cleanup; unexpected process-wrapper defects must propagate. | Narrow both `Popen.terminate()` and `Popen.kill()` catches to `OSError`, preserving the existing timeout/escalation structure. | `./.venv/bin/python -m pytest -q tests/system/test_manager_detached_launcher.py`; targeted Ruff and mypy | accepted: four terminate/kill cases prove `ProcessLookupError` containment and `RuntimeError` propagation; fresh clean Python expert `NET POSITIVE`; test-double exit-race and pre-existing permission/liveness caveats recorded |
| MR-267 | `weft/manager_detached_launcher.py::main` | `BLE001=2`; `BLE001@112; BLE001@126` | Adjacent payload and launch phases normalize ordinary failure to exit 2 with phase-specific stderr or structured stdout; control-flow `BaseException` propagates exactly without output. Diagnostics are verbatim/semantic-verbatim and unbounded. | Preserve both local mappings and use exact group RUFF-SUP-345, including the pre-existing post-spawn handle-loss caveat. | `./.venv/bin/python -m pytest -q tests/system/test_manager_detached_launcher.py tests/cli/test_cli_manager.py` | candidate: behavior/locality/exact review `NET POSITIVE`; owner approved 2026-08-07 |
| MR-268 | `weft/manager_process.py::main` | accepted `BLE001=0` | Malformed encoded broker/spec/config arguments, broker-target filesystem resolution, and Pydantic TaskSpec values return exit code 2 with the existing phase diagnostic. Unexpected type/programming defects propagate. | Catch `(OSError, ValueError)` around argument decoding and `ValueError` around TaskSpec validation; fire every parser family plus unexpected `TypeError`/`RuntimeError`. | `./.venv/bin/python -m pytest -n0 -q tests/system/test_manager_process.py` | initial review `NET NEGATIVE`; candidate preserved at `/tmp/weft-rw-manager-process`; corrected 14-test rework and different clean review `NET POSITIVE` |
| MR-269 | `bin/pytest-pg::_initialize_broker_schema` | `FLY002=1`; `FLY002@323` | The line-list join keeps the generated broker-initialization program readable and intentionally has no final newline. | Preserve the join and propose the exact FLY002 group suppression in Section 18. | `./.venv/bin/python -m pytest -q tests/system/test_pytest_pg_script.py` | candidate: independent suppression/locality review |
| MR-270 | `tests/cli/test_cli_run.py::test_cli_run_spec_bundle_declared_args_shape_work_item` | `FLY002=1`; `FLY002@1242` | The line-list join keeps a branched embedded adapter readable with exact blank lines, indentation, and final newline. | Preserve the join and propose the exact FLY002 group suppression in Section 18. | `./.venv/bin/python -m pytest -q tests/cli/test_cli_run.py -k declared_args_shape_work_item` | candidate: independent suppression/locality review |
| MR-271 | `tests/cli/test_cli_run.py::test_cli_run_spec_bundle_parameterization_materializes_before_run_input` | `FLY002=1`; `FLY002@1571` | The line-list join keeps a multi-function embedded parameterization module readable with exact blank lines, indentation, and final newline. | Preserve the join and propose the exact FLY002 group suppression in Section 18. | `./.venv/bin/python -m pytest -q tests/cli/test_cli_run.py -k parameterization_materializes_before_run_input` | candidate: independent suppression/locality review |
| MR-272 | `tests/cli/test_cli_run.py::test_cli_run_spec_bundle_parameterization_requires_declared_parameter` | `FLY002=1`; `FLY002@1653` | The line-list join keeps a two-function embedded parameterization module readable with exact blank lines, indentation, and final newline. | Preserve the join and propose the exact FLY002 group suppression in Section 18. | `./.venv/bin/python -m pytest -q tests/cli/test_cli_run.py -k parameterization_requires_declared_parameter` | candidate: independent suppression/locality review |
| MR-273 | `tests/cli/test_cli_validate.py::test_validate_taskspec_bundle_directory_loads_bundle_local_environment_profile` | `FLY002=1`; `FLY002@408` | The line-list join keeps a nested-constructor embedded profile readable with exact blank lines, indentation, and final newline. | Preserve the join and propose the exact FLY002 group suppression in Section 18. | `./.venv/bin/python -m pytest -q tests/cli/test_cli_validate.py -k bundle_directory_loads_bundle_local_environment_profile` | candidate: independent suppression/locality review |
| MR-274 | `tests/core/test_agent_resolution.py::test_bundle_local_resolver_and_tool_profile` | `FLY002=1`; `FLY002@90` | The line-list join keeps a multi-function embedded resolver/profile module readable with exact blank lines, indentation, and final newline. | Preserve the join and propose the exact FLY002 group suppression in Section 18. | `./.venv/bin/python -m pytest -q tests/core/test_agent_resolution.py -k bundle_local_resolver_and_tool_profile` | candidate: independent suppression/locality review |
| MR-275 | `tests/core/test_environment_profiles.py::test_materialize_runner_environment_loads_bundle_local_profile` | `FLY002=1`; `FLY002@131` | The line-list join keeps a nested-constructor embedded environment profile readable with exact blank lines, indentation, and final newline. | Preserve the join and propose the exact FLY002 group suppression in Section 18. | `./.venv/bin/python -m pytest -q tests/core/test_environment_profiles.py -k bundle_local_profile` | candidate: independent suppression/locality review |
| MR-276 | `tests/core/test_spec_parameterization.py::test_materialize_taskspec_template_supports_bundle_local_adapter` | `FLY002=1`; `FLY002@95` | The line-list join keeps a branching embedded materializer readable with exact blank lines, indentation, and final newline. | Preserve the join and propose the exact FLY002 group suppression in Section 18. | `./.venv/bin/python -m pytest -q tests/core/test_spec_parameterization.py -k supports_bundle_local_adapter` | candidate: independent suppression/locality review |
| MR-277 | `tests/core/test_spec_parameterization.py::test_materialize_taskspec_template_rejects_spec_type_change` | `FLY002=1`; `FLY002@137` | The line-list join keeps a nested mapping literal in the embedded materializer readable with exact blank lines, indentation, and final newline. | Preserve the join and propose the exact FLY002 group suppression in Section 18. | `./.venv/bin/python -m pytest -q tests/core/test_spec_parameterization.py -k rejects_spec_type_change` | candidate: independent suppression/locality review |
| MR-278 | `tests/core/test_spec_parameterization.py::test_materialize_taskspec_template_request_payload_is_json_like_copy` | `FLY002=1`; `FLY002@169` | The line-list join keeps a nested call in the embedded materializer readable with exact blank lines, indentation, and final newline. | Preserve the join and propose the exact FLY002 group suppression in Section 18. | `./.venv/bin/python -m pytest -q tests/core/test_spec_parameterization.py -k request_payload_is_json_like_copy` | candidate: independent suppression/locality review |
| MR-279 | `tests/helpers/long_session_utils.py::write_command_script` | `FLY002=1`; `FLY002@108` | The line-list join keeps the long-session command program readable with exact blank lines, indentation, and final newline. | Preserve the join and propose the exact FLY002 group suppression in Section 18. | `./.venv/bin/python -m pytest -q -m "" tests/cli/test_cli_long_session.py -k identical_transcript` | candidate: independent suppression/locality review |
| MR-280 | `tests/tasks/test_runner.py::test_task_runner_agent_session_startup_uses_dedicated_ready_timeout` | `FLY002=1`; `FLY002@1467` | The line-list join keeps the embedded slow plugin module readable with exact decorators, blank lines, indentation, and final newline. | Preserve the join and propose the exact FLY002 group suppression in Section 18. | `./.venv/bin/python -m pytest -q tests/tasks/test_runner.py -k dedicated_ready_timeout` | candidate: independent suppression/locality review |
| MR-281 | `tests/helpers/weft_harness.py::WeftTestHarness._cleanup_preserving_database` | `SIM102=1`; `SIM102@1030` | Cleanup must establish 100 ms of task/PID quiescence before running the effectful database releasability probe; the probe performs garbage collection and Windows rename/restore checks. | Preserve the staged rate gate and effectful probe; propose the exact SIM102 suppression in Section 18. | `./.venv/bin/python -m pytest -q -n 0 tests/test_harness_registration.py -k 'cleanup_preserve_database_waits_for_database_release or cleanup_preserve_database_extends_windows_release_budget or cleanup_preserve_database_raises_if_database_stays_locked'`; direct source inspection establishes that the 100 ms gate precedes the probe | candidate: clean final exact review `NET POSITIVE`; pending explicit owner approval |
| MR-282 | `weft/core/manager_runtime.py::_await_manager_stop_confirmation` | `SIM102=4`; `SIM102@1307; SIM102@1308; SIM102@1319; SIM102@1328` | Stop confirmation is an ordered evidence protocol: acceptable/observed absence, recorded PID death, local-process absence-or-exit gate; outside the explicit foreground-serve early return, stopped-row evidence plus PID death before that gate; and rate-limit timestamp advancement before liveness and process checks. | Preserve the visible proof stages and mutation order; propose one four-directive SIM102 group in Section 18. | `./.venv/bin/python -m pytest -q -n 0 tests/commands/test_run.py -k 'stop_manager_confirms_observed_absence_in_evidence_order or stop_manager_waits_for_pid_exit_after_stopped_status'`; direct source inspection confirms `pid_checked_at` advances before the rate-limited liveness probe | candidate: clean final exact review `NET POSITIVE` after four reviews drove proof/wording corrections; pending explicit owner approval |
| MR-283 | `weft/core/tasks/multiqueue_watcher.py::MultiQueueWatcher._apply_topology_mutation` | `SIM102=1`; `SIM102@636` | Failed topology publication rolls back a candidate waiter only when it exists, was not installed, and rollback ownership was retained; candidate config cleanup remains adjacent and independent. | Preserve explicit rollback ownership nesting; propose the exact SIM102 suppression in Section 18. | `./.venv/bin/python -m pytest -q -n 0 tests/tasks/test_multiqueue_watcher.py -k 'background_post_replace_publication_failure_restores_old_waiter or same_waiter_replacement_publication_failure_preserves_installed_owner'` | candidate: clean final exact review `NET POSITIVE`; pending explicit owner approval |
| MR-284 | `weft/__init__.py::<module>` | `RUF022=1`; `RUF022@27` | The root facade inventory is a semantic index: metadata, client types, then logging helpers. Order is observable but no declared or internal contract depends on it. | Preserve semantic adjacency and propose the exact RUF022 suppression in Section 18. | `./.venv/bin/python -m pytest -q tests/architecture/test_import_boundaries.py -k 'public_facade_exports_keep_identity or lazy_facades_preserve_inventory_identity_and_cache'` | candidate: clean final exact review `NET POSITIVE`; pending explicit owner approval |
| MR-285 | `weft/commands/__init__.py::<module>` | `RUF022=1`; `RUF022@17` | The commands facade keeps callable capabilities together and the public `manager` module last. Order is observable but no declared or internal contract depends on it. | Preserve capability/module adjacency and propose the exact RUF022 suppression in Section 18. | `./.venv/bin/python -m pytest -q tests/architecture/test_import_boundaries.py -k 'lazy_facades_preserve_inventory_identity_and_cache or commands_manager_export_supports_attribute_from_and_star_imports'` | candidate: clean final exact review `NET POSITIVE`; pending explicit owner approval |
| MR-286 | `weft/commands/manager.py::<module>` | `RUF022=1`; `RUF022@291` | Runtime query functions lead, `serve_manager` stays visible, start/stop mutation adapters remain beside their matching capabilities, and read-only list/status adapters remain together at the end. Order is observable but no declared or internal contract depends on it. | Preserve the visible query/mutation/adapter structure and propose the exact RUF022 suppression in Section 18. | `./.venv/bin/python -m pytest -q tests/commands/test_manager_commands.py` | candidate: exact review attempt 1 found and corrected an adjacency overclaim; final clean review `NET POSITIVE`; pending explicit owner approval |
| MR-287 | `weft/commands/queue.py::<module>` | `RUF022=1`; `RUF022@1251` | Queue value types, raw operations, command adapters, and alias operations form visible API families. Order is observable but no declared or internal contract depends on it. | Preserve the strongest role grouping and propose the exact RUF022 suppression in Section 18. | `./.venv/bin/python -m pytest -q tests/commands/test_queue.py tests/cli/test_cli_queue.py` | candidate: clean final exact review `NET POSITIVE`; pending explicit owner approval |
| MR-288 | `weft/core/__init__.py::<module>` | `RUF022=1`; `RUF022@38` | The core facade inventory follows owning runtime modules and mirrors `_LAZY_EXPORTS`; related tasks, runners, callable helpers, targets, TaskSpec types, and TID helpers stay adjacent. Order is observable but no declared or internal contract depends on it. | Preserve owner-module adjacency and propose the exact RUF022 suppression in Section 18. | `./.venv/bin/python -m pytest -q tests/architecture/test_import_boundaries.py -k 'public_facade_exports_keep_identity or lazy_facades_preserve_inventory_identity_and_cache'` | candidate: clean final exact review `NET POSITIVE`; pending explicit owner approval |
| MR-289 | `weft/core/tasks/__init__.py::<module>` | `RUF022=1`; `RUF022@20` | Base/mixin, consumer, observer, monitor/debug/service, and pipeline families remain adjacent. Order is observable but no declared or internal contract depends on it. | Preserve task-family adjacency and propose the exact RUF022 suppression in Section 18. | `./.venv/bin/python -m pytest -q tests/tasks/test_tasks_simple.py tests/tasks/test_task_observer_behavior.py tests/tasks/test_task_endpoints.py` | candidate: clean final exact review `NET POSITIVE`; pending explicit owner approval |
| MR-290 | `weft/core/tasks/agent_session_protocol.py::<module>` | `RUF022=1`; `RUF022@168` | Protocol constructors, parsers/classifiers, diagnostics, and predicates follow protocol roles instead of spelling. Order is observable but no declared or internal contract depends on it. | Preserve protocol-role adjacency and propose the exact RUF022 suppression in Section 18. | `./.venv/bin/python -m pytest -q tests/tasks/test_runner.py -k 'agent_session'` | candidate: clean final exact review `NET POSITIVE`; pending explicit owner approval |

## Blocked groups

None. `pending` and `candidate` rows are not accepted edits: they still require the row's named compatibility, locality, independent suppression, and owner-approval gates.

## Cardinality check

```text
required=415
BLE001=186
FLY002=12
PLW1510=10
PYI025=1
PYI034=1
PYI063=11
PYI064=15
RUF009=1
RUF012=15
RUF022=7
RUF034=1
S102=3
S110=46
S112=2
SIM102=6
SIM115=1
SIM117=21
TRY004=75
TRY203=1
PLR0402=9
SIM905=2
rows=294
blocked=0
```

## 17. Refactor Rework Queue

FIFO. Preserve `NET NEGATIVE` candidates in dedicated worktrees. Activation
requires every row `accepted`.

| Queue ID | Baseline/worktree | Rules/symbol | Attempt | Verdict | Improvement criteria | State | Accepted checkpoint |
|---|---|---|---:|---|---|---|---|
| RW-RUF100-001 | Current uncommitted Task 4 worktree; initial candidate recorded by clean review | `RUF100` prose-preserving sites | 1 | `NET NEGATIVE` | Put the third SLF001 rationale beside the exact `_agent` access; remove obsolete D401 lint-history prose that interrupts the import block. | accepted | Attempt 2 `NET POSITIVE` after 168 focused tests and clean Ruff/suppression/diff gates; owner approved closeout 2026-08-07. |
| RW-RUF012-002 | Baseline `29e9ae54`; `/tmp/weft-rw-ruf012-queue-monitor` | MR-078, `test_watch_queue_uses_queue_monitor::_FakeContext.config` | 1 | `NET NEGATIVE` | Model the real per-instance context contract; initialize a fresh config in `__init__` instead of declaring shared mutable `ClassVar` state. | accepted | Attempt 2 `NET POSITIVE`; named test and clean Ruff/suppression/diff gates pass. |
| RW-SIM103-003 | Baseline `aa4a1474`; `/tmp/weft-rw-sim103-manager-supersession` | `Manager._self_superseded_manager_record_visible` | 1 | `NET NEGATIVE` | Keep the policy explicit: a missing/non-integer timestamp defaults to visible, and only a proven expired integer timestamp suppresses visibility. Avoid conflating unchecked and checked-fresh states in one boolean. | accepted | Attempt 2 `NET POSITIVE`; full manager suite, mypy, and clean Ruff gates pass. |
| RW-SIM115-004 | Baseline `80604b8a`; `/tmp/weft-rw-sim115-process-target` | MR-127, `tests/tasks/process_target.py::run_task` | 1 | `NET NEGATIVE` | Preserve workload-length descriptor ownership, FIFO best-effort close, close-error containment, and unlink continuation without nesting unrelated setup. Prefer the explicit lifecycle plus a narrow suppression if context-manager machinery adds ceremony or changes cleanup. | accepted | Exact `RUFF-SUP-308` suppression owner-approved 2026-08-07. |
| RW-BLE001-005 | Baseline `f4a12c55`; `/tmp/weft-rw-result-enumeration` | MR-163, `weft.commands.result::_collect_all_results` | 1 | `NET NEGATIVE` | Preserve the no-user-facing-traceback command contract for failures raised by `db.list_queues()` across every supported SimpleBroker backend. Do not claim a closed exception tuple until SimpleBroker translates backend-specific operational failures such as Postgres `PoolTimeout` to `BrokerError`. Prefer the local broad command boundary plus an exact suppression. | accepted | Narrowed candidate preserved; different broad-boundary rework review `NET POSITIVE`; first exact review corrected overclaims; different corrected exact `RUFF-SUP-309` review `NET POSITIVE`; owner approved 2026-08-07. |
| RW-BLE001-006 | Baseline `f4a12c55`; `/tmp/weft-rw-monitor-lifecycle` | MR-216, MR-219, MR-220; host configured-monitor lifecycle | 1 | `NET NEGATIVE` | Preserve improved start cleanup, cached metrics, snapshot fallback, and stop continuation while keeping each broad adapter phase explicit, local, observable through fixed privacy-safe warnings, and eligible for exact suppression review. Inline one-call observation/cleanup helpers; retain shared startup code only if reuse outweighs locality cost. | accepted | Candidate preserved; first rework review found silent missing-PID disablement; corrected rework always warns, retains one three-call startup helper, passed a different fresh clean review `NET POSITIVE`, and activated exact `RUFF-SUP-313`. |
| RW-BLE001-007 | Baseline `f4a12c55`; `/tmp/weft-rw-monitor-lifecycle` | MR-223; subprocess configured-monitor lifecycle | 1 | `NET NEGATIVE` | Preserve improved early/normal cleanup and cached metrics while replacing hidden `suppress(Exception)` blocks with explicit local phase policy, fixed privacy-safe warnings, and exact suppression candidates. Keep only helpers with real reuse. | accepted | Candidate preserved; rework retains two genuinely reused phase-aware helpers, explicit warnings, cached metrics, and primary outcomes; different fresh clean Python expert `NET POSITIVE`; exact `RUFF-SUP-311` activated. |
| RW-BLE001-008 | Baseline `f4a12c55`; `/tmp/weft-rw-manager-force-stop-taxonomy` | `weft.core.manager_runtime::_stop_manager` force termination | 1 | `NET NEGATIVE` | Never publish a stopped registry record from an exception class alone. For PID and `Popen` paths, permission denial fails; lookup and other OS failures require post-error dead-process evidence; live or uncertain evidence fails. Keep this resolution adjacent to the force-stop owner. | accepted | Candidate preserved. Rework removes false-success `ProcessLookupError` branches and adds an 11-case PID/process matrix; focused tests, full manager-command/CLI suites, Ruff, and mypy pass; different clean review `NET POSITIVE`. |

States: `candidate`, `queued`, `reworking`, `awaiting-review`,
`awaiting-owner-checkpoint`, `accepted`.

## 18. Proposed Suppression Disposition

Empty at plan review. Task 7 appends candidates before directives. Each needs
independent review and separate explicit owner approval.

| Group | Ledger IDs and exact candidate sites/snippets | Rules/cardinality | Protected invariant | Real proof | Rejected alternatives | Review | Owner approval |
|---|---|---|---|---|---|---|---|
| `RUFF-SUP-308`: intentional workload-length file descriptors | MR-127; `tests/tasks/process_target.py:59`, use `tmp = tempfile.NamedTemporaryFile(delete=False)  # noqa: SIM115 approved [TS-3.1] [RUFF-SUP-308] exception` | `SIM115=1` | All requested descriptors remain open while resource monitoring samples the workload; cleanup closes every handle FIFO with per-handle `OSError` containment, then attempts every unlink. | Both fd/multiple-limit runner tests pass; direct `--fds 3` target returns the requested result. | ExitStack candidate preserved at `/tmp/weft-rw-sim115-process-target`: it nested unrelated setup, changed close order, allowed close errors to escape, and could skip unlinking. `pop_all()` or wrapper machinery is less comprehensible than the explicit lifecycle. | Fresh clean Python expert: attempt 1 `NET NEGATIVE`; narrow suppression recommended. | owner approved 2026-08-07 |
| `RUFF-SUP-309`: backend-neutral result enumeration boundary | MR-163; `weft/commands/result.py:437`, use `except Exception as exc:  # noqa: BLE001 approved [TS-3.1] [RUFF-SUP-309] exception` | `BLE001=1` | Queue enumeration crosses the selected SimpleBroker backend. Any ordinary exception raised by `db.list_queues()` must become the command's one-line error rather than escape as a user-facing CLI traceback. | A boundary-injection test makes a fake broker raise the real `psycopg_pool.PoolTimeout` class and asserts the exact one-line error; the full result owner suite passes. Source audit shows SimpleBroker's Postgres runner acquires from `ConnectionPool.getconn()` before its psycopg translation block, so pool exhaustion can cross `list_queues()` untranslated. | The preserved tuple candidate at `/tmp/weft-rw-result-enumeration` catches `BrokerError`, `OSError`, and `RuntimeError`, but misses `PoolTimeout`. Adding `PoolTimeout` would couple Weft to one backend and remain incomplete for arbitrary supported backend plugins. Backend-neutral taxonomy belongs in SimpleBroker. Moving the catch outward would also hide aggregation defects; moving it into a helper would reduce locality. | First narrowing review `NET NEGATIVE`; different broad-boundary rework review `NET POSITIVE`; first exact review `NET NEGATIVE` corrected stale coverage and scope/proof overclaims; different corrected exact review `NET POSITIVE`. | owner approved 2026-08-07 |
| `RUFF-SUP-310`: timeout diagnostic fallback | MR-084; `tests/conftest.py:439`, use `except Exception:  # noqa: BLE001 approved [TS-3.1] [RUFF-SUP-310] exception` | `BLE001=1` | `WeftTestHarness.dump_debug_state()` is secondary evidence after `subprocess.TimeoutExpired`. Any ordinary diagnostic failure must not replace the primary timeout; fallback text must not expose exception payloads. | A direct `run_cli` test injects the exact `TimeoutExpired` object and a sentinel dump `RuntimeError`, proves object identity on re-raise, asserts the fixed fallback note, and excludes the sentinel secret. | The harness diagnostic helper has no closed exception contract and crosses SimpleBroker queue construction/read/close plus state formatting; supported backend/plugin failures are not exhaustively typed here. Removing containment replaces the primary timeout. Logging or exception repr can expose task/backend data and adds another fallible diagnostic path. Moving the catch outward could hide command construction or timeout-note defects; a helper reduces locality without reuse. | Behavior/locality review `NET POSITIVE`; first exact review `NET NEGATIVE` corrected a process/filesystem scope overclaim; different corrected exact review `NET POSITIVE`. | owner approved 2026-08-07 |
| `RUFF-SUP-311`: subprocess resource-monitor lifecycle | MR-223; `weft/core/runners/subprocess_runner.py:122,263,335,571,588`, at each exact `except Exception:` use `except Exception:  # noqa: BLE001 approved [TS-3.1] [RUFF-SUP-311] exception` | `BLE001=5` | Configured resource-monitor adapters have no shared exception base. Startup failure must attempt stop then detach; limit-check/metrics failure must fail open while preserving cached metrics; final snapshot/stop failure must not replace cancellation, timeout, limit, or completion outcomes. Every failure remains visible through a phase-specific fixed warning without exception payloads. | Six focused monitor tests fire startup plus startup-stop, limit check plus metrics read, early limit metrics plus stop, final metrics/snapshot/stop, cancellation cleanup, and timeout cleanup. The timeout test uses a controlled clock that makes timeout eligible only after the first metrics sample. Together they prove call order, cached-metric fallback, all four primary outcomes, exact warnings, and absence of sentinel exception text/`exc_info`; the full subprocess-runner owner suite passes. | Silent `contextlib.suppress(Exception)` candidate preserved at `/tmp/weft-rw-monitor-lifecycle` was `NET NEGATIVE` because it hid policy and lint. Narrowing is not exhaustive for configured adapters. Removing containment lets optional observation replace task execution. Inlining the reused metrics/stop policy duplicates it across cancellation, timeout, limit, and completion exits; moving catches outward hides non-monitor defects. | Initial hidden-boundary refactor `NET NEGATIVE`; explicit phase-aware rework review `NET POSITIVE`; two exact reviews drove cancellation/timeout proof, count, and clock corrections; final exact review `NET POSITIVE`. | owner approved 2026-08-07 |
| `RUFF-SUP-312`: fixture teardown continuation | MR-082, MR-083, MR-086; `tests/conftest.py:311,340,363`, at each exact `except Exception:` use `except Exception:  # noqa: BLE001 approved [TS-3.1] [RUFF-SUP-312] exception` | `BLE001=3` | Queue/task fixture teardown crosses backend and runtime cleanup boundaries. One arbitrary ordinary cleanup failure must not replace the test result or prevent later declared resources from being closed/stopped; each failure remains visible through a fixed warning without exception payloads. | Three direct fixture-generator tests make the first of two resources raise `RuntimeError`, exhaust teardown, prove the second cleanup still runs, and assert one exact warning with no `exc_info` or sentinel secret. | Narrowing is not exhaustive across queue backends and task runtime adapters. Removing containment replaces the test result and skips later cleanup. Moving the catch outside each loop loses per-resource continuation; helper extraction separates cleanup from its local policy without reuse. | Behavior/locality review `NET POSITIVE`; independent exact review `NET POSITIVE`. | owner approved 2026-08-07 |
| `RUFF-SUP-313`: host resource-monitor lifecycle | MR-216, MR-219, MR-220; `weft/core/runners/host.py:371,377,381,617,621,787,794,805,1138`, at each exact `except Exception:` use `except Exception:  # noqa: BLE001 approved [TS-3.1] [RUFF-SUP-313] exception` | `BLE001=9` | Configured resource-monitor adapters have no shared exception base. Missing PID and startup failure must remain visible, attempt cleanup, and detach; polling failure must fail open while preserving cached metrics; final metrics/snapshot/stop and agent-startup cleanup failures must not replace task/session outcomes. Every failure uses fixed warnings without exception payloads. | Eight focused host-monitor tests fire all nine sites and cover missing PID with successful and failed cleanup, failed start plus cleanup, command-session ownership clearing, poll/check/metrics failure with cache, no-cache `last_metrics -> snapshot -> stop`, final stop failure, and agent-session construction cleanup. They prove call order, primary outcomes, cache/fallback behavior, exact warnings, and privacy; the full host-runner owner suite passes. | Silent `contextlib.suppress(Exception)` candidate preserved at `/tmp/weft-rw-monitor-lifecycle` was `NET NEGATIVE`. Narrowing is not exhaustive for configured adapters. Removing containment lets optional monitoring replace execution/cleanup. One shared startup helper is reused by one-shot, command-session, and agent-session paths; the one-call observation/finalization helpers from the rejected candidate were inlined to preserve locality. Moving catches outward hides non-monitor defects. | Initial hidden-boundary refactor `NET NEGATIVE`; first explicit rework review found silent missing-PID disablement; corrected different review `NET POSITIVE`; independent exact review `NET POSITIVE` with test-count wording corrected. | owner approved 2026-08-07 |
| `RUFF-SUP-314`: Postgres test-schema cleanup boundaries | MR-107, MR-108; `tests/helpers/test_backend.py:220,259`, at each exact `except Exception:` use `except Exception:  # noqa: BLE001 approved [TS-3.1] [RUFF-SUP-314] exception` | `BLE001=2` | Backend-plugin cleanup failure must not replace test teardown. Batch cleanup must continue in declared schema order; single-root cleanup must return without discarding the still-live registration. Both warnings retain the failed schema. Batch discovery also retains config path and therefore its root prefix, while excluding DSN, exception text, and traceback; the single-root warning excludes root path as well. | Two direct tests make `plugin.cleanup_target()` raise. The batch test proves failed-then-successful call order and structured schema/config diagnostics; the single-root test seeds and proves the registration remains, retains schema context, and excludes root/DSN/exception data. | Plugin cleanup has no shared exhaustive exception family. Removing containment can abort teardown; catching outside the batch loop skips later schemas. A fixed message without structured fields was preserved at `/tmp/weft-rw-test-backend-cleanup` and reviewed `NET NEGATIVE` because it erased actionable schema/config context. | Initial diagnostic-erasing refactor `NET NEGATIVE`; corrected different review `NET POSITIVE`; first exact review corrected registration-proof and privacy-scope overclaims; different corrected exact review `NET POSITIVE`. | owner approved 2026-08-07 |
| `RUFF-SUP-315`: harness queue debug-read fallback | MR-114; `tests/helpers/weft_harness.py:281`, use `except Exception:  # noqa: BLE001 approved [TS-3.1] [RUFF-SUP-315] exception` | `BLE001=1` | Queue inspection is secondary debug/timeout evidence. Any ordinary backend/read failure must return one fixed non-sensitive fallback without replacing the primary operation; the opened queue must still close exactly once. | A direct harness test injects a queue whose `peek_many()` raises a sentinel `RuntimeError`, asserts `['<queue read unavailable>']`, proves one close, and proves the returned text excludes the supplied queue name and exception text. Source inspection proves the catch emits no log or exception data and formatting remains outside its scope. | Supported queue backends/plugins do not expose one exhaustive read-exception family. Removing containment can replace the primary debug/timeout path. Returning exception text leaks diagnostics; moving the catch outward can hide formatting defects or skip local close policy; extraction reduces locality without reuse. | Behavior/locality review `NET POSITIVE`; first exact review corrected timeout-only scope and proof overclaims; different corrected exact review `NET POSITIVE`. | owner approved 2026-08-07 |
| `RUFF-SUP-316`: harness registered-PID cleanup continuation | harness residual audit; `tests/helpers/weft_harness.py:1157`, use `except Exception:  # noqa: BLE001 approved [TS-3.1] [RUFF-SUP-316] exception` | `BLE001=1` | Best-effort process-tree teardown must contain an unexpected ordinary helper/library failure for one identity-checked PID so every other registered managed PID is still attempted and all PID registries are cleared. Failure remains visible without PID or exception payload. | A direct harness cleanup test makes the first termination call raise regardless of set iteration order, proves both managed PIDs are attempted without ordering them, asserts the fixed warning without PID/secret/`exc_info`, and proves owner, managed, combined, and create-time PID registries are empty afterward. | The immediate helper already contains documented psutil/OS failures, but harness teardown must still prevent an unexpected ordinary helper/library failure from skipping other registered PIDs and registry clearing. Catching outside the per-PID call loses continuation; logging identity/exception data can expose task details; extraction moves policy away from identity checks. | Behavior/locality review `NET POSITIVE`; first exact review corrected set-order, registry-scope, and taxonomy overclaims; different corrected exact review `NET POSITIVE`. | owner approved 2026-08-07 |
| `RUFF-SUP-317`: Microsandbox control and compatibility adapters | Microsandbox residual audit; `extensions/weft_microsandbox/weft_microsandbox/_runtime.py:227,238,251,433,450`, at each exact `except Exception:` use `except Exception:  # noqa: BLE001 approved [TS-3.1] [RUFF-SUP-317] exception` | `BLE001=5` | Microsandbox SDK/FFI and compatible dynamic members expose no exhaustive ordinary exception family. Stop/kill failure returns `False`; describe failure returns the exact `missing` description; a post-kill execution task that does not complete cleanly remains a cancelled result; name lookup failure returns the supplied fallback. Each failure emits a fixed warning without sandbox identity, fallback value, exception text, or traceback. Provider-task `CancelledError` maps to requested cancellation, while outer coroutine cancellation propagates. | Direct async tests fire all five catches and assert exact bool/description/name/cancellation results plus fixed private warnings. Separate post-kill provider-failure and outer-cancellation tests prove the accurate completion warning and `CancelledError` partition; the full extension suite passes. | A sentinel/boolean `contextlib.suppress(Exception)` candidate preserved at `/tmp/weft-rw-microsandbox-adapters` was `NET NEGATIVE` because it hid policy and added bookkeeping. Narrowing to documented SDK errors is incomplete across FFI/version-compatible member shapes. Removing containment breaks the stable bool/missing/name/cancel adapters; moving catches outward hides unrelated conversion defects. | Hidden-boundary refactor `NET NEGATIVE`; explicit local rework review `NET POSITIVE`; independent exact review `NET POSITIVE`. | owner approved 2026-08-07 |
| `RUFF-SUP-318`: ordered TaskSpec extension validation stages | specs validation audit; `weft/commands/specs.py:523,534,548,564,579,595`, at each exact `except Exception as exc:` use `except Exception as exc:  # noqa: BLE001 approved [TS-3.1] [RUFF-SUP-318] exception` | `BLE001=6` | The ordered validation engine executes project parameterization/run-input adapters, environment-profile callables, runner plugins, agent runtimes, and tool-profile hooks. No stage shares an exhaustive exception family. An ordinary stage exception whose `str()` succeeds must become the exact stage/field validation issue while preserving the caller payload and first-failure order. | One six-case parameterized test injects `RuntimeError` at each stage and asserts the exact preserved payload, issue stage, field, and verbatim message; command and CLI validation owner suites pass. | Narrowing cannot cover arbitrary import execution and extension callbacks. Moving catches outward loses stage/field precision and ordered first-failure locality; a generic wrapper separates each capability call from its result mapping. Exception text remains a pre-existing verbatim diagnostic/privacy tradeoff; the proof does not claim sanitization or containment of broken exception stringification. | Behavior/locality review `NET POSITIVE`; independent exact directive review `NET POSITIVE`. | owner approved 2026-08-07 |
| `RUFF-SUP-319`: external task-log final-handler transaction | external-log cleanup audit; `weft/core/monitor/external_log.py:115,119`, at each exact `except BaseException as exc:` use `except BaseException as exc:  # noqa: BLE001 approved [TS-3.1] [RUFF-SUP-319] exception` | `BLE001=2` | Handler detachment occurs before cleanup. Flush and close must both be attempted even when either raises `KeyboardInterrupt`, `SystemExit`, or another `BaseException`; afterward the first exact failure object is re-raised. If both fail, close failure is intentionally secondary and not surfaced separately. | A direct writer test injects two distinct non-`Exception` `BaseException` objects, proves `flush` then `close`, exact flush-object identity on re-raise, and handler/logger detachment. Existing tests prove fresh writer acquisition after ordinary flush-only and close-only failures. | Catching only `Exception` lets control-flow interruption skip close. A context manager or combined catch cannot preserve both attempts and first-object identity as clearly. Moving cleanup outside the lock or detachment after calls can leave poisoned writer state. | Behavior/locality review `NET POSITIVE`; independent exact directive review `NET POSITIVE`. | owner approved 2026-08-07 |
| `RUFF-SUP-320`: serve-log serialization suppression | serve-log serialization audit; `weft/core/serve_log.py:149`, use `except Exception:  # noqa: BLE001 approved [TS-3.1] [RUFF-SUP-320] exception` | `BLE001=1` | Best-effort operational logging must not replace manager work. Arbitrary record-mapping/JSON hook failure suppresses that diagnostic entirely so no invalid partial event or record/exception data is emitted. Control-flow `BaseException` remains outside the boundary. | A direct test uses a dict subclass whose `items()` raises a sentinel `RuntimeError`, proves no escape, exact empty stderr, and no secret output; the serve-log owner suite passes. | The fixed partial-event candidate preserved at `/tmp/weft-rw-serve-log-fallback` was `NET NEGATIVE` because it omitted required event schema/index fields. Narrowing cannot cover arbitrary mapping hooks. Moving the catch outward hides stderr/queue defects; emitting exception data leaks diagnostics. | Partial-event refactor `NET NEGATIVE`; silent-suppression rework review `NET POSITIVE`; independent exact directive review `NET POSITIVE`. | owner approved 2026-08-07 |
| `RUFF-SUP-321`: replaceable stderr sink fallback | serve-log sink audit; `weft/core/serve_log.py:155`, use `except Exception:  # noqa: BLE001 approved [TS-3.1] [RUFF-SUP-321] exception` | `BLE001=1` | A replaceable file-like stderr sink may run arbitrary `write`/`flush` hooks. Its failure must not replace manager work. The fallback bypasses `sys.stderr` and writes one fixed data-free warning to fd 2; an `OSError` from that low-level warning is contained. | One direct test injects a stderr object whose `write()` raises with record data, proves exact fd-2 warning bytes and no secret; a second makes `os.write()` raise `OSError` and proves no escape. | No exhaustive ordinary exception family exists for replaceable file-like hooks. Removing containment replaces runtime work; retrying through `sys.stderr` can recurse; logging record/exception data leaks fields. The fixed `os.write` path has the closed `OSError` family and is already narrowed. | Behavior/locality review `NET POSITIVE`; independent exact directive review `NET POSITIVE`. | owner approved 2026-08-07 |
| `RUFF-SUP-322`: task-monitor worker-local resource closes | MR-201; `weft/core/monitor/task_monitor.py:1009,1017,1039`, at each exact `except Exception as exc:` use `except Exception as exc:  # noqa: BLE001 approved [TS-3.1] [RUFF-SUP-322] exception` | `BLE001=3` | Worker-local store, external sink, and deduplicated queue resources are detached before close. An ordinary close failure whose `str()` succeeds becomes one close-error entry; later resource categories still run. Declared category order remains store, sink, then queues. Close exception text is retained verbatim without sanitization, and queue close errors also retain the queue name. | The six-case worker-entry matrix fires all three catches for both worker kinds. Store and sink failure cases prove later categories still close; every case proves the exact typed close error plus coherent retry/ownership state. The proof does not claim an ordering or continuation contract among distinct queue-map entries. | These resource owners cross backend, logging-handler, and queue implementations without one exhaustive close family. Removing containment skips later categories; one outer catch loses per-resource error ownership; helper extraction fragments the visible cleanup transaction. | Behavior/locality review `NET POSITIVE`; first exact review `NET NEGATIVE` corrected stringification, diagnostic, and queue-continuation overclaims; different corrected exact review `NET POSITIVE`. | owner approved 2026-08-07 |
| `RUFF-SUP-323`: task-monitor operational-log fallback | MR-202; `weft/core/monitor/task_monitor.py:1304`, use `except Exception:  # noqa: BLE001 approved [TS-3.1] [RUFF-SUP-323] exception` | `BLE001=1` | Operational logging is secondary evidence and must not replace monitor work. Any ordinary record/conversion/emission failure attempts one fixed data-free fd-2 warning; low-level `OSError`, including a failed warning attempt, is contained. | A direct test injects a custom nonstandard exception at the operational-log boundary, proves the monitor call survives, and records the exact fixed warning attempt without exception payload. Its second arm makes `os.write` raise after recording the call and proves no escape. | Record construction and the serve-log adapter cross dynamic values and hooks without one exhaustive family. Moving the catch outward hides monitor work; logging the exception can leak runtime data or recurse; silent suppression loses bounded visibility. | Behavior/locality review `NET POSITIVE`; first exact review `NET NEGATIVE` corrected the write-success overclaim and missing failed-attempt proof; different corrected exact review `NET POSITIVE`. | owner approved 2026-08-07 |
| `RUFF-SUP-324`: task-monitor store setup rollback close | MR-203; `weft/core/monitor/task_monitor.py:2296`, use `except Exception as close_exc:  # noqa: BLE001 approved [TS-3.1] [RUFF-SUP-324] exception` | `BLE001=1` | When monitor-store setup and rollback-close exceptions both stringify successfully, the setup error remains primary while one appended close detail is retained and ownership clears. Both exception strings are retained verbatim without sanitization. | A direct setup-plus-close failure test fires the catch and proves the exact primary-plus-close diagnostic, `task._monitor_store is None`, and unavailable store status. | The store close path crosses backend cleanup with no exhaustive family. Removing containment replaces the primary setup failure; moving the catch outside the setup handler loses error priority and local ownership clearing. | Behavior/locality review `NET POSITIVE`; first exact review `NET NEGATIVE` corrected bounded/sanitized wording and added the ownership assertion; different corrected exact review `NET POSITIVE`. | owner approved 2026-08-07 |
| `RUFF-SUP-325`: terminal-control cleanup worker transport | MR-206; `weft/core/monitor/task_monitor.py:3654`, use `except Exception as exc:  # noqa: BLE001 approved [TS-3.1] [RUFF-SUP-325] exception` | `BLE001=1` | An unexpected ordinary worker-local cleanup failure whose `str()` succeeds becomes a typed pending cleanup result and unavailable monitor status; worker resources still close in `finally`, and later closes continue after one close failure. Worker and close exception strings are retained verbatim without sanitization. | A direct test injects a custom exception outside the inner typed taxonomy, proves it is the first cleanup error, asserts `monitor_status.available is False`, proves a sink close failure remains separate, and proves sink-then-queue close continuation plus no next cleanup slice. | Narrowing to the inner operational tuple would let unexpected worker defects escape the service-worker transport path. Removing containment can skip typed retry state; moving the catch outward obscures the worker/result conversion and cleanup `finally`; extraction reduces locality without reuse. | Initial review `NET NEGATIVE` because the test hit the inner typed catch; corrected firing-evidence rework review `NET POSITIVE`; first exact review `NET NEGATIVE` corrected diagnostic scope and missing status evidence; different corrected exact review `NET POSITIVE`. | owner approved 2026-08-07 |
| `RUFF-SUP-326`: built-in monitor worker transport | MR-204; `weft/core/monitor/task_monitor.py:5218`, use `except Exception as exc:  # noqa: BLE001 approved [TS-3.1] [RUFF-SUP-326] exception` | `BLE001=1` | An unexpected ordinary built-in worker failure whose `str()` succeeds becomes a failed processor result, disables runtime-cleanup readiness for that cycle, and still closes worker resources. The exception string is retained verbatim without sanitization. | A direct test injects a custom exception outside the inner operational families, proves the typed failed result and exact verbatim error, and proves sink and queue cleanup continue. | Built-in worker code spans processor, broker, runtime, and cleanup seams without one exhaustive family. Removing containment can terminate the worker lane without a typed result; moving the catch outward loses result semantics and the local cleanup `finally`. | Behavior/locality review `NET POSITIVE`; first exact review `NET NEGATIVE` corrected the diagnostic-sanitization overclaim; different corrected exact review `NET POSITIVE`. | owner approved 2026-08-07 |
| `RUFF-SUP-327`: custom task-monitor processor boundary | MR-205; `weft/core/monitor/task_monitor.py:5487`, use `except Exception as exc:  # noqa: BLE001 approved [TS-3.1] [RUFF-SUP-327] exception` | `BLE001=1` | An ordinary custom-processor configuration, resolution, or invocation exception whose `str()` succeeds becomes a failed processor result; the monitor process survives and no checkpoint advances for the failed cycle. The exception string is retained verbatim without sanitization. | A direct configured-processor invocation test raises a nonstandard exception and proves the exact verbatim failed result, unchanged checkpoint, failed-cycle state, and still-running monitor. It does not claim a subsequent cycle succeeds. | Configured processor loading and calls have no exhaustive exception family. Removing containment lets plugin failure terminate the monitor lane; moving the catch outward loses the exact processor-result mapping; a wrapper separates the extension call from its contract without reuse. | Behavior/locality review `NET POSITIVE`; first exact review `NET NEGATIVE` corrected boundary scope, diagnostic scope, and the unproven later-cycle claim; different corrected exact review `NET POSITIVE`. | owner approved 2026-08-07 |
| `RUFF-SUP-328`: replaceable stdin terminal probe | MR-264; `weft/helpers/__init__.py:77`, use `except Exception:  # noqa: BLE001 approved [TS-3.1] [RUFF-SUP-328] exception` | `BLE001=1` | A replaceable file-like stdin object may run an arbitrary `isatty()` hook. An ordinary hook failure must conservatively return `False`; the helper itself emits no stream or exception data, and control-flow `BaseException` remains outside the boundary. | A direct helper test supplies a stream whose `isatty()` raises a custom `Exception` subclass and proves the exact `False` result with no observer path. A direct control-flow probe confirms a custom `BaseException` propagates. | File-like hooks have no exhaustive exception family. Narrowing to `OSError` misses user-defined streams; moving the catch outward can hide `closed`/selection defects; extraction reduces locality without reuse. | New firing test and independent exact directive/locality review `NET POSITIVE`. | owner approved 2026-08-07 |
| `RUFF-SUP-329`: optional cancel callback containment | MR-263; `weft/helpers/__init__.py:87`, use `except Exception:  # noqa: BLE001 approved [TS-3.1] [RUFF-SUP-329] exception` | `BLE001=1` | An optional external cancellation callback is an arbitrary observer. An ordinary callback failure must mean no cancellation request and must not replace the owning execution path; control-flow `BaseException` remains outside the boundary. | A direct helper test proves missing, truthy, and raising callback behavior, with the raising callback returning exact `False`; a direct control-flow probe confirms a custom `BaseException` propagates. | Arbitrary callbacks have no exhaustive exception family. Removing containment lets optional observation replace execution; moving the catch to every caller duplicates policy; extraction adds indirection to the already shared helper. | Independent exact directive/locality review `NET POSITIVE`. | owner approved 2026-08-07 |
| `RUFF-SUP-330`: dump command-result phase boundaries | MR-156; `weft/commands/_dump_support.py:85,118`, at each exact `except Exception as exc:` use `except Exception as exc:  # noqa: BLE001 approved [TS-3.1] [RUFF-SUP-330] exception` | `BLE001=2` | Context resolution and the coherent export transaction each convert an ordinary exception whose `str()` succeeds into the phase-specific `(1, diagnostic)` command result. Both diagnostics retain exception text verbatim without sanitization, length, or single-line guarantees. Control-flow `BaseException` remains outside containment. | Two direct tests inject distinct custom exceptions into `build_context()` and `_write_dump()`, proving exit code 1 and the exact verbatim context-resolution or export diagnostic; the full dump/load owner suite passes. | Context resolution spans filesystem, configuration, and backend-plugin resolution. Export spans broker acquisition/cleanup, owner-only file operations, backend dump iteration, serialization, and claimed-message reporting. No exhaustive family covers either phase. Moving catches outward loses phase precision; per-operation catches fragment each transaction; extraction reduces locality without reuse. | Behavior/locality and independent exact review `NET POSITIVE`. | owner approved 2026-08-07 |
| `RUFF-SUP-331`: load command-result phase boundaries | MR-158; `weft/commands/_load_support.py:521,537,550`, at each exact `except Exception as exc:` use `except Exception as exc:  # noqa: BLE001 approved [TS-3.1] [RUFF-SUP-331] exception` | `BLE001=3` | Context resolution and input-file I/O plus plan construction/enrichment convert ordinary exceptions whose `str()` succeeds into their exact phase-specific exit-1 results. The apply broad catch does the same for non-`ImportError` ordinary exceptions; the preceding dedicated `ImportError` branch retains its distinct `weft load: {exc}` result. All diagnostics retain exception text verbatim without sanitization, size, or single-line guarantees; control-flow `BaseException` remains outside containment. | Three direct tests inject two custom exceptions and one `RuntimeError` at context, plan, and non-`ImportError` apply stages, proving exact exit code and verbatim phase diagnostic; the full dump/load owner suite and rollback tests pass. | Each phase crosses backend, extension, or file-adapter boundaries without one exhaustive family. Moving catches outward loses phase precision; a type-based redaction helper preserved at `/tmp/weft-rw-cmd-load` was `NET NEGATIVE` because coarse `RuntimeError`/`ValueError`/`ImportError` families misclassified defects and broken `str()` still escaped. The corrected local boundaries preserve existing behavior and comprehensibility. | Initial formatter review `NET NEGATIVE`; corrected local rework and different clean review `NET POSITIVE`; first exact review `NET NEGATIVE` corrected phase/test precision; different corrected exact review `NET POSITIVE`. | owner approved 2026-08-07 |
| `RUFF-SUP-332`: interactive monitor completion fallback | MR-167a; `weft/commands/run.py:636`, use `except Exception:  # noqa: BLE001 approved [TS-3.1] [RUFF-SUP-332] exception` | `BLE001=1` | Monitor-store acquisition/read is optional interactive completion evidence. Any ordinary backend/driver failure returns `False` silently so the client/control and global-log channels can remain authoritative; record interpretation stays outside the catch and control-flow `BaseException` propagates. | A direct interactive-session test makes the monitor read raise a custom exception, completes through the client channel, proves the exact completed result, and proves no stdout/stderr leakage. | Monitor backends expose no exhaustive family. Removing containment lets optional observation replace execution; logging each poll failure can spam interactive output; moving the catch outward hides record interpretation defects. | Behavior/locality review and independent exact directive review `NET POSITIVE`. | owner approved 2026-08-07 |
| `RUFF-SUP-333`: managed run command-result boundaries | MR-165, MR-166; `weft/commands/run.py:1191,1328`, at each exact `except Exception as exc:` use `except Exception as exc:  # noqa: BLE001 approved [TS-3.1] [RUFF-SUP-333] exception` | `BLE001=2` | Inline and TaskSpec managed-execution failures whose `str()` succeeds become the distinct structured submission-error results. Diagnostics retain exception text verbatim without sanitization, size, or single-line guarantees and do not add exception type or traceback; control-flow `BaseException` propagates. | Two direct tests inject custom adapter failures, proving empty TID plus exact `Error submitting task: adapter detail` and `Error submitting TaskSpec: adapter detail` results without the exception class name; full run/interactive owner suites pass. | Managed execution spans submission, manager bootstrap, cleanup propagation, and optional wait callbacks with extension seams and no exhaustive family. Moving catches outward hides validation/context/stdin/spec defects; per-operation catches fragment one structured result boundary. The legacy word “submitting” can be imprecise for late wait failure, but this slice preserves that established contract. | Behavior/locality review and independent exact directive review `NET POSITIVE`. | owner approved 2026-08-07 |
| `RUFF-SUP-334`: pytest-postgres CLI boundary | MR-007; `bin/pytest-pg:572`, use `except Exception as exc:  # noqa: BLE001 approved [TS-3.1] [RUFF-SUP-334] exception` | `BLE001=1` | An unexpected ordinary environment/schema/pytest boundary failure after container assignment whose `str()` succeeds becomes exit code 1 and is printed verbatim to stderr; no sanitization, length, or single-line guarantee is claimed. `CalledProcessError` and `KeyboardInterrupt` retain their earlier dedicated branches. On nonzero exit, `finally` prints container logs before cleanup. A failure inside container startup before it returns can still strand partially created state because no container name has been assigned. | A direct `main()` test returns a named container, makes schema initialization raise a custom exception, proves exit code 1 and exact stderr, then proves declared `logs:test-container` followed by `cleanup:test-container`. | The boundary spans Docker CLI, environment, schema initialization, subprocess launch, and pytest execution without one exhaustive family. Narrowing would let extension/process failures escape; moving the catch outward would include argument parsing; extraction separates the result policy from the lifecycle `finally`. The preserved `/tmp/weft-rw-pytest-pg-main` candidate raised during startup and was `NET NEGATIVE` because it could not prove post-assignment cleanup. | Initial exact review `NET NEGATIVE`; corrected post-assignment firing proof and different exact review `NET POSITIVE`. | owner approved 2026-08-07 |
| `RUFF-SUP-335`: optional active-manager reconciliation evidence | MR-173; `weft/commands/system.py:852`, use `except Exception:  # noqa: BLE001 approved [TS-3.1] [RUFF-SUP-335] exception` | `BLE001=1` | Active-manager selection and returned-record inspection are optional reconciliation evidence. An ordinary failure degrades that datum to `None`; task-log replay and snapshot reduction continue silently. Reconciliation quality may be reduced. | A direct test injects a custom selection exception, proves the attempt, reconstructs a real TaskSpec-bearing completed snapshot, and proves no output. An early return would fail. | Registry/backend, normalization, liveness, and dynamic mapping boundaries share no exhaustive family. Removing containment lets optional evidence abort snapshots; moving it outward hides replay/reducer defects; extraction separates one datum from its fallback. | Initial evidence review `NET NEGATIVE`; corrected firing test and two different clean reviews `NET POSITIVE`. | owner approved 2026-08-07 |
| `RUFF-SUP-336`: status watch-loop command boundary | MR-174; `weft/commands/system.py:1586`, use `except Exception as exc:  # noqa: BLE001 approved [TS-3.1] [RUFF-SUP-336] exception` | `BLE001=1` | After log-queue acquisition, ordinary monitor-construction or watch-loop failure whose stringification and stderr write succeed becomes exit 1; `KeyboardInterrupt` remains the successful stop path, and `finally` attempts cleanup. Exception text is verbatim, unsanitized, unbounded, and may be multiline. Cleanup or rendering failure may still escape. | A direct watch test injects a custom log-reader exception and proves exact exit 1, empty stdout, and exact stderr; the full status owner suites pass. | Queue iteration, monitors, decoded hooks, formatting, and output streams share no exhaustive family. Moving the catch outward includes queue acquisition; moving it inward leaves loop failures as tracebacks; extraction separates loop ownership from command policy. | Behavior/locality and independent exact review `NET POSITIVE`. | owner approved 2026-08-07 |
| `RUFF-SUP-337`: initial status-source command boundary | MR-175; `weft/commands/system.py:1614`, use `except Exception as exc:  # noqa: BLE001 approved [TS-3.1] [RUFF-SUP-337] exception` | `BLE001=1` | Ordinary initial context, filter, broker-status, or manager-record failure whose stringification succeeds becomes the phase-specific `(1, error)` result. Later watch, snapshot, service, and rendering work remains outside. Exception text is verbatim, unsanitized, unbounded, and may be multiline. | A direct status test injects a custom broker-status failure and proves the exact returned exit code and diagnostic; the full status owner suites pass. | Initial sources span context, filesystem/environment, broker, queue, and registry seams without one exhaustive family. A wider catch hides later defects; per-source catches duplicate one transaction result and reduce locality. | Behavior/locality and independent exact review `NET POSITIVE`. | owner approved 2026-08-07 |
| `RUFF-SUP-338`: test-thread throwable handoff | MR-079, MR-080, MR-087, MR-088; `tests/commands/test_task_evidence.py:227,724`, `tests/core/test_manager.py:4240,4330`, at each exact `except BaseException as exc:` use `except BaseException as exc:  # noqa: BLE001 approved [TS-3.1] [RUFF-SUP-338] exception` | `BLE001=4` | Test helper threads must transfer every throwable, including direct `BaseException` subclasses, to the owning test thread without changing object identity; each thread is joined before shared handoff state is inspected or cleared. | Four custom non-`Exception` sentinel cases fire the four exact catches, prove thread termination and sentinel identity, restore replaced callables, and then execute the owning lifecycle tests; both full owner suites pass. No mapping or set order is involved. | Python thread targets do not propagate failures to callers. Narrowing to `Exception`, warning/excepthook capture, or executors loses synchronous exact-object handoff. A shared wrapper or custom thread subclass would obscure distinct drive loops, lifecycle events, joins, and re-raise points. | Behavior/locality and independent exact review `NET POSITIVE`. | owner approved 2026-08-07 |
| `RUFF-SUP-339`: topology-request throwable priority | MR-233; `weft/core/tasks/multiqueue_watcher.py:662`, use `except Exception as exc:  # noqa: BLE001 approved [TS-3.1] [RUFF-SUP-339] exception`; `:665`, use `except BaseException as exc:  # noqa: BLE001 approved [TS-3.1] [RUFF-SUP-339] exception` | `BLE001=2` | Ordinary topology-request failure stays on that request and later deque entries continue FIFO. A fatal throwable releases the current and queued callers, clears transaction state, and is re-raised by exact identity. If SIGINT was deferred in the same atomic transaction, public stop runs and consumes it, but [QUEUE.8] gives the more specific transaction failure priority. | Direct ordinary and fatal tests prove request-local identity plus FIFO continuation, exhaustive caller release, exact fatal identity, cleared transaction/owner/thread state, stop-path execution, and exactly-once drive-thread waiter cleanup. The fatal/SIGINT collision tests use the real waiter-replacement critical section. No mapping or set order is used. | Narrowing the ordinary catch misses unforeseen request defects; enumerating fatal built-ins violates the arbitrary exact-object contract. One catch with type branching obscures the priority ladder while retaining BLE001. Helper extraction disperses deque, request, critical-SIGINT, and cleanup state. | First review found deferred-SIGINT masking; second review required normative priority and stronger stop/thread proof; corrected different review `NET POSITIVE`. | owner approved 2026-08-07 |
| `RUFF-SUP-340`: fatal pipeline-bootstrap cleanup priority | MR-235; `weft/core/tasks/pipeline.py:460,468`, at each exact `except BaseException:` use `except BaseException:  # noqa: BLE001 approved [TS-3.1] [RUFF-SUP-340] exception` | `BLE001=2` | While a non-`Exception` bootstrap failure is already in flight, every admitted child gets an independent STOP attempt and registry deletion remains a separate phase. A cleanup `KeyboardInterrupt`, `SystemExit`, or other `BaseException` cannot replace the original fatal object or traceback. Failures emit fixed-format warnings with pipeline/control-queue scope but no cleanup exception payload or traceback. Logging infrastructure failure is outside this guarantee. | A production-path regression submits two real children, fails the third with a fatal sentinel, makes both STOP writes fail, performs real registry deletion then raises there too, and proves every attempt, empty registry, three scoped warnings without secret exception data, unchanged pipeline state, and exact original object identity. The full pipeline owner suite passes. No mapping order is used; declared runtime lists own child order. | Narrowing to `Exception` permits a cleanup control-flow throwable to replace the primary fatal. One combined catch loses per-child continuation; extraction separates bootstrap ownership and priority from its rollback. This group is separate from edge result mapping. | First review found missing targeted fatal cleanup; second found STOP failure masking and registry skip; corrected different review `NET POSITIVE`. | owner approved 2026-08-07 |
| `RUFF-SUP-341`: pipeline-edge handoff result boundary | MR-234; `weft/core/tasks/pipeline.py:164`, use `except Exception as exc:  # noqa: BLE001 approved [TS-3.1] [RUFF-SUP-341] exception` | `BLE001=1` | An ordinary exception raised by `_handoff_payload()` whose stringification succeeds becomes failed TaskSpec state and a failed `edge_terminal` with verbatim, unsanitized, unbounded, potentially multiline text; the configured reserved policy is applied. Broken stringification or failure while publishing state/events or policy may escape. A control-flow `BaseException` propagates exactly and leaves status `running`. | A real broker-backed `process_once()` test proves actual source reservation, exact failed state/event, default `KEEP` retention of the exact payload in reserved, and empty source. A fatal counterpart proves exact object identity and retained running status. Tests pass in either order; one exact Ruff finding maps to this site. | Handoff crosses broker movement, edge cardinality, override serialization, and reserved cleanup without one exhaustive family. Narrowing changes the result contract; moving outward hides reactor defects; extraction separates handoff ownership from state, event, and policy mapping. This group is separate from bootstrap cleanup. | First exact review required reserved-policy proof; corrected different exact review `NET POSITIVE`. | owner approved 2026-08-07 |
| `RUFF-SUP-342`: host one-shot worker execution boundary | MR-222; `weft/core/runners/host.py:189`, use `except Exception as exc:  # noqa: BLE001 approved [TS-3.1] [RUFF-SUP-342] exception` | `BLE001=1` | Function, agent, and command execution crosses arbitrary runtime/extension code. An ordinary failure becomes one terminal error outcome carrying child traceback and bounded host execution diagnostics; the result sender closes. Control-flow `BaseException` propagates by exact identity. | Direct ordinary and custom `BaseException` tests fire the site, prove error outcome/traceback/diagnostics and sender close, or exact fatal identity; the full host owner suite passes. | Narrowing misses configured target failures. Parent-side conversion loses child traceback and duplicates the terminal boundary; catching `BaseException` swallows control-flow exits. Conversion stays adjacent to execution. | Fresh clean execution-boundary and exact suppression review `NET POSITIVE`. | owner approved 2026-08-07 |
| `RUFF-SUP-343`: host lifecycle callback containment | MR-218; `weft/core/runners/host.py:477,482`, at each exact `except Exception:` use `except Exception:  # noqa: BLE001 approved [TS-3.1] [RUFF-SUP-343] exception` | `BLE001=2` | Caller-supplied worker-start and runtime-handle observers are arbitrary callbacks. Ordinary failure does not replace the spawned outcome and emits its distinct fixed warning without PID, handle, callback value, exception text, traceback, or `exc_info`. Control-flow `BaseException` propagates exactly; enclosing cleanup owns process and pipes. | Direct ordinary and custom fatal callback tests fire both sites, prove successful outcome/warnings/privacy or exact identity, and retain process/pipe cleanup; the full host owner suite passes. | Arbitrary callbacks have no exhaustive family. Removing containment lets observation replace execution; merging or extracting separates each publication from its phase warning without reuse. | Fresh clean callback-boundary/locality and exact suppression review `NET POSITIVE`. | owner approved 2026-08-07 |
| `RUFF-SUP-344`: session limit-monitor fail-open boundary | MR-241, MR-246; `weft/core/tasks/sessions.py:200,784`, at each exact `except Exception:` use `except Exception:  # noqa: BLE001 approved [TS-3.1] [RUFF-SUP-344] exception` | `BLE001=2` | A confirmed violation returns before optional metrics. An ordinary configured-monitor adapter or metrics failure silently disables monitoring for the remainder of the session, returns `(True, None)`, captures cached metrics during cleanup, stops once, and clears ownership. Control-flow `BaseException` propagates exactly and retains monitor ownership. | Mirrored parameterized tests fire both session sites for ordinary and fatal failures, prove violation priority, fail-open result, cache/stop/clear behavior or exact fatal identity/retained ownership, and pass in reverse order. Full owner suites and mypy pass. | Configured adapters expose no exhaustive ordinary family. Removing containment can replace execution; helper extraction hides two short state-owning policies. Silent loss of enforcement is an explicit preserved RM-5.1 tradeoff, not a diagnostic/privacy claim. | Failing-first behavior rework and independent exact/locality review `NET POSITIVE`. | owner approved 2026-08-07 |
| `RUFF-SUP-345`: detached-launcher ordinary failure protocol boundaries | MR-267; `weft/manager_detached_launcher.py:112,126`, at each exact `except Exception as exc:` use `except Exception as exc:  # noqa: BLE001 approved [TS-3.1] [RUFF-SUP-345] exception` | `BLE001=2` | Payload-phase ordinary failure whose stringification/stderr write succeed returns 2 with verbatim unsanitized/unbounded stderr and no stdout. After validation, launch-phase ordinary failure whose stringification/event write succeed returns 2 with one `spawn_failed` JSON event carrying the exact semantic string and stderr path, and no stderr. JSON wire encoding escapes syntax/control characters. Control-flow `BaseException` propagates exactly without output. | Four direct ordinary/fatal tests fire both sites and prove exact exit/channel/payload or fatal identity/no output in both explicit orders. Launcher and CLI-manager suites, exact Ruff cardinality, and mypy pass. | The launcher owns one ordinary/fatal handshake partition despite phase-specific outputs. Narrow tuples weaken unforeseen/platform launch containment; moving outward hides command validation or signal-loop defects; extraction reduces locality. Stringification/output may escape. Pre-existing `_launch_runtime()` can spawn then lose its handle if stderr-file close fails, so no no-child-spawn guarantee is made. | Fresh clean exact behavior/locality/cardinality/caveat/order review `NET POSITIVE`. | owner approved 2026-08-07 |
| `RUFF-SUP-346`: manager child-launch admission failure | MR-192, MR-193; `weft/core/manager.py:949,1078`, at each exact `except Exception as exc:` use `except Exception as exc:  # noqa: BLE001 approved [TS-3.1] [RUFF-SUP-346] exception` | `BLE001=2` | Ordinary initial or stale-retry service-worker admission failure stays request-local, clears active/start/retry bookkeeping, and delegates once with the same request, `process=None`, `launched_ns=0`, and exact error. Initial admission returns false; retry reports recovered work. Downstream stringification is verbatim and may escape. | Two direct tests prove full result shape, exact identity, one delegation, and all bookkeeping cleanup; manager and spawn-retry suites pass. | Worker admission crosses queue/thread startup and partial unwind without one exhaustive family. Narrowing risks bypassing cleanup; moving outward hides request construction/resolution; helpers separate lifecycle positions from local bookkeeping. | Initial evidence review required full envelopes/cleanup; corrected exact review `NET POSITIVE`. | owner approved 2026-08-07 |
| `RUFF-SUP-347`: typed worker fatal transport | MR-194, MR-231, MR-236; `weft/core/manager.py:1139`, `weft/core/tasks/base.py:673`, `weft/core/tasks/service.py:562`, at each exact `except BaseException as exc:` use `except BaseException as exc:  # noqa: BLE001 approved [TS-3.1] [RUFF-SUP-347] exception` | `BLE001=3` | Daemon-worker boundaries transport every throwable through typed owner channels. Manager preserves request/defaults/error; BaseTask preserves lane, `value=None`, and error; Service preserves complete event metadata plus inactive/stopping state. Publication/bookkeeping must themselves succeed. | Three direct non-`Exception` tests prove exact identity and each complete envelope/event/transition without collection-order dependence; owner suites pass. | Narrowing loses arbitrary fatal identity; `threading.excepthook` cannot supply typed reactor envelopes. A shared wrapper obscures distinct Manager, lane, and Service contracts. | Initial evidence review required complete metadata; corrected exact review `NET POSITIVE`. | owner approved 2026-08-07 |
| `RUFF-SUP-348`: optional control-diagnostic fallbacks | MR-229, MR-230; `weft/core/tasks/base.py:1712,1770`, at each exact `except Exception as exc:` use `except Exception as exc:  # noqa: BLE001 approved [TS-3.1] [RUFF-SUP-348] exception` | `BLE001=2` | Ordinary PONG-extension failure remains nested while keyed core PONG/request ID survive. Ordinary runner-description failure preserves runner/id, unknown state, merged metadata, and `describe_error`. Fatal `BaseException` propagates from both. Error strings are verbatim, unsanitized, unbounded, and may be multiline; broken stringification may escape. | A real keyed control request proves core PONG and nested error; runtime-summary proof covers full fallback. Direct fatal probes prove exact propagation. | Providers/plugins expose no exhaustive family. Moving outward can suppress the whole PONG or hide response defects; combining/extracting obscures different fallback shapes. | Initial review required real control-path proof; corrected exact review `NET POSITIVE`. | owner approved 2026-08-07 |
| `RUFF-SUP-349`: Consumer ordinary work-result boundary | MR-232; `weft/core/tasks/consumer.py:343`, use `except Exception as exc:  # noqa: BLE001 approved [TS-3.1] [RUFF-SUP-349] exception` | `BLE001=1` | Ordinary reactor work failure becomes `_ConsumerWorkResult` with original timestamp/transition flag, streaming false, outcome none, and exact error. Fatal `BaseException` remains outside and crosses BaseTask's typed lane unchanged. | One direct test proves the complete ordinary result and fatal partition; Consumer/task-execution owner suites pass. | Runner/session/extensions lack one exhaustive ordinary family. Catching `BaseException` collapses the inner/outer partition; moving outward loses per-work metadata; extraction separates local result construction. | Initial evidence review required omitted result fields; corrected exact review `NET POSITIVE`. | owner approved 2026-08-07 |
| `RUFF-SUP-350`: subprocess lifecycle callback containment | MR-223; `weft/core/runners/subprocess_runner.py:105,110`, at each exact `except Exception:` use `except Exception:  # noqa: BLE001 approved [TS-3.1] [RUFF-SUP-350] exception` | `BLE001=2` | Ordinary worker-start/runtime-handle callback failure does not replace execution and emits a distinct fixed warning without callback/runtime/exception data. Callback `BaseException` propagates exactly only after best-effort cleanup: guarded stop/bounded wait, runtime kill after timeout, guarded poll, direct kill if live/unknown, and final bounded reap. Every cleanup throwable remains subordinate. | Real-process tests fire both callback positions and prove exact fatal identity, no private logs, and production reap before finalizer. Deterministic seam tests fire stop failure, timeout/runtime kill, runtime-kill failure, poll failure, and direct-kill failure followed by reap. Ordinary dual failure proves successful output and fixed warnings. | Arbitrary callbacks lack an exhaustive family. The small helper owns a genuinely shared multistage fatal-reap policy; duplication or wider catches obscure callback publication or hide monitor/stream/execution defects. | Three reviews corrected missing cleanup, unguarded poll/kill, cleanup masking, and finalizer false proof; final clean review `NET POSITIVE`. | owner approved 2026-08-07 |
| `RUFF-SUP-351`: named TaskSpec resolution boundary | MR-155; `weft/cli/validate_taskspec.py:116`, use `except Exception as exc:  # noqa: BLE001 approved [TS-3.1] [RUFF-SUP-351] exception` | `BLE001=1` | For named references only, ordinary resolver failure whose stringification/Rich output succeed returns none and the command exits 1. Direct fatal exits propagate exactly without output. Diagnostics are unsanitized, unbounded, potentially multiline, and interpreted as Rich markup, so no verbatim-rendering guarantee exists. Explicit paths, reads, validation, and summary rendering remain outside. | Direct ordinary/fatal tests prove exact fallback text/none or fatal identity/no output; the full validation suite passes. | Named resolution spans context, stored/builtin/filesystem/extension paths without one exhaustive family. Wider containment hides explicit-path or later defects; extraction reduces one-call locality. | Fresh clean exact behavior/locality/cardinality/caveat review `NET POSITIVE`. | owner approved 2026-08-07 |
| `RUFF-SUP-352`: init backend/plugin command boundary | MR-161; `weft/commands/init.py:91`, use `except Exception as exc:  # noqa: BLE001 approved [TS-3.1] [RUFF-SUP-352] exception` | `BLE001=1` | Ordinary permission-tightening, backend-resolution, SimpleBroker init, or return-code coercion failure whose normalization/stringification/output succeed returns 1 with established quiet/non-quiet behavior; fatal exits propagate. Diagnostics are raw except two normalized backend-install hints, otherwise unsanitized/unbounded/multiline. Later context/config persistence stays outside. | Direct ordinary/fatal tests prove exact exit/channels or identity/no output; full init suite passes. | Filesystem/plugins/SimpleBroker lack one exhaustive family. Per-operation catches fragment the transaction; moving outward hides post-init defects. | Fresh clean exact behavior/locality/cardinality/caveat review `NET POSITIVE`. | owner approved 2026-08-07 |
| `RUFF-SUP-353`: result context/backend resolution boundary | MR-164; `weft/commands/result.py:949`, use `except Exception as exc:  # noqa: BLE001 approved [TS-3.1] [RUFF-SUP-353] exception` | `BLE001=1` | Ordinary initial context failure whose stringification succeeds becomes exact `(1, "weft: failed to resolve context: <detail>")`; fatal exits propagate. Text is verbatim, unsanitized, unbounded, and may be multiline. Later validation, TID parsing, enumeration, waiting, aggregation, and rendering remain outside. | Direct ordinary/fatal tests prove exact tuple or identity; result command/CLI suites pass. | Context spans filesystem/config/backend/plugin seams without exhaustive taxonomy. This smallest result boundary stays separate from RUFF-SUP-309 queue enumeration. | Fresh clean exact behavior/locality/cardinality/caveat review `NET POSITIVE`. | owner approved 2026-08-07 |
| `RUFF-SUP-354`: queue-first manager-startup reconciliation boundary | MR-171; `weft/commands/submission.py:220`, use `except Exception as exc:  # noqa: BLE001 approved [TS-3.1] [RUFF-SUP-354] exception` | `BLE001=1` | Ordinary manager-start failure is retained, then durable reconciliation runs. Spawned proof succeeds; rejection/uncertain results chain from startup failure; confirmed rollback re-raises it. Fatal exits bypass reconciliation exactly. This catch emits/stringifies nothing; reconciliation/rollback failure remains outside. | Direct tests prove required startup-then-reconcile order, spawned result, rejection cause identity, and fatal zero-reconciliation; full suite passes. | Startup spans registry/launcher/broker/process/backend seams. Wider containment hides reconciliation defects and corrupts queue-first policy; extraction separates capture from the immediately owned branches. | Fresh clean exact cause/order/locality review `NET POSITIVE`. | owner approved 2026-08-07 |
| `RUFF-SUP-355`: Monitor-store task-snapshot fallback | MR-179; `weft/commands/tasks.py:545`, use `except Exception as exc:  # noqa: BLE001 approved [TS-3.1] [RUFF-SUP-355] exception` | `BLE001=1` | Ordinary store open/read failure whose stringification succeeds returns none for schema-missing or a fixed-shape unknown snapshot with `monitor_store_unavailable` reconciliation and raw error. Error is unsanitized/unbounded/multiline. Fatal exits propagate. Successful record interpretation/filtering stays outside. | Direct ordinary/fatal tests prove exact degraded mapping/error or identity; mapping is compared by equality, not order; full task-command suite passes. | Configured store/backend lacks exhaustive taxonomy. Wider containment hides conversion defects; extraction adds indirection to one fallback. Fixed structure does not mean bounded diagnostic length. | Fresh clean exact behavior/locality/cardinality/caveat review `NET POSITIVE`. | owner approved 2026-08-07 |
| `RUFF-SUP-356`: host persistent-agent worker failure boundaries | MR-221; `weft/core/runners/host.py:313,347`, at each exact `except Exception as exc:` use `except Exception as exc:  # noqa: BLE001 approved [TS-3.1] [RUFF-SUP-356] exception` | `BLE001=2` | Ordinary work-item normalization/runtime failure becomes a traceback-bearing execute result. Outer startup failure becomes startup_error/runtime-startup diagnostics; post-ready loop/context failure becomes traceback-bearing execute result. Fatal `BaseException` propagates. Session teardown precedes exact-once IPC endpoint attempts without endpoint-order contract. Error tracebacks are unsanitized/unbounded; shared diagnostic tails are bounded. | Direct tests fire inner, startup, post-ready, and fatal paths with exact response/diagnostic/cleanup assertions. A real spawned production worker feeds a real AgentSession wait/execute path, proving traceback/execute diagnostics survive immediate exit and the session invalidates. Full 164-test owner suite and mypy pass. | Runtime, normalization, request transport, and context unwind lack one exhaustive family. Removing inner conversion can let unwind replace work-item evidence; parent conversion loses child traceback; helpers obscure startup/post-ready choice. Session-close cleanup remains separate RUFF-SUP-298. | Four reviews corrected protocol, cleanup, and production-frame proof; final different clean exact review `NET POSITIVE`. | owner approved 2026-08-07 |
| `RUFF-SUP-357`: DOM-15 checker executable boundary | support-script residual audit; `bin/check-dom15-fixtures:217`, use `except Exception as exc:  # noqa: BLE001 approved [TS-3.1] [RUFF-SUP-357] exception` | `BLE001=1` | An ordinary top-level checker failure whose stringification and stdout write succeed becomes exit 2 with the raw internal-error diagnostic. Fatal `BaseException` propagates exactly with no output. Detail is unsanitized, unbounded, and may be multiline; broken stringification or output can escape. | Direct ordinary/fatal tests prove exact exit/channel behavior and fatal identity; the checker self-test passes. | The executable crosses parser, filesystem, and policy checks without one exhaustive ordinary family. Moving the catch inward fragments one process result; moving it outward is impossible. | Fresh clean exact behavior/locality/cardinality/caveat review `NET POSITIVE`. | owner approved 2026-08-07 |
| `RUFF-SUP-358`: fake provider-CLI fixture tool-call boundary | support-script residual audit; `tests/fixtures/provider_cli_fixture.py:600`, use `except Exception as exc:  # noqa: BLE001 approved [TS-3.1] [RUFF-SUP-358] exception` | `BLE001=1` | This is test-fixture behavior only; Weft does not ship MCP. For the fake provider CLI's `use_mcp:` path, configuration resolution follows declared mapping order: skip non-dict entries and select the first usable dict. An ordinary fake tool-call failure whose stringification and stderr write succeed becomes exit 1 with raw detail; fatal exits propagate exactly with no output. | A three-entry fixture test declares an unusable entry followed by two distinct usable entries and proves only the first usable config and requested token reach the fake tool once. Direct ordinary/fatal tests prove exit/channel behavior and fatal identity. | Configuration selection remains outside the catch. Catching less than ordinary `Exception` misstates the fake tool boundary; moving the catch outward could hide fixture parsing or selection defects. | Fresh clean exact order, behavior, locality, and cardinality review `NET POSITIVE`. | owner approved 2026-08-07 |
| `RUFF-SUP-359`: SIGINT probe thread handoff | support-script residual audit; `tests/helpers/multiqueue_sigint_probe.py:103`, use `except BaseException as exc:  # noqa: BLE001 approved [TS-3.1] [RUFF-SUP-359] exception` | `BLE001=1` | The helper thread captures every throwable from `add_queue()`, including fatal objects, so the parent can join and publish one JSON probe result. Identity is intentionally converted to `<type>: <raw detail>`; detail is unsanitized, unbounded, and may be multiline, and broken stringification can escape within the thread. | A direct custom `BaseException` test proves the fatal object is reported and the mutator thread terminates. | Narrowing to `Exception` can strand fatal thread failures outside the parent-visible probe result. A Future/helper adds indirection to one test-only handoff. | Fresh clean exact thread-handoff/locality/cardinality review `NET POSITIVE`. | owner approved 2026-08-07 |
| `RUFF-SUP-360`: long-session benchmark executable boundary | support-script residual audit; `tests/long_session_surface_benchmark.py:1195`, use `except Exception as exc:  # noqa: BLE001 approved [TS-3.1] [RUFF-SUP-360] exception` | `BLE001=1` | Ordinary benchmark execution/setup failure whose stringification and stderr write succeed becomes exit 1 with empty stdout and raw stderr; fatal exits propagate exactly with no output. Detail is unsanitized, unbounded, and may be multiline. Argument parsing and result rendering remain outside. | Direct ordinary/fatal tests prove exact exit/channel behavior and fatal identity. | Benchmark setup/runtime has no exhaustive ordinary family. Per-operation catches fragment the executable result boundary; a wider catch could hide parsing/rendering defects. | Fresh clean exact behavior/locality/cardinality/caveat review `NET POSITIVE`. | owner approved 2026-08-07 |
| `RUFF-SUP-361`: polling benchmark executable boundary | support-script residual audit; `tests/multiqueue_polling_benchmark.py:709`, use `except Exception as exc:  # noqa: BLE001 approved [TS-3.1] [RUFF-SUP-361] exception` | `BLE001=1` | Ordinary `run_benchmarks()` failure whose stringification and stderr write succeed becomes exit 1 with empty stdout and raw stderr; fatal exits propagate exactly with no output. Detail is unsanitized, unbounded, and may be multiline. Parsing and rendering remain outside. | Direct ordinary/fatal tests prove exact exit/channel behavior and fatal identity. | Benchmark execution has no exhaustive ordinary family. Per-operation catches lose one coherent result boundary; a wider catch could hide parsing/rendering defects. | Fresh clean exact behavior/locality/cardinality/caveat review `NET POSITIVE`. | owner approved 2026-08-07 |
| `RUFF-SUP-362`: Microsandbox provider-result conversion boundary | provider/fallback residual audit; `extensions/weft_microsandbox/weft_microsandbox/plugin.py:355`, use `except Exception as exc:  # noqa: BLE001 approved [TS-3.1] [RUFF-SUP-362] exception` | `BLE001=1` | After successful sandbox execution, ordinary provider parsing or result-construction failure whose stringification succeeds becomes the primary error `RunnerOutcome` while preserving stdout, stderr, return code, and runtime handle. Fatal exits propagate. Provider tempdir cleanup still runs. Error text is raw, unsanitized, unbounded, and may be multiline; broken `__str__` escapes. | Separate tests fire parser and result-builder failures, assert the exact outcome and tempdir removal, and prove fatal result-builder identity with cleanup. | Provider adapters and result builders lack an exhaustive ordinary family. Moving the catch outward hides runtime/stream defects; moving it inward loses the shared result boundary. | Fresh clean exact behavior/cleanup/locality/cardinality review `NET POSITIVE`. | owner approved 2026-08-07 |
| `RUFF-SUP-363`: ordered provider-image preparation continuation | provider/fallback residual audit; `weft/builtins/agent_images.py:80`, use `except Exception as exc:  # noqa: BLE001 approved [TS-3.1] [RUFF-SUP-363] exception` | `BLE001=1` | One ordinary provider failure whose stringification succeeds becomes that provider's failed report and does not prevent later providers in the declared `requested_providers` sequence from running. Reports preserve that sequence; no claim depends on mapping-key order. Fatal exits propagate. Error text is raw, unsanitized, unbounded, and may be multiline; broken `__str__` aborts continuation. | A two-provider test makes the first declared provider fail and proves exact attempt/report sequence, later continuation, and summary counts; a fatal signal propagates by identity. | Provider, Docker, filesystem, and builder failures lack one exhaustive family. Catching outside the loop loses per-provider continuation; a helper separates one call from its report mapping. | Fresh clean exact declared-order/continuation/locality/cardinality review `NET POSITIVE`. | owner approved 2026-08-07 |
| `RUFF-SUP-364`: Monitor-store status fallback | provider/fallback residual audit; `weft/core/monitor/store.py:2387`, use `except Exception as exc:  # noqa: BLE001 approved [TS-3.1] [RUFF-SUP-364] exception` | `BLE001=1` | Ordinary checkpoint/store access failure whose stringification succeeds returns the exact unavailable status shape; fatal exits propagate. Successful schema/status construction remains outside. The cached error is raw, unsanitized, unbounded, and may be multiline; broken `__str__` escapes. | Direct ordinary/fatal tests assert every fallback field or exact fatal identity. | Configured stores/backends lack an exhaustive exception taxonomy. Moving the catch outward hides successful-status construction defects; extraction adds indirection to one fallback. | Fresh clean exact status-shape/locality/cardinality review `NET POSITIVE`. | owner approved 2026-08-07 |
| `RUFF-SUP-365`: runtime-description evidence fallback | provider/fallback residual audit; `weft/core/task_evidence.py:772`, use `except Exception as exc:  # noqa: BLE001 approved [TS-3.1] [RUFF-SUP-365] exception` | `BLE001=1` | Ordinary runner-plugin resolution or `describe()` failure whose stringification succeeds returns exact best-effort evidence with original runner/id, unknown state, and `metadata.describe_error`; fatal exits propagate. External-supervisor, plugin-none, and successful interpretation remain outside. The error is raw, unsanitized, unbounded, and may be multiline; broken `__str__` escapes. | Direct ordinary/fatal tests assert the exact fallback mapping or fatal identity; existing tests cover the paths outside the catch. | Entry-point loading and arbitrary runner plugins lack an exhaustive ordinary family. Moving the catch outward hides evidence interpretation defects; moving it inward loses resolution fallback. | Fresh clean exact evidence-shape/locality/cardinality review `NET POSITIVE`. | owner approved 2026-08-07 |
| Readable embedded Python source construction | MR-269..MR-280; `bin/pytest-pg:323`, `tests/cli/test_cli_run.py:1242,1571,1653`, `tests/cli/test_cli_validate.py:408`, `tests/core/test_agent_resolution.py:90`, `tests/core/test_environment_profiles.py:131`, `tests/core/test_spec_parameterization.py:95,137,169`, `tests/helpers/long_session_utils.py:108`, and `tests/tasks/test_runner.py:1467`; at each exact `"\n".join(` call use `"\n".join(  # noqa: FLY002 approved [TS-3.1] [RUFF-SUP-239] exception` | `FLY002=12` | Each 7-to-18-line embedded program remains visually parseable as source; blank lines, indentation, and the intentional final-newline policy remain explicit. `bin/pytest-pg` intentionally omits a final newline; the other eleven include one. | The twelve owner probes named by MR-269..MR-280 execute or validate the generated modules; the expanded Ruff inventory maps exactly twelve residual FLY002 diagnostics to these sites. | Ruff's escaped single literals hide program structure. Parenthesized adjacent literals add `\n` noise at every line. `textwrap.dedent` adds normalization semantics and obscures byte construction. The same mechanical alternatives were accepted only for the eleven shorter fixtures where the actual formatted result remained local. | Three clean Python experts: initial disposition `NET POSITIVE`; two directive reviews found and corrected only plan defects; final exact directive review `NET POSITIVE`. | owner approved 2026-08-07 |
| Harness database-release quiescence | MR-281; `tests/helpers/weft_harness.py:1030`, use `elif time.time() - quiescent_since >= 0.1:  # noqa: SIM102 approved [TS-3.1] [RUFF-SUP-240] exception` | `SIM102=1` | The harness first proves 100 ms of task/PID quiescence, then invokes an effectful database release probe that performs garbage collection and Windows rename/restore checks. | Three preserve-database cleanup tests pass; source inspection establishes the 100 ms gate precedes the probe; exact residual Ruff inventory maps one SIM102 diagnostic to this gate. | Merge the elapsed-time gate with `_database_files_releasable()`; rejected because it hides the phase boundary and puts an effectful rollback-capable probe inside a compound predicate. | Fresh clean Python expert disposition and final exact review: `NET POSITIVE`; intermediate reviews drove proof tightening. | owner approved 2026-08-07 |
| Manager stop-confirmation evidence stages | MR-282; `weft/core/manager_runtime.py:1307`, use `if stop_if_absent or entry_observed:  # noqa: SIM102 approved [TS-3.1] [RUFF-SUP-241] exception`; `:1308`, use `if not _is_pid_alive(_record_pid(last_record)):  # noqa: SIM102 approved [TS-3.1] [RUFF-SUP-241] exception`; `:1319`, use `if current.get("status") == "stopped" and not _is_pid_alive(  # noqa: SIM102 approved [TS-3.1] [RUFF-SUP-241] exception`; `:1328`, use `if not _is_pid_alive(current_pid):  # noqa: SIM102 approved [TS-3.1] [RUFF-SUP-241] exception` | `SIM102=4` | Stop success requires ordered evidence. Absence must first be acceptable or previously observed, then the recorded PID must be dead, then the local-process absence-or-exit gate must pass. Outside the explicit foreground-serve early return, a stopped registry row requires PID death before that gate. Rate-limited fallback advances `pid_checked_at` before liveness and gate checks. | The direct disappearing-record test proves registry absence -> PID extraction -> PID death -> local-process gate. The stopped-status test fires the stopped-row and rate-limited PID paths; source inspection confirms `pid_checked_at = now` precedes that liveness probe. Exact residual Ruff inventory maps four SIM102 diagnostics to these stages. | Flatten nested checks into compound predicates; rejected because PID-death evidence and the local-process gate become one less-legible condition. A shared PID/process/deadline helper could serve all three branches, but would hide both gates behind a boolean and erase why absence, stopped-row, and rate-limited evidence authorize them. Extracting an entire evidence branch would instead move record or timestamp state across the boundary. | Fresh clean Python expert disposition `NET POSITIVE`; four exact reviews `NET NEGATIVE` for proof/wording defects; final closure review `NET POSITIVE` after all corrections. | owner approved 2026-08-07 |
| Watcher publication rollback ownership | MR-283; `weft/core/tasks/multiqueue_watcher.py:636`, use `if candidate_waiter is not None and not candidate_installed:  # noqa: SIM102 approved [TS-3.1] [RUFF-SUP-242] exception` | `SIM102=1` | After failed topology publication, only a candidate that exists, was not installed, and remains rollback-owned may be closed; candidate config cleanup stays adjacent and independent. | Two publication-failure owner tests pass; exact residual Ruff inventory maps one SIM102 diagnostic to this rollback gate. | Merge publication failure, candidate state, and rollback ownership into one predicate; rejected because ownership and cleanup phases become less legible. Move rollback to a helper; rejected because candidate waiter/config and publication/install/ownership state would all cross the boundary. | Fresh clean Python expert disposition and final exact review: `NET POSITIVE`; intermediate reviews drove proof tightening. | owner approved 2026-08-07 |
| Root facade semantic export index | MR-284; `weft/__init__.py:27`, use `__all__ = [  # noqa: RUF022 approved [TS-3.1] [RUFF-SUP-243] exception` | `RUF022=1` | Metadata, client-facing types, and logging helpers remain visible as three adjacent API families. No behavior depends on their observable order. | Root facade identity plus lazy inventory/cache tests; exact residual inventory maps one RUF022 finding to this list. | Alphabetic sorting preserves membership but interleaves the semantic API index. Split lists would complicate the facade and lazy-map comparison without changing behavior. | Fresh clean Python expert disposition: sorting `NET NEGATIVE`; final exact review `NET POSITIVE`. | owner approved 2026-08-07 |
| Commands facade callable/module index | MR-285; `weft/commands/__init__.py:17`, use `__all__ = [  # noqa: RUF022 approved [TS-3.1] [RUFF-SUP-244] exception` | `RUF022=1` | Callable command capabilities remain together and the public `manager` module remains visibly distinct at the end. No behavior depends on observable order. | Lazy facade inventory/cache and attribute/from/star-import tests; exact residual inventory maps one RUF022 finding to this list. | Alphabetic sorting moves the module among callables. Separate exports would add structure without a runtime or maintenance benefit. | Fresh clean Python expert disposition: sorting `NET NEGATIVE`; final exact review `NET POSITIVE`. | owner approved 2026-08-07 |
| Manager query/mutation/adapter export index | MR-286; `weft/commands/manager.py:291`, use `__all__ = [  # noqa: RUF022 approved [TS-3.1] [RUFF-SUP-245] exception` | `RUF022=1` | Runtime query functions lead, `serve_manager` stays visible, start/stop mutation capabilities remain beside their matching adapters, and read-only list/status adapters remain together at the end. No behavior depends on observable order. | Full manager-command owner suite; exact residual inventory maps one RUF022 finding to this list. | Alphabetic sorting breaks this visible structure and separates the start/stop mutation pairs. Additional subgroup constants would add indirection to one short inventory. | Fresh clean Python expert disposition: sorting `NET NEGATIVE`; exact review attempt 1 `NET NEGATIVE` for an adjacency overclaim; final review `NET POSITIVE` after correction. | owner approved 2026-08-07 |
| Queue capability-family export index | MR-287; `weft/commands/queue.py:1251`, use `__all__ = [  # noqa: RUF022 approved [TS-3.1] [RUFF-SUP-246] exception` | `RUF022=1` | Value types, raw operations, command adapters, and alias operations remain distinct and locally comparable. No behavior depends on observable order. | Command and CLI queue owner suites; exact residual inventory maps one RUF022 finding to this list. | Alphabetic sorting destroys the clearest family index of the seven lists. Multiple concatenated subgroup lists would make membership and uniqueness harder to audit. | Fresh clean Python expert disposition: sorting `NET NEGATIVE`; final exact review `NET POSITIVE`. | owner approved 2026-08-07 |
| Core owner-module export index | MR-288; `weft/core/__init__.py:38`, use `__all__ = [  # noqa: RUF022 approved [TS-3.1] [RUFF-SUP-247] exception` | `RUF022=1` | Exports remain grouped by owning runtime module and aligned with `_LAZY_EXPORTS`: tasks, manager/launcher, resource monitoring, callable helpers, targets, TaskSpec, then TID helpers. No behavior depends on observable order. | Core facade identity plus lazy inventory/cache tests; exact residual inventory maps one RUF022 finding to this list. | Alphabetic sorting breaks visual alignment with the owner mapping. Generated or concatenated inventories would obscure the public surface and uniqueness. | Fresh clean Python expert disposition: sorting `NET NEGATIVE`; final exact review `NET POSITIVE`. | owner approved 2026-08-07 |
| Core task-family export index | MR-289; `weft/core/tasks/__init__.py:20`, use `__all__ = [  # noqa: RUF022 approved [TS-3.1] [RUFF-SUP-248] exception` | `RUF022=1` | Base/mixin, consumer, observer, monitor/debug/service, and pipeline families remain adjacent. No behavior depends on observable order. | Simple task, observer behavior, and endpoint owner suites; exact residual inventory maps one RUF022 finding to this list. | Alphabetic sorting separates paired task roles. Subgroup concatenation adds more syntax than this small facade warrants. | Fresh clean Python expert disposition: sorting `NET NEGATIVE`; final exact review `NET POSITIVE`. | owner approved 2026-08-07 |
| Agent-session protocol-role export index | MR-290; `weft/core/tasks/agent_session_protocol.py:168`, use `__all__ = [  # noqa: RUF022 approved [TS-3.1] [RUFF-SUP-249] exception` | `RUF022=1` | Request/response constructors, parsers/classifiers, startup diagnostics, and response predicates follow protocol roles. No behavior depends on observable order. | Agent-session runner tests; exact residual inventory maps one RUF022 finding to this list. | Alphabetic sorting erases the protocol reading order. Separate subgroup inventories would fragment one compact protocol surface. | Fresh clean Python expert disposition: sorting `NET NEGATIVE`; final exact review `NET POSITIVE`. | owner approved 2026-08-07 |
| TaskMonitor worker-snapshot field ledgers | MR-148; `weft/_constants.py:1233`, use `""".split()  # noqa: SIM905 approved [TS-3.1] [RUFF-SUP-250] exception`; `weft/_constants.py:1265`, use `""".split()  # noqa: SIM905 approved [TS-3.1] [RUFF-SUP-250] exception` | `SIM905=2` | The 178-field expected ledger and 78-field replaced ledger remain compact, visually grouped, unordered classification sets; replaced remains a subset of expected. | Worker-local snapshot ownership and unclassified-field rejection tests; direct 178/78 uniqueness and subset probe; exact residual inventory maps two SIM905 findings to the `.split()` anchors. | Ruff's explicit item literals expand roughly 100 compact lines to about 262 one-item lines and increase scroll distance between related policies. They do not improve duplicate detection because `frozenset` still collapses duplicates; they exchange whitespace-separator risk for comma/adjacent-literal risk. | Fresh clean Python expert disposition: literal rewrite `NET NEGATIVE`; final exact preserve-and-suppress review `NET POSITIVE`. | owner approved 2026-08-07 |
| Commands direct sibling-module ownership | MR-162, MR-172, MR-178; `weft/commands/result.py:15`, use `import weft.commands.task_evidence as task_evidence  # noqa: PLR0402 approved [TS-3.1] [RUFF-SUP-251] exception`; `weft/commands/system.py:22`, use `import weft.commands.task_evidence as task_evidence  # noqa: PLR0402 approved [TS-3.1] [RUFF-SUP-251] exception`; `weft/commands/tasks.py:23`, use `import weft.commands.task_evidence as task_evidence  # noqa: PLR0402 approved [TS-3.1] [RUFF-SUP-251] exception` | `PLR0402=3` | Command leaves name their sibling implementation owner directly and never route dependency edges through the lazy `weft.commands` compatibility facade. | Exact `test_restored_facade_backedges_are_detected` and `test_leaf_modules_do_not_import_their_own_package_facade` architecture proofs plus result, status, and task-command owner suites; exact residual inventory maps three PLR0402 findings to these lines. | Ruff's `from weft.commands import task_evidence` form is runtime-equivalent today but violates the syntactic facade rule. Relative `from . import task_evidence` has the same forbidden syntactic target. Direct-symbol imports would replace cohesive namespaces of five, five, and ten functions/types with wide local binding lists. | Fresh clean Python/architecture expert: Ruff rewrite `NET NEGATIVE`; two exact reviews drove narrow-import refactors and claim corrections; final closure review `NET POSITIVE`. | owner approved 2026-08-07 |
| Core direct sibling-module ownership | MR-199; `weft/core/monitor/runtime.py:27`, use `import weft.core.task_evidence as task_evidence  # noqa: PLR0402 approved [TS-3.1] [RUFF-SUP-252] exception` | `PLR0402=1` | The core monitor leaf names its sibling implementation owner directly instead of routing a dependency edge through the lazy `weft.core` compatibility facade. | Exact `test_restored_facade_backedges_are_detected`, `test_leaf_modules_do_not_import_their_own_package_facade`, `test_weft_has_no_eager_import_cycles`, and `test_core_task_evidence_import_does_not_initialize_manager_or_monitor` proofs plus monitor-runtime owner suites; exact residual inventory maps one PLR0402 finding to this line. | Ruff's `from weft.core import task_evidence` is runtime-equivalent and acyclic today but violates the syntactic facade rule and couples the leaf to future facade changes. Wide direct-symbol imports lose the cohesive namespace used by functions and annotations. | Fresh clean Python/architecture expert: Ruff rewrite `NET NEGATIVE`; exact reviews drove claim/proof tightening; final closure review `NET POSITIVE`. | owner approved 2026-08-07 |
| Commands-to-core explicit implementation ownership | MR-176; `weft/commands/task_monitor.py:27`, use `import weft.core.task_evidence as task_evidence  # noqa: PLR0402 approved [TS-3.1] [RUFF-SUP-253] exception` | `PLR0402=1` | The command layer names its allowed lower-layer implementation dependency explicitly; the package-from syntax obscures that non-exported implementation owner even though Python resolves the same child module today. | Task-monitor owner suite plus exact `test_internal_import_boundaries`, `test_weft_has_no_eager_import_cycles`, and `test_core_task_evidence_import_does_not_initialize_manager_or_monitor` proofs; exact residual inventory maps one PLR0402 finding to this line. | Ruff's `from weft.core import task_evidence` is runtime-equivalent today but is inconsistent with the repository's explicit-owner convention. A direct three-symbol child import is viable, but removes the repeated `task_evidence.` domain-owner qualifier from calls and annotations. | Fresh clean Python/architecture expert: Ruff rewrite `NET NEGATIVE`; exact reviews drove ownership/proof/alternative corrections; final closure review `NET POSITIVE`. | owner approved 2026-08-07 |
| Runtime import-form architecture fixtures | MR-068, MR-069; `tests/architecture/test_import_boundaries.py:941`, use `exec("from weft.core.agents.backends import *", namespace)  # noqa: S102 approved [TS-3.1] [RUFF-SUP-254] exception`; `:978`, use `exec("from weft.commands import manager", namespace)  # noqa: S102 approved [TS-3.1] [RUFF-SUP-254] exception`; `:982`, use `exec("from weft.commands import *", namespace)  # noqa: S102 approved [TS-3.1] [RUFF-SUP-254] exception` | `S102=3` | The tests execute real explicit/star import grammar in isolated dictionaries and compare imported objects by identity in the current process without adding names to module globals. | Both exact architecture owner tests pass; Ruff's own directive placement maps exactly three S102 findings to these calls. | `importlib`/`getattr` tests attribute lookup rather than from/star-import semantics. A star import is illegal inside a function. A subprocess preserves grammar but loses same-process identity and adds process overhead; AST-only checks do not execute the facade contract. | Fresh clean Python/security expert exact review `NET POSITIVE`; fixed literals and test-only isolation verified. | owner approved 2026-08-07 |
| MCP remote content-shape failure | MR-103; `tests/fixtures/mcp_stdio_fixture.py:113`, use `raise RuntimeError("fixture MCP server returned invalid content")  # noqa: TRY004 approved [TS-3.1] [RUFF-SUP-255] exception` | `TRY004=1` | A malformed remote tool response remains an operational protocol failure (`RuntimeError`), distinct from invalid caller arguments. | Direct fake-transport test reaches the exact response shape and asserts class/message; exact residual inventory maps one TRY004 finding to this raise. | `TypeError` misclassifies data received from the fixture server as a local call-signature failure. | Fresh clean Python exception-contract expert: final exact review `NET POSITIVE`. | owner approved 2026-08-07 |
| MCP JSON-RPC remote-error failure | MR-101; `tests/fixtures/mcp_stdio_fixture.py:306`, use `raise RuntimeError(  # noqa: TRY004 approved [TS-3.1] [RUFF-SUP-256] exception` | `TRY004=1` | A JSON-RPC error object remains an operational remote failure even when its optional message is not a string. | Direct response-stream test asserts fallback class/message; exact residual inventory maps one TRY004 finding to this raise. | `TypeError` would describe the malformed optional message rather than the governing remote request failure. | Fresh clean Python exception-contract expert: final exact review `NET POSITIVE`. | owner approved 2026-08-07 |
| Claude fixture MCP configuration value failures | MR-105; `tests/fixtures/provider_cli_fixture.py:651`, use `raise ValueError("Claude MCP config must be a JSON object")  # noqa: TRY004 approved [TS-3.1] [RUFF-SUP-257] exception`; `:654`, use `raise ValueError("Claude MCP config must contain mcpServers")  # noqa: TRY004 approved [TS-3.1] [RUFF-SUP-257] exception` | `TRY004=2` | Parsed JSON with an invalid configuration shape remains a value error. Missing and empty-list `mcpServers` values retain their separate proven normalization to `{}`. | Two direct raise-site cases assert exact class/message, including a truthy non-dict `mcpServers`; two characterization cases prove missing and empty-list values normalize to `{}`; exact residual inventory maps two TRY004 findings to these raises. | `TypeError` would change the fixture CLI's validation class for syntactically valid but semantically invalid JSON. Changing normalization is a separate behavior decision. | Fresh clean Python exception-contract expert: final exact review `NET POSITIVE`. | owner approved 2026-08-07 |
| Decoded TaskSpec validation value taxonomy | MR-186, MR-188..MR-190, MR-212, MR-213; `weft/core/agents/validation.py:105`, use `raise ValueError(f"{name} must be an object")  # noqa: TRY004 approved [TS-3.1] [RUFF-SUP-258] exception`; `weft/core/environment_profiles.py:173`, use `raise ValueError(f"{name} must be a mapping of strings to strings")  # noqa: TRY004 approved [TS-3.1] [RUFF-SUP-258] exception`; `:180`, use `raise ValueError(f"{name} must be an object")  # noqa: TRY004 approved [TS-3.1] [RUFF-SUP-258] exception`; `:186`, use `raise ValueError(f"{name} must be a string")  # noqa: TRY004 approved [TS-3.1] [RUFF-SUP-258] exception`; `weft/core/runner_validation.py:132`, use `raise ValueError(f"{name} must be an object")  # noqa: TRY004 approved [TS-3.1] [RUFF-SUP-258] exception`; `:138`, use `raise ValueError(f"{name} must be a string")  # noqa: TRY004 approved [TS-3.1] [RUFF-SUP-258] exception` | `TRY004=6` | Agent validation, environment materialization, and runner validation normalize malformed values inside an already-decoded TaskSpec payload to `ValueError` with field-qualified messages. Python object-contract failures outside this payload-validation boundary remain free to use `TypeError`. | Five existing direct cases plus two public agent-validation cases assert exact built-in class and full field-qualified messages across all three entry points; the two new public cases also assert no chained cause; exact residual inventory maps six TRY004 findings to these raises. | Changing only runner validation creates conflicting taxonomies and was independently `NET NEGATIVE`; candidate preserved at `/tmp/weft-rw-try004-runner-validation`. A coordinated three-module taxonomy change is a wider compatibility change without a governing spec requirement. | Prior five-site clean review `NET POSITIVE`; first enlarged-group review `NET NEGATIVE` for a proof overclaim; corrected fresh exact review `NET POSITIVE`. | owner approved 2026-08-07 |
| Parameterization adapter JSON-object shape failures | MR-259, MR-260; `weft/core/taskspec/parameterization.py:84`, use `raise ValueError(  # noqa: TRY004 approved [TS-3.1] [RUFF-SUP-259] exception`; `:120`, use `raise ValueError("spec.parameterization adapter must return a TaskSpec payload")  # noqa: TRY004 approved [TS-3.1] [RUFF-SUP-259] exception` | `TRY004=2` | After JSON serializability is established, a wrong top-level object shape or wrong `spec` object shape remains a parameterization value-contract failure. Non-serializable Python objects retain their separate existing exception path. | Two direct adapter-result tests assert exact built-in class and full message; exact residual inventory maps two TRY004 findings to these raises. | `TypeError` is idiomatic for direct Python argument types but would reclassify decoded adapter result values and split these shape failures from the adjacent forbidden-TID and changed-`spec.type` `ValueError` contract. | Fresh clean Python exception-contract expert: exact review `NET POSITIVE`. | owner approved 2026-08-07 |
| TaskMonitor worker-snapshot classification invariants | MR-207; `weft/core/monitor/task_monitor.py:840`, use `raise RuntimeError(  # noqa: TRY004 approved [TS-3.1] [RUFF-SUP-260] exception`; `:849`, use `raise RuntimeError(  # noqa: TRY004 approved [TS-3.1] [RUFF-SUP-260] exception` | `TRY004=2` | Fields declared as plain callable shares must remain stateless function/type objects; fields declared as explicit shares must remain non-callable. A mismatch means the internal worker-snapshot policy is stale or violated. | One direct clone test mutates each classified field in turn, reaches both raises, asserts exact built-in class/full message, restores both fields, and completes normal cleanup; exact residual inventory maps two TRY004 findings to these raises. | `TypeError` would describe the incidental runtime shape while hiding the governing internal classification failure. Moving checks away from the clone loop would reduce locality between classification and enforcement. | Fresh clean Python exception-contract expert: final exact review `NET POSITIVE`. | owner approved 2026-08-07 |
| State-machine transition-source value contract | MR-227; `weft/core/state_machines.py:262`, use `raise ValueError(  # noqa: TRY004 approved [TS-3.1] [RUFF-SUP-261] exception` | `TRY004=1` | Transition definitions are declarative state-machine values. A source outside the accepted string/frozenset forms remains a constructor validation error beside missing states, duplicate IDs, and invalid transition topology. | Existing set-source test now asserts exact built-in class and full message; exact residual inventory maps one TRY004 finding to this raise. | `TypeError` is idiomatic for a direct argument contract but would split one malformed-transition validation family based only on which transition field failed. | Fresh clean Python exception-contract expert: exact review `NET POSITIVE`. | owner approved 2026-08-07 |
| Pipeline stage-template spec-shape value contract | MR-208; `weft/core/pipelines.py:288`, use `raise ValueError(f"stage '{stage.name}' is missing a valid spec section")  # noqa: TRY004 approved [TS-3.1] [RUFF-SUP-262] exception` | `TRY004=1` | Stored stage targets are declarative pipeline values. A missing/non-mapping `spec` section remains a stage-template validation error beside nested-pipeline, persistent, interactive, and streaming incompatibilities. | A public `compile_linear_pipeline` test supplies a non-mapping stage `spec` and asserts exact built-in class/full message; exact residual inventory maps one TRY004 finding to this raise. | `TypeError` would classify one invalid stage value by incidental Python shape and split the compiler's stage-template `ValueError` family. Directly testing the private validator would weaken boundary coverage. | Fresh clean Python exception-contract expert: exact review `NET POSITIVE`. | owner approved 2026-08-07 |
| Exact-ID spawn writer capability invariant | MR-225; `weft/core/spawn_requests.py:122`, use `raise RuntimeError(  # noqa: TRY004 approved [TS-3.1] [RUFF-SUP-263] exception` | `TRY004=1` | Explicit-TID submission requires a broker connection that exposes callable `insert_messages`; its absence is a runtime dependency/capability failure. | A direct private-boundary test supplies an object without the method and asserts exact built-in class/full capability-based message; public explicit-TID integration still proves the supported path; exact residual inventory maps one TRY004 finding to this raise. | `TypeError` would describe the incidental missing/callability shape while obscuring the required broker capability. Retaining the old `simplebroker>=4.3.0` text was rejected because Weft already requires `simplebroker>=6.0.1`; missing support now indicates a connection/backend mismatch. Testing only through public submission cannot construct an unsupported real broker without replacing the boundary under test. | Fresh clean Python exception-contract expert: final exact review `NET POSITIVE`. | owner approved 2026-08-07 |
| Stored spec JSON top-level value contract | MR-226; `weft/core/spec_store.py:72`, use `raise ValueError(f"Spec file {path} must contain a JSON object")  # noqa: TRY004 approved [TS-3.1] [RUFF-SUP-264] exception` | `TRY004=1` | A decoded non-object top-level JSON value is invalid stored spec data, not misuse of the Python `Path` argument. | A public reader test writes a list payload and asserts exact built-in class/full path-qualified message with no chained cause; existing missing/malformed tests prove the separate read/decode path; exact residual inventory maps one TRY004 finding to this raise. | `TypeError` would misclassify the decoded file value as a Python call-signature error. | Fresh clean Python exception-contract expert: exact review `NET POSITIVE`. | owner approved 2026-08-07 |
| Task-monitor checkpoint top-level value contract | MR-177; `weft/commands/task_monitor.py:225`, use `raise ValueError(f"Invalid task monitor checkpoint: {path}")  # noqa: TRY004 approved [TS-3.1] [RUFF-SUP-265] exception` | `TRY004=1` | A decoded non-object checkpoint value is invalid persisted monitor state, not misuse of the Python `Path` or monitor-name arguments. | A direct loader test writes a list payload and asserts exact built-in class/full path-qualified message with no chained cause; the public corrupt-checkpoint test proves command failure rendering; exact residual inventory maps one TRY004 finding to this raise. | `TypeError` would misclassify persisted JSON content as a Python call-signature error and escape `run_task_monitor()`'s current ValueError handling. | Fresh clean Python exception-contract expert: exact review `NET POSITIVE`. | owner approved 2026-08-07 |
| Provider container descriptor top-level value contract | MR-180; `weft/core/agents/provider_cli/container_runtime.py:404`, use `raise ValueError(  # noqa: TRY004 approved [TS-3.1] [RUFF-SUP-266] exception` | `TRY004=1` | A decoded non-object bundled descriptor is invalid provider runtime data, not misuse of the provider-name Python argument. | A public loader test injects a fake package resource containing a JSON list and asserts exact built-in class/full resource-qualified message with no chained cause; supported-provider tests prove all five descriptor names declared by `[AR-7]`; exact residual inventory maps one TRY004 finding to this raise. | `TypeError` would misclassify decoded resource content as Python API misuse. | Fresh clean Python exception-contract expert: exact review `NET POSITIVE`. | owner approved 2026-08-07 |
| Provider CLI persisted settings shape contract | MR-181 through MR-184; `weft/core/agents/provider_cli/settings.py:56`, `:103`, `:218`, `:226`, use `raise ValueError(  # noqa: TRY004 approved [TS-3.1] [RUFF-SUP-267] exception`; `:253`, use `raise ValueError(f"Invalid {label}: root value must be an object")  # noqa: TRY004 approved [TS-3.1] [RUFF-SUP-267] exception` | `TRY004=5` | Values decoded from project-local `agents.json` must have object shapes at the root, provider namespace, providers namespace, and individual provider-entry boundaries; malformed persisted configuration uses one `ValueError` family. | Four public loader cases and one public writer case assert the exact built-in class/full boundary-specific message with no chained cause; exact residual inventory maps five TRY004 findings to the five raises. | `TypeError` would misclassify user-authored persisted configuration as Python API misuse. Splitting by helper would obscure the common file-data contract without reducing the exact site inventory. | Fresh clean Python exception-contract expert: exact review `NET POSITIVE`. | owner approved 2026-08-07 |
| Persistent agent runtime capability contract | MR-185; `weft/core/agents/runtime.py:292`, use `raise RuntimeError(  # noqa: TRY004 approved [TS-3.1] [RUFF-SUP-268] exception` | `TRY004=1` | The registered adapter satisfies the one-shot `AgentRuntimeAdapter` protocol, but the requested persistent operation needs an additional callable `start_session` capability. | A direct public-boundary test registers a real execute-only adapter and asserts exact built-in `RuntimeError`, full message, and no chained cause; persistent provider-session tests prove the supported path; exact residual inventory maps one TRY004 finding to this raise. | `TypeError` would describe the attribute's incidental non-callable shape rather than the unavailable requested operation. Keeping `ValueError` would incorrectly blame valid caller configuration for a runtime capability gap. | Fresh clean Python refactor/exception-contract expert: exact review `NET POSITIVE`. | owner approved 2026-08-07 |
| Persisted RunnerHandle value-shape contract | MR-262; `weft/ext.py:151`, use `raise ValueError("runner handle requires string runner, kind, and id")  # noqa: TRY004 approved [TS-3.1] [RUFF-SUP-269] exception`; `:153`, use `raise ValueError("runner handle control must be a mapping")  # noqa: TRY004 approved [TS-3.1] [RUFF-SUP-269] exception`; `:155`, use `raise ValueError("runner handle observations must be a mapping")  # noqa: TRY004 approved [TS-3.1] [RUFF-SUP-269] exception`; `:157`, use `raise ValueError("runner handle metadata must be a mapping")  # noqa: TRY004 approved [TS-3.1] [RUFF-SUP-269] exception` | `TRY004=4` | `RunnerHandle.from_dict()` validates persisted queue/runtime payload values, not direct constructor arguments. Reached wrong required scalar types and nested mapping shapes remain one `ValueError` deserialization family. | Four public factory cases use truthy malformed values and assert exact built-in class/full message with no chained cause; the round-trip and legacy-key tests prove supported and adjacent invalid paths; exact residual inventory maps four TRY004 findings to the four raises. | `TypeError` would bypass existing persisted-handle readers that catch `ValueError` and make the same payload family vary by failed field. Direct `RunnerHandle(...)` misuse is separately changed to `TypeError`. Pre-existing `payload.get(...) or {}` normalization means falsy non-mapping nested values do not all reach these raises: empty `control` later fails missing authority, while empty `observations` or `metadata` normalize to `{}`. Changing that behavior needs separate contract authority, not a lint-driven edit. | Fresh clean Python refactor/exception-contract expert: exact review `NET POSITIVE`. | owner approved 2026-08-07 |
| Prepare-agent-images work-item value contract | MR-152 and MR-153; `weft/builtins/agent_images.py:134`, use `raise ValueError("prepare-agent-images expects a JSON object input")  # noqa: TRY004 approved [TS-3.1] [RUFF-SUP-270] exception`; `:137`, use `raise ValueError("prepare-agent-images input must be a JSON object")  # noqa: TRY004 approved [TS-3.1] [RUFF-SUP-270] exception`; `:154`, use `raise ValueError("prepare-agent-images input field 'refresh' must be a boolean")  # noqa: TRY004 approved [TS-3.1] [RUFF-SUP-270] exception`; `:164`, use `raise ValueError(  # noqa: TRY004 approved [TS-3.1] [RUFF-SUP-270] exception` | `TRY004=4` | The builtin's `work_item` is queue/stdin task data. Its object shape, decoded JSON root, `refresh`, and `providers` values use one user-input `ValueError` family. | Four public builtin cases replace only platform/Docker availability prerequisites, fire each exact branch in production parse order, and assert exact built-in class/full message with no chained cause; exact residual inventory maps four TRY004 findings to the four raises. | `TypeError` would classify task data by incidental in-process representation. A JSON string and its already-decoded mapping must retain the same value-validation family. Direct private-parser tests would prove less of the public task boundary. Pre-existing behavior accepts any non-string `Sequence` for `providers` despite the word “list” in the message; this lint slice does not alter that input contract. | Fresh clean Python exception-contract expert: exact review `NET POSITIVE`. | owner approved 2026-08-07 |
| Packaged builtin TaskSpec value-shape contract | MR-150 and MR-151; `weft/builtins/__init__.py:115`, use `raise ValueError(f"Builtin TaskSpec {path} must contain a JSON object")  # noqa: TRY004 approved [TS-3.1] [RUFF-SUP-271] exception`; `:119`, use `raise ValueError(f"Builtin TaskSpec {path} is missing a spec object")  # noqa: TRY004 approved [TS-3.1] [RUFF-SUP-271] exception`; `:173`, use `raise ValueError(  # noqa: TRY004 approved [TS-3.1] [RUFF-SUP-271] exception` | `TRY004=3` | Catalog construction decodes packaged JSON resources. Wrong root, `spec`, and `supported_platforms` shapes are invalid bundled values in one `ValueError` family. | Three public catalog cases replace only the builtin path inventory, fire each exact branch, and assert exact built-in class/full path-qualified message with no chained cause; existing packaged-asset and platform tests prove supported values; exact residual inventory maps three TRY004 findings to the three raises. | `TypeError` would misclassify packaged resource content as misuse of `builtin_task_catalog()`'s zero-argument Python API. The helper accepts any non-string `Sequence` despite the word “list” in the message, but normal JSON loading makes only arrays/lists observable at the public resource boundary; this lint slice does not alter that pre-existing helper behavior. | Fresh clean Python exception-contract expert: exact review `NET POSITIVE`. | owner approved 2026-08-07 |
| macOS sandbox decoded TaskSpec section contract | MR-040; `extensions/weft_macos_sandbox/weft_macos_sandbox/plugin.py:348`, use `raise ValueError(f"{name} must be an object")  # noqa: TRY004 approved [TS-3.1] [RUFF-SUP-272] exception` | `TRY004=1` | The extension plugin validates decoded TaskSpec sections supplied to `validate_taskspec()`. Non-object `spec`, `spec.runner`, and `spec.runner.options` values use the extension's field-qualified `ValueError` validation family. | Three public plugin cases reach the same helper with each exact field name and assert exact built-in class/full message with no chained cause; supported and adjacent validation tests remain in the owner suite; exact residual inventory maps one TRY004 finding to the raise. | `TypeError` would classify the same decoded TaskSpec value differently from the plugin's other configuration validation failures. Keeping an extension-owned group avoids coupling suppression ownership and cardinality to the core decoded-TaskSpec group. | Fresh clean Python exception-contract expert: exact review `NET POSITIVE`. | owner approved 2026-08-07 |
| Microsandbox decoded TaskSpec option value contract | MR-043 through MR-048; `extensions/weft_microsandbox/weft_microsandbox/_options.py:230`, `:241`, `:252`, `:271`, `:301`, `:307`, use inline `# noqa: TRY004 approved [TS-3.1] [RUFF-SUP-273] exception` on each complete raise; `:275`, `:286`, use the same directive on each multiline `raise ValueError(` | `TRY004=8` | The internal production `parse_options_from_payload()` parser normalizes decoded TaskSpec sections and runner-option values. Reached wrong mode, network, workspace, mounts, env, and section shapes use one field-qualified `ValueError` family alongside invalid values of the correct Python shape. | Six option-shape cases plus five named section/env cases exercise every exact raise and every shared-helper field name through the production parser; all assert exact built-in class/full message with no chained cause; exact residual inventory maps eight TRY004 findings to eight raises. | `TypeError` would split one decoded TaskSpec validation surface according to incidental Python shape. One extension-owned group keeps its inventory independent from other packages. Pre-existing `spec.get("env") or {}` normalizes falsy non-mappings to an empty environment, so the env proof uses a truthy list and does not claim all non-objects are rejected. `_normalize_mounts` accepts any non-string `Sequence`, including tuples, despite the word “list”; JSON normally yields lists, and this slice does not alter direct-call behavior. | Fresh clean Python exception-contract expert: exact review `NET POSITIVE`. | owner approved 2026-08-07 |
| Docker agent-runner option value contract | MR-016 through MR-019; `extensions/weft_docker/weft_docker/agent_runner.py:571`, `:599`, `:625`, `:628`, `:761`, `:770`, use inline `# noqa: TRY004 approved [TS-3.1] [RUFF-SUP-274] exception` on each complete raise; `:575`, `:604`, reformat the current single-line raises only as needed to place the same directive on `raise ValueError(` | `TRY004=8` | `DockerProviderCLIRunner` normalizes decoded runner options for explicit and work-item mounts plus network. Wrong collection, entry, and scalar shapes use one field-qualified `ValueError` family alongside invalid values of the correct Python shape. | Nine constructor cases exercise every exact raise and both source/target names sharing `_normalize_required_text`; all assert exact built-in class/full message with no chained cause; supported mount behavior remains covered; exact residual inventory maps eight TRY004 findings to eight raises. | `TypeError` would split one runner-option validation surface by incidental Python shape. An extension/module-owned group keeps this runtime contract separate from Docker plugin validation and persisted profiles. Both mount normalizers accept any non-string `Sequence` despite saying “list”; JSON normally yields lists. Pre-existing `dict(runner_options or {})` normalizes falsy direct-call values before nested validation; this group does not claim to validate the outer constructor argument or change either behavior. | First clean exact review `NET NEGATIVE` for two non-firing cases; corrected fresh exact review `NET POSITIVE`. | owner approved 2026-08-07 |
| Docker plugin decoded configuration value contract | MR-027 through MR-030, MR-032, MR-033; `extensions/weft_docker/weft_docker/plugin.py:1432`, `:1447`, `:1473`, `:1482`, `:1491`, `:1500`, `:1509`, use inline `# noqa: TRY004 approved [TS-3.1] [RUFF-SUP-275] exception` on each current complete raise, reformatting an individual raise only if the resulting line exceeds the formatter limit and then placing the directive on `raise ValueError(` | `TRY004=7` | Docker plugin validation/materialization normalizes decoded TaskSpec, runner-option, build, mount, environment, and working-directory values. Wrong shapes remain one field-qualified `ValueError` configuration family beside invalid values of the correct Python shape. | Seven direct normalizer cases fire each exact raise and assert exact built-in class/full message with no chained cause; the full 33-test plugin suite covers public composition and supported paths; exact residual inventory maps seven TRY004 findings to seven raises. | `TypeError` would split one decoded configuration surface by incidental Python shape. Direct normalizer tests provide exact site proof without claiming that every shared-helper call name is independently exercised. `_normalize_mounts` and `_string_list` accept any non-string `Sequence`; `_string_list` stringifies elements despite saying “strings”; and `spec.get("env") or {}` normalizes falsy outer values. These pre-existing behaviors remain unchanged. | First clean exact review `NET NEGATIVE` for two inaccurate placement descriptions; corrected fresh exact review `NET POSITIVE`. | owner approved 2026-08-07 |
| Docker TOML decoder postcondition contract | MR-034; `extensions/weft_docker/weft_docker/profiles.py:209`, use `raise RuntimeError(  # noqa: TRY004 approved [TS-3.1] [RUFF-SUP-276] exception` | `TRY004=1` | A non-mapping result from `tomllib.load()` violates the standard-library decoder postcondition. It is an internal capability failure, not malformed caller input. | A direct test replaces only the decoder result, fires the otherwise unreachable defensive branch, and asserts exact built-in `RuntimeError`, full path-qualified message, and no chained cause; the full profile suite covers real TOML success and decode failure; exact residual inventory maps one TRY004 finding to the raise. | `TypeError` would blame the valid `Path` argument, while `ValueError` would classify an impossible decoder result as user-authored malformed TOML. The retained message is conservative but still describes the failed expected root shape; changing it is not needed for this lint slice. | Fresh clean Python refactor/locality review `NET POSITIVE`; separate fresh exact suppression review `NET POSITIVE`. | owner approved 2026-08-07 |
| Docker profile materialization value contract | MR-035 through MR-039; `extensions/weft_docker/weft_docker/profiles.py:321`, `:330`, `:392`, `:401`, `:410`, `:419`, use inline `# noqa: TRY004 approved [TS-3.1] [RUFF-SUP-277] exception` on each complete raise, reformatting an individual raise only if the resulting line exceeds the formatter limit and then placing the directive on `raise ValueError(` | `TRY004=6` | Container-profile materialization normalizes persisted TOML profile fields and explicit TaskSpec environment values. Wrong collection, mapping-entry, scalar, and section shapes remain one field-qualified `ValueError` family beside invalid values of the correct Python shape. | Six direct normalizer cases fire each exact raise and assert exact built-in class/full message with no chained cause; the full profile suite covers public materialization and supported paths; exact residual inventory maps six TRY004 findings to six raises. | `TypeError` would split one configuration validation surface by incidental Python shape. `_resolve_profile_mounts` and `_string_list` accept any non-string `Sequence`; `_string_list` still rejects non-string elements through `_required_text`. Environment collisions follow the declared resolution order `profile_env`, then forwarded host env, then explicit TaskSpec env, with later sources winning. No logic depends on mapping iteration order, and `_mapping_of_strings` performs no key normalization or collision resolution. Pre-existing `env or {}` normalization of falsy values remains unchanged. | First clean exact review `NET NEGATIVE` because the tests did not prove both collision-precedence edges; corrected fresh exact review `NET POSITIVE`. | owner approved 2026-08-07 |
| Docker agent lifecycle callback containment | MR-015a; `extensions/weft_docker/weft_docker/agent_runner.py:244`, `:251`, use `except Exception:  # pragma: no cover - callback safety  # noqa: BLE001 approved [TS-3.1] [RUFF-SUP-278] exception` | `BLE001=2` | Worker-start and runtime-handle callbacks are caller/extension-supplied observers. Their arbitrary failures must not replace container execution. | One real runner flow makes both callbacks raise, retains an `ok` outcome, and asserts exact bounded warnings without exception info or secret text; exact residual inventory maps two BLE001 findings to the catches. | Narrowing cannot be exhaustive for arbitrary callbacks. Removing containment would let observation failure replace execution. A helper or `contextlib.suppress` would separate each callback from its distinct warning and reduce locality. | Fresh clean Python refactor review and separate fresh exact suppression review: both `NET POSITIVE`. | owner approved 2026-08-07 |
| Docker agent stream callback containment | MR-015b; `extensions/weft_docker/weft_docker/agent_runner.py:293`, `:298`, use `except Exception:  # pragma: no cover - callback safety  # noqa: BLE001 approved [TS-3.1] [RUFF-SUP-279] exception` | `BLE001=2` | Stdout and stderr consumers are caller/extension-supplied observers. Their arbitrary failures must not replace execution or remove captured output from the outcome. | One real runner flow makes both callbacks raise, retains `ok`, stdout, and stderr values, and asserts exact bounded warnings without exception info, chunk data, or secret text; exact residual inventory maps two BLE001 findings to the catches. | Narrowing cannot be exhaustive for arbitrary callbacks. Removing containment would replace the task result; moving the catches would separate delivery from its failure policy. | Fresh clean Python refactor review and separate fresh exact suppression review: both `NET POSITIVE`. | owner approved 2026-08-07 |
| Docker agent provider-result boundary | MR-015c; `extensions/weft_docker/weft_docker/agent_runner.py:340`, use `except Exception as exc:  # noqa: BLE001 approved [TS-3.1] [RUFF-SUP-280] exception` | `BLE001=1` | Provider parsing and result construction are extension/runtime boundaries. Any ordinary failure becomes the returned primary `RunnerOutcome(status="error", error=str(exc))`. | Two runs separately make the provider parser and result builder raise; each returns the exact error outcome with captured output preserved; exact residual inventory maps one BLE001 finding to the catch. | Provider adapters and result construction do not share a stable exhaustive exception taxonomy. Narrowing would let an unlisted provider failure escape instead of following the runner contract. An extra warning would duplicate the primary error outcome. | Fresh clean Python exact suppression review `NET POSITIVE`. | owner approved 2026-08-07 |
| Docker agent container cleanup containment | MR-015d; `extensions/weft_docker/weft_docker/agent_runner.py:368`, use `except Exception:  # pragma: no cover - best effort cleanup  # noqa: BLE001 approved [TS-3.1] [RUFF-SUP-281] exception` | `BLE001=1` | Docker removal failure must not replace either a completed outcome or an in-flight primary failure. | Removal fails once after success and once while container start raises; success and the exact primary exception both survive, and each path emits one fixed warning without exception info, identity, or secret text; exact residual inventory maps one BLE001 finding to the catch. | Docker SDK, transport, and daemon cleanup failures do not expose one stable exhaustive exception set. Narrowing risks replacing the primary result with an unlisted cleanup failure. Moving cleanup out of `finally` would weaken the lifetime guarantee. | Fresh clean Python refactor review and separate fresh exact suppression review: both `NET POSITIVE`. | owner approved 2026-08-07 |
| Manager interpreter-shutdown containment | MR-191; `weft/core/manager.py:6919`, use `except Exception:  # pragma: no cover - interpreter shutdown cleanup  # noqa: BLE001,S110 approved [TS-3.1] [RUFF-SUP-283] exception` | `BLE001=1, S110=1` | The registered atexit callback must keep unexpected `Exception`-derived queue, context, or backend cleanup failure out of Python's atexit exception-reporting path. This is last-chance cleanup, not a claim that normal atexit execution lacks usable globals or streams. | A direct callback test constructs only the manager state required by the method, makes `_unregister_manager` raise an exact `RuntimeError`, proves the callback returns, and captures the manager logger at DEBUG plus stdout and stderr to prove observer-free containment; exact residual inventory maps both diagnostics to the single catch. | Narrowing is not exhaustive across the cleanup stack and its backends. Adding logging creates another fallible teardown operation that would itself need containment and cannot recover the failed unregister. `contextlib.suppress(Exception)` would obscure the policy and would not clear BLE001. | First clean review `NET NEGATIVE` corrected the atexit rationale and DEBUG-capture gap; different fresh clean rework review `NET POSITIVE`. | owner approved 2026-08-07 |
| Manager normal atexit-hook removal | MR-195; `weft/core/manager.py:6938`, use `except Exception:  # pragma: no cover - best-effort normal cleanup  # noqa: BLE001 approved [TS-3.1] [RUFF-SUP-284] exception` | `BLE001=1` | Failure while removing the now-redundant interpreter-shutdown hook must not replace already completed manager resource cleanup. CPython's `atexit.unregister()` compares registered callbacks with equality, so an unrelated callback's arbitrary `__eq__` can raise any `Exception`. The callback reference is cleared before the attempt, so repeat cleanup does not retry. | A parameterized real-manager cleanup test injects failure at the `atexit.unregister` boundary instead of mutating process-global atexit state, invokes real Manager cleanup twice, proves exactly one unregister attempt, and asserts one fixed warning without exception info or exception text; exact residual inventory maps one BLE001 finding to the catch. | CPython defines no narrow failure family, and its documented equality comparison can invoke callback-owned code. Removing the catch would allow an unrelated registered callback's equality failure to replace successful manager cleanup. `contextlib.suppress(Exception)` would lose the warning and still require BLE001 disposition; extracting a helper would separate the operation from its failure policy. | Fresh clean Python exact suppression/locality review `NET POSITIVE`; concrete equality mechanism and boundary-injection proof wording corrected before approval. | owner approved 2026-08-07 |
| Harness failed-entry rollback | MR-109; `tests/helpers/weft_harness.py:127`, use `except Exception:  # noqa: BLE001 approved [TS-3.1] [RUFF-SUP-286] exception` | `BLE001=1` | When `__enter__` fails, the original `BaseException` remains primary while every ordinary rollback failure is recorded by bounded step name and later rollback phases still run. Rollback order is cwd, prepared root under patched backend env, environment, then tempdir. | One firing test injects context initialization failure plus prepared-root rollback failure; proves primary exception identity, exact bounded note, declared phase order, restored cwd/environment, tempdir removal, and closed state. A separate invariant forbids inherited or local `__del__`; construction-site audit finds only context managers or explicit teardown owners. | Sequential cleanup without containment can replace the primary failure and skip later phases. A narrow tuple is not exhaustive across backend plugin, filesystem, environment, and tempdir operations. One adjacent loop keeps phase order and failure policy visible; per-phase helpers would fragment the transaction. | Fresh clean Python transactional-lifecycle, refactor/locality, and exact suppression review `NET POSITIVE`. | owner approved 2026-08-07 |
| Harness live-queue cleanup containment | MR-111; `tests/helpers/weft_harness.py:1251`, use `except Exception:  # pragma: no cover - cleanup best effort  # noqa: BLE001 approved [TS-3.1] [RUFF-SUP-287] exception` | `BLE001=1` | A failure closing one live queue must not prevent attempts to close later queues bound to the harness database; otherwise one connection can keep teardown locked. Failure remains visible through a fixed warning without queue identity or backend exception data. | Two real Queue objects are supplied in a declared test sequence; the first close raises an exact `RuntimeError`, the second close still runs, and one fixed warning contains no exception info or secret text. Exact residual inventory maps one BLE001 to the catch. Production does not depend on `gc.get_objects()` order. | `Queue.close()` crosses arbitrary waiter and backend-connection close implementations with no stable exhaustive exception family. Removing containment can skip every later close. Moving the catch or logging into a helper would separate each queue from its local failure policy; `contextlib.suppress` would hide the warning and retain BLE001. | Fresh clean Python cleanup, refactor/locality, and exact suppression review `NET POSITIVE`. | owner approved 2026-08-07 |
| Microsandbox lifecycle callback containment | MR-058, MR-060; `extensions/weft_microsandbox/weft_microsandbox/plugin.py:271`, `:541`, use `except Exception:  # pragma: no cover - callback safety  # noqa: BLE001 approved [TS-3.1] [RUFF-SUP-288] exception` | `BLE001=2` | Caller-supplied runtime-handle and worker-start observers must not replace sandbox execution. | One real runner flow makes both lifecycle callbacks and both stream deliveries raise; the `ok` outcome, exact runtime-handle ID, and captured output survive. Exact ordered warnings contain no exception info, PID, handle, callback value, or secret text; residual inventory maps two BLE001 findings to the lifecycle catches. | Arbitrary callbacks have no exhaustive exception family. Removing containment lets observation replace execution. Extracting or merging the two sites would separate publication from its phase-specific warning. | Fresh clean Python runner-callback, refactor/locality, and exact suppression review `NET POSITIVE`; round-two evidence verification `PASS`. | owner approved 2026-08-07 |
| Microsandbox stream callback containment | MR-059; `extensions/weft_microsandbox/weft_microsandbox/plugin.py:529`, use `except Exception:  # pragma: no cover - callback safety  # noqa: BLE001 approved [TS-3.1] [RUFF-SUP-289] exception` | `BLE001=1` | Caller-supplied stdout and stderr observers must not replace sandbox execution or remove captured output from the result. | The same real runner flow reaches the shared catch once for each explicit stream name, preserves both outputs and `ok`, and emits exact bounded stdout/stderr warnings with no chunk or exception data; exact residual inventory maps one source finding to the shared catch. | Arbitrary callbacks have no exhaustive exception family. Removing containment replaces the result. Duplicating the helper per stream would add code without changing the one shared delivery contract. | Fresh clean Python runner-callback, refactor/locality, and exact suppression review `NET POSITIVE`. | owner approved 2026-08-07 |
| Microsandbox stop/remove cleanup containment | MR-052; `extensions/weft_microsandbox/weft_microsandbox/_runtime.py:450`, `:454`, use `except Exception:  # noqa: BLE001 approved [TS-3.1] [RUFF-SUP-290] exception` | `BLE001=2` | Stop and remove are independent best-effort phases. Failure in either SDK operation must not replace the primary run result; stop failure must not prevent removal. | A direct async cleanup test makes both operations raise, proves declared call order `stop`, then `remove`, and asserts distinct fixed warnings without sandbox name, exception info, or secret text; exact residual inventory maps two BLE001 findings to the catches. | Installed Microsandbox exposes `MicrosandboxError` for documented SDK failures, but this `finally` boundary also contains unexpected ordinary SDK/FFI or adapter failures so cleanup cannot replace the primary outcome. Removing the first catch skips removal; removing the second can replace the primary result. Merging phases or extracting a helper would obscure their independent guarantees. | Fresh clean Python cleanup/refactor/locality and exact suppression review `NET POSITIVE`; exception-family rationale corrected before approval. | owner approved 2026-08-07 |
| Microsandbox copy-back containment | MR-053; `extensions/weft_microsandbox/weft_microsandbox/_runtime.py:337`, use `except Exception:  # noqa: BLE001 approved [TS-3.1] [RUFF-SUP-291] exception` | `BLE001=1` | One failed requested output copy must not prevent attempts for later declared copy-back items. Failure remains visible without exposing host or guest paths. | A direct async test supplies two declared `FileCopyBack` items, makes the first raise, proves the second still runs in declaration order, and asserts one fixed warning without exception info, path data, or secret text; exact residual inventory maps one BLE001 to the catch. | Installed `MicrosandboxError` covers documented SDK failures, but filesystem/SDK/FFI/adapter code may raise other ordinary exceptions; copy-back is best effort after execution and must not replace the primary result or skip later items. Moving containment outside the loop would lose per-item continuation; a helper would separate each declaration from its failure policy. | Fresh clean Python post-execution transfer, refactor/locality, and exact suppression review `NET POSITIVE`. | owner approved 2026-08-07 |
| Microsandbox optional configuration metadata | MR-049; `extensions/weft_microsandbox/weft_microsandbox/_runtime.py:264`, use `except Exception:  # pragma: no cover - best-effort SDK metadata  # noqa: BLE001 approved [TS-3.1] [RUFF-SUP-292] exception` | `BLE001=1` | A callable SDK config accessor is optional enrichment. Its failure must not erase the refreshed sandbox state and timestamps. | A direct async description test makes the config accessor raise, preserves exact state/name/timestamps with config omitted, and asserts one fixed warning without sandbox ID, exception info, or secret text; exact residual inventory maps one BLE001 to the catch. | `MicrosandboxError` covers documented SDK failure, but the config member is dynamically shaped and SDK/FFI or version-compatibility code may raise other ordinary exceptions. Removing containment converts optional enrichment into total description failure. A helper would separate the optional field from its local fallback. | Fresh clean Python dynamic-metadata, refactor/locality, and exact suppression review `NET POSITIVE`; callable ownership wording corrected before approval. | owner approved 2026-08-07 |
| Microsandbox cancellation kill containment | MR-054; `extensions/weft_microsandbox/weft_microsandbox/_runtime.py:421`, use `except Exception:  # noqa: BLE001 approved [TS-3.1] [RUFF-SUP-294] exception` | `BLE001=1` | Once cancellation is requested, kill failure must not skip the subsequent execution-task drain/cancel attempt or replace the cancelled result. | A direct async test makes kill raise with no execution side effect, shortens the real `wait_for` timeout, proves the retained execution task finishes cancelled, returns the cancellation sentinel, and asserts one fixed warning without sandbox identity, exception info, or secret text; exact residual inventory maps one BLE001 to the catch. | `MicrosandboxError` covers documented SDK failure, but cancellation cleanup must also preserve the cancellation result across unexpected ordinary SDK/FFI failures. Narrowing could let adapter drift skip task cleanup. Removing containment skips the drain/cancel attempt; merging kill and wait would obscure their separate policies. `wait_for` is not claimed as a hard wall-clock bound because it waits for cancellation completion. | First fresh review `NET NEGATIVE` corrected the kill-side-effect proof and bounded-wait overclaim; different fresh clean rework review `NET POSITIVE`. | owner approved 2026-08-07 |
| Host multiprocessing-queue cleanup | MR-214; `weft/core/runners/host.py:945`, `:949`, use `except Exception:  # pragma: no cover - defensive cleanup  # noqa: BLE001 approved [TS-3.1] [RUFF-SUP-295] exception` | `BLE001=2` | Queue handle close and feeder-thread join are independent cleanup phases. Failure in either must not replace the primary runner outcome; close failure must not skip join. | A direct cleanup test makes both methods raise, proves declared call order `close`, then `join`, and asserts distinct fixed warnings without queue identity, exception info, or secret text; exact residual inventory maps two BLE001 findings to the catches. | Multiprocessing queue cleanup reaches finalizers, synchronization primitives, and feeder-thread joining without one stable exhaustive exception family. Removing the first catch skips join; removing the second can replace the primary outcome. Merging phases or extracting a helper would weaken the visible independent-phase policy. | Fresh clean Python multiprocessing/refactor/locality and exact suppression review `NET POSITIVE`; warning privacy is an explicit tradeoff, and test doubles are evidence tooling rather than a production justification. | owner approved 2026-08-07 |
| Host process-handle cleanup | MR-215; broad `RUFF-SUP-296` proposal rejected | `BLE001=0` | Releasing the multiprocessing process wrapper is secondary cleanup in failure and finally paths. Declared live-process rejection and platform-handle cleanup failure must not replace the primary runner outcome; unexpected defects must escape. | Parameterized direct cleanup tests make `close` raise `ValueError` and `OSError`, then assert one fixed warning without process identity, exception info, or secret text; a separate `RuntimeError` case proves unexpected defects escape; exact residual inventory is zero. | `BaseProcess.close` declares `ValueError` for a live process and delegates to platform handle/file-descriptor cleanup represented by `OSError`. Catching unexpected `RuntimeError` would hide a programming defect. Extracting one call would reduce locality without reuse. | First broad-suppression review `NET NEGATIVE`; different fresh clean narrowed-rework review `NET POSITIVE`. Windows handle behavior was source-inspected rather than executed locally, and uses the `OSError` family. | no owner approval needed; no directive |
| Host pre-escalation process join | MR-217; broad `RUFF-SUP-297` proposal rejected | `BLE001=0` | An OS-level wait failure after process-tree termination must not itself skip direct terminate/kill escalation when the subsequent liveness check succeeds. A second platform failure from that liveness check may still propagate. Unexpected ownership/state or programming defects must escape. | A direct `OSError` test proves the declared order from tree termination through join failure, successful liveness recheck, and direct termination, and asserts one fixed warning without process identity, exception info, or secret text. A separate `RuntimeError` case proves unexpected defects escape; exact residual inventory is zero. | Multiprocessing process waiting delegates to OS wait primitives represented by `OSError`. The helper has already proved the process is live and owned; `AssertionError`, `ValueError`, and unexpected runtime failures indicate invalid state rather than an ordinary wait failure. Broad containment would hide those defects. | Fresh clean Python multiprocessing/refactor/locality review `NET POSITIVE`; exact order is a safety contract, not incidental collection order; liveness qualification corrected before closure. | no owner approval needed; no directive |
| Host child agent-runtime session cleanup | MR-221; `weft/core/runners/host.py:358`, use `except Exception:  # pragma: no cover - defensive  # noqa: BLE001 approved [TS-3.1] [RUFF-SUP-298] exception` | `BLE001=1` | Runtime-owned session cleanup failure during child teardown must not skip request-queue and response-sender cleanup or replace the worker outcome. No contract depends on the relative order of the two IPC closes. | A direct worker-entry test makes runtime `close` raise, proves it occurs before both IPC endpoints are attempted exactly once without ordering those endpoint attempts, and asserts one fixed warning without task/runtime identity, exception info, or secret text; exact residual inventory maps one BLE001 finding to the catch. | Runtime sessions are supplied by configured runtime adapters and do not share one exhaustive close-exception family. Removing containment skips later IPC cleanup and can replace the child outcome. Moving containment around all cleanup phases would weaken independent continuation; extracting one call would reduce locality without reuse. | First fresh review `NET NEGATIVE` rejected an invented dependency on relative IPC-close order; different fresh clean rework review `NET POSITIVE`. | owner approved 2026-08-07 |
| Command-session cleanup callback | MR-245; `weft/core/tasks/sessions.py:188`, use `except Exception:  # pragma: no cover - defensive  # noqa: BLE001 approved [TS-3.1] [RUFF-SUP-299] exception` | `BLE001=1` | The explicit cleanup callback is a caller/adapter seam. Its failure must not make repeated session close fail, and the callback must not be retried after the session is marked closed. | A direct session test makes the callback raise, calls `close()` twice, proves one callback attempt, and asserts one fixed warning without callback/process identity, exception info, or secret text; exact residual inventory maps one BLE001 finding to the catch. | Arbitrary callbacks do not share an exhaustive exception family. Removing containment makes cleanup failure replace the owning shutdown path. Extracting the one callback invocation would reduce locality without reuse. No in-tree production caller currently supplies the callback; deleting the established extension-facing constructor seam requires a separate compatibility audit. | Fresh clean Python callback-boundary/refactor/locality and exact suppression review `NET POSITIVE`. | owner approved 2026-08-07 |
| Command-session monitor cleanup | MR-247; `weft/core/tasks/sessions.py:206`, `:210`, use `except Exception:  # pragma: no cover - process may have exited  # noqa: BLE001 approved [TS-3.1] [RUFF-SUP-300] exception` for metrics and `except Exception:  # pragma: no cover - defensive  # noqa: BLE001 approved [TS-3.1] [RUFF-SUP-300] exception` for stop | `BLE001=2` | Final metrics capture precedes monitor stop by declared cleanup policy. Failure in either adapter-owned phase must not prevent the next required action or ownership clearing. | A direct session test makes both methods raise, proves declared `last_metrics`, then `stop` order, proves monitor ownership clears and repeated stop is inert, and asserts distinct fixed warnings without monitor/process identity, exception info, or secret text; exact residual inventory maps two BLE001 findings to the catches. | Resource-monitor implementations are configured adapters and do not share one exhaustive exception family. Final metrics must precede `stop()` because an adapter may invalidate metric access while stopping. Removing the first catch skips stop; removing the second skips ownership clearing. Merging phases weakens independent continuation; extracting two calls reduces locality without reuse. | Fresh clean Python monitor-boundary/refactor/locality and exact suppression review `NET POSITIVE`. | owner approved 2026-08-07 |
| Agent startup late-response join | MR-238; broad `RUFF-SUP-301` proposal rejected | `BLE001=0` | OS wait failure while briefly joining a dead startup child must not itself skip draining one late response. Unexpected ownership/state or programming defects must escape. No correctness claim depends on relative join/drain order. | A direct `OSError` test independently proves one join attempt, one zero-timeout late drain, the retained drain result, and one fixed warning without process identity, exception info, or secret text. A separate `RuntimeError` case proves unexpected defects escape without treating the observation as successful; exact residual inventory is zero. | Multiprocessing join delegates to platform wait primitives represented by `OSError`. Assertions, state errors, and unexpected runtime failures indicate invalid ownership or lifecycle state. Broad containment would hide those defects; removing all containment would lose a potentially decisive late startup response after an ordinary OS wait failure. | First fresh review `NET NEGATIVE` rejected an unsupported relative-order claim; different fresh clean rework review `NET POSITIVE`. | no owner approval needed; no directive |
| Agent-session process-handle cleanup | MR-237; broad `RUFF-SUP-302` proposal rejected | `BLE001=0` | Supported process-wrapper close failures remain secondary to agent-session IPC cleanup; unexpected ownership/state or programming defects must escape. No correctness claim depends on relative order among the owned endpoint attempts. | Parameterized direct cleanup tests make process `close` raise `OSError` and `ValueError`, prove all four owned cleanup operations are attempted exactly once without ordering them, and assert one fixed warning without process identity, exception info, or secret text. A separate `RuntimeError` case proves unexpected defects escape; exact residual inventory is zero. | `BaseProcess.close` declares `ValueError` for a live process and delegates to platform handle/file-descriptor cleanup represented by `OSError`. Catching unexpected `RuntimeError` would hide a defect. Broad containment is unnecessary; removing supported containment would make routine handle cleanup failure replace the owning close path. | Fresh clean Python multiprocessing/cleanup/refactor/locality review `NET POSITIVE`. | no owner approval needed; no directive |
| Agent-session monitor cleanup | MR-242; `weft/core/tasks/sessions.py:783`, `:787`, use `except Exception:  # pragma: no cover - process may have exited  # noqa: BLE001 approved [TS-3.1] [RUFF-SUP-303] exception` for metrics and `except Exception:  # pragma: no cover - defensive  # noqa: BLE001 approved [TS-3.1] [RUFF-SUP-303] exception` for stop | `BLE001=2` | Final metrics capture precedes monitor stop by declared persistent-session cleanup policy. Failure in either adapter-owned phase must not prevent the next required action or ownership clearing. | A direct session test makes both methods raise, proves declared `last_metrics`, then `stop` order, proves monitor ownership clears and repeated stop is inert, and asserts distinct fixed warnings without monitor/process identity, exception info, or secret text; exact residual inventory maps two BLE001 findings to the catches. | Resource-monitor implementations are configured adapters and do not share one exhaustive exception family. Final metrics must precede `stop()` because an adapter may invalidate metric access while stopping. Removing the first catch skips stop; removing the second skips ownership clearing. | Fresh clean Python monitor-boundary/refactor/locality and exact suppression review `NET POSITIVE`. | owner approved 2026-08-07 |
| Agent-session optional metrics observation | MR-240; `weft/core/tasks/sessions.py:796`, use `except Exception:  # pragma: no cover - optional monitor observation  # noqa: BLE001 approved [TS-3.1] [RUFF-SUP-304] exception` | `BLE001=1` | A configured-monitor snapshot is optional observation. Failure returns the last cached metrics and retains monitor ownership so a later call may recover. | A direct test seeds cached metrics, makes the first adapter snapshot raise, proves the cached object and attached monitor survive, then returns fresh metrics on the second call; it asserts one fixed warning for that failed read without monitor/process identity, exception info, or secret text; exact residual inventory maps one BLE001 finding to the catch. | Configured resource-monitor adapters do not share one exhaustive snapshot-exception family. Removing containment turns optional observation into session failure; detaching or stopping the monitor would prevent transient recovery. Extracting the local fallback would reduce locality without reuse. The property emits one warning per failed read; it is consumed at result boundaries, not in the monitor polling loop. | Fresh clean Python monitor-observation/refactor/locality and exact suppression review `NET POSITIVE`; warning-frequency and catch-comment wording corrected before closure. | owner approved 2026-08-07 |
| Agent-session termination joins | MR-243; broad `RUFF-SUP-305` proposal rejected | `BLE001=0` | Each termination branch treats OS wait failure as an observation loss, not a reason to hide unexpected ownership/state defects. Deadline branches retain their bounded return policy; the ordinary alive branch continues direct escalation when liveness remains observable. | A four-case matrix crosses deadline presence with initial liveness, makes the first reached join raise `OSError`, independently proves expected join/kill/terminate/tree action counts, and asserts one branch-specific fixed warning without process identity, exception info, or secret text. A representative `RuntimeError` case proves unexpected defects propagate without warning; exact residual inventory is zero. | Multiprocessing joins delegate to platform wait primitives represented by `OSError`. Assertions, `ValueError`, and unexpected runtime failures indicate invalid ownership/lifecycle state and must escape. Four local catches keep each branch policy visible; a helper would need policy flags or callbacks and reduce comprehensibility. | First fresh review `NET NEGATIVE` requested a firing unexpected-defect proof; different fresh clean rework review `NET POSITIVE`. | no owner approval needed; no directive |
| Agent startup sealed-channel join | MR-244; broad `RUFF-SUP-306` proposal rejected | `BLE001=0` | OS wait failure after the startup channel seals must not itself suppress the final late-response probe. Unexpected ownership/state or programming defects must escape. No correctness claim depends on relative join/probe order. | A direct `OSError` test independently proves one join attempt, one `timeout=0.2` late probe, and acceptance of its ready response, with one fixed warning without process identity, exception info, or secret text. A representative `RuntimeError` case proves unexpected defects escape without treating the probe as successful; exact residual inventory is zero. | Multiprocessing join delegates to platform wait primitives represented by `OSError`. Assertions, `ValueError`, and unexpected runtime failures indicate invalid ownership/lifecycle state. Broad containment would hide those defects; removing all containment would lose a potentially decisive final response after an ordinary OS wait failure. | Fresh clean Python multiprocessing/startup/refactor/locality review `NET POSITIVE`. | no owner approval needed; no directive |
| Agent-session stop-request publication | MR-239; `weft/core/tasks/sessions.py:814`, use `except Exception:  # pragma: no cover - queue cleanup boundary  # noqa: BLE001 approved [TS-3.1] [RUFF-SUP-307] exception` | `BLE001=1` | Stop-request publication is an adapter/IPC boundary. Its arbitrary failure must not skip process termination or final monitor and IPC cleanup. The stop payload is constructed outside the catch, and termination is guaranteed by an inner `finally`. | A direct queue-failure close test proves one publication attempt, skipped join, termination and both final cleanup phases, idempotent repeated close, and one fixed warning without process/queue identity, exception info, or secret text. Separate join tests prove `OSError` containment and that an unexpected `RuntimeError` still triggers termination and both final cleanup phases before propagating when those later phases succeed; exact residual inventory maps one BLE001 finding to queue `put`. | Multiprocessing queue implementations and adapter doubles do not expose one exhaustive publication-exception family. Keeping payload construction and liveness outside the catch prevents programming and lifecycle defects from being hidden. Splitting publication from join makes each exception policy local; nested `finally` blocks express termination ownership before resource release. If termination or final cleanup itself raises, existing Python exception priority applies and retains the join defect as context; this slice adds no broad containment or exception bookkeeping. | First fresh review `NET NEGATIVE` found a live-child leak on unexpected join failure; different fresh clean rework review `NET POSITIVE`. | owner approved 2026-08-07 |

## 19. Execution Evidence

Promotion state: atomic activation committed as
`bb6e47769a3f6f3c8f279931c11f364a7e67a7f8`. The normative testing spec records
behavior and suppression invariants, not execution SHAs.

| Slice | Baseline | Command/proof | Result | Reviewer | Disposition |
|---|---|---|---|---|---|
| Task 2 initial probe, invalidated | `d0254ce45a757a7351fa0e7ca76f389790a1800b`; Ruff 0.16.1 | Initial Section 6 `--isolated` normal/raw probe | Reported 674 normal/848 raw, but discarded `isort.known-first-party` and created 75 false `I001` findings. The 453-code set comparison remained valid. | Parent fresh-eyes implementation check | Superseded immediately by the corrected probe below; no source cleanup began from these counts. |
| Task 2 corrected baseline freeze | Same baseline and Ruff | Temporary real `pyproject.toml` with only `select` -> `extend-select`; normal/raw Ruff plus `--show-settings` exact-set comparison | Normal: 599 diagnostics, 146 files, 38 codes, 228 offered fixes. Raw: 773 diagnostics, 156 files, 41 codes. Effective: 453; current: 171; added: 282; removed: 0; exact set difference from SimpleBroker: 0/0. | Parent integration; manual-risk grouping pending | Authoritative activation baseline reproduced on 2026-08-06. |
| Task 2 manual-risk ledger plus Task 4/6 suppression candidates | `d0254ce45a757a7351fa0e7ca76f389790a1800b`; corrected baseline above | AST attribution matching `bin/ruff_suppression_index.py`, direct source inspection, and closest-test mapping for every named finding; includes requested `PLR0402` facade and `SIM905` sites plus the twelve FLY002, six SIM102, and seven RUF022 candidates | 415 required findings plus 11 requested additions accounted for across 290 owner/category rows. The initial generated pass flagged 9 blocked groups; direct follow-up established their current invariants and proof paths. Later cleanup appended MR-269..MR-290 after the FLY002 split, SIM102 disposition pass, and RUF022 semantic-index review. Candidate and pending rows remain edit-gated by their named compatibility, locality, suppression-review, and owner-approval requirements. | Clean Task 2 risk-ledger analysis plus independent Task 4/6 disposition reviews | Cardinalities are machine-checkable in Section 16; no hard-blocked row remains. |
| Task 3 policy RED | Current `lint.select`; uncommitted policy-test slice | `pytest -n 0 tests/specs/test_ruff_policy.py -q`; temporary candidate-policy Ruff on the changed test file | Exactly two intended RED failures and 39 passes: structural `extend-select` requirement and missing real `BLE001` sentinel. Candidate-policy Ruff clean. | Clean independent Python/pytest reviewer | PASS after adding passing controls before every policy, inventory, extensionless, per-file, and CI mutation. |
| Task 4 `FURB136` batch | One expression in `weft/shell/__init__.py::prepare_command` | Focused shell tests; candidate-policy rule check; suppression check; clean Python-expert locality review | 5 passed, 1 Windows skip; lint and suppression clean; algebraically equivalent for all list lengths. | Fresh clean Python expert | `NET POSITIVE`; accepted without rework. |
| Task 4 `FURB167` batch | `bin/coalesce-check` regex flag alias | Complete checker output comparison; candidate-policy rule check; suppression check; clean Python-expert locality review | Output identical; `re.MULTILINE` is the same flag as `re.M` and states anchor behavior explicitly. | Fresh clean Python expert | `NET POSITIVE`; accepted without rework. |
| Task 4 `PIE790` CLI owner | Docstring-bodied `weft.cli.app::main` | Full `tests/cli/test_cli.py`; candidate-policy rule check; suppression check; clean Python-expert locality review | 11 passed; signature, docstring, bytecode behavior, and Typer callback semantics preserved. | Fresh clean Python expert | `NET POSITIVE`; accepted without rework. |
| Task 4 `PIE790` monitor owner | Docstring-bodied `weft.core.resource_monitor::ResourceMonitor` | Focused resource/runner tests; candidate-policy rule check; focused mypy; suppression check; clean Python-expert locality review | 11 passed; distinct class identity, inheritance, loader behavior, and public export preserved. | Fresh clean Python expert | `NET POSITIVE`; accepted without rework. |
| Task 4 `PIE807` harness owner | Sixteen empty zero-argument test factories in `tests/test_harness_registration.py` | Full owning test file; candidate-policy rule check; suppression/diff checks; clean Python-expert locality review | 29 passed; every call remains zero-argument and returns a fresh empty container; no identity/name/signature dependency. | Fresh clean Python expert | `NET POSITIVE`; accepted without rework. |
| Task 4 `PIE807` manager owner | One empty zero-argument manager-record factory in the leadership-drain test | Exact owning test; candidate-policy rule check; suppression check; clean Python-expert locality review | Named test passed; production calls the patched attribute with no arguments and receives a fresh dictionary. | Fresh clean Python expert | `NET POSITIVE`; accepted without rework. |
| Task 4 `RUF100` pure removals | 33 stale directives without explanatory prose | Full 453-rule candidate inventory; focused owning tests; current Ruff; suppression and diff checks | Candidate `RUF100` fell from 38 to 0; approved pointer count remains 155; no [TS-3.1] pointer removed. | Fresh clean Python expert | `NET POSITIVE`; accepted. |
| Task 4 `RUF100` prose sites | Five stale directives whose rationale required separate disposition | 168 focused tests; current Ruff; suppression/diff checks; two clean Python-expert reviews | Attempt 1 `NET NEGATIVE`: one rationale was nonlocal and obsolete D401 history interrupted imports. Rework localized the SLF001 comment, removed obsolete history, and retained four useful comments. | Two different fresh clean Python experts | Attempt 2 `NET POSITIVE`; RW-RUF100-001 accepted; owner approved closeout 2026-08-07. |
| Task 4 `RUF022` leaf inventories | Sixteen short/leaf `__all__` lists | 172 focused/architecture tests; current Ruff; suppression/diff checks; candidate inventory | Exact unique membership and identity preserved; candidate `RUF022` fell from 25 to the nine locality-sensitive grouped inventories. | Fresh clean Python expert | `NET POSITIVE`; accepted. |
| Task 4 test-only `PLR0402` | Two `weft.runtime_liveness` test imports | 67 focused and architecture tests; current Ruff; suppression/diff checks; module-identity inspection | Exact `sys.modules` identity and monkeypatch visibility preserved; production import graph unchanged; candidate `PLR0402` fell 9→7. | Fresh clean Python expert | `NET POSITIVE`; accepted. |
| Task 5 `PYI063` | MR-248..MR-258, all Pydantic `model_post_init` overrides | RED runtime-signature test; 64 TaskSpec tests; focused mypy; targeted/current Ruff; suppression/diff checks | All eleven overrides now match Pydantic's `(self, context, /)` runtime contract; positional invocation and super calls preserved. | Fresh clean Python expert | `NET POSITIVE`; accepted; owner approved closeout 2026-08-07. |
| Task 5 `RUF009` | MR-099, fake runner-plugin capabilities default | Full owning test file; targeted/current Ruff; suppression/diff checks | 5 passed; distinct but equal frozen capability values replace incidental shared identity. | Fresh clean Python expert | `NET POSITIVE`; accepted; owner approved closeout 2026-08-07. |
| Task 5 `PYI025` | MR-159, reducer abstract-set annotation | 24 reducer tests; focused mypy; targeted/current Ruff; suppression/diff checks | `AbstractSet` names the existing abstract set-like contract; resolved annotation and runtime behavior are unchanged. | Fresh clean Python expert | `NET POSITIVE`; accepted; owner approved closeout 2026-08-07. |
| Task 5 `PYI034` | MR-110, `WeftTestHarness.__enter__` | 29 harness tests; full configured mypy; direct helper mypy with only unrelated assignment error disabled; targeted/current Ruff; suppression/diff checks | `Self` matches the existing return-self behavior and preserves subclass typing. | Fresh clean Python expert | `NET POSITIVE`; accepted; owner approved closeout 2026-08-07. |
| Task 5 `PYI064` | MR-149, fifteen literal constants | 81 constants tests; full configured mypy; direct `assert_type` probes for all direct/alias forms; targeted/current Ruff; suppression/diff checks | Bare `Final` retains every exact inferred literal type, including aliased `DEFAULT_STATUS`, while removing redundant duplicated annotations. | Fresh clean Python expert | `NET POSITIVE`; accepted. |
| Task 5 `RUF012` Microsandbox controls | MR-040, `ControlRuntime` call-history lists | RED instance-isolation test; full owning test file; targeted/current Ruff; suppression/diff checks | 3 passed; stop/kill history is now owned by each injected fake runtime and cannot leak across instances/tests. | Fresh clean Python expert | `NET POSITIVE`; accepted. |
| Task 5 `RUF012` Microsandbox filesystem fake | MR-042, local `Fs` call-record lists | RED instance-isolation assertions; full owning test file; targeted/current Ruff; suppression/diff checks | 9 passed; mkdir/copy records are owned by each fake filesystem while the single sandbox path remains unchanged. | Fresh clean Python expert | `NET POSITIVE`; accepted. |
| Task 5 `RUF012` queue-monitor context | MR-078, first local `_FakeContext.config` | Exact owner test; current/targeted Ruff; suppression/diff checks; two clean reviews | Attempt 1 `ClassVar` was `NET NEGATIVE` because sharing was incidental. Rework initializes per-instance config to match `WeftContext`. | Two different fresh clean Python experts | Attempt 2 `NET POSITIVE`; RW-RUF012-002 accepted. |
| Task 5 `RUF012` queue-close context | MR-077, second local `_FakeContext.config` | RED instance-isolation assertion; full owning test file; current/targeted Ruff; suppression/diff checks | 19 passed; config now matches real per-context ownership while queue and generator-close behavior remain unchanged. | Fresh clean Python expert | `NET POSITIVE`; accepted. |
| Task 5 `RUF012` manager terminal-queue registry | MR-089, first local `FakeTerminalQueue.instances` | Exact owner test; current/targeted Ruff; suppression/diff checks | Shared class registry intentionally captures both proof-read and write queue instances within one test-local class. | Fresh clean Python expert | `NET POSITIVE`; `ClassVar` accepted. |
| Task 5 `RUF012` manager terminal-proof registry | MR-090, second local `FakeTerminalQueue.instances` | Exact owner test; current/targeted Ruff; suppression/diff checks | Shared class registry intentionally captures aggregate queue construction history and is recreated per test invocation. | Fresh clean Python expert | `NET POSITIVE`; `ClassVar` accepted. |
| Task 5 `RUF012` realtime materialization context | MR-098, first shared-ops `_FakeContext.config` | RED instance-isolation assertion; exact owner test; current/targeted Ruff; suppression/diff checks | Context config now matches real per-instance ownership while terminal-materialization event behavior is unchanged. | Fresh clean Python expert | `NET POSITIVE`; accepted. |
| Task 5 `RUF012` realtime snapshot context | MR-097, second shared-ops `_FakeContext.config` | RED instance-isolation assertion; full owning test file; current/targeted Ruff; suppression/diff checks | 10 passed; context config now matches real per-instance ownership while snapshot-derived terminal event order remains unchanged. | Fresh clean Python expert | `NET POSITIVE`; accepted. |
| Task 5 `RUF012` Docker ID-fallback container | MR-011, first local `FakeContainer.attrs` | Exact owner test; current/targeted Ruff; suppression/diff checks | Mutable Docker-model attributes are now instance-owned; fixed immutable container ID and fallback lookup behavior remain unchanged. | Fresh clean Python expert | `NET POSITIVE`; accepted. |
| Task 5 `RUF012` Docker list-fallback container | MR-012, second local `FakeContainer.attrs` | Exact owner test; current/targeted Ruff; suppression/diff checks | Mutable Docker-model attributes are now instance-owned; fixed immutable ID/name and list fallback behavior remain unchanged. | Fresh clean Python expert | `NET POSITIVE`; accepted. |
| Task 5 `RUF012` Docker startup-wait container | MR-010, mutable `FakeContainer.attrs` | RED instance-isolation assertion; exact owner test; current/targeted Ruff; suppression/diff checks | Exercised container alone transitions `created`→`running`; the extra isolation probe does not consume transition state or affect callback timing. | Fresh clean Python expert | `NET POSITIVE`; accepted. |
| Task 5 `RUF012` Docker stuck-container timeout | MR-013, mutable `FakeContainer.attrs` | RED instance-isolation assertion; exact owner test; current/targeted Ruff; suppression/diff checks | Proven instance exercises the unchanged reload and fake-clock timeout path; container state no longer leaks across instances. | Fresh clean Python expert | `NET POSITIVE`; accepted. |
| Task 5 `RUF012` Docker startup-failure cleanup | MR-009, final mutable `FakeContainer.attrs` | RED instance-isolation assertion; full owning test file; current/targeted Ruff; suppression/diff checks | 26 passed; runner and cleanup still use the proven instance, with process kill/wait and container removal behavior unchanged. | Fresh clean Python expert | `NET POSITIVE`; accepted; repository candidate `RUF012=0`. |
| Task 6 `RUF019` Django TaskSpec name override | `integrations/weft_django/weft_django/client.py::_apply_taskspec_payload_overrides` | Full Django integration test file; targeted/current Ruff; suppression/diff checks | 30 passed; the truthy override value is fetched once, bound at the check, and used immediately. Missing and falsey names retain the existing behavior. | Fresh clean Python expert | `NET POSITIVE`; accepted. |
| Task 6 `RUF034` resource-monitor initialization | MR-210, `PsutilResourceMonitor.__init__` | Both resource-monitor test modules; targeted/current Ruff; suppression/diff checks | 7 passed; `_process` is unconditionally unset at construction, while availability validation and process creation remain delayed in `start_monitoring`. | Fresh clean Python expert | `NET POSITIVE`; accepted. |
| Task 6 `RUF046` redundant integer casts | Consumer and interactive CPU metrics; relative-duration fallback | Four owning test modules; focused mypy; targeted/current Ruff; suppression/diff checks | `round(value)` without `ndigits` already returns `int`; values, rounding behavior, exceptional inputs, and downstream types remain unchanged at all three sites. | Fresh clean Python expert | `NET POSITIVE`; accepted. |
| Task 6 `RET501`/`PLR1711` terminal `None` returns | Seven protocol, callback, and test-double methods | Four owning test files; focused production mypy; targeted/current Ruff; suppression/diff checks | 136 passed; falling off each `-> None` or callback body returns the same singleton after the same observable work. Empty no-op methods retain their required body statement. | Fresh clean Python expert | `NET POSITIVE`; accepted. |
| Task 6 `SIM103` PG launcher predicate | `tests/conftest.py::_pg_cli_requires_active_env` | Direct five-case command matrix; targeted/current Ruff; pytest collection; suppression/diff checks | The empty guard and one-time string conversion remain; membership names exactly the prior `manager`/`run` command set beside its rationale. | Fresh clean Python expert | `NET POSITIVE`; accepted. |
| Task 6 `SIM103` manager liveness projection | `Manager._manager_record_is_live` | Six live/stale/unknown manager tests; targeted/current Ruff; suppression/diff checks | The tri-state helper is called once; only `"live"` maps to `True`, while `"stale"` and `"unknown"` remain false. | Fresh clean Python expert | `NET POSITIVE`; accepted. |
| Task 6 `SIM103` manager supersession expiry | `Manager._self_superseded_manager_record_visible` | Full 202-test manager suite; focused mypy; targeted/current Ruff; two clean reviews | Attempt 1 was `NET NEGATIVE` because one boolean conflated unchecked timestamps with checked-fresh entries. Rework states the non-integer default-visible policy as a guard and keeps integer expiry in the final return. | Two different fresh clean Python experts | Attempt 2 `NET POSITIVE`; RW-SIM103-003 accepted. |
| Task 6 `SIM103` stalled-control actionability | `Manager._manager_control_pending_is_actionable` | Exact stuck-control retry test; full 202-test manager suite; focused mypy; targeted/current Ruff | The missing-timestamp default remains explicit; a valid timestamp is actionable exactly when its retry window is inactive, with one helper call. | Fresh clean Python expert | `NET POSITIVE`; accepted; manager candidate `SIM103=0`. |
| Task 6 `SIM103` consumer start-token predicate | `Consumer._is_start_token` | New 13-case truth table; reserved-queue consequence plus signal-deferral/interactive tests; focused mypy; targeted/current Ruff | Earlier guards remain explicit; the final direct predicate accepts only a one-key mapping whose `close` value is the boolean singleton `True`, with short-circuit safety for non-mappings. | Fresh clean Python expert | `NET POSITIVE`; accepted; repository candidate `SIM103=0`. |
| Task 6 `FURB188` prefix/suffix removal | Four release, TID, and stream-normalization sites | Remote/TID/chunk-boundary characterization; 104 owner tests; focused mypy; targeted/current Ruff | Guarded literal slices became one-shot `removeprefix`/`removesuffix` calls. No-match, repeated delimiter, and split CRLF output remain exact. | Fresh clean Python expert | `NET POSITIVE`; accepted; repository candidate `FURB188=0`. |
| Task 6 `PLC0208` TaskSpec log-field iteration | `test_to_log_dict_contains_expected_fields` | Exact owning test; targeted/current Ruff; suppression/diff checks | A tuple preserves the five-field inventory and makes assertion order deterministic; duplicate semantics are immaterial for the short literal. | Fresh clean Python expert | `NET POSITIVE`; accepted; repository candidate `PLC0208=0`. |
| Task 6 `SIM115` process-target file lifetime | MR-127, workload-length temporary descriptors | Two fd/resource-limit tests; direct target probe; current/targeted Ruff on candidate; clean locality/resource review | ExitStack attempt preserved normal descriptor lifetime but changed close order/error containment and nested unrelated work. The explicit lifecycle remains live; exact one-site `RUFF-SUP-308` is activated. | Fresh clean Python expert | Attempt 1 `NET NEGATIVE`; candidate preserved; exact suppression owner approved and activated 2026-08-07. |
| Task 6 `SIM118` declared-option mapping iteration | `parse_declared_option_args::option_to_name` | Dedicated run-input/parameterization and related CLI/TaskSpec tests; focused mypy; targeted/current Ruff | Direct `Mapping` iteration is canonical key traversal. The derived dict is lookup-only, normalized-name collisions are rejected upstream, and missing diagnostics are sorted. Default insertion order is separate pre-existing behavior and is unchanged. | Fresh clean Python expert | `NET POSITIVE`; accepted; repository candidate `SIM118=0`. |
| Task 6 `PIE810` combined prefix checks | DOM-15 fences, GitHub remotes, and kubepods cgroup segments | DOM-15 self-test; 54 release/container tests; focused mypy; targeted/current Ruff | Each tuple contains only strings and disjoint prefixes. Dynamic kubepods f-strings are side-effect-free plain-string expressions; evaluation order and exceptions remain immaterial. | Fresh clean Python expert | `NET POSITIVE`; accepted; repository candidate `PIE810=0`. |
| Task 6 `SIM114` long-session endpoint samples | `test_cli_long_session_produces_identical_transcript_across_backends` | Full-range/edge equivalence probe; slow-test collection; targeted/current Ruff | Membership selects the first and last rounds without changing loop order or extension order; the collapsed one-round edge still extends once. | Fresh clean Python expert | `NET POSITIVE`; accepted. |
| Task 6 `SIM114` manager autostart due condition | `Manager._managed_service_convergence_active_reasons` | New no-clock short-circuit test; existing stale-scan test; focused mypy; targeted/current Ruff | The unscanned path remains immediately due without reading the clock; nonzero scans read it once and retain the inclusive elapsed-time boundary. | Fresh clean Python expert | `NET POSITIVE`; accepted. |
| Task 6 `SIM114` watcher inactive condition | `MultiQueueWatcher._process_queue_message` | New stopped/no-probe firing test; full watcher test file; focused mypy; targeted/current Ruff | A stop set by the handler still short-circuits the queue probe; non-stopped paths probe once, and both conditions add the same queue name to the inactive set. | Fresh clean Python expert | `NET POSITIVE`; accepted. |
| Task 6 `SIM114` TaskSpec start timestamp | `TaskSpec.set_status` | Full state-transition and TaskSpec test files; focused mypy; targeted/current Ruff | `spawning` and `running` retain one shared initialize-if-absent rule; other states avoid the field/clock path, and the disjoint terminal `elif` remains unchanged. | Fresh clean Python expert | `NET POSITIVE`; accepted; repository candidate `SIM114=0`. |
| Task 6 `PLW1510` explicit subprocess status policy | Ten git/CLI/Docker/callable subprocess calls | Coalesce checker; four owner test files; focused production/tool mypy; targeted/current Ruff | Each call intentionally consumes or forwards nonzero status, so explicit `check=False` preserves return-code, timeout, and startup-error behavior. `make_callable` retains its domain-level post-construction check. | Fresh clean Python expert | `NET POSITIVE`; accepted; repository candidate `PLW1510=0`. |
| Task 6 `RUF059` intentional discard bindings | Nineteen diagnostics across CLI, manager, monitor, observability, and result owners | 70 scoped tests; focused production mypy; targeted/current Ruff; suppression/diff checks | Underscore prefixes mark only intentionally discarded tuple slots; right-hand sides, tuple shapes, used bindings, and control flow remain unchanged. The result path retains the exact timestamp and discards only the peek payload. | Fresh clean Python expert | `NET POSITIVE`; accepted; repository candidate `RUF059=0`. |
| Task 6 `FLY002` prune summary output | `weft.cli.app::prune`, plain `--family all` output | New firing output-label test; focused mypy; targeted/current Ruff; diff check | One f-string states the fixed four-string layout directly. The typed command results are strings, the three separators and no-trailing-newline behavior remain exact, and each label stays beside its value. | Fresh clean Python expert | `NET POSITIVE`; accepted; production candidate `FLY002=0`. |
| Task 6 `ISC004` collection-element grouping | Ten tool, test, CLI, command, manager, and pruning strings | 35 focused owner tests; focused production mypy; targeted/current Ruff; suppression/diff checks | Parentheses make each multiline implicit concatenation one explicit collection element. Before/after ASTs, string values, interpolation, arity, argument positions, and evaluation are unchanged. | Fresh clean Python expert | `NET POSITIVE`; accepted; repository candidate `ISC004=0`. |
| Task 6 `FLY002` short literal fixtures | Eleven short embedded config/Python files across six test modules | 25 focused owner tests; current Ruff; exact residual candidate count; diff check | Static literals preserve every byte, including blank lines, indentation, and final newlines. Two-line fixtures stay compact; four-line fixtures retain one adjacent literal per generated-code line. | Fresh clean Python expert | `NET POSITIVE`; accepted; repository candidate `FLY002=12`, all long-program sites. |
| Task 6 `FLY002` long-program suppression proposal | MR-269..MR-280; twelve exact embedded-source joins | 24 owner tests including slow long-session integration; exact residual inventory and cardinality checks; three directive-review passes | Line-list joins preserve visually parseable 7-to-18-line programs and explicit newline policy. Alternatives obscure source or introduce normalization semantics. Two negative directive reviews exposed only bookkeeping defects, all corrected before final review. | Three different fresh clean Python experts | Final exact review `NET POSITIVE`; `RUFF-SUP-239` proposal ready, owner approved 2026-08-07; directives activated. |
| Task 6 `SIM102` DOM-15 negative-trigger predicate | `bin/check-dom15-fixtures::check`; retired `RUFF-SUP-223` | Checker self-test; 41 suppression-policy tests; targeted/current Ruff; suppression/diff checks; two clean reviews | The class, non-escalation, and missing-marker clauses jointly authorize one diagnostic. Short-circuit and exception order remain exact. Removing redundant nesting genuinely lowers complexity 11→10, so the source directive, normative row, generated index, expected group set, and raw inventory move together. | Two different fresh clean Python experts | Final full-slice review `NET POSITIVE`; accepted; tool candidate `SIM102=0`, C901 registry 117 groups/146 directives. |
| Task 6 `SIM102` macOS environment passthrough validation | `MacOSSandboxRunnerPlugin.validate_taskspec`; retired `RUFF-SUP-207` | 7 extension owner tests; 41 suppression-policy tests; focused mypy; targeted/current/raw Ruff; suppression/diff checks | The optional-value guard and invalid-list predicate now authorize one unchanged error. `None`, non-list short-circuiting, item order, error text, and preflight precedence remain exact. Complexity genuinely falls 11→10, so the complete registry state moves atomically. | Fresh clean Python expert | `NET POSITIVE`; accepted; extension candidate `SIM102=0`, C901 registry 116 groups/145 directives. |
| Task 6 `SIM102` result polling readiness | `tests/cli/test_cli_result_all.py::_wait_for_result_all_output` | Full 6-test owner file; targeted/current Ruff; diff check | Return-code success, required-content presence, and excluded-content absence jointly define one retry readiness predicate. Left-to-right short-circuiting, failure capture, sleep, and retry remain exact. | Fresh clean Python expert | `NET POSITIVE`; accepted; owner candidate `SIM102=0`. |
| Task 6 `SIM102` task-stop fallback eligibility | `weft.commands.tasks::stop_tasks` | Five stop-task owner tests; focused mypy; targeted/current Ruff; suppression/diff checks | Fallback runs only when convergence requests escalation/unknown reporting and the runner has not already handled the stop. Membership remains first, the flag is initialized per iteration, and no intermediate work is hidden. | Fresh clean Python expert | `NET POSITIVE`; accepted; owner candidate `SIM102=0`. |
| Task 6 `SIM102` inline function-target validation | `weft.commands.run::_execute_inline` | New two-case pre-context firing test plus valid inline path; focused mypy; targeted/current/raw Ruff; suppression/diff checks | Function-lane selection, missing-target, and separator checks retain exact left-to-right short-circuit order. Command targets skip the validation and invalid function targets still fail before context construction. | Fresh clean Python expert | `NET POSITIVE`; accepted; owner candidate `SIM102=0`; live `RUFF-SUP-113` retained at raw C901 13. |
| Task 6 `SIM102` TaskSpec resource limits | `TaskSpec.check_limits_exceeded`, four resource predicates | New four-case firing test; focused mypy; targeted/current Ruff; suppression/diff checks | Each configured-limit guard, observed-value guard, and comparison retain exact left-to-right evaluation. Resource precedence, field access, messages, zero/`None` fallthrough, and return behavior remain unchanged. | Fresh clean Python expert | `NET POSITIVE`; accepted; TaskSpec candidate `SIM102=2`. |
| Task 6 `SIM102` TaskSpec memory default | `apply_taskspec_defaults` limits mapping | Three creation-default tests; focused mypy; targeted/current Ruff; suppression/diff checks | Mapping validation, one `memory_mb` lookup, and assignment retain exact order and mutation target, including dict subclasses. The two inseparable preconditions now sit beside the assignment they authorize. | Fresh clean Python expert | `NET POSITIVE`; accepted; TaskSpec candidate `SIM102=1`. |
| Task 6 `SIM102` TaskSpec timestamp ordering | `StateSection.validate_state_consistency` | New ordering-error firing test plus full state-transition class; focused mypy; targeted/current/raw Ruff; suppression/diff checks | Presence and ordering retain exact short-circuit, truthiness, and exception order before state-specific checks. Pre-existing semantics remain explicit: timestamp `0` is treated as absent and equal timestamps are accepted despite the error wording. | Fresh clean Python expert | `NET POSITIVE`; accepted; TaskSpec candidate `SIM102=0`; live `RUFF-SUP-053` retained at raw C901 15. |
| Task 6 `SIM102` final stdout transform | `Consumer._emit_live_stream_chunk` | Two live stream-output tests; focused mypy; targeted/current Ruff; suppression/diff checks | Only final stdout chunks receive `result_transform="strip"`; stderr and non-final chunks remain unchanged. Stream comparison still short-circuits the finality predicate. | Fresh clean Python expert | `NET POSITIVE`; accepted; consumer candidate `SIM102=0`. |
| Task 6 `SIM102` stopped-manager idempotence | `manager_runtime._stop_manager` | Exact no-op owner test; focused mypy; targeted/current/raw Ruff; suppression/diff checks | Record type, stopped lifecycle state, and stale handle jointly authorize one early success. Non-dicts never receive `.get`; non-stopped records never invoke handle helpers; exception timing remains exact. | Fresh clean Python expert | `NET POSITIVE`; accepted; owner candidate `SIM102=4`; live C901 suppression retained at raw 18. |
| Task 6 `SIM102` host producer-exit observation | `HostRunner._run_one_shot_terminal_handoff` | 31 one-shot handoff tests; focused mypy; targeted/current/raw Ruff; suppression/diff checks | The reducer edge must remain eligible and the process must be dead before join, exit-code observation, state mutation, and event creation. Short-circuiting, side effects, exception boundaries, and loop timing remain exact. | Fresh clean Python expert | `NET POSITIVE`; accepted; host candidate `SIM102=0`; live C901 suppression retained at raw 39. |
| Task 6 `SIM102` agent-session producer-exit observation | `AgentSession.execute` | Ten paired/persistent handoff tests; focused mypy; targeted/current/raw Ruff; suppression/diff checks | The reducer edge remains eligible and the session is dead before process join, exit-code observation, state mutation, and event creation. Observation eligibility stays in the guard; cleanup remains in the body. | Fresh clean Python expert | `NET POSITIVE`; accepted; sessions candidate `SIM102=0`; live C901 suppression retained at raw 32. |
| Task 6 `SIM102` managed-service candidate PID | `Manager._service_candidate_pid` | New four-case preference/fallback test; focused mypy; targeted/current Ruff; suppression/diff checks | Positive integer child PIDs win; booleans and non-positive values fall through to the unchanged TaskSpec PID lookup. Type, boolean, and comparison checks retain exact order. | Fresh clean Python expert | `NET POSITIVE`; accepted; manager candidate `SIM102=3`. |
| Task 6 `SIM102` managed-service duplicate-scan settlement | `Manager._reconcile_managed_services` | New two-service firing test plus tracked-candidate reuse test; focused mypy; targeted/current Ruff; suppression/diff checks | A pending marker clears only when the key needed evidence and its tracked candidate exists in the live state. Attribute access, short-circuiting, and set-discard timing remain exact. | Fresh clean Python expert | `NET POSITIVE`; accepted; manager candidate `SIM102=2`. |
| Task 6 `SIM102` autostart restart budget | `Manager._autostart_ensure_obligation_pending` | Restart-budget and stale-liveness owner tests; focused mypy; targeted/current Ruff; suppression/diff checks | Integer, non-boolean, and below-budget checks retain exact order after unchanged state/launched/unlimited early returns. Missing, boolean, exhausted, and pre-existing negative-count behavior remain unchanged. | Fresh clean Python expert | `NET POSITIVE`; accepted; manager candidate `SIM102=1`. |
| Task 6 manager convergence reason locality | `Manager._managed_service_convergence_active_reasons`, unsettled service reasons | Stable reason-order test plus new active-TID/spawn-pending characterization; focused mypy; current Ruff; diff check | A local `service_unsettled` fact now feeds independent ordered reason checks. The pre-existing `missing_active_tid` reason can still appear with a non-null active TID when spawn is pending; its name remains semantic debt and is not concealed. | Fresh clean Python expert | `NET POSITIVE`; accepted as an independently useful clarity change; not the remaining SIM102 diagnostic. |
| Task 6 `SIM102` autostart scan due predicate | `Manager._managed_service_convergence_active_reasons`; retired one `RUFF-SUP-012` directive | Stale-due, unscanned/no-clock, and disabled/no-clock tests; 41 suppression-policy tests; focused mypy; targeted/current/C901/RUF100 Ruff | Enabled/configured state and due timing now form one short-circuit predicate. Disabled and never-scanned paths still avoid the clock. Combined with the accepted reason-locality cleanup, method complexity is within threshold; the shared group retains its three other owners. | Fresh clean Python expert | `NET POSITIVE`; accepted; manager candidate `SIM102=0`, C901 registry 116 groups/144 directives. |
| Task 6 `SIM102` client-to-core boundary predicate | `tests.architecture.test_import_boundaries::test_internal_import_boundaries` | Exact architecture boundary test; targeted/current Ruff; diff check | Source and target membership now state the forbidden `weft.client -> weft.core` edge directly. Both helpers are pure, and `and` preserves the nested form's left-to-right short-circuit order. | Fresh clean Python expert | `NET POSITIVE`; accepted; architecture candidate `SIM102=0`. |
| Task 6 `SIM102` staged-nesting suppression proposal | MR-281..MR-283; exact `RUFF-SUP-240..242` candidates | Seven owner tests; exact residual inventory and directive-location simulation; 41 suppression-policy tests; ledger cardinality check | Harness quiescence precedes its effectful release probe; manager absence, PID, and local-process gates stay visibly staged; watcher rollback ownership stays distinct from candidate existence/installation. A new direct test proves the previously uncovered manager absence-path order. | Initial disposition plus five different fresh clean exact reviewers | Final exact review `NET POSITIVE` after four `NET NEGATIVE` reviews exposed and corrected proof and wording defects; three groups covering `SIM102=6` are ready, owner approved 2026-08-07; directives activated. |
| Task 6 `SIM117` Django transaction and settings scopes | MR-061..MR-065; five nested context pairs | Five exact owner tests; targeted/current Ruff; diff check | Contexts still enter left-to-right and exit right-to-left. Transactions observe the exception and roll back before `pytest.raises` suppresses it; settings remain active while the expected validation error is raised. | Fresh clean Python expert | `NET POSITIVE`; accepted; Django candidate `SIM117=0`. |
| Task 6 `SIM117` CLI mock scopes | MR-070; `TestCLIConstants.test_constants_override` | Exact owner test; targeted/current Ruff; direct mock-after-restoration probe; diff check | `pytest.raises` still handles `SystemExit` before either patch restores its target. Restoration only rebinds module attributes; captured mocks retain their call history for assertions immediately afterward. | Fresh clean Python expert | `NET POSITIVE`; accepted; CLI candidate `SIM117=0`. |
| Task 6 `SIM117` monitor-store broker/session scopes | MR-091..MR-096; six nested context pairs | Full 39-test owner file; targeted/current Ruff; partial-entry protocol trace; diff check | Broker enters before the dependent sidecar is evaluated; partial sidecar entry failure exits the broker; successful scopes exit session before broker. Transaction rollback still occurs before `pytest.raises` suppresses the test exception. | Fresh clean Python expert | `NET POSITIVE`; accepted; monitor-store candidate `SIM117=0`. |
| Task 6 `SIM117` benchmark environment/output scopes | MR-116; `ApiSurface._invoke_run` | New collected firing test; targeted/current Ruff; context-manager protocol inspection; diff check | Environment/stdin enters before stdout then stderr redirection; exit remains stderr, stdout, then environment restoration. The firing test proves scoped inputs, captured outputs, and normal-path restoration; Python's multi-context protocol preserves partial-entry and exceptional unwind. | Fresh clean Python expert | `NET POSITIVE`; accepted; benchmark candidate `SIM117=0`. The prior ledger proof collected zero tests and was replaced. |
| Task 5 `PYI041` TaskSpec limit-test annotation | `tests.taskspec.test_taskspec::test_check_limits_exceeded_reports_each_configured_limit` | Exact four-case parametrized test; focused mypy numeric-tower probe; targeted/current Ruff; diff check | Runtime values remain unchanged and annotations do not coerce them. PEP 484 permits `int` where `float` is expected, so the union was redundant and no annotation consumer exists. | Fresh clean Python expert | `NET POSITIVE`; accepted; repository candidate `PYI041=0`. |
| Task 4 `RUF022` flat export inventories | `weft.commands.task_evidence`; `weft.core.taskspec` | 79 owner/facade tests; targeted/current Ruff; membership/identity and order-dependency inspection; diff check | Exact export membership and object identity remain unchanged; no caller depends on order. Constants, types, then functions now follow one predictable convention in inventories that were not organized by source module. | Fresh clean Python expert | `NET POSITIVE`; accepted; repository candidate `RUF022=7`. |
| Task 4 `RUF022` semantic export-index suppression proposal | MR-284..MR-290; exact `RUFF-SUP-243..249` candidates | 77 manager/queue, 25 task-family, and 52 facade/session test selections; exact residual inventory and directive-location simulation; ledger cardinality check | Seven public inventories retain distinct semantic indices. Their order remains observable, but no declared or internal behavior depends on it; the protected property is adjacency and comprehensibility. | Initial disposition plus two different fresh clean exact reviewers | First exact review `NET NEGATIVE` for one manager adjacency overclaim; final review `NET POSITIVE` after correction; seven one-directive groups ready, owner approved 2026-08-07; directives activated. |
| Task 6 `SIM905` worker-snapshot ledger suppression proposal | MR-148; exact `RUFF-SUP-250` candidate | Three owner-test cases; exact directive-location simulation; 178/78 uniqueness and subset probe; diff check | Two compact whitespace-delimited ledgers remain adjacent unordered classification policies. Literal expansion adds roughly 160 lines without improving duplicate detection or behavioral safety. | Two different fresh clean Python experts | Literal rewrite disposition `NET NEGATIVE`; final exact proposal review `NET POSITIVE`; one two-directive group ready, owner approved 2026-08-07; directives activated. |
| Task 7 `PLR0402` narrow direct-symbol imports | MR-160, MR-169; `events` and `submission` command leaves | 74 selected owner/architecture tests; targeted/current Ruff; import and monkeypatch search; diff check | One task-evidence helper and two spec-resolution helpers bind directly from their relative child owners. No consumer relies on rebinding the owner modules after import; architecture edges remain direct children. | Fresh clean Python/architecture expert | `NET POSITIVE`; accepted; repository candidate `PLR0402=5`. |
| Task 7 `PLR0402` explicit-owner suppression proposal | MR-162, MR-172, MR-176, MR-178, MR-199; exact `RUFF-SUP-251..253` candidates | Five owner-suite families; exact residual/directive simulation; own-facade, internal-boundary, eager-cycle, and lightweight-import architecture proofs | Three command siblings and one core sibling preserve executable direct-owner edges; the cross-layer command preserves a visible non-exported core owner. All Ruff rewrites are runtime-equivalent today; suppression protects dependency syntax and domain qualification, not identity. | Initial disposition plus three different fresh clean exact reviewers | First two exact reviews `NET NEGATIVE` and drove two narrow refactors plus claim/proof corrections; final closure review `NET POSITIVE`; 3/1/1 groups ready, owner approved 2026-08-07; directives activated. |
| Task 7 `S102` runtime import-form suppression proposal | MR-068, MR-069; exact `RUFF-SUP-254` candidate | Two owner tests; exact residual/directive simulation; fixed-input security inspection; diff check | Three fixed-literal test calls execute real explicit/star-import grammar in isolated same-process namespaces so object identity remains assertable. | Fresh clean Python/security expert | Exact review `NET POSITIVE`; one three-directive group ready, owner approved 2026-08-07; directives activated. |
| Task 7 `TRY203` Django stream cancellation | MR-066; `TaskEventsConsumer._stream_events` | Cooperative-disconnect test plus new forced-cancellation firing test; targeted/current Ruff; Django package mypy; diff check | Removing the immediate catch/re-raise preserves the original cancellation traceback and natural propagation. The unchanged `finally` sets cancellation and closes the iterator before any failure reaches the caller. | Fresh clean Python expert | `NET POSITIVE`; accepted; repository candidate `TRY203=0`. |
| Task 7 `S112` CLI-output JSON parsing | MR-081; `tests.conftest::_register_cli_outputs` | Three exact owner tests, including new malformed-input and forced-unexpected-error probes; targeted S112 Ruff; diff check | Ordinary non-JSON output still advances to the next line. Only `JSONDecodeError` is contained; unrelated parser failures now escape instead of being silently treated as mixed CLI output. | Fresh clean Python expert | `NET POSITIVE`; accepted; owner candidates `BLE001=0, S112=0`; repository candidate `S112=1`. |
| Task 7 `S112` Postgres test-schema cleanup | MR-108; `tests.helpers.test_backend::cleanup_prepared_roots` | Three owner tests, including an explicitly ordered failure-then-success cleanup probe; targeted/current Ruff; structured-log field and no-exception-info assertions; diff check | A backend-specific failure remains contained and the next schema is still attempted. One fixed warning identifies only the schema and config path; DSN and backend exception text remain absent. | Four fresh clean Python reviewers across the candidate and rework | Initial logging candidate `NET POSITIVE`, exact final candidate `NET NEGATIVE` because a set erased order evidence; rejected candidate preserved at `/tmp/weft-rw-s112-backend-logging`; ordered secure rework `NET POSITIVE`; accepted; repository candidate `S112=0`, `BLE001=186`. |
| Task 7 `S110` test-fixture finalizers | MR-082, MR-083, MR-086; `broker_env`, `queue_factory`, and `task_factory` | Three new direct generator-finalization tests plus the three adjacent CLI-parser tests; targeted/current Ruff; no-exception-info assertions; diff check | A first queue-close or task-stop failure remains contained and the next resource is still finalized in creation order. Each failure emits one fixed warning without exception text. | Fresh clean Python expert | `NET POSITIVE`; accepted; repository candidate `S110=43`, `BLE001=186`. |
| Task 7 `S110` subprocess lifecycle callbacks | MR-223; `run_monitored_subprocess` worker-start and runtime-handle hooks | Full three-test owner suite, including a new real-subprocess dual-failure probe; targeted/current Ruff; focused mypy; no-exception-info assertions; diff check | Both lifecycle callback failures remain contained and do not replace the successful process status, return code, or output. Each lost publication emits one bounded fixed warning. | Fresh clean Python expert | `NET POSITIVE`; accepted; repository candidate `S110=41`, `BLE001=186`. |
| Task 7 `S110` host lifecycle callbacks | MR-218; `HostTaskRunner.run_with_hooks` worker-start and runtime-handle hooks | Full 119-test owner file, including a new real-spawn dual-failure probe; targeted/current Ruff; focused mypy; no-exception-info assertions; diff check | Both parent-side lifecycle callback failures remain contained and do not replace a successful spawned-function terminal handoff. Fixed warnings omit PID, handle, callback value, and exception text. | Fresh clean Python expert | `NET POSITIVE`; accepted; repository candidate `S110=39`, `BLE001=186`. |
| Task 7 `TRY004` delegated-runtime fixture exceptions | MR-101..MR-103, MR-105; exact `RUFF-SUP-255..257` candidates | Six exact raise-site cases plus two normalization characterizations; targeted/current Ruff; exact type/message assertions; direct source and caller inspection; diff check | Two local argument-type branches now raise `TypeError`; remote MCP failures remain `RuntimeError`; parsed Claude MCP configuration shape failures remain `ValueError`. Missing and empty-list `mcpServers` normalization is proven. | Three clean Python exception-contract reviewers across proposal and rework | Two reviews `NET NEGATIVE` exposed weak exactness, an unproven broad normalization claim, and a false exception-family rationale. Final rework `NET POSITIVE`; refactor clears `TRY004=2`; three groups cover residual `TRY004=4`; repository candidate `TRY004=73`; no directive added; owner approved 2026-08-07. |
| Task 7 `TRY004` decoded TaskSpec validation proposal | MR-186, MR-188..MR-190, MR-212, MR-213; exact `RUFF-SUP-258` candidate | Five existing exact class/message cases plus two public agent-validation shape cases that also assert no cause; targeted/current Ruff; caller and duplicate-helper inspection; diff check | Agent validation, environment materialization, and runner validation retain one field-qualified `ValueError` taxonomy for malformed decoded TaskSpec values. | Two prior fresh clean Python exception-contract reviewers plus two enlarged-group reviewers | Isolated TypeError candidate `NET NEGATIVE` and preserved for coordinated rework; first enlarged review `NET NEGATIVE` for an evidence overclaim; corrected exact review `NET POSITIVE`; enlarged six-directive group activated, owner approved 2026-08-07. |
| Task 7 `TRY004` parameterization adapter proposal | MR-259, MR-260; exact `RUFF-SUP-259` candidate | Full eight-test owner suite with two new exact result-shape cases; targeted/current Ruff; exact type/message assertions; adjacent contract inspection; diff check | JSON-serializable adapter results with invalid required object shapes retain `ValueError`; non-serializable objects remain outside this proposal. | Fresh clean Python exception-contract expert | Exact review `NET POSITIVE`; one two-directive group activated; owner approved 2026-08-07. |
| Task 7 `TRY004` TaskMonitor snapshot-invariant proposal | MR-207; exact `RUFF-SUP-260` candidate | Three snapshot-policy tests, including a new direct two-branch classification probe; targeted/current Ruff; exact type/message assertions and finally-based state restoration; diff check | Misclassified shared fields remain internal `RuntimeError` invariant failures adjacent to their clone-loop enforcement. | Two fresh clean Python exception-contract reviewers | First review `NET NEGATIVE` for an evidence overclaim; corrected final review `NET POSITIVE`; one two-directive group activated; owner approved 2026-08-07. |
| Task 7 `TRY004` state-machine transition proposal | MR-227; exact `RUFF-SUP-261` candidate | Full state-machine owner suite with tightened exact set-source case; targeted/current Ruff; adjacent constructor-validation inspection; diff check | Invalid transition source form remains a `ValueError` within the declarative machine-definition validation family. | Fresh clean Python exception-contract expert | Exact review `NET POSITIVE`; one one-directive group activated; owner approved 2026-08-07. |
| Task 7 `TRY004` pipeline stage-template proposal | MR-208; exact `RUFF-SUP-262` candidate | Full pipeline owner suite with new public compiler shape case; targeted/current Ruff; exact type/message assertion; adjacent stage-incompatibility inspection; diff check | A stored stage with an invalid `spec` object shape remains a `ValueError` in the compiler's declarative stage-template validation family. | Fresh clean Python exception-contract expert | Exact review `NET POSITIVE`; one one-directive group activated; owner approved 2026-08-07. |
| Task 7 `TRY004` exact-ID broker capability proposal | MR-225; exact `RUFF-SUP-263` candidate | Full three-test spawn-request owner suite plus architecture supported-path test; targeted/current Ruff; exact type/message assertion; dependency-floor and supported-path inspection; diff check | Missing callable `insert_messages` remains a `RuntimeError` dependency capability failure. Its message now names the missing connection capability instead of an obsolete package floor. | Two fresh clean Python exception-contract reviewers | Initial proposal review `NET NEGATIVE` for stale guidance; source/message reworked; final review `NET POSITIVE`; one one-directive group activated; owner approved 2026-08-07. |
| Task 7 `TRY004` stored spec JSON proposal | MR-226; exact `RUFF-SUP-264` candidate | Full three-test spec-store owner suite with new decoded-list case; targeted/current Ruff; exact type/message/cause assertions; adjacent read/decode failure inspection; diff check | A decoded non-object spec file remains a `ValueError`; filesystem and JSON decode failures retain their explicit chained causes. | Fresh clean Python exception-contract expert | Exact review `NET POSITIVE`; one one-directive group activated; owner approved 2026-08-07. |
| Task 7 `TRY004` task-monitor checkpoint proposal | MR-177; exact `RUFF-SUP-265` candidate | Full task-monitor command owner suite with new decoded-list loader case; targeted/current Ruff; exact type/message/cause assertion; public corrupt-checkpoint inspection; diff check | A decoded non-object checkpoint remains a `ValueError`; read/decode failures retain the same command-visible invalid-checkpoint family with chained causes. | Fresh clean Python exception-contract expert | Exact review `NET POSITIVE`; one one-directive group activated; owner approved 2026-08-07. |
| Task 7 `TRY004` provider descriptor proposal | MR-180; exact `RUFF-SUP-266` candidate | Full provider-container-runtime owner suite in parallel and sequential modes with a cache-independent fake-resource decoded-list case; targeted/current Ruff; exact type/message/cause assertion; supported descriptor inspection; diff check | A decoded non-object bundled provider descriptor remains a `ValueError`; the provider-name call contract and JSON decode path remain distinct. | Fresh clean Python exception-contract expert | Exact review `NET POSITIVE`; one one-directive group activated; owner approved 2026-08-07. |
| Task 7 `TRY004` provider settings proposal | MR-181 through MR-184; exact `RUFF-SUP-267` candidate | Full eight-test provider-settings owner suite with five firing malformed-value cases; targeted/current Ruff; exact type/message/cause assertions; exact five-finding residual inventory; diff check | Malformed values decoded from project-local `agents.json` remain one `ValueError` configuration family across load and ensure paths. | Fresh clean Python exception-contract expert | Exact review `NET POSITIVE`; one five-directive group activated; owner approved 2026-08-07. |
| Task 7 `TRY004` persistent runtime capability refactor/proposal | MR-185; exact `RUFF-SUP-268` candidate | Eighteen runtime and persistent-provider-session tests with one exact unsupported-capability case; targeted/current Ruff and mypy; caller exception-path inspection; exact one-finding residual inventory; diff check | Missing optional persistent-session support changes from `ValueError` to `RuntimeError`; supported session behavior and the message remain unchanged. | Fresh clean Python refactor/locality and exception-contract expert | Exact review `NET POSITIVE`; source refactor accepted; one one-directive group activated; owner approved 2026-08-07. |
| Task 7 `TRY004` submission dump-contract refactor | MR-170; `weft/commands/submission.py:93` | Sixteen submission/ops tests with one exact wrong-dump-shape case; targeted/current Ruff; source-module mypy; TaskSpec schema and caller inspection; diff check | A non-mapping `spec` returned by a supplied `TaskSpec.model_dump()` changes from `ValueError` to `TypeError`; valid submission behavior and the message remain unchanged; TRY004 is eliminated with no suppression. | Fresh clean Python refactor/locality and exception-contract expert | Exact review `NET POSITIVE`; source/test refactor accepted. |
| Task 7 `TRY004` RunnerHandle constructor/deserializer split | MR-261 and MR-262; exact `RUFF-SUP-269` candidate | Nine focused RunnerHandle tests: two exact direct-constructor type cases, four exact persisted-shape value cases, round-trip/identity behavior, and legacy rejection; targeted/current Ruff; source-module mypy; caller inspection; diff check | Direct constructor mapping misuse changes to `TypeError`; persisted `from_dict()` shape failures remain `ValueError`; four TRY004 findings remain for an exact plan-only proposal. Pre-existing falsy nested-value normalization is recorded, not changed. | Fresh clean Python refactor/locality and exception-contract expert | Exact review `NET POSITIVE`; source/test refactor accepted; one four-directive group activated, owner approved 2026-08-07. |
| Task 7 `TRY004` prepare-agent-images proposal | MR-152 and MR-153; exact `RUFF-SUP-270` candidate | Seven-test builtin owner suite with four public malformed-work-item cases; targeted/current Ruff; exact type/message/cause assertions; exact four-finding residual inventory; diff check | Queue/stdin work-item shape and field failures remain one `ValueError` input family. Docker/platform prerequisites are replaced only to reach production parsing. The existing non-string-Sequence acceptance is recorded, not changed. | Fresh clean Python exception-contract expert | Exact review `NET POSITIVE`; one four-directive group activated, owner approved 2026-08-07. |
| Task 7 `TRY004` packaged builtin catalog proposal | MR-150 and MR-151; exact `RUFF-SUP-271` candidate | Eight catalog/platform tests with three public malformed-resource cases; targeted/current Ruff; exact type/message/cause assertions; exact three-finding residual inventory; diff check | Wrong bundled root, spec, and supported-platforms shapes remain one `ValueError` catalog family. The helper's broader non-string-Sequence acceptance is not normally observable through JSON and remains unchanged. | Fresh clean Python exception-contract expert | Exact review `NET POSITIVE`; one three-directive group activated, owner approved 2026-08-07. |
| Task 7 `TRY004` macOS sandbox TaskSpec proposal | MR-040; exact `RUFF-SUP-272` candidate | Ten-test extension owner suite with three public named-section cases; targeted/current Ruff; extension mypy; exact type/message/cause assertions; exact one-finding residual inventory; diff check | The extension retains one field-qualified `ValueError` taxonomy for non-object decoded TaskSpec sections. | Fresh clean Python exception-contract expert | Exact review `NET POSITIVE`; one one-directive extension-owned group activated, owner approved 2026-08-07. |
| Task 7 `TRY004` Microsandbox option proposal | MR-043 through MR-048; exact `RUFF-SUP-273` candidate | Twenty-three option/plugin tests with 11 new production-parser cases; targeted/current Ruff; extension mypy; exact type/message/cause assertions; exact eight-finding residual inventory; diff check | Decoded TaskSpec sections and Microsandbox option shapes remain one field-qualified `ValueError` family owned by the extension. Pre-existing falsy-env normalization and broader mounts Sequence acceptance are recorded, not changed. | Fresh clean Python exception-contract expert | Exact review `NET POSITIVE`; one eight-directive extension-owned group activated, owner approved 2026-08-07. |
| Task 7 `TRY004` Django private override invariants | MR-067; no suppression | Full 33-test Django integration suite with exact clone-result and nested-spec private invariant cases; targeted/current Ruff; integration-package mypy; caller inspection; diff check | Both impossible wrong-type states change from `ValueError` to `TypeError`; TRY004 is eliminated with no suppression. | Two fresh clean Python refactor/locality reviewers | First mixed candidate `NET NEGATIVE` for a false public-boundary claim and preserved at `/tmp/weft-rw-django-try004-boundary` commit `6c74919`; coordinated TypeError rework `NET POSITIVE` and accepted. |
| Task 7 `TRY004` Docker agent-runner option proposal | MR-016 through MR-019; exact `RUFF-SUP-274` candidate | Fourteen-test agent-runner owner suite with nine invalid-option constructor cases; targeted/current Ruff; source-module mypy; exact type/message/cause assertions; exact eight-finding residual inventory; diff check | Explicit/work-item mount and network option shapes remain one field-qualified `ValueError` family in the runtime agent runner. Broader non-string-Sequence acceptance and outer falsy normalization are recorded, not changed. | Two fresh clean Python exception-contract reviewers | First exact review `NET NEGATIVE` for two non-firing cases; corrected exact review `NET POSITIVE`; one eight-directive group activated, owner approved 2026-08-07. |
| Task 7 `TRY004` Docker plugin configuration proposal | MR-027 through MR-030, MR-032, MR-033; exact `RUFF-SUP-275` candidate | Full 33-test Docker plugin suite with seven new exact direct-normalizer cases; targeted/current Ruff; source-module mypy; exact type/message/cause assertions; exact seven-finding residual inventory; diff check | Decoded TaskSpec and Docker plugin option shapes remain one field-qualified `ValueError` family. Sequence/stringification and falsy-env nuances are recorded, not changed. | Two fresh clean Python exception-contract reviewers | First review `NET NEGATIVE` for inaccurate placement wording; corrected exact review `NET POSITIVE`. One seven-directive module-owned group activated, owner approved 2026-08-07. |
| Task 7 `TRY004` Docker TOML decoder postcondition | MR-034; exact `RUFF-SUP-276` candidate | Fourteen-test profile suite serial and xdist; targeted/current Ruff; source-module mypy; exact mocked decoder-result class/message/cause assertion; exact one-finding residual inventory; diff check | The defensive decoder postcondition branch changes from `ValueError` to `RuntimeError`; Ruff still reports TRY004, so the semantically correct residual needs a narrow suppression candidate. | Two fresh clean Python experts: refactor/locality and exact suppression | Both reviews `NET POSITIVE`; directive activated; one-site candidate activated owner approved 2026-08-07. |
| Task 7 `TRY004` Docker profile materialization proposal | MR-035 through MR-039; exact `RUFF-SUP-277` candidate | Full 20-test profile owner suite with six new exact direct-normalizer cases and independent collision-precedence edges; targeted/current Ruff; source-module mypy; exact type/message/cause assertions; exact six-finding residual inventory; diff check | Persisted profile and explicit environment value shapes remain one field-qualified `ValueError` family. Broader Sequence acceptance, falsy normalization, and declared environment collision precedence are recorded, not changed. | Two fresh clean Python exception-contract reviewers | First review `NET NEGATIVE` for incomplete precedence proof; corrected exact review `NET POSITIVE`. One six-directive module-owned group activated, owner approved 2026-08-07. |
| Task 7 `S110` Docker agent lifecycle callbacks | MR-015a; worker-start and runtime-handle hooks | Full 14-test agent-runner owner suite with two firing callback failures; targeted/current Ruff; focused mypy; exact warning order plus no-exception-info and no-secret assertions; diff check | Both lifecycle callback failures remain contained and do not replace a successful Docker agent outcome. Each lost publication emits one bounded fixed warning without PID, handle, callback value, or exception text. | Fresh clean Python refactor/locality expert | `NET POSITIVE`; accepted; exact `RUFF-SUP-278` activated. |
| Task 7 `S110` Docker agent stream callbacks | MR-015b; stdout and stderr hooks | Full 14-test agent-runner owner suite with two firing stream callback failures; targeted/current Ruff; focused mypy; preserved output plus exact warning order/no-exception-info/no-secret assertions; diff check | Both stream callback failures remain contained and do not replace a successful outcome or remove captured stdout/stderr. Each lost delivery emits one bounded fixed warning without chunk, callback, or exception data. | Fresh clean Python refactor/locality expert | `NET POSITIVE`; accepted; exact `RUFF-SUP-279` activated. |
| Task 7 `S110` Docker agent container cleanup | MR-015d; `container.remove()` boundary | Full 14-test agent-runner owner suite with cleanup failure after success and during a primary start failure; targeted/current Ruff including zero owner S110; focused mypy; exact warning/no-exception-info/no-secret assertions; diff check | Docker cleanup failure remains contained, does not replace either a successful outcome or an in-flight primary exception, and emits one bounded fixed warning without container identity or exception text. | Fresh clean Python refactor/locality expert | `NET POSITIVE`; accepted; exact `RUFF-SUP-281` activated. |
| Task 7 `BLE001` Docker agent boundary proposals | MR-015a through MR-015d; exact `RUFF-SUP-278..281` candidates | Full 14-test agent-runner owner suite with lifecycle/stream callback, provider parser, result builder, and two cleanup-failure paths; targeted/current Ruff; focused mypy; exact outcome/output/warning/privacy assertions; exact six-finding residual inventory; diff check | Four distinct boundary contracts retain broad `Exception`: lifecycle publication, stream delivery, provider-result conversion, and Docker cleanup. All silent paths now have bounded warnings; provider-result failure remains the returned primary error. | Fresh clean Python exact suppression reviewer | `NET POSITIVE`; four groups totaling six directives activated, owner approved 2026-08-07. |
| Task 7 `BLE001`/`S110` result CLI poll rework | MR-074; no suppression | Full 10-test CLI-result owner suite plus one direct documented `OperationalError` failure-then-success case; targeted/current Ruff; per-instance close assertions; zero owner BLE001/S110 inventory; diff check | Only the documented locked/busy read family remains a silent retry. Explicit fallback removes the empty handler; programming errors escape and every queue closes before retry or return. | Two fresh clean Python reviewers | Broad proposal `NET NEGATIVE`; narrowed source/test rework `NET POSITIVE` and accepted with no directive. |
| Task 7 `BLE001`/`S110` interactive prompt completion rework | MR-167b; no suppression | Full run/interactive-client owner suites plus two real PromptToolkit pipe-input/DummyOutput race cases with short timeout and thread-exception guard; targeted/current Ruff; focused mypy; exact result/client/log cleanup/no-output assertions; zero prompt-owner BLE001/S110 inventory; diff check | Completion before prompt startup and during an active prompt both release on the PromptToolkit thread. The broad silent cross-thread `Application.exit()` catch is removed; only closed-loop scheduling race remains locally contained as `RuntimeError`. | Fresh clean Python concurrency/refactor/locality expert | `NET POSITIVE`; accepted with no directive after repeated race-case execution. |
| Task 7 `S110` manager normal atexit unregister | MR-195; `Manager._unregister_atexit_callback` | Parameterized real-manager cleanup test covering successful and failed `atexit.unregister`, repeated cleanup, exact warning privacy; targeted/current Ruff; focused mypy; diff check | Callback-removal failure remains nonfatal after normal manager cleanup and emits one fixed warning. The cleared callback reference prevents retry on repeated cleanup. | Fresh clean Python shutdown/refactor/locality expert | `NET POSITIVE`; accepted; exact `RUFF-SUP-284` activated. |
| Task 7 `BLE001`/`S110` manager interpreter-shutdown proposal | MR-191; exact `RUFF-SUP-283` candidate | Direct callback failure test with manager DEBUG capture; full manager owner suites; targeted/current Ruff; exact no-log/no-stream assertions; exact one-catch/two-diagnostic residual; 41 suppression-policy gates; diff check | The last-chance atexit callback contains unexpected `Exception`-derived cleanup failure without adding another fallible observer. One combined directive is proposed for the one broad silent catch. | First fresh clean review `NET NEGATIVE`; different fresh clean rework review `NET POSITIVE`. | Rationale and log proof corrected; candidate activated, owner approved 2026-08-07. |
| Task 7 `BLE001` manager normal atexit-hook proposal | MR-195; exact `RUFF-SUP-284` candidate | Parameterized real-manager success/failure cleanup proof with boundary injection; full manager owner suites; targeted/current Ruff; exact attempt-count and bounded-warning assertions; exact one-finding residual; diff check | Redundant-hook removal failure remains nonfatal after resource cleanup. CPython callback equality supplies a real arbitrary-`Exception` path; the callback reference is cleared first and the warning remains adjacent to the failed operation. | Fresh clean Python exact suppression/locality reviewer | `NET POSITIVE`; one-site candidate activated, owner approved 2026-08-07. |
| Task 7 harness finalizer removal and transactional-entry rework | MR-109; exact `RUFF-SUP-286` candidate | Full 31-test harness-registration owner suite; repository-wide construction-site audit; targeted/current Ruff and format; explicit no-finalizer invariant; failed-entry primary/rollback/order proof; diff check | Hidden GC/interpreter teardown ownership is removed. Failed explicit entry now rolls back every phase in declared dependency order while preserving the primary failure; one residual BLE001 covers ordinary rollback failure containment. | Initial suppression and first removal reviewers both `NET NEGATIVE`; different fresh clean Python refactor/locality and exact suppression review `NET POSITIVE`. | Rework accepted; finalizer `BLE001`/`S110` eliminated; one rollback `BLE001` candidate activated, owner approved 2026-08-07. |
| Task 7 `S110` harness live-queue cleanup | MR-111; exact `RUFF-SUP-287` candidate | Failure-then-success real-Queue cleanup test; full 32-test harness-registration owner suite; targeted/current Ruff and format; exact warning/privacy assertions; exact one-finding residual; diff check | One close failure remains contained and visible, and the next supplied queue is still attempted. No correctness claim depends on production GC traversal order. | Fresh clean Python refactor/locality and exact suppression reviewer | `NET POSITIVE`; harness `S110` falls from one to zero; one BLE001 candidate activated, owner approved 2026-08-07. |
| Task 7 `S110` Microsandbox callback containment | MR-058 through MR-060; exact `RUFF-SUP-288..289` candidates | Full seven-test command/control owner suites; one real runner flow fires worker, handle, stdout, and stderr callback failures; targeted/current Ruff; extension mypy; exact outcome/output/handle/warning/privacy assertions; exact three-finding residual; diff check | All four callback failures remain contained and visible without replacing an `ok` result, runtime handle, or captured output. Explicit stream names diagnose delivery loss without exposing chunks. | Fresh clean Python refactor/locality and exact suppression reviewer plus round-two evidence check | `NET POSITIVE` and `PASS`; plugin `S110` falls from three to zero; two BLE001 groups totaling three directives activated, owner approved 2026-08-07. |
| Task 7 `S110` Microsandbox stop/remove cleanup | MR-052; exact `RUFF-SUP-290` candidate | Full ten-test runtime-adapter owner suite; direct dual-failure async cleanup proof; targeted/current Ruff and format; extension mypy; exact order/warning/privacy assertions; exact two-finding residual; diff check | Stop and remove failures remain independently contained and visible; removal still runs after stop failure. Broad containment extends beyond documented `MicrosandboxError` only at this primary-outcome-preserving `finally` boundary. | Fresh clean Python cleanup/refactor/locality and exact suppression reviewer | `NET POSITIVE`; exception rationale corrected; runtime `S110` falls from six to four; one two-directive BLE001 group activated, owner approved 2026-08-07. |
| Task 7 `S110` Microsandbox copy-back containment | MR-053; exact `RUFF-SUP-291` candidate | Failure-then-success direct async copy-back test; full 11-test runtime-adapter owner suite; targeted/current Ruff and format; extension mypy; exact order/warning/privacy assertions; exact one-finding residual; diff check | One failed declared copy remains contained and visible; the next declared copy still runs. Declared sequence order is intentional and not a mapping-order dependency. | Fresh clean Python post-execution transfer, refactor/locality, and exact suppression reviewer | `NET POSITIVE`; runtime `S110` falls from four to three; one BLE001 candidate activated, owner approved 2026-08-07. |
| Task 7 `S110` Microsandbox optional metadata | MR-049; exact `RUFF-SUP-292` candidate | Direct config-accessor failure description test; full runtime-adapter owner suite; targeted/current Ruff and format; extension mypy; exact retained-metadata/warning/privacy assertions; exact one-finding residual; diff check | Optional config failure remains contained and visible; core description state and timestamps survive. | Fresh clean Python dynamic-metadata, refactor/locality, and exact suppression reviewer | `NET POSITIVE`; runtime `S110` falls from three to two; one BLE001 candidate activated, owner approved 2026-08-07. |
| Task 7 `S110` Microsandbox guest-directory preparation | MR-056; no suppression | Documented SDK-failure continuation test plus exact unexpected-error propagation test; full 14-test runtime-adapter owner suite; targeted/current Ruff and format; extension mypy; exact call/warning/privacy assertions; zero owner BLE001/S110 residual; diff check | SDK filesystem failures remain contained and visible while following declared copy is attempted; unexpected adapter errors escape before copy. Shared directory-only callers no longer inherit broad suppression of programming errors. | First broad proposal and warning-only rework reviews both `NET NEGATIVE`; narrowed fresh clean review `NET POSITIVE`. | Broad `RUFF-SUP-293` rejected; narrowed rework eliminates both findings without a directive. |
| Task 7 `S110` Microsandbox cancellation kill | MR-054; exact `RUFF-SUP-294` candidate | Direct no-side-effect kill failure plus retained-task cancellation proof; full 15-test runtime-adapter owner suite; targeted/current Ruff and format; extension mypy; exact result/task/warning/privacy assertions; exact one-finding residual; diff check | Kill failure remains contained and visible; the subsequent drain/cancel attempt leaves the retained task done and cancelled, and the cancellation result survives. | First reviewer `NET NEGATIVE`; different fresh clean rework review `NET POSITIVE`. | Kill-side-effect proof and hard-bound wording corrected; Microsandbox `S110` is zero; one BLE001 candidate activated, owner approved 2026-08-07. |
| Task 7 `S110` host multiprocessing-queue cleanup | MR-214; exact `RUFF-SUP-295` candidate | Direct dual-failure cleanup test; targeted/current Ruff and format; host mypy; exact order/warning/privacy assertions; exact two-finding residual; diff check | Queue close and feeder-thread join failures remain independently contained and visible; join still runs after close failure. | Fresh clean Python multiprocessing/refactor/locality and exact suppression reviewer `NET POSITIVE`. | Host `S110` falls from five to three; one two-directive BLE001 group activated, owner approved 2026-08-07. |
| Task 7 `S110` host process-handle cleanup | MR-215; no suppression | Parameterized `ValueError`/`OSError` close-failure cleanup test plus unexpected-`RuntimeError` propagation test; targeted/current Ruff and format; host mypy; exact warning/privacy assertions; zero owner BLE001/S110 residual; diff check | Supported process-wrapper close failures remain secondary to the owning runner path and become visible without runtime identity or exception text; unexpected defects escape. | First broad proposal reviewer `NET NEGATIVE`; different fresh clean narrowed-rework reviewer `NET POSITIVE`. | Broad `RUFF-SUP-296` rejected; host `S110` falls from three to two with no directive. |
| Task 7 `S110` host pre-escalation process join | MR-217; no suppression | OS-wait failure escalation-order test plus unexpected-`RuntimeError` propagation test; targeted/current Ruff and format; host mypy; exact warning/privacy assertions; zero owner BLE001/S110 residual; diff check | OS wait failure remains visible and does not itself skip direct escalation when the liveness recheck succeeds; unexpected defects escape. | Fresh clean Python multiprocessing/refactor/locality reviewer `NET POSITIVE`; qualified the contract for a possible second platform failure from the liveness check. | Broad `RUFF-SUP-297` rejected; host `S110` falls from two to one with no directive. |
| Task 7 `S110` host child agent-runtime session cleanup | MR-221; exact `RUFF-SUP-298` candidate | Direct runtime-close failure worker-entry test; targeted/current Ruff and format; host mypy; session-first plus unordered exact-once IPC attempts; exact warning/privacy assertions; exact one-finding residual; diff check | Runtime session close failure remains contained and visible; child request-queue and response-sender cleanup are both attempted, with no dependency on their relative order. | First reviewer `NET NEGATIVE`; different fresh clean rework reviewer `NET POSITIVE`. | Host `S110` falls from one to zero; one BLE001 candidate activated, owner approved 2026-08-07. |
| Task 7 `S110` command-session cleanup callback | MR-245; exact `RUFF-SUP-299` candidate | Direct callback-failure plus repeated-close test; targeted/current Ruff and format; sessions mypy; exact call-count/warning/privacy assertions; exact one-finding residual; diff check | Callback failure remains contained and visible; the already-closed guard prevents retry. | Fresh clean Python callback-boundary/refactor/locality and exact suppression reviewer `NET POSITIVE`. | Sessions `S110` falls from fourteen to thirteen; one BLE001 candidate activated, owner approved 2026-08-07. |
| Task 7 `S110` command-session monitor cleanup | MR-247; exact `RUFF-SUP-300` candidate | Direct dual-failure cleanup test; targeted/current Ruff and format; sessions mypy; exact order/ownership/warning/privacy assertions; exact two-finding residual; diff check | Final metrics and stop failures remain independently contained and visible; stop follows snapshot failure and monitor ownership clears. | Fresh clean Python monitor-boundary/refactor/locality and exact suppression reviewer `NET POSITIVE`. | Sessions `S110` falls from thirteen to eleven; one two-directive BLE001 group activated, owner approved 2026-08-07. |
| Task 7 `S110` agent startup late-response join | MR-238; no suppression | OS-wait failure join/drain attempt-count and result test plus unexpected-`RuntimeError` propagation test; targeted/current Ruff and format; sessions mypy; exact warning/privacy assertions; zero owner-method BLE001/S110 residual; diff check | OS wait failure remains visible and does not itself skip the late drain; no claim depends on join/drain relative order; unexpected defects escape. | First reviewer `NET NEGATIVE`; different fresh clean rework reviewer `NET POSITIVE`. | Broad `RUFF-SUP-301` rejected; sessions `S110` falls from eleven to ten with no directive. |
| Task 7 `S110` agent-session process-handle cleanup | MR-237; no suppression | Parameterized supported close-failure cleanup test plus unexpected-`RuntimeError` propagation test; targeted/current Ruff and format; sessions mypy; exact attempt-count/warning/privacy assertions; zero owner-method BLE001/S110 residual; diff check | All owned IPC cleanup operations are attempted; supported process-handle failures remain visible and contained without an order dependency; unexpected defects escape. | Fresh clean Python multiprocessing/cleanup/refactor/locality reviewer `NET POSITIVE`. | Broad `RUFF-SUP-302` rejected; sessions `S110` falls from ten to nine with no directive. |
| Task 7 `S110` agent-session monitor cleanup | MR-242; exact `RUFF-SUP-303` candidate | Direct dual-failure cleanup test; targeted/current Ruff and format; sessions mypy; exact order/ownership/warning/privacy assertions; exact two-finding residual; diff check | Final metrics and stop failures remain independently contained and visible; stop follows snapshot failure and persistent-session monitor ownership clears. | Fresh clean Python monitor-boundary/refactor/locality and exact suppression reviewer `NET POSITIVE`. | Sessions `S110` falls from nine to seven; one two-directive BLE001 group activated, owner approved 2026-08-07. |
| Task 7 `S110` agent-session optional metrics observation | MR-240; exact `RUFF-SUP-304` candidate | Cached-failure-then-refresh direct test; targeted/current Ruff and format; sessions mypy; exact identity/ownership/call-count/warning/privacy assertions; exact one-finding residual; diff check | Transient snapshot failure remains contained and visible, returns the cached object, retains the monitor, and permits a later refresh. | Fresh clean Python monitor-observation/refactor/locality reviewer `NET POSITIVE`; warning frequency specified as per failed property read. | Sessions `S110` falls from seven to six; one BLE001 candidate activated, owner approved 2026-08-07. |
| Task 7 `S110` agent-session termination joins | MR-243; no suppression | Four-case deadline/liveness branch matrix with OS join failure plus representative unexpected-`RuntimeError` propagation test; targeted/current Ruff and format; sessions mypy; exact action-count/warning/privacy assertions; zero owner-method BLE001/S110 residual; diff check | OS wait failures remain visible under each existing branch policy; unexpected defects propagate without warning; no assertion depends on incidental call order. | First reviewer `NET NEGATIVE`; different fresh clean rework reviewer `NET POSITIVE`. | Broad `RUFF-SUP-305` rejected; sessions `S110` falls from six to two with no directive. |
| Task 7 `S110` agent startup sealed-channel join | MR-244; no suppression | OS-wait failure join/probe attempt-count and ready-result test plus representative unexpected-`RuntimeError` propagation test; targeted/current Ruff and format; sessions mypy; exact warning/privacy assertions; zero owner-method BLE001/S110 residual; diff check | OS wait failure remains visible and does not itself suppress the final ready response; no claim depends on join/probe relative order; unexpected defects escape. | Fresh clean Python multiprocessing/startup/refactor/locality reviewer `NET POSITIVE`. | Broad `RUFF-SUP-306` rejected; sessions `S110` falls from two to one with no directive. |
| Task 7 `S110` agent-session close boundaries | MR-239; exact `RUFF-SUP-307` queue-publication candidate | Queue failure, join-`OSError`, and unexpected-join tests; full serial runner/session owner suites; targeted/current Ruff and format; sessions mypy; exact action-count/warning/privacy assertions; exact one BLE001 residual; zero owner S110; diff check | Queue publication failure remains contained and visible while termination/final cleanup continue; join handles only OS failure; unexpected defects trigger termination and both final cleanup phases before propagating when those later phases succeed. Full-suite follow-up made callback-warning assertions owner-specific and repaired two process fakes to implement/assert required final joins instead of relying on broad production containment. No assertion depends on relative action order. | First reviewer `NET NEGATIVE`; different fresh clean rework reviewer `NET POSITIVE`; exception-priority qualification recorded. | Sessions `S110` falls from one to zero; full runner/session owner suites pass; one BLE001 candidate activated, owner approved 2026-08-07. |
| Tasks 8-9 atomic activation and completion review | Diff base `f4a12c55d31a3bfe8b4cde7edf064200d539c4c2`; current uncommitted worktree | `ruff check .`; formatter check over canonical roots; suppression-index write/check; 53 policy/metadata tests; canonical mypy; full xdist pytest plus exact serial rerun | Ruff, format, suppression registry, policy tests, diff hygiene, and 198-file mypy are clean. Full xdist pytest has only the documented pre-existing same-turn agent-session diagnostic race; its exact serial test passes. The 453-rule fixture exactly matches SimpleBroker. | Independent completion reviewer | Technical `NET POSITIVE`, no code/suppression blocker. Two documentation-closure findings corrected: execution SHA removed from normative spec, current diff-base worktree marker and activation state recorded here, stale pending/activation wording reconciled. Owner approved closeout and commit 2026-08-07. |

## 20. Independent Review Loop

Plan prompt:

> Read this plan, exact [TS-3] delta, and atomic strategy at its baseline.
> Read current Ruff policy/tests, completed C901 and SimpleBroker reference
> plans, representative high-risk findings, and named lessons. Look for errors,
> bad ideas, ambiguities, or ceremony without risk reduction. Could a
> zero-context engineer account for all findings, preserve exception/resource/
> subprocess/typing contracts, govern suppressions, and activate 453 codes
> atomically? Answer PASS or BLOCKED based on confident implementation and no
> degradation of robustness.

Completion prompt:

> Review promoted [TS-3], plan, config, fixture/tests, suppression state/tool,
> complete cleanup diff, ledgers, and current evidence. Focus on regressions,
> containment, resource order, subprocess semantics, typing, output, logical
> locality, comprehensibility, missing tests, and policy drift. Answer PASS or
> BLOCKED and tie blockers to implementability or degradation.

Reproduce each finding. Update accepted points, record declined rationale, and
request scoped re-review. Material invariant/ownership/authority/blast-radius
changes re-enter full review.

## 21. Revision Log

| Date | Reviewed baseline | Revision | Reason | Re-review |
|---|---|---|---|---|
| 2026-08-05 | Initial draft against `d0254ce45a757a7351fa0e7ca76f389790a1800b` | Added the exact tracked-file isolated reproduction probe and required set comparison, rather than leaving Task 2 to reconstruct the authoring command from prose. | Fresh-eyes review found that flags and counts were reproducible in principle but not executable by a zero-context implementer without rebuilding file discovery. | Author reran an equivalent probe: 453 effective codes; no set difference from SimpleBroker; 674 normal and 848 raw diagnostics. |
| 2026-08-05 | Independent review of the first complete draft | Moved all approved suppression edits and reconciliation into Task 8's expanded-config candidate state; retained the full [TS-3] firing-gate sentence and added the complete mutation matrix; bound suppression approval to exact ledger IDs/sites/snippets; defined review units as one owner/invariant group or one mechanical rule/rewrite shape. | Round 1 found three implementation blockers and one review-scope ambiguity. | Scoped round 2 PASS on all four accepted findings with no new defect. |
| 2026-08-06 | Implementation preflight against the reviewed draft | Replaced the `--isolated` diagnostic probe with a temporary full project config that changes only `select` to `extend-select`; corrected normal/raw counts from 674/848 to 599/773 and removed 75 false `I001` findings from the worklist. | `--isolated` also discarded non-selection Ruff settings, specifically `isort.known-first-party`, so the original diagnostic baseline did not model real activation. | Exact effective set remains 453 with zero SimpleBroker difference; corrected counts, fix totals, current/added/removed inventory, and all remaining per-rule counts reproduce. Scoped plan re-review PASS. |

## 22. Review Log

| Review | Date | Verdict | Disposition |
|---|---|---|---|
| Author fresh-eyes review | 2026-08-05 | PASS at plan-authoring time, later baseline flaw found | Added the then-exact isolated probe and confirmed its 39 rows summed to 674; confirmed Task 8 preserves Weft's six-file Python discovery instead of copying SimpleBroker's broad `bin/*`; metadata/spec-hygiene and DOM-15 gates passed. The 2026-08-06 implementation preflight later invalidated only the diagnostic probe/count claim, not the reviewed activation or safety design. |
| Clean independent plan review, round 1 | 2026-08-05 | BLOCKED | Accepted F1: the old 171-rule config cannot reconcile new-rule suppressions before activation. Accepted F2: the first [TS-3] delta weakened the existing every-setting-fires contract. Accepted F3: owner approval lacked exact candidate sites. Accepted F4: review units were underspecified. |
| Scoped independent plan re-review | 2026-08-05 | PASS | Confirmed coherent Task 8 suppression activation, complete setting mutation/firing proof, one-to-one site approval, and bounded owner/invariant or mechanical-batch review units. No new defect. |
| Corrected-baseline scoped review | 2026-08-06 | BLOCKED | Corrected 599/773 counts and the removal of false `I001` findings reproduced, but the probe did not prove the adjacent current/added/removed inventory or offered-fix claims. Updated it to compare the 171-code Weft fixture, fail on removed rules, and print offered-fix counts; scoped re-review pending. |
| Corrected-baseline scoped re-review | 2026-08-06 | PASS | Reproduced effective/current/added/removed inventory, normal/raw diagnostic/file/rule/fix counts, and exact SimpleBroker parity. No remaining defect. |
| Task 3 clean policy-test review | 2026-08-06 | PASS | Initial review blocked mutation tests that lacked passing controls. After correction, each mutation proves its unmutated candidate first; candidate-policy Ruff passes and only the two intended pre-activation RED tests fail. |
| Task 9 independent completion review | 2026-08-07 | PASS after documentation closure | Technical review `NET POSITIVE`: no code, suppression, layering, order, locality, or comprehensibility blocker. Initial documentation-only block found a stale normative execution SHA and contradictory pending/activated plan language. Removed the SHA from the normative spec; recorded the uncommitted activation against diff base `f4a12c55d31a3bfe8b4cde7edf064200d539c4c2`; marked the Task 2 ledger historical; reconciled rework and activation records. The known xdist-only agent-session wording race predates this work and passes serially. |

The activation commit exists and the checklist below records the completed
technical closure. This status-closure ledger change is intentionally separate
from the atomic implementation commit, as required by Task 9.

## 23. Fresh-Eyes Checklist

- [x] 453-code target exactly matches SimpleBroker and removes no Weft rule.
- [x] Discovery includes tracked Python/shebang tools and excludes Bash.
- [x] Every manual-risk finding has owner, invariant, disposition, proof, and
  review state before edits.
- [x] Mechanical batches are narrow and independently reviewed.
- [x] Each behavior refactor gets fresh Python-expert locality review.
- [x] Negative candidates are preserved/reworked; none unresolved at activation.
- [x] Behavior, containment, resource/subprocess order, typing, and output stay
  fixed unless directly proven and separately scoped.
- [x] No global/per-file ignore, blanket directive, threshold raise, or second
  suppression system.
- [x] New suppressions, if any, are separately reviewed and owner-approved.
- [x] Spec, config, fixture, tests, docs, and suppression state activate atomically.
- [x] Formatter ownership and CI ordering stay unchanged.
- [x] Full verification and independent completion review pass, with the
  pre-existing xdist-only agent-session wording race isolated for a separate
  follow-up commit.
