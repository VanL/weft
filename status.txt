
⏺ Status Report: Main Weft Repo Spec Alignment

  Summary

  The main repo (/Users/van/Developer/weft) has significant spec alignment work in progress with ~38 modified files. Core changes are working, but some integration tests are failing.

  Changes Applied ✅

  | Area            | Change                                                    | Status     |
  |-----------------|-----------------------------------------------------------|------------|
  | Queue Names     | weft.tasks.log → weft.log.tasks                           | ✅ Applied |
  |                 | weft.workers.registry → weft.state.workers                | ✅ Applied |
  |                 | weft.state.process.tid_mappings → weft.state.tid_mappings | ✅ Applied |
  |                 | weft.state.streaming.sessions → weft.state.streaming      | ✅ Applied |
  | TaskSpec        | process_target: array → string                            | ✅ Applied |
  |                 | max_* → peak_* in StateSection                            | ✅ Applied |
  |                 | State transition validation tightened                     | ✅ Applied |
  |                 | mark_* methods refactored to use set_status()             | ✅ Applied |
  | ResourceMonitor | Expanded with limits integration                          | ✅ Applied |
  | Runner          | Updated for string process_target                         | ✅ Applied |
  | Specs           | All 11 spec docs updated                                  | ✅ Applied |
  | Quick Reference | New 00-Quick_Reference.md added                           | ✅ New     |

  Test Status

  | Test Category       | Result                              |
  |---------------------|-------------------------------------|
  | TaskSpec unit tests | ✅ 12 passed                        |
  | Context tests       | ✅ 10 passed                        |
  | Shell tests         | ✅ 1 passed, 1 skipped (Windows)    |
  | Interactive tests   | ❌ Failing (empty stdout)           |
  | CLI run tests       | ❌ Failing (related to interactive) |
  | Result tests        | ⚠️ Timeout                          |

  Core tests pass (22/23). Integration tests involving interactive streaming are failing.

  Root Cause of Failures

  The interactive test failures show:
  ERROR simplebroker.watcher:watcher.py:192 Handler error: Cannot transition from terminal state 'completed'

  The state transition changes (tightening created → no longer allows killed) may be interacting with the interactive task lifecycle. The Consumer may be trying to update state after completion.

  Files Still Modified (Uncommitted)

  38 files changed, +1273, -998 lines

  Key implementation files:
  - weft/_constants.py - Queue name renames
  - weft/core/taskspec.py - process_target string, peak_* renames, state transitions
  - weft/core/tasks/runner.py - String process_target handling
  - weft/core/resource_monitor.py - Expanded monitoring
  - weft/commands/run.py - Updated for new patterns

  Recommendations

  1. Debug interactive test failure - The state transition error suggests Consumer is calling set_status() after task completes. Check weft/core/tasks/consumer.py for the completion flow.
  2. Consider whether created → killed should be valid - The spec change removed this transition, but it may be needed for tasks killed before they start.
  3. Run full test suite after fixing interactive - The timeout suggests tests may be hanging waiting for state changes.
  4. Commit in stages:
    - First: Spec docs + Quick Reference
    - Second: Constants (queue renames)
    - Third: TaskSpec changes
    - Fourth: Runner/Monitor changes
    - Fifth: Test fixes

  New Files Added

  ?? AGENTS.md
  ?? CLAUDE.md
  ?? docs/specifications/00-Quick_Reference.md
  ?? docs/specifications/12-Future_Ideas.md
  ?? docs/specifications/99-Quick_Reference.md  # Duplicate?
  ?? tests/specs/

  Note: There appear to be two Quick Reference files (00- and 99-). One should likely be removed.
